---
phase: 06-example-project-http-github-webhook
plan: 08
type: execute
wave: 3
depends_on:
  - 06-05
  - 06-06
files_modified:
  - examples/http-github-webhook/README.md
  - examples/http-github-webhook/.skytime-credentials.example
  - README.md
  - docs/for-extension-developers/README.md
autonomous: true
requirements:
  - EX-04
must_haves:
  truths:
    - "examples/http-github-webhook/README.md exists with all seven sections from D-DOCS-README: (1) What this example shows, (2) Quick start, (3) Coverage matrix, (4) Authenticated walkthrough, (5) Flow-by-flow tour, (6) Running the tests, (7) Building your own custom binary"
    - "Quick start contains the four-command (≤5) walkthrough from RESEARCH § 6: cd, temporal start-dev, go build, ./extbin run public_repo_check.star --flow public_repo_check --input '{\"repo\":\"octocat/Hello-World\"}'"
    - "Authenticated walkthrough mentions the chmod 600 step verbatim per RESEARCH Pitfall 4 (file-mode permissions warn on world-readable on Linux/macOS)"
    - "Coverage matrix copies the EXACT table from CONTEXT.md D-FLOWS-COVERAGE-MATRIX (column headers verbatim: Flow / seq / block / if_cond / script / for_each_par / call_flow / retries / timeouts / credentials / cancellation)"
    - "Root README.md gains a single-line addition under the existing 'Where to Go Next' bullet list pointing to examples/http-github-webhook/README.md per D-DOCS-MAIN-README"
    - "docs/for-extension-developers/README.md gains a short reference paragraph for pkg/extension/credfile/ describing its schema, default path, and the file-mode security note per D-DOCS-CRED-RESOLVER"
  artifacts:
    - path: examples/http-github-webhook/README.md
      provides: "Front-door walkthrough doc; takes a reader from git-clone to a successful flow run in ≤5 commands; cites the coverage matrix mechanically pinned by 06-06's TestFlows_CoverageMatrix"
      min_lines: 180
    - path: README.md
      provides: "One-line bullet-list addition under 'Where to Go Next' linking to the rich example"
      contains: "examples/http-github-webhook/README.md"
    - path: docs/for-extension-developers/README.md
      provides: "Short reference section for pkg/extension/credfile/ (schema / path / file-mode warning)"
      contains: "pkg/extension/credfile"
  key_links:
    - from: examples/http-github-webhook/README.md
      to: examples/http-github-webhook/.skytime-credentials.example (06-01)
      via: "Authenticated walkthrough section says: cp .skytime-credentials.example ~/.skytime-credentials && chmod 600 ~/.skytime-credentials"
      pattern: "cp \\.skytime-credentials\\.example"
    - from: examples/http-github-webhook/README.md
      to: docs/cli-binary.md
      via: "'Building your own custom binary' section forward-links per D-DOCS-README"
      pattern: "docs/cli-binary\\.md"
    - from: examples/http-github-webhook/README.md
      to: examples/http-github-webhook/issue_triage_test.star (06-07)
      via: "'Running the tests' section invokes ./extbin test ./examples/http-github-webhook/ (which runs issue_triage_test.star)"
      pattern: "extbin test"
    - from: README.md (root)
      to: examples/http-github-webhook/README.md
      via: "'Where to Go Next' bullet pointing to the rich example project"
      pattern: "examples/http-github-webhook/README\\.md"
    - from: docs/for-extension-developers/README.md
      to: pkg/extension/credfile/ (06-02)
      via: "Reference paragraph describing the TOML schema + default path + chmod 600 warning"
      pattern: "pkg/extension/credfile"
---

<objective>
Ship the user-facing documentation that satisfies EX-04 ("A README walkthrough takes a reader from `git clone` to a successfully-executed flow against `skytime dev-server` in under five commands"). The example's README is the front-door walkthrough; the root README's existing "Where to Go Next" gets one line linking to it; the extension-developers doc gains a short reference for `pkg/extension/credfile/`. The `.skytime-credentials.example` (created in 06-01 with the locked TOML schema) gets a tiny refinement pass to align its inline comments with the README narrative — but the SCHEMA stays untouched (D-CREDS-FORMAT lock).

Purpose: EX-04 success criterion 1 is human-readable: "A reader can `git clone`, follow the README walkthrough, and execute a `.star` flow against `skytime dev-server` in under five commands." The CI smoke (06-09) verifies the COMMANDS work; this plan ensures a reader who has never touched Skytime can FIND those commands and EXECUTE them without confusion. The seven-section README structure is locked verbatim by D-DOCS-README; the coverage matrix is locked verbatim by D-FLOWS-COVERAGE-MATRIX (mechanically pinned by 06-06's `TestFlows_CoverageMatrix`).

Output: One new file (`examples/http-github-webhook/README.md`, ~180-300 lines) and three precise edits (one bullet in root `README.md`, a paragraph in `docs/for-extension-developers/README.md`, and an optional comment-only refinement to `.skytime-credentials.example`). Depends on 06-05 (extbin binary path) and 06-06 (the five flows + the coverage matrix to cite). Can run parallel to 06-07 (no shared files) and is depended on by 06-09 (CI's walkthrough_smoke.sh asserts the README's documented commands work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-example-project-http-github-webhook/06-CONTEXT.md
@.planning/phases/06-example-project-http-github-webhook/06-RESEARCH.md
@.planning/phases/06-example-project-http-github-webhook/06-01-SUMMARY.md
@.planning/phases/06-example-project-http-github-webhook/06-02-SUMMARY.md
@.planning/phases/06-example-project-http-github-webhook/06-05-SUMMARY.md
@.planning/phases/06-example-project-http-github-webhook/06-06-SUMMARY.md
@CLAUDE.md
@README.md
@docs/cli-binary.md
@docs/for-extension-developers/README.md
@docs/for-flow-authors/testing.md
@examples/skeleton/README.md
@examples/README.md
@examples/http-github-webhook/.skytime-credentials.example

<interfaces>
<!-- Locked content the planner must copy verbatim into the README. -->

The seven-section structure (CONTEXT.md D-DOCS-README, lock — DO NOT REORDER):
  1. What this example shows (one paragraph)
  2. Quick start (the ≤5 command walkthrough, public-API-only)
  3. Coverage matrix (table from D-FLOWS-COVERAGE-MATRIX)
  4. Authenticated walkthrough (second-stage; covers credfile setup, webhook.site URL, GitHub PAT)
  5. Flow-by-flow tour (one paragraph per flow + a "what it demonstrates" bullet)
  6. Running the tests (./extbin test ./examples/http-github-webhook/)
  7. Building your own custom binary (forward-link to docs/cli-binary.md)

The four-command Quick Start (RESEARCH § 6 — VERBATIM):
```
1. cd skytime/examples/http-github-webhook
2. temporal server start-dev --headless &     (in another terminal, or background as shown)
3. go build -o ./extbin ./cmd/extbin
4. ./extbin run public_repo_check.star --flow public_repo_check --input '{"repo":"octocat/Hello-World"}'
```
That's 4 commands (under the ≤5 cap). `git clone` is the implicit "step 0".

Optional 5th command (authenticated section):
```
5. cp .skytime-credentials.example ~/.skytime-credentials && chmod 600 ~/.skytime-credentials && $EDITOR ~/.skytime-credentials
```

Coverage matrix (CONTEXT.md D-FLOWS-COVERAGE-MATRIX — VERBATIM column headers + body):

| Flow | seq | block | if_cond | script | for_each_par | call_flow | retries | timeouts | credentials | cancellation |
|------|-----|-------|---------|--------|--------------|-----------|---------|----------|-------------|--------------|
| public_repo_check | ✓ | ✓ | ✓ | ✓ |  |  |  |  |  |  |
| pr_to_webhook | ✓ |  |  | ✓ | ✓ |  | ✓ |  | ✓ |  |
| issue_triage | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ (incidental) |
| batch_label_issues | ✓ | ✓ |  | ✓ |  |  | ✓ | ✓ | ✓ |  |
| weekly_digest | ✓ |  |  | ✓ | ✓ |  | ✓ |  | ✓ |  |

(Note: `issue_triage` row's `call_flow=✓` corresponds to the parser's parsed top-level `issue_triage` flow, which calls `triage_issue` via `call_flow`. The subflow itself doesn't need its own row.)

Pitfall 4 (file-mode permissions) — VERBATIM call-out for Authenticated section:
> On Linux and macOS, new files inherit `umask 022` → mode `0o644` (group + world readable). The credfile resolver warns on `mode & 0o044 != 0`. Run `chmod 600 ~/.skytime-credentials` after copying — the warning message points at the fix.

Pitfall 2 (mock router keys on REGISTERED name) — VERBATIM call-out for Running-the-tests section:
> When writing tests, `tester.mock_action(extension="github", op="...")` uses the REGISTERED extension name `"github"`, NOT the local variable `gh = github.client(...)`. See `docs/for-flow-authors/testing.md` for the full Tier-3 reference.

Per-flow paragraphs (one per flow; pull from CONTEXT.md D-FLOWS-LINEUP + 06-06-SUMMARY for the actual implemented composition):
- public_repo_check.star — Headline demo. No credentials. Demonstrates: sequential step + script + if_cond + block. Reads a public GitHub repo and conditionally lists issues+PRs based on stars threshold.
- pr_to_webhook.star — Authenticated. Demonstrates: sequential step + for_each_parallel + script + retries + credentials. Lists open PRs in a repo and posts each PR's title to a webhook URL.
- issue_triage.star — Deepest flow. Two flows in one file (top-level + subflow). Demonstrates: every primitive (seq + block + if_cond + script + for_each_parallel + call_flow) + retries + timeouts + credentials + (incidental) cancellation. Top-level lists open issues; for each issue, calls the `triage_issue` subflow which fetches the issue, classifies it, and (for stale issues) appends a comment + label.
- batch_label_issues.star — Block-batch demo with idempotent ops. Demonstrates: dynamic block_fn + script + retries + timeouts + credentials. Lists open issues, filters to unlabeled, then dynamically builds a single-activity-invocation batch of `gh.add_label` calls.
- weekly_digest.star — Aggregation demo. Demonstrates: sequential + for_each_parallel + script + retries + credentials. Lists recently-merged PRs, groups by author, builds per-author summary blocks in parallel, posts a final consolidated digest to a webhook.

Root README "Where to Go Next" bullet text (D-DOCS-MAIN-README — placement: under the existing "Browse more examples" bullet OR at the end of the bulleted list; planner picks the most natural slot, but ABOVE the existing "Build a custom CLI" bullet because the example IS a custom CLI in action):
> - **Try the rich example project** —
>   [`examples/http-github-webhook/README.md`](examples/http-github-webhook/README.md)
>   walks through the dogfooding example: three extensions
>   (HTTP + GitHub + Webhook), five `.star` flows covering every DSL
>   primitive, a Tier-3 test, and the canonical `cmd/extbin` custom
>   binary. From `git clone` to a real workflow run in under five commands.

docs/for-extension-developers/README.md — credfile reference paragraph (D-DOCS-CRED-RESOLVER):
> ## Credential resolution: `pkg/extension/credfile/`
>
> Skytime ships a file-based credential resolver at
> [`pkg/extension/credfile/`](../../pkg/extension/credfile/). Use it
> from your custom CLI binary via `cli.WithCredentialHandler(credfile.New(...))`.
>
> **Schema** — TOML with explicit `type` tag per credential:
>
> ```toml
> [credentials.github_token]
> type  = "bearer"
> token = "ghp_..."
>
> [credentials.basic_id]
> type     = "basic"
> username = "..."
> password = "..."
>
> [credentials.apikey_id]
> type  = "apikey"
> key   = "X-API-Key"   # header NAME
> value = "..."         # header VALUE (the secret)
> ```
>
> **Default path** — `$HOME/.skytime-credentials`. Override via
> `credfile.New(credfile.WithPath("/etc/skytime/credentials.toml"))`.
>
> **Security** — On Linux/macOS, the resolver warns when the file is
> world-readable (`mode & 0o044 != 0`) and refuses to load in strict
> mode (`credfile.WithStrictMode()`). Always `chmod 600
> ~/.skytime-credentials` after copying the example template.
>
> **Worked example** — see `examples/http-github-webhook/cmd/extbin/main.go`,
> which wires `credfile.New(...)` into a custom CLI binary alongside three
> registered extensions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>06-08-01</id>
  <name>Task 1: Author examples/http-github-webhook/README.md (the seven-section walkthrough)</name>
  <read_first>
    - .planning/phases/06-example-project-http-github-webhook/06-CONTEXT.md (D-DOCS-README — locked seven-section structure; D-FLOWS-COVERAGE-MATRIX — locked table; D-FLOWS-LINEUP — per-flow descriptions; D-DOCS-CRED-RESOLVER — extension-developers doc reference; D-DOCS-MAIN-README — root README addition)
    - .planning/phases/06-example-project-http-github-webhook/06-RESEARCH.md (§ 6 — README Walkthrough Structure; § "Pitfalls" Pitfall 2 + Pitfall 4 verbatim call-outs)
    - .planning/phases/06-example-project-http-github-webhook/06-06-SUMMARY.md (the actual composition of the five flows as implemented; primitive set per flow output by TestFlows_CoverageMatrix)
    - .planning/phases/06-example-project-http-github-webhook/06-05-SUMMARY.md (extbin entry-point name + path)
    - .planning/phases/06-example-project-http-github-webhook/06-02-SUMMARY.md (credfile API surface + WithStrictMode option name + the chmod 600 narrative)
    - examples/skeleton/README.md (style + voice reference for example READMEs)
    - examples/README.md (sibling examples index — verify this README's voice matches and check for any cross-link expectations)
    - docs/cli-binary.md (forward-link target for section 7)
    - docs/for-flow-authors/testing.md (forward-link target for section 6 — Pitfall 2 mention)
    - examples/http-github-webhook/.skytime-credentials.example (06-01 — the schema the README cites)
    - examples/http-github-webhook/issue_triage_test.star (06-07 — cited by section 6 as the running test)
    - examples/http-github-webhook/cmd/extbin/main.go (06-05 — verify the binary supports `extbin run --flow=... --input=...`)
  </read_first>
  <files>examples/http-github-webhook/README.md</files>
  <action>
    Create `examples/http-github-webhook/README.md` with the seven sections in the LOCKED order from D-DOCS-README. The file should be ~180-300 lines of clear prose with code blocks and the coverage table. Voice: clear, minimal jargon, NO vendor-decision-laden language (per D-FLOWS-STYLE).

    **Title:** `# HTTP + GitHub + Webhook — the rich example project`

    **Front-matter blockquote (above section 1):** one or two sentences describing the example at a glance:
    > Three extensions (HTTP + GitHub + Webhook), five `.star` flows covering
    > every DSL primitive, a Tier-3 test demonstrating attempt-aware retries
    > and replay determinism, and the canonical `cmd/extbin` custom binary
    > pattern. From `git clone` to a real workflow run in under five commands.

    **Section 1 — `## What this example shows`** (one paragraph, ~5-8 lines):
    Explain the two-tier authoring model in concrete terms: HTTP extension is bundled (Phase 4); GitHub + Webhook are example-local; all three are registered together by the example's custom CLI binary at `cmd/extbin/main.go`. The example is the dogfood vehicle — every Phase 1-5 capability shows up in at least one flow. Mention the headline demo (`public_repo_check.star`) needs no credentials and the second-stage (authenticated flows) needs a TOML credfile and a webhook.site URL.

    **Section 2 — `## Quick start`** (the four-command walkthrough VERBATIM from RESEARCH § 6):

    Open with one sentence: "After `git clone`, four commands take you from a fresh checkout to a real workflow run against the public GitHub API."

    Numbered list of FOUR steps, each in a code block:
    ```bash
    # 1. Enter the example directory
    cd skytime/examples/http-github-webhook

    # 2. Start a local Temporal dev server (in another terminal, or background as shown)
    temporal server start-dev --headless &

    # 3. Build the example binary
    go build -o ./extbin ./cmd/extbin

    # 4. Run the headline flow against the public GitHub API (no credentials needed)
    ./extbin run public_repo_check.star \
      --flow public_repo_check \
      --input '{"repo":"octocat/Hello-World"}'
    ```

    Closing line: "Expected output: a series of `step_start` / `step_complete` lines, ending with `flow_complete`. If you see `flow_failed`, scroll up — the renderer prints the offending step's error inline."

    Sub-section `### Don't have temporal CLI yet?`:
    Two-line note + the curl install line + the brew alternative — match `pkg/cli/dev_server.go:106-110` style:
    ```
    brew install temporal
    # OR
    curl -sSf https://temporal.download/cli.sh | sh
    ```

    **Section 3 — `## Coverage matrix`**:
    One opening line: "Every Skytime DSL primitive and every operational concern shows up in at least one flow. The mapping is mechanically pinned by `TestFlows_CoverageMatrix` in `flows_test.go` — drift between this table and the actual DAG fails CI."

    Then the EXACT 5-row table from CONTEXT.md D-FLOWS-COVERAGE-MATRIX (column headers verbatim: Flow / seq / block / if_cond / script / for_each_par / call_flow / retries / timeouts / credentials / cancellation). Use markdown table; preserve the `✓` checkmarks and `✓ (incidental)` annotation.

    **Section 4 — `## Authenticated walkthrough`** (the second-stage section):

    Open: "The headline demo uses GitHub's public unauthenticated API. The other four flows need a GitHub PAT and a webhook URL. Setup is one extra command."

    Then a numbered list of THREE steps (each with code block):

    1. **Get a webhook URL.** Visit https://webhook.site (no account needed). Copy your unique URL — looks like `https://webhook.site/abc-123-def-456`.

    2. **Get a GitHub PAT.** Visit https://github.com/settings/personal-access-tokens. Generate a fine-grained PAT with the `public_repo` scope (read-only is enough for the example flows; the comment + label flows need write access — `repo` scope — when running against your own repos).

    3. **Copy and edit the credfile:**
       ```bash
       cp .skytime-credentials.example ~/.skytime-credentials
       chmod 600 ~/.skytime-credentials
       $EDITOR ~/.skytime-credentials  # paste your webhook URL + PAT
       ```

    Then a callout box with the file-mode warning (Pitfall 4 verbatim):

    > **Note: `chmod 600` is required.** On Linux and macOS, new files
    > inherit `umask 022` → mode `0o644` (group + world readable). Skytime's
    > `pkg/extension/credfile/` resolver warns when the file is
    > world-readable and refuses to load it in strict mode. The first
    > `extbin run` after a fresh setup will print this warning until you
    > run `chmod 600 ~/.skytime-credentials`.

    Then a "Try an authenticated flow" code block:
    ```bash
    ./extbin run pr_to_webhook.star \
      --flow pr_to_webhook \
      --input '{"owner":"octocat","repo":"Hello-World"}'
    ```
    Closing line: "Each open PR's title is POSTed to your webhook URL. Refresh https://webhook.site to see them appear in the browser."

    **Section 5 — `## Flow-by-flow tour`**:

    One sub-heading per flow (`### public_repo_check.star`, `### pr_to_webhook.star`, ...), each with:
    - One paragraph (~3-5 lines) describing what the flow does in plain language.
    - A bullet list `**Demonstrates:**` enumerating the primitives + concerns it exercises (cite from the coverage matrix; pull from the per-flow paragraphs in the interfaces block above).
    - A pointer to the file path: `→ [public_repo_check.star](public_repo_check.star)`.

    Order: public_repo_check, pr_to_webhook, issue_triage, batch_label_issues, weekly_digest (matches D-FLOWS-LINEUP order — same as the coverage matrix).

    **Section 6 — `## Running the tests`**:

    Open: "The example ships a Tier-3 `*_test.star` test (`issue_triage_test.star`) that mocks every action and exercises attempt-aware retries + replay determinism. Run it with the example's binary:"

    Code block:
    ```bash
    ./extbin test ./
    ```

    Then a sub-heading `### Mock router pitfall` with the Pitfall 2 verbatim call-out:

    > When writing your own `*_test.star` files, `tester.mock_action(extension="github", op="...")` uses the REGISTERED extension name `"github"`, NOT the local variable from `gh = github.client(...)`. See [docs/for-flow-authors/testing.md](../../docs/for-flow-authors/testing.md) for the full Tier-3 reference.

    Then a forward-link to the testing docs:
    > Full Tier-3 reference: [`docs/for-flow-authors/testing.md`](../../docs/for-flow-authors/testing.md). Tutorial: [`docs/for-flow-authors/testing-tutorial.md`](../../docs/for-flow-authors/testing-tutorial.md).

    **Section 7 — `## Building your own custom binary`**:

    One paragraph + forward-link. Open: "The `cmd/extbin/main.go` in this directory is the canonical implementation of Skytime's two-tier authoring model: import `pkg/cli`, register your extensions via `cli.WithExtensions(...)`, wire a credential resolver via `cli.WithCredentialHandler(...)`, and you have your own customer-specific Skytime binary with `validate` / `run` / `dev-server` / `test` subcommands."

    Closing forward-link sentence: "Full guide: [`docs/cli-binary.md`](../../docs/cli-binary.md). Credential resolver reference: [`docs/for-extension-developers/README.md`](../../docs/for-extension-developers/README.md#credential-resolution-pkgextensioncredfile)."

    Style discipline (D-FLOWS-STYLE):
    - Sparing comments inside code blocks — only where the WHY isn't obvious.
    - No vendor-decision-laden language ("we chose X because...") — just describe what's there.
    - Match the voice of `examples/skeleton/README.md` (assuming it exists with that voice; if not, match `docs/getting-started.md`).
    - Section heading style: `##` for top-level sections, `###` for sub-sections within a section. Never go deeper than `###`.

    Output target: 180-300 lines. If under 180, sections are too thin; if over 300, prose has bloated — tighten.
  </action>
  <acceptance_criteria>
    - `test -f examples/http-github-webhook/README.md` exits 0
    - `[ "$(wc -l < examples/http-github-webhook/README.md)" -ge "180" ]` (file has substance)
    - `grep -q '^## What this example shows' examples/http-github-webhook/README.md` (section 1)
    - `grep -q '^## Quick start' examples/http-github-webhook/README.md` (section 2)
    - `grep -q '^## Coverage matrix' examples/http-github-webhook/README.md` (section 3)
    - `grep -q '^## Authenticated walkthrough' examples/http-github-webhook/README.md` (section 4)
    - `grep -q '^## Flow-by-flow tour' examples/http-github-webhook/README.md` (section 5)
    - `grep -q '^## Running the tests' examples/http-github-webhook/README.md` (section 6)
    - `grep -q '^## Building your own custom binary' examples/http-github-webhook/README.md` (section 7)
    - The four locked Quick Start commands appear (each grepped):
      - `grep -q 'cd skytime/examples/http-github-webhook' examples/http-github-webhook/README.md`
      - `grep -q 'temporal server start-dev --headless' examples/http-github-webhook/README.md`
      - `grep -q 'go build -o ./extbin ./cmd/extbin' examples/http-github-webhook/README.md`
      - `grep -q './extbin run public_repo_check.star' examples/http-github-webhook/README.md`
    - Exact input flag pinning (06-09 walkthrough_smoke.sh asserts the same):
      - `grep -q -- '--flow public_repo_check' examples/http-github-webhook/README.md`
      - `grep -qF '"repo":"octocat/Hello-World"' examples/http-github-webhook/README.md`
    - chmod 600 callout (Pitfall 4):
      - `grep -q 'chmod 600' examples/http-github-webhook/README.md`
      - `grep -q 'world-readable' examples/http-github-webhook/README.md` (or `world readable`)
    - Coverage matrix column headers (D-FLOWS-COVERAGE-MATRIX VERBATIM):
      - `grep -q 'Flow.*seq.*block.*if_cond.*script.*for_each_par.*call_flow.*retries.*timeouts.*credentials.*cancellation' examples/http-github-webhook/README.md`
    - All five flow names appear in the matrix:
      - `grep -q '| public_repo_check |' examples/http-github-webhook/README.md`
      - `grep -q '| pr_to_webhook |' examples/http-github-webhook/README.md`
      - `grep -q '| issue_triage |' examples/http-github-webhook/README.md`
      - `grep -q '| batch_label_issues |' examples/http-github-webhook/README.md`
      - `grep -q '| weekly_digest |' examples/http-github-webhook/README.md`
    - Authenticated walkthrough cites webhook.site + GitHub PAT:
      - `grep -q 'webhook.site' examples/http-github-webhook/README.md`
      - `grep -q 'personal-access-tokens\|GitHub PAT\|PAT' examples/http-github-webhook/README.md`
    - Cred file copy command present:
      - `grep -q 'cp .skytime-credentials.example' examples/http-github-webhook/README.md`
    - Tests section invokes the binary:
      - `grep -q 'extbin test' examples/http-github-webhook/README.md`
    - Pitfall 2 mock-router callout present:
      - `grep -q 'extension="github"\|extension = "github"' examples/http-github-webhook/README.md`
      - `grep -q 'REGISTERED\|registered' examples/http-github-webhook/README.md` (case-insensitive presence of REGISTERED qualifier)
    - Custom binary section forward-links docs/cli-binary.md:
      - `grep -q 'docs/cli-binary.md' examples/http-github-webhook/README.md`
    - flow_complete substring is mentioned in the expected-output narrative (06-09 grepss for this string in CI):
      - `grep -q 'flow_complete' examples/http-github-webhook/README.md`
    - File parses as Markdown (no broken inline code blocks): the count of triple-backtick fences is even:
      - `[ "$(($(grep -c '^```' examples/http-github-webhook/README.md) % 2))" -eq "0" ]`
  </acceptance_criteria>
  <verify>
    <automated>test -f examples/http-github-webhook/README.md && [ "$(wc -l < examples/http-github-webhook/README.md)" -ge "180" ] && grep -q '^## Quick start' examples/http-github-webhook/README.md && grep -q '^## Coverage matrix' examples/http-github-webhook/README.md && grep -q '^## Authenticated walkthrough' examples/http-github-webhook/README.md && grep -q '^## Running the tests' examples/http-github-webhook/README.md && grep -q 'go build -o ./extbin ./cmd/extbin' examples/http-github-webhook/README.md && grep -q -- '--flow public_repo_check' examples/http-github-webhook/README.md && grep -qF '"repo":"octocat/Hello-World"' examples/http-github-webhook/README.md && grep -q 'chmod 600' examples/http-github-webhook/README.md && grep -q 'flow_complete' examples/http-github-webhook/README.md && grep -q 'docs/cli-binary.md' examples/http-github-webhook/README.md && [ "$(($(grep -c '^```' examples/http-github-webhook/README.md) % 2))" -eq "0" ]</automated>
  </verify>
  <done>
    `examples/http-github-webhook/README.md` exists with all seven sections in locked order, the four-command Quick Start matches RESEARCH § 6 verbatim (06-09's CI smoke will execute these EXACT commands), the coverage matrix column headers match D-FLOWS-COVERAGE-MATRIX verbatim, Pitfalls 2 + 4 are surfaced as call-outs, and the file is 180-300 lines of clear prose.
  </done>
</task>

<task type="auto">
  <id>06-08-02</id>
  <name>Task 2: Add the "Where to Go Next" bullet to root README.md + credfile reference paragraph to docs/for-extension-developers/README.md</name>
  <read_first>
    - README.md (FULL FILE — find the "Where to Go Next" section and identify the precise insertion point; preserve existing bullet style)
    - docs/for-extension-developers/README.md (FULL FILE — find a coherent place to drop the credfile reference section, e.g. after the "Start Here" list and before any deep-dive sections; preserve existing voice + section style)
    - .planning/phases/06-example-project-http-github-webhook/06-CONTEXT.md (D-DOCS-MAIN-README — exact wording for the root README bullet; D-DOCS-CRED-RESOLVER — placement for the credfile reference)
    - .planning/phases/06-example-project-http-github-webhook/06-02-SUMMARY.md (credfile API surface — verify the option names match: `WithPath`, `WithStrictMode`, `WithLogger`)
    - examples/http-github-webhook/cmd/extbin/main.go (06-05 — the worked-example reference cited in the credfile section)
  </read_first>
  <files>README.md, docs/for-extension-developers/README.md</files>
  <action>
    **Edit 1 — root README.md "Where to Go Next" addition:**

    Find the existing "Where to Go Next" section (around line 339 per the source). The existing bulleted list ends near a "Build a custom CLI" bullet pointing at `docs/cli-binary.md`. Insert a NEW bullet for the rich example. The natural slot is BETWEEN "Browse more examples" (which currently points at `examples/README.md` indexing the skeleton fixtures) and the section that follows — so the rich example sits as a step-up from the small skeleton fixtures.

    Use the Edit tool. The new bullet text (D-DOCS-MAIN-README — copy verbatim, adjust whitespace/indent to match existing list style):

    ```
    - **Try the rich example project** —
      [`examples/http-github-webhook/README.md`](examples/http-github-webhook/README.md)
      walks through the dogfooding example: three extensions
      (HTTP + GitHub + Webhook), five `.star` flows covering every DSL
      primitive, a Tier-3 test, and the canonical `cmd/extbin` custom
      binary. From `git clone` to a real workflow run in under five commands.
    ```

    Place it right AFTER the existing "Browse more examples" bullet (the one pointing to `examples/README.md`). Preserve every other bullet untouched. Do NOT alter the section heading, the introductory sentence, or any other bullet.

    **Edit 2 — docs/for-extension-developers/README.md credfile reference section:**

    Find a coherent insertion point. Read the file end-to-end first; the natural slot is AFTER the "Start Here" numbered list (which references the architecture / SDK source / HTTP extension) and BEFORE any deeper sections. If a section like `## Credentials` already exists in that file, append the credfile reference under it; otherwise add a new top-level section.

    Use the Edit tool. The new section (D-DOCS-CRED-RESOLVER — VERBATIM body from the interfaces block above):

    ```markdown
    ## Credential resolution: `pkg/extension/credfile/`

    Skytime ships a file-based credential resolver at
    [`pkg/extension/credfile/`](../../pkg/extension/credfile/). Use it
    from your custom CLI binary via `cli.WithCredentialHandler(credfile.New(...))`.

    **Schema** — TOML with explicit `type` tag per credential:

    ```toml
    [credentials.github_token]
    type  = "bearer"
    token = "ghp_..."

    [credentials.basic_id]
    type     = "basic"
    username = "..."
    password = "..."

    [credentials.apikey_id]
    type  = "apikey"
    key   = "X-API-Key"   # header NAME
    value = "..."         # header VALUE (the secret)
    ```

    **Default path** — `$HOME/.skytime-credentials`. Override via
    `credfile.New(credfile.WithPath("/etc/skytime/credentials.toml"))`.

    **Security** — On Linux/macOS, the resolver warns when the file is
    world-readable (`mode & 0o044 != 0`) and refuses to load in strict
    mode (`credfile.WithStrictMode()`). Always `chmod 600
    ~/.skytime-credentials` after copying the example template.

    **Worked example** — see [`examples/http-github-webhook/cmd/extbin/main.go`](../../examples/http-github-webhook/cmd/extbin/main.go),
    which wires `credfile.New(...)` into a custom CLI binary alongside three
    registered extensions.
    ```

    Important verifications BEFORE writing:
    - **Confirm `WithStrictMode` is the actual option name** (06-02 may have shipped it as `WithStrict` or `WithRefuseInsecureMode` — read `pkg/extension/credfile/options.go` and match what's actually exported).
    - **Confirm `WithPath` is the actual option name.**
    - **Confirm the schema field names (`token`, `username`, `password`, `key`, `value`)** match what `pkg/extension/credfile/file.go::credentialEntry` expects after 06-02 ships. The schema in CONTEXT.md D-CREDS-FORMAT is the spec; if 06-02 deviated for any reason, the README documents what's REAL, not what was specced.

    DO NOT alter any other content in `docs/for-extension-developers/README.md` — just insert the new section.
  </action>
  <acceptance_criteria>
    - **Root README.md:**
      - `grep -q 'examples/http-github-webhook/README.md' README.md` (the new link is present)
      - `grep -q 'Try the rich example project\|rich example project' README.md` (the new bullet's lead phrase is present)
      - The bullet is inside the "Where to Go Next" section: `awk '/^## Where to Go Next/,/^## /' README.md | grep -q 'examples/http-github-webhook/README.md'` (the link sits between "## Where to Go Next" and the next "## " heading)
      - The existing bullets are still present (sanity — rerun 5 grep checks for known-existing lines):
        - `grep -q 'docs/architecture.md' README.md`
        - `grep -q 'docs/reference/builtins.md' README.md`
        - `grep -q 'docs/reference/cli.md' README.md`
        - `grep -q 'docs/cli-binary.md' README.md`
        - `grep -q 'docs/for-flow-authors/README.md' README.md`
    - **docs/for-extension-developers/README.md:**
      - `grep -q 'pkg/extension/credfile' docs/for-extension-developers/README.md`
      - `grep -q '^## Credential resolution' docs/for-extension-developers/README.md` (new section heading)
      - `grep -q 'chmod 600' docs/for-extension-developers/README.md` (security guidance present)
      - `grep -q 'WithPath' docs/for-extension-developers/README.md` (option mention)
      - `grep -q 'WithStrictMode\|WithStrict\|strict' docs/for-extension-developers/README.md` (strict-mode mention; permissive across renames)
      - `grep -q 'examples/http-github-webhook/cmd/extbin/main.go' docs/for-extension-developers/README.md` (forward-link to worked example)
      - The existing "Start Here" section is intact: `grep -q '## Start Here' docs/for-extension-developers/README.md`
    - The Markdown fence count in `docs/for-extension-developers/README.md` is even (no broken code blocks introduced):
      - `[ "$(($(grep -c '^```' docs/for-extension-developers/README.md) % 2))" -eq "0" ]`
    - The Markdown fence count in `README.md` is unchanged from before (proxy: still even, since we only added bullet text, no code fences):
      - `[ "$(($(grep -c '^```' README.md) % 2))" -eq "0" ]`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'examples/http-github-webhook/README.md' README.md && awk '/^## Where to Go Next/,/^## /' README.md | grep -q 'examples/http-github-webhook/README.md' && grep -q '^## Credential resolution' docs/for-extension-developers/README.md && grep -q 'chmod 600' docs/for-extension-developers/README.md && grep -q 'examples/http-github-webhook/cmd/extbin/main.go' docs/for-extension-developers/README.md && [ "$(($(grep -c '^```' docs/for-extension-developers/README.md) % 2))" -eq "0" ] && [ "$(($(grep -c '^```' README.md) % 2))" -eq "0" ]</automated>
  </verify>
  <done>
    Root README has the new bullet under "Where to Go Next" pointing at the example's README. `docs/for-extension-developers/README.md` has a new "Credential resolution" section with the schema, default path, security note (chmod 600 + WithStrictMode), and a worked-example forward-link to `cmd/extbin/main.go`. No existing content disturbed.
  </done>
</task>

<task type="auto">
  <id>06-08-03</id>
  <name>Task 3: Comment-only refinement of .skytime-credentials.example to align with the README narrative</name>
  <read_first>
    - examples/http-github-webhook/.skytime-credentials.example (06-01 — the FILE; verify the four credential entries: github_token / basic_id / apikey_id / webhook_url)
    - examples/http-github-webhook/README.md (just authored in 06-08-01 — verify the cross-reference: the README's authenticated section says "paste GitHub PAT into github_token, paste webhook.site URL into webhook_url"; the .example comments should align with this narrative)
    - .planning/phases/06-example-project-http-github-webhook/06-CONTEXT.md (D-CREDS-FORMAT — schema is LOCKED; this task only touches comments)
  </read_first>
  <files>examples/http-github-webhook/.skytime-credentials.example</files>
  <action>
    Read the existing file (created by 06-01 with the locked TOML schema). Make COMMENT-ONLY refinements to align the inline guidance with the README narrative authored in 06-08-01.

    **Hard rule: do NOT change ANY of the following:**
    - Any TOML key (`[credentials.github_token]`, `type`, `token`, `username`, `password`, `key`, `value`).
    - Any of the four credential table headers.
    - The four `type =` values (`bearer`, `basic`, `apikey`, `bearer`).
    - The placeholder strings (`ghp_REPLACE_WITH_YOUR_PAT`, `https://webhook.site/REPLACE_WITH_YOUR_TOKEN`, `your-username`, `your-password`, `your-api-key-secret`, `X-API-Key`).

    **Permitted edits (comments only — lines starting with `#`):**
    1. After the existing chmod 600 callout in the header, ADD a one-line cross-reference: `# See README.md "Authenticated walkthrough" for the full setup story.`
    2. Above `[credentials.github_token]`, ADD a one-line context comment: `# GitHub fine-grained PAT — used by gh.list_open_issues / gh.get_issue / gh.add_comment / gh.add_label / gh.list_prs / gh.list_recent_merged_prs.`
    3. Above `[credentials.webhook_url]`, ADD a one-line context comment: `# webhook.site URL (the URL itself is the secret) — used by webhook.post in pr_to_webhook.star and weekly_digest.star.`

    DO NOT add comments above `[credentials.basic_id]` or `[credentials.apikey_id]` — those are demonstration entries showing the schema variants and don't map to a flow in this example. Adding flow-pointer comments there would be misleading.

    Optional permitted edit (only if the existing file's comment structure tolerates it — read first; if it would break readability, SKIP this):
    4. Above `[credentials.basic_id]`, add `# (demonstration of the schema for HTTP basic auth — not used by any flow in this example)`.
    5. Above `[credentials.apikey_id]`, add `# (demonstration of the schema for header-based API keys — not used by any flow in this example)`.

    Use the Edit tool. Each insertion is a single new line; do not rewrite the file. After editing, re-verify that all the acceptance criteria from 06-01-02 STILL hold (defense-in-depth — making sure we didn't accidentally break the schema).
  </action>
  <acceptance_criteria>
    - All 06-01-02 acceptance criteria still pass (the schema is preserved):
      - `grep -q '^\[credentials.github_token\]' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q '^\[credentials.basic_id\]' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q '^\[credentials.apikey_id\]' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q '^\[credentials.webhook_url\]' examples/http-github-webhook/.skytime-credentials.example`
      - `[ "$(grep -c '^type = "bearer"' examples/http-github-webhook/.skytime-credentials.example)" -ge "2" ]`
      - `grep -q '^type = "basic"' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q '^type = "apikey"' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q 'chmod 600' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q 'webhook.site' examples/http-github-webhook/.skytime-credentials.example`
    - At least ONE new comment cross-referencing the README:
      - `grep -q 'README' examples/http-github-webhook/.skytime-credentials.example`
    - At least ONE new comment naming a flow file (proves the comment edits actually happened):
      - `grep -E 'pr_to_webhook\.star|weekly_digest\.star|gh\.list_open_issues|gh\.add_comment' examples/http-github-webhook/.skytime-credentials.example`
    - The placeholder strings are unchanged (proves no schema/value drift):
      - `grep -q 'ghp_REPLACE_WITH_YOUR_PAT' examples/http-github-webhook/.skytime-credentials.example`
      - `grep -q 'https://webhook.site/REPLACE_WITH_YOUR_TOKEN' examples/http-github-webhook/.skytime-credentials.example`
    - File still parses as TOML (validated indirectly via the credfile resolver tests from 06-02 — running them must still pass):
      - `go test -race -count=1 ./pkg/extension/credfile/...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^\[credentials.github_token\]' examples/http-github-webhook/.skytime-credentials.example && grep -q '^\[credentials.webhook_url\]' examples/http-github-webhook/.skytime-credentials.example && grep -q 'chmod 600' examples/http-github-webhook/.skytime-credentials.example && grep -q 'README' examples/http-github-webhook/.skytime-credentials.example && grep -q 'ghp_REPLACE_WITH_YOUR_PAT' examples/http-github-webhook/.skytime-credentials.example && go test -race -count=1 ./pkg/extension/credfile/...</automated>
  </verify>
  <done>
    `.skytime-credentials.example` schema is unchanged (all 06-01 acceptance criteria still pass), new comment cross-references the README's "Authenticated walkthrough" section, and new comments above github_token + webhook_url name the flows that use them. The credfile resolver tests still pass — proof we didn't break the TOML parse.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# Example README has all seven sections
grep -q '^## What this example shows' examples/http-github-webhook/README.md && \
grep -q '^## Quick start' examples/http-github-webhook/README.md && \
grep -q '^## Coverage matrix' examples/http-github-webhook/README.md && \
grep -q '^## Authenticated walkthrough' examples/http-github-webhook/README.md && \
grep -q '^## Flow-by-flow tour' examples/http-github-webhook/README.md && \
grep -q '^## Running the tests' examples/http-github-webhook/README.md && \
grep -q '^## Building your own custom binary' examples/http-github-webhook/README.md

# The exact under-five-commands recipe (06-09 will run these)
grep -q 'go build -o ./extbin ./cmd/extbin' examples/http-github-webhook/README.md && \
grep -q -- '--flow public_repo_check' examples/http-github-webhook/README.md && \
grep -qF '"repo":"octocat/Hello-World"' examples/http-github-webhook/README.md

# chmod 600 callout
grep -q 'chmod 600' examples/http-github-webhook/README.md

# Coverage matrix headers verbatim
grep -q 'Flow.*seq.*block.*if_cond.*script.*for_each_par.*call_flow.*retries.*timeouts.*credentials.*cancellation' examples/http-github-webhook/README.md

# Root README addition
awk '/^## Where to Go Next/,/^## /' README.md | grep -q 'examples/http-github-webhook/README.md'

# Extension-developers credfile section
grep -q '^## Credential resolution' docs/for-extension-developers/README.md && \
grep -q 'chmod 600' docs/for-extension-developers/README.md

# Cred file unchanged schema, refined comments
grep -q '^\[credentials.github_token\]' examples/http-github-webhook/.skytime-credentials.example && \
grep -q 'README' examples/http-github-webhook/.skytime-credentials.example && \
go test -race -count=1 ./pkg/extension/credfile/...

# Full project still green
go build ./... && go test -race -count=1 ./...
```

All MUST exit 0.
</verification>

<success_criteria>
- `examples/http-github-webhook/README.md` has all seven sections in locked order; the four-command Quick Start matches RESEARCH § 6 verbatim; the coverage matrix headers match D-FLOWS-COVERAGE-MATRIX verbatim; chmod 600 callout (Pitfall 4) present; mock-router-pitfall callout (Pitfall 2) present
- Root README's "Where to Go Next" gains exactly one new bullet linking to the rich example — no other content disturbed
- `docs/for-extension-developers/README.md` gains a "Credential resolution: `pkg/extension/credfile/`" section with the TOML schema, default path, security note, and a worked-example forward-link
- `.skytime-credentials.example` schema is unchanged; comments now cross-reference the README's "Authenticated walkthrough" and name the flows that use github_token + webhook_url
- The full Go test suite still passes (no regressions to the credfile resolver tests, the flow tests, or the EX-03 tests authored in 06-07)
- 06-09's CI smoke can use the README's documented commands as its assertion target — the README is now the source-of-truth for the under-five-commands UX
</success_criteria>

<output>
After completion, create `.planning/phases/06-example-project-http-github-webhook/06-08-SUMMARY.md` with:
- Files modified (paths + line counts)
- Confirmation of the seven sections + the four-command Quick Start in the example README
- Confirmation of the root README addition placement (which existing bullet it sits next to)
- Confirmation of the credfile reference section in `docs/for-extension-developers/README.md`
- Note that 06-09's `walkthrough_smoke.sh` will execute the EXACT commands documented in the Quick Start — drift between the README and the smoke script is a CI failure
- Any deviations from D-DOCS-README's seven-section structure or D-FLOWS-COVERAGE-MATRIX with justification
</output>
</content>
</invoke>