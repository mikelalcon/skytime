---
phase: 04.3
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
autonomous: true
requirements:
  - D-15
must_haves:
  truths:
    - "docs/architecture.md exists at the documented path and serves both audiences (flow-authors AND extension-developers per CONTEXT.md D-15)"
    - "Document explains the parse/execute split — Starlark at parse time, Temporal at execution time, absolute boundary, no string compilation, no dynamic activities, no context bleed"
    - "Includes an ASCII boundary diagram showing user .star file → parser → DAG → Temporal SDK → workflow.Go → activity.GetInfo → extension I/O (per CONTEXT.md specifics)"
    - "Covers the lambda capture model: parse-time *starlark.Function captured with stable IDs; D4.2-05 dual-fail() carve-out reference; deterministic re-parse on workflow start (Option B from Phase 3)"
    - "Cites concrete source files for every architectural claim (parse/execute split → pkg/parser + pkg/interpreter; lambda capture → pkg/dag/lambda.go; bridge → pkg/bridge; activity → pkg/activity)"
    - "Document is grep-verifiable: contains every required phrase from acceptance_criteria; section headings match the locked structure"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Required-reading architecture overview for both audiences"
      contains: "## The Parse/Execute Split"
  key_links:
    - from: "docs/architecture.md"
      to: "pkg/parser/ + pkg/interpreter/ + pkg/bridge/ + pkg/activity/ + pkg/dag/"
      via: "Inline file-path references after each architectural claim"
      pattern: "pkg/(parser|interpreter|bridge|activity|dag)"
    - from: "docs/architecture.md"
      to: "docs/reference/builtins.md"
      via: "Cross-link from builtins discussion to the auto-generated reference"
      pattern: "docs/reference/builtins\\.md"
---

<objective>
Write `docs/architecture.md` — the required-reading architecture page for BOTH audiences (flow-authors and extension-developers per D-15). It explains the project's foundational architectural decision (parse/execute split) at a level that lets either audience understand WHY the constraints exist before they hit the constraints in their own code.

The doc must:
1. State the parse/execute split as the foundational design — Starlark at parse time (deterministic DAG generation, no I/O), Temporal at execute time (durable orchestration, history-recorded). The boundary is absolute and architectural.
2. Render the boundary as ASCII art diagram (per CONTEXT.md specifics — no Mermaid in v1).
3. Explain the lambda capture model: parse-time `*starlark.Function` instances are captured with stable content-hash IDs; the workflow re-parses on start (Option B from Phase 3) so lambdas survive Temporal serialization.
4. Document the three "no" rules from PROJECT.md: no string compilation, no dynamic activities, no context bleed. For each, explain WHY (architectural rationale) — referencing the D4.1-22 and D4.2-05 carve-outs.
5. Cite concrete source files (pkg/parser/globals.go, pkg/dag/lambda.go, pkg/interpreter/SkytimeWorkflow, pkg/activity/ExecuteBatch, pkg/bridge/CallLambda) so a reader can jump from prose to code.
6. End with cross-links: forward into docs/reference/builtins.md (auto-generated reference), into docs/for-flow-authors/ and docs/for-extension-developers/ (audience landings — created in plan 09).

Length target: 300-500 lines. Tone: pragmatic, code-forward, no marketing speak (matches CLAUDE.md's existing tone). Audience: someone with Go experience who has read the README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-CONTEXT.md
@CLAUDE.md
@pkg/parser/globals.go
@pkg/parser/doc.go
@pkg/dag/lambda.go
@pkg/interpreter/doc.go

<interfaces>
<!-- The locked section structure for docs/architecture.md. -->

## Required Section Headings (in order)

```
# Skytime Architecture

(introductory paragraph — 3-5 sentences; the parse/execute split is the project's whole reason to exist)

## The Parse/Execute Split

(THE foundational diagram + prose; ~80 lines)

## The Boundary Diagram

(ASCII art: user .star file → parser → DAG → Temporal SDK → workflow.Go → activity.GetInfo → extension I/O)

## Why Starlark at Parse Time

(no I/O, deterministic, lambdas-only, sandboxed; ~40 lines)

## Why Temporal at Execution Time

(durable, history-recorded, retryable, replay-deterministic; ~30 lines)

## Lambda Capture Model

(parse-time *starlark.Function captured with stable content-hash IDs; D-18 LambdaID format; ~40 lines)

## Deterministic Re-parse on Workflow Start

(Option B from Phase 3; FlowRegistry; content-hash keys; Build IDs handle versioning; ~30 lines)

## The Three "No" Rules

### No String Compilation

(reference D4.1-22 and D4.2-05 carve-outs; ~25 lines)

### No Dynamic Activities

(extensions are plain Go; activity.GetInfo is the ONE point that imports go.temporal.io/sdk/activity; ~20 lines)

### No Context Bleed

(workflow.Context never enters Starlark; *starlark.Thread never crosses the activity boundary; ~20 lines)

## Two-Tier Authoring Model

(library/extension developers in Go; consultants/integrators in Starlark; the boundary IS the audience split; ~25 lines)

## Where to Go Next

- Flow authors → [docs/for-flow-authors/README.md](for-flow-authors/README.md)
- Extension developers → [docs/for-extension-developers/README.md](for-extension-developers/README.md)
- Builtin reference → [docs/reference/builtins.md](reference/builtins.md)
- Getting started tutorial → [docs/getting-started.md](getting-started.md)
```

## ASCII Boundary Diagram (verbatim)

```
                           PARSE TIME (no I/O, deterministic)
                                            │
   user .star file ──► pkg/parser ──► dag.Flow (typed, JSON-serializable)
   (consultant team)    (Starlark        with captured *starlark.Function
                         interpreter)    instances keyed by stable content-hash
                                            │
                                            │
   ════════════════════════════════════════ │ ════════════════════════════
                                            │      WORKFLOW BOUNDARY
   ════════════════════════════════════════ │ ════════════════════════════
                                            │
                                            ▼
                            EXECUTE TIME (durable, history-recorded)
                                            │
   pkg/interpreter ──► workflow.Go ──► pkg/bridge ──► pkg/activity ──► extension I/O
   (SkytimeWorkflow)    (deterministic     (lambda eval)   (ExecuteBatch    (real HTTP/
                        fan-out via                         single generic    DB/queue
                        workflow.Selector)                  activity)         calls)

   The workflow re-parses the .star file at start (Option B, Phase 3) so
   *starlark.Function values survive Temporal serialization. Only LambdaID
   strings cross history. content-hash keys + Build ID versioning let
   in-flight workflows complete on the original code while new starts use
   updated code.
```

## Required Phrase Inventory (for grep-verification)

The doc MUST contain these exact phrases somewhere:
- "parse-time" (mentioned ≥ 5 times)
- "execution-time" or "execute-time" (mentioned ≥ 5 times)
- "no string compilation"
- "no dynamic activities"
- "no context bleed"
- "deterministic re-parse" or "re-parse on workflow start"
- "lambda capture" or "captured lambda"
- "content-hash" or "content hash"
- "FlowRegistry"
- "workflow.Context never"
- "ExecuteBatch"
- "SkytimeWorkflow"
- "two-tier" or "two tier"
- "starlark.Function"
- "syntax.Position"
- "docs/reference/builtins.md"
- "docs/getting-started.md"
- "D4.1-22" (the carve-out reference)
- "D4.2-05" (the dual-fail carve-out reference)
- The exact ASCII diagram block (verbatim from above)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write docs/architecture.md with locked section structure + ASCII diagram</name>
  <files>docs/architecture.md</files>
  <read_first>
    - .planning/PROJECT.md (the parse/execute split rationale + the D4.1-22 and D4.2-05 carve-out blockquotes — verbatim quotation candidates)
    - pkg/parser/doc.go (the dual-fail() example — useful prior art for showing parse-time vs lambda-time)
    - pkg/parser/globals.go (newParseTimeGlobals — primary citation for "parse-time globals" claim)
    - pkg/dag/lambda.go (CapturedLambda struct — primary citation for "lambda capture model" section)
    - pkg/interpreter/doc.go (SkytimeWorkflow doc — primary citation for "execute-time" section)
    - pkg/activity/ (browse to confirm ExecuteBatch is the file/function name to cite)
    - pkg/bridge/ (browse for CallLambda citation)
    - .planning/STATE.md decisions section (look for Phase 3 Option B decision — quote the rationale)
    - .planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-CONTEXT.md (D-15 + specifics on ASCII diagram + canonical references)
  </read_first>
  <behavior>
    File is hand-written prose; verification is grep-based. The doc is structurally complete when:
    - Every locked section heading from <interfaces> appears as an H1/H2/H3 in the same order
    - The ASCII boundary diagram appears verbatim
    - The required phrase inventory all appears (each phrase present ≥ 1 time, with "parse-time" and "execution-time"/"execute-time" each appearing ≥ 5 times)
    - Cross-links to docs/reference/builtins.md and docs/getting-started.md are present
    - Source-file citations are present for major architectural claims
  </behavior>
  <action>
    Step 1 — Read the canonical sources listed in <read_first> to ensure architectural claims match shipped code. Pay special attention to:
    - PROJECT.md "Strict directives from the spec" block — copy the D4.1-22 and D4.2-05 blockquotes verbatim into the "No String Compilation" section
    - PROJECT.md Constraints section — Architecture row ("Strict parse/execute separation. — Required for the safety properties (no I/O at parse, no Go escape hatch at execute, no context bleed)") — quote in the introduction
    - STATE.md decisions for Phase 3 Option B (re-parse on workflow start) — confirm the rationale in your "Deterministic Re-parse" section

    Step 2 — Create docs/architecture.md following the locked structure. Use a pragmatic, code-forward tone. Each major section should:
    - State the claim in the first sentence
    - Cite the source-of-truth file (e.g., "pkg/parser/globals.go::newParseTimeGlobals")
    - Show one ≤8-line example or ASCII fragment
    - Explain the rationale (the WHY)

    Section-by-section content guide:

    **Introduction (above first H2):** "Skytime is a Go library that lets teams declare durable workflows in Starlark and execute them on Temporal. The boundary between Starlark (parse-time, deterministic graph generation) and Temporal (execution-time, durable orchestration) is absolute and architectural. This page exists because every other architectural choice — extension contract, lambda capture, error rendering, the entire CLI — is downstream of that one boundary."

    **The Parse/Execute Split:** Open with "Skytime splits workflow authoring into two phases that do not communicate via shared mutable state, runtime callbacks, or escape hatches." List what happens at parse time (3-5 bullets — Starlark interpretation, lambda capture, kwarg validation, DAG construction, no I/O). List what happens at execute time (3-5 bullets — Temporal workflow, lambda re-eval inside workflow, activity dispatch, retries, history recording). Cite pkg/parser and pkg/interpreter.

    **The Boundary Diagram:** Reproduce the ASCII art diagram from <interfaces> verbatim inside a fenced code block (no language tag — plain ASCII). Add a short "How to read this diagram" paragraph below.

    **Why Starlark at Parse Time:** Cover the four properties: deterministic, sandboxed (lambda-time globals strict subset, see pkg/bridge/lambda_globals.go), expressive enough (lambdas + struct injection give data access without string parsers — quote PROJECT.md Key Decisions row "Starlark over CEL or custom DSL"), and parser-pure (Parser.Parse is a pure function of file contents, see PROJECT.md Key Decision row).

    **Why Temporal at Execution Time:** Cover three properties: durable (workflow history is the source of truth), retryable (RetryPolicy + Timeouts on every step, DSL-08), composable (call_flow → child workflows). Reference pkg/interpreter/SkytimeWorkflow as THE entry point.

    **Lambda Capture Model:** Open with the problem statement: "Lambdas are captured at parse time and must survive Temporal's serialization boundary." Show the CapturedLambda struct (verbatim from pkg/dag/lambda.go — copy the type definition into the doc). Explain D-18 stable IDs (`sha256(fileBytes)[:8] + ":" + line + ":" + col`). Quote the D4.1-22 and D4.2-05 carve-outs verbatim from PROJECT.md.

    **Deterministic Re-parse on Workflow Start:** Open with "Phase 3 chose Option B (re-parse on workflow start) over a custom DataConverter." Explain: the workflow boots a `FlowRegistry` keyed by content-hash; `WorkflowInput` carries `{FlowName, ContentHash, InitState}` over the wire; only `LambdaID` strings cross Temporal history. Versioning is operational via Temporal Build IDs — running workflows complete on the original code, new starts use updated code.

    **The Three "No" Rules:**
    - **No String Compilation:** Quote the strict directive verbatim: "No string compilation (no CEL, no string parsers for conditionals/data mapping) — only native Starlark lambdas." Then quote the D4.1-22 carve-out blockquote verbatim. Then explain WHY: "String parsers re-introduce the parse-time evaluation surface that lambdas eliminate." Cite pkg/parser/builtins.go::desugarInterpolation as the SINGLE place where ${ctx.expr} desugars (and only at parse time, into a real lambda).
    - **No Dynamic Activities:** "Extensions are plain Go functions; they never import `go.temporal.io/sdk/activity`." Cite the firewall test (pkg/activity/firewall_test.go) and the test name (`TestNoTemporalImportsInExtensionPackage`). Explain: dynamic activity registration would couple extensions to Temporal's lifecycle, break the single-generic-activity batching model (ACT-01), and prevent plain-Go testing of extensions.
    - **No Context Bleed:** "Never pass `workflow.Context` into a Starlark thread, never pass a Starlark `*starlark.Thread` over the network into an activity." Cite pkg/bridge/CallLambda (constructs a fresh thread; thread.Cancel wires to a native chan struct{} bridged from workflow.Context.Done() via workflow.Go reader, see pkg/interpreter/cancel_watchdog.go). Explain: any path that lets workflow.Context leak into Starlark breaks workflowcheck (no native goroutines, no time.*, no rand.* inside workflow code).

    **Two-Tier Authoring Model:** "Library/extension developers and workflow authors live on opposite sides of the parse/execute boundary." Library devs work in Go (pkg/extension): typed kwargs, pure data, return ActionRef intents. Consultants work in Starlark (.star files): compose extensions, write lambdas, define flows. The Go layer is reusable across customers; the Starlark layer is per-customer. Cross-link to docs/for-flow-authors/ and docs/for-extension-developers/.

    **Where to Go Next:** Cross-link bullet list — flow authors landing, extension developers landing, builtin reference, getting started tutorial.

    Step 3 — Write the file using the Write tool. Aim for 300-500 lines including code blocks and the ASCII diagram. Use markdown idioms: H2 for major sections, H3 for sub-sections, fenced code blocks with language tags (`go`, `python` for .star, none for ASCII).

    Step 4 — Verify the doc passes the grep checklist (run the verify command).
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && wc -l docs/architecture.md && grep -c -E '^## ' docs/architecture.md && grep -c -E '^### ' docs/architecture.md && grep -c 'parse-time' docs/architecture.md && grep -c -E 'execution-time|execute-time' docs/architecture.md && grep -c 'docs/reference/builtins.md' docs/architecture.md && grep -c 'workflow.Context never\|workflow\.Context.*never' docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l docs/architecture.md` shows >= 200 and <= 800 lines
    - `grep -c -E '^## ' docs/architecture.md` returns >= 8 (8 H2 sections per locked structure)
    - `grep -c -E '^### ' docs/architecture.md` returns >= 3 (3 H3 sub-sections under "The Three No Rules")
    - `grep -nE '^## (The Parse/Execute Split|The Boundary Diagram|Why Starlark at Parse Time|Why Temporal at Execution Time|Lambda Capture Model|Deterministic Re-parse on Workflow Start|The Three .No. Rules|Two-Tier Authoring Model|Where to Go Next)' docs/architecture.md` returns 9 matches (all 9 H2 headings present)
    - `grep -nE '^### (No String Compilation|No Dynamic Activities|No Context Bleed)' docs/architecture.md` returns 3 matches
    - `grep -c 'parse-time' docs/architecture.md` returns >= 5
    - `grep -c -E 'execution-time|execute-time' docs/architecture.md` returns >= 5
    - `grep -c 'no string compilation\|No String Compilation' docs/architecture.md` returns >= 2
    - `grep -c 'no dynamic activities\|No Dynamic Activities' docs/architecture.md` returns >= 2
    - `grep -c 'no context bleed\|No Context Bleed' docs/architecture.md` returns >= 2
    - `grep -c 'content.hash\|FlowRegistry' docs/architecture.md` returns >= 2
    - `grep -c 'CapturedLambda\|starlark.Function' docs/architecture.md` returns >= 2
    - `grep -c 'ExecuteBatch' docs/architecture.md` returns >= 1
    - `grep -c 'SkytimeWorkflow' docs/architecture.md` returns >= 1
    - `grep -c 'D4.1-22\|D4\\.1-22' docs/architecture.md` returns >= 1
    - `grep -c 'D4.2-05\|D4\\.2-05' docs/architecture.md` returns >= 1
    - `grep -c 'two-tier\|two tier\|Two-Tier\|Two Tier' docs/architecture.md` returns >= 1
    - `grep -c 'docs/reference/builtins.md' docs/architecture.md` returns >= 1
    - `grep -c 'docs/getting-started.md' docs/architecture.md` returns >= 1
    - `grep -nE 'pkg/(parser|interpreter|bridge|activity|dag)' docs/architecture.md | wc -l` returns >= 5 (source-file citations)
    - `grep -c 'WORKFLOW BOUNDARY' docs/architecture.md` returns 1 (the ASCII diagram divider line)
    - `grep -c 'pkg/parser ──►\|workflow.Go ──►' docs/architecture.md` returns >= 2 (ASCII diagram arrows)
  </acceptance_criteria>
  <done>
    docs/architecture.md exists with all 9 H2 sections + 3 H3 sub-sections in locked order. ASCII boundary diagram is present verbatim. The doc cites concrete source files (pkg/parser, pkg/interpreter, pkg/bridge, pkg/activity, pkg/dag) for major architectural claims. The D4.1-22 and D4.2-05 carve-outs are quoted verbatim. Cross-links to docs/reference/builtins.md, docs/getting-started.md, and the audience landings exist (the audience landings will be created by plan 09).
  </done>
</task>

</tasks>

<verification>
**Plan-level verification:**
- docs/architecture.md exists at the documented path
- All 9 H2 sections + 3 H3 sub-sections present in the locked order
- ASCII boundary diagram present verbatim
- All required phrases from <interfaces> Required Phrase Inventory present (each at least the specified count)
- Cross-links to docs/reference/builtins.md (plan 02 output) and docs/getting-started.md (plan 07) and audience landings (plan 09) are present
- Source-file citations: pkg/parser, pkg/interpreter, pkg/bridge, pkg/activity, pkg/dag each appear ≥ 1 time
- D4.1-22 and D4.2-05 carve-outs quoted verbatim
- File length 200-800 lines
</verification>

<success_criteria>
- docs/architecture.md is the required-reading architecture doc for both audiences (D-15)
- The parse/execute split is rendered both prosaically and via ASCII diagram
- The three "no" rules are explained with rationale and source-file citations
- The doc bridges high-level claims (e.g., "deterministic re-parse on workflow start") to concrete source locations a reader can jump to
- Cross-linking is bidirectional with planned downstream pages (reference, tutorial, audience landings)
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-03-SUMMARY.md` documenting:
- Final file created (1: docs/architecture.md)
- Section structure (9 H2 + 3 H3)
- Length (line count)
- Key claims and their source citations
- Any deviations (e.g., if a section needed to be merged or split for length; if the ASCII diagram needed adjustment for terminal-width readability)
- Open items for downstream plans: docs/getting-started.md (plan 07), docs/for-flow-authors/README.md (plan 09), docs/for-extension-developers/README.md (plan 09)
</output>
