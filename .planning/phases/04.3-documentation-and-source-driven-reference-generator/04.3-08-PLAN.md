---
phase: 04.3
plan: 08
type: execute
wave: 2
depends_on: ["04.3-03", "04.3-07"]
files_modified:
  - README.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-06
must_haves:
  truths:
    - "README.md is the repo front door — covers what Skytime is, why it exists, the parse/execute pitch, and embeds the full getting-started tutorial verbatim (D-02 mid-size, D-06 embed)"
    - "Length is mid-sized — target 500-800 lines per D-02 (NOT a thin link-page; readers can evaluate the project without clicking through)"
    - "Embeds docs/getting-started.md content VERBATIM (D-06) including its H2 headings (## Prerequisites, ## 1. Clone..., ...) under the README's ## Getting Started H2 — no heading demotion; markdown renders nested H2s fine. Attribution note at the top of the section points at the canonical file"
    - "Includes a 'What This Is / What This Is Not' upfront so readers know within 30 seconds whether Skytime fits their problem"
    - "Cross-links into docs/ for deeper material — architecture, reference, audience landings, examples"
    - "No status badges, no marketing speak; pragmatic + code-forward tone matching CLAUDE.md"
  artifacts:
    - path: "README.md"
      provides: "Repo front door — pitch + embedded tutorial + links"
      contains: "# Skytime"
  key_links:
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "Verbatim embed of the tutorial body + attribution note"
      pattern: "docs/getting-started\\.md|getting started"
    - from: "README.md"
      to: "docs/architecture.md + docs/reference/builtins.md + docs/reference/cli.md + examples/README.md"
      via: "Cross-links section"
      pattern: "docs/(architecture|reference|getting-started)"
    - from: "README.md"
      to: "docs/for-flow-authors/README.md + docs/for-extension-developers/README.md"
      via: "Audience-split entry points (created by plan 09)"
      pattern: "docs/for-(flow-authors|extension-developers)"
---

<objective>
Write `README.md` (D-02 mid-size, D-06 embed) as the repo front door. It must serve as the single page that lets a new reader (a) understand what Skytime is and isn't within 30 seconds, (b) walk through the canonical getting-started tutorial without clicking out, and (c) know where to go next based on their goal (write a flow, build an extension, learn the architecture).

The README must:
1. **Hero pitch** — "Skytime is a Go library that lets teams declare durable workflows in Starlark and execute them on Temporal." + the parse/execute split as the foundational design.
2. **What This Is / What This Is Not** — bullet lists. Helps readers self-select within 30 seconds.
3. **Status** — be honest about phase progress (Phase 1-4 + 04.1 + 04.2 complete; Phase 5 + 6 are next; production-readiness assessment).
4. **Embedded tutorial** — verbatim copy of `docs/getting-started.md`'s body (steps 1-5 + What Just Happened) per D-06. Add a top note pointing readers at the canonical file.
5. **Where to Go Next** — section with cross-links by audience: flow authors → docs/for-flow-authors/, extension developers → docs/for-extension-developers/, architecture-first readers → docs/architecture.md, reference seekers → docs/reference/builtins.md.
6. **Project layout** — mini tree showing top-level directories (cmd/, pkg/, examples/, docs/, tests/) with one-line descriptions.
7. **Contributing** — minimal — what `make test` runs, where to file issues. Optional; can be a paragraph not a section.
8. **License** — license name + link to LICENSE file (if a LICENSE file exists; if not, omit and note "license TBD" — verify by `ls LICENSE*` first).

Length target: 500-800 lines per D-02. Tone: pragmatic, code-forward, no marketing speak, no emoji density. Audience: a developer who arrived from Google or a colleague's Slack message and has 60 seconds to evaluate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-CONTEXT.md
@CLAUDE.md
@docs/getting-started.md
@docs/architecture.md
@examples/README.md
@docs/reference/builtins.md
@docs/reference/cli.md
@docs/for-flow-authors/extensions/http.md

<interfaces>
<!-- Locked structure for README.md. -->

## Required Section Structure (locked)

```
# Skytime

(one-liner pitch: "Starlark-defined durable workflows on Temporal")

(intro paragraph — 4-6 sentences explaining what + why; mentions
parse/execute split + two-tier authoring)

## What This Is

(bullet list — 5-7 items; concrete capabilities)

## What This Is Not

(bullet list — 5-7 items; explicit non-goals from PROJECT.md Out of Scope)

## Status

(honest phase progress; what's shipping; what's next; production-readiness note)

## Getting Started

(EMBEDDED VERBATIM — body of docs/getting-started.md from "## Prerequisites"
through "## What Just Happened?" copied AS-IS, including the H2 headings
("## Prerequisites", "## 1. Clone and Build", etc.). Add a top note
attributing the canonical file. The README ends up with multiple H2s in
the Getting Started region; that is correct per D-06 verbatim. NOTE: D-06
says copy-edit OK for v1; do not maintain a sync mechanism)

## Where to Go Next

(audience-split cross-links + reference + architecture)

## Project Layout

(mini tree showing cmd/, pkg/, examples/, docs/, tests/ with descriptions)

## Contributing

(brief — go test ./..., go vet, file issues, etc.)

## License

(license name + link if LICENSE file exists; otherwise omit or note "TBD")
```

## Hero Pitch (locked verbatim)

```markdown
# Skytime

> Starlark-defined durable workflows on Temporal — a strict parse/execute
> split that gives consultant teams a safe DSL on top of Temporal's
> retry/timeout/child-workflow guarantees.

Skytime is a Go library that lets teams declare durable workflows in
Starlark and execute them on Temporal. The boundary between Starlark
(parse-time, deterministic graph generation) and Temporal
(execution-time, durable orchestration) is **absolute and architectural**
— no string compilation, no dynamic activities, no context bleed.

The two-tier authoring model:

- **Library/extension developers** write Go *extensions* — typed I/O wrappers, reusable across customers
- **Consultant/integrator teams** compose those extensions in `.star` files specialized per customer

A consultant team can take an extension catalog and a customer brief,
write a `.star` file, and have a production-grade durable workflow
running on Temporal — without touching Go and without giving up
Temporal's retry/timeout/child-workflow guarantees.
```

## What This Is (bullet list — locked verbatim)

- Six naked DSL primitives (`flow`, `step`, `if_cond`, `script`, `for_each_parallel`, `call_flow`) with parse-time DAG generation
- Lambda capture with content-hash IDs; lambdas evaluate inside the workflow against state injected as nested struct types
- Single generic Temporal activity (`ExecuteBatch`) batches idempotent extension I/O; non-idempotent ops execute one-per-invocation
- Just-in-time credential resolution — secrets never enter workflow state
- Static validation tier (`skytime validate`) sharing the parser with the runtime; CI corpus differential test pins parse + dryrun agreement
- Embedded HTTP extension out of the box; bring-your-own for GitHub/Slack/AWS/etc.
- Bazel-style colored CLI: `skytime run --flow=... --input=...` streams progress live; multi-line block on TTY, static line-per-event under `--verbose`
- Expression-mode `if_cond` with strict-equality result-binding (Phase 04.2 — the most distinctive parse-time type system feature)

## What This Is Not (bullet list — locked verbatim)

- Not a hosted SaaS. Skytime is a Go library; deployment is your problem.
- Not a CEL/JSONPath/Jinja replacement. Lambdas only — runtime template engines are explicitly forbidden (parser-time syntactic sugar like `${ctx.expr}` is permitted, see [docs/architecture.md](docs/architecture.md#no-string-compilation)).
- Not a Temporal alternative. Skytime sits ON TOP of Temporal; you still need a Temporal cluster (cloud or self-hosted; `skytime dev-server` for local).
- Not a workflow versioning helper. Use Temporal's Build IDs + content-addressed flows; Skytime-specific versioning is out of scope for v1.
- Not a Web UI. Temporal's UI is sufficient for v1.
- Not Starlark-the-language with full Python compatibility — `lambda` and `def` are usable; some stdlib (time, random, I/O) is NOT in the lambda-time predeclared environment.
- Not Tier 2 (Starlark unit tests) in v1. Tier 1 (static validation) and Tier 3 (E2E with `temporal_test`) ship in v1; pure-Starlark unit testing of `def` blocks moves to v2.

## Status (template)

```markdown
## Status

Pre-v1; foundational architecture stable. Phase progress:

- ✓ **Phase 1** — Type spine, extension contract, parser/bridge foundations (22/22 reqs)
- ✓ **Phase 2** — Generic activity, block-batch dispatch, credentials (6/6 reqs)
- ✓ **Phase 3** — Lambda-serialization decision (Option B: re-parse on workflow start), interpreter, worker (10/10 reqs)
- ✓ **Phase 4** — Static validation, CLI skeleton (`skytime run`/`validate`/`info`/`dev-server`), HTTP extension (7/7 reqs)
- ✓ **Phase 04.1** — Dynamic step kwargs, `${ctx.expr}` interpolation, multi-line live progress (7/7 reqs)
- ✓ **Phase 04.2** — Expression-mode `if_cond` with strict-equality result-binding (3/3 reqs)
- ⏳ **Phase 5** — Tier-3 E2E test harness (`temporal_test` Starlark builtin)
- ⏳ **Phase 6** — Example project (HTTP + GitHub + Slack) — the proof-of-life

**v1 readiness:** 65/65 v1 requirements mapped to phases; 55/65 complete
(see [.planning/REQUIREMENTS.md](.planning/REQUIREMENTS.md)). Phases 5-6
deliver the test harness + dogfooded example before v1 is declared done.

**Production-readiness:** the runtime path (Phases 1-4) has been
shipped with full test coverage including replay-determinism assertions
and `workflowcheck` analysis. Use it for non-critical workloads today;
audit the credential-resolver wiring in your CLI binary before
production rollout.
```

## Project Layout (template)

```markdown
## Project Layout

```
skytime/
├── cmd/
│   ├── skytime/            # the CLI binary (cmd/skytime/main.go is a thin wrapper around pkg/cli)
│   └── skytime-docgen/     # AST-driven generator for docs/reference/builtins.md
├── pkg/
│   ├── parser/             # Starlark → dag.Flow parser (D-09: pure function of file bytes)
│   ├── bridge/             # *starlarkstruct.Struct construction + CallLambda
│   ├── dag/                # Pure data types (Flow, Step, IfCond, Script, ForEachParallel, CallFlow, Result, Fail, ActionRef)
│   ├── extension/          # Extension SDK + Credential types + per-parser registry
│   │   └── builtin/http/   # Bundled HTTP extension (5 ops; D4-14 idempotence)
│   ├── activity/           # Single generic ExecuteBatch activity (Temporal-specific code lives here)
│   ├── interpreter/        # SkytimeWorkflow — the generic interpreter
│   ├── worker/             # Worker bootstrap (3 named client constructors)
│   ├── validator/          # Thin facade for static validation; AlwaysOkDispatch dryrun
│   └── cli/                # Reusable cobra root (NewRootCommand + WithExtensions/WithCredentialHandler)
├── examples/skeleton/      # 3 hand-curated .star fixtures (CI-tested via tests/differential_test.go)
├── docs/                   # Hand-written narrative + auto-gen reference
│   ├── architecture.md     # Required reading: parse/execute split + boundary diagram
│   ├── getting-started.md  # The canonical 5-10 min tutorial
│   ├── reference/          # docs/reference/builtins.md is auto-generated (cmd/skytime-docgen)
│   ├── for-flow-authors/   # Starlark consultants
│   ├── for-extension-developers/  # Go developers
│   └── cli-binary.md       # Building binaries with custom extensions
└── tests/                  # Cross-package firewalls + corpus differential tests
```
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify LICENSE existence and detect any blockers</name>
  <files></files>
  <read_first>
    - (none — this is a discovery step using shell commands)
  </read_first>
  <behavior>
    Run a small shell verification step before composing README.md:
    - `ls -1 LICENSE*` — does a LICENSE file exist? If yes, the README's License section links to it; if no, the section says "TBD" with a one-line note
    - `ls -1 README.md` — does a README already exist? If yes, READ it before overwriting; we may want to preserve any existing content the user added (this plan creates a new README; existing content gets superseded)
    - Confirm `docs/getting-started.md` exists (created by plan 07) — if not, FAIL the task with a clear message: this plan depends on plan 07 + plan 03 per frontmatter `depends_on`
    - Confirm `docs/architecture.md` exists (created by plan 03) — same dependency check
  </behavior>
  <action>
    Run from the module root:
    ```sh
    ls -1 LICENSE* 2>/dev/null
    ls -1 README.md 2>/dev/null
    ls -1 docs/getting-started.md docs/architecture.md docs/reference/builtins.md docs/reference/cli.md examples/README.md docs/for-flow-authors/extensions/http.md 2>&1
    ```
    Record results. If `docs/getting-started.md` or `docs/architecture.md` does NOT exist, do NOT proceed to Task 2 — instead surface a clear error: "Plan 04.3-08 depends on plans 04.3-03 and 04.3-07; please execute those first."

    If `LICENSE*` does NOT exist, the License section in Task 2 will say "TBD; license to be selected before v1." If `LICENSE` does exist (any case), Task 2 references it via relative link `[LICENSE](LICENSE)`.

    If `README.md` already exists with content beyond a stub, READ it carefully — there may be content the user wants preserved (e.g. a reference to a personal blog post, a roadmap link). Either preserve those bits in the new file or note in the SUMMARY that they were superseded.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && ls -1 docs/getting-started.md docs/architecture.md 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/getting-started.md` exits 0 (plan 07 dependency satisfied)
    - `ls docs/architecture.md` exits 0 (plan 03 dependency satisfied)
    - LICENSE existence is recorded for the License section content choice
  </acceptance_criteria>
  <done>
    Discovery step complete. Dependencies on plan 03 and plan 07 verified. LICENSE existence determined.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compose README.md with locked structure + embedded tutorial body</name>
  <files>README.md</files>
  <read_first>
    - docs/getting-started.md (entire file — Task 2 EMBEDS the body verbatim per D-06; only the # H1 header is replaced with a `## Getting Started` H2)
    - docs/architecture.md (skim — for cross-link context only; do NOT embed)
    - .planning/PROJECT.md (Hero pitch raw material; What This Is bullets; Out of Scope → What This Is Not bullets)
    - .planning/ROADMAP.md (Phase progress for the Status section; phase numbering)
    - .planning/REQUIREMENTS.md (count totals — 65 mapped, 55 complete — for the Status section)
    - CLAUDE.md (tone — pragmatic, no marketing speak)
    - .planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-CONTEXT.md (D-02 mid-size + D-06 embed)
  </read_first>
  <behavior>
    Hand-written prose with one large embedded block (the tutorial body). Verification via grep:
    - All H2 sections present per locked structure: What This Is, What This Is Not, Status, Getting Started, Where to Go Next, Project Layout, Contributing, License
    - Hero pitch verbatim from <interfaces>
    - What This Is and What This Is Not bullet lists verbatim from <interfaces>
    - Status section has phase checkmarks for 1-04.2 + ⏳ for 5-6
    - Getting Started section embeds docs/getting-started.md body (Prerequisites through What Just Happened)
    - Project Layout tree present with cmd/, pkg/, examples/, docs/, tests/ entries
    - Where to Go Next has audience-split links to docs/for-flow-authors/, docs/for-extension-developers/, plus architecture/reference cross-links
    - File length 500-800 lines (D-02)
  </behavior>
  <action>
    Step 1 — Read docs/getting-started.md in full. The tutorial body (from `## Prerequisites` through `## What Just Happened?`) will be embedded VERBATIM (per D-06) under the README's `## Getting Started` H2 section. The embedded H2 headings (`## Prerequisites`, `## 1. Clone and Build`, etc.) are preserved as-is; markdown renders multiple H2s in a document just fine, and verbatim preservation is the locked requirement (D-06: "tutorial file is canonical; README reproduces by reference"). Result: the README has multiple `^## ` lines coming from the embedded tutorial, in addition to the README's own H2 sections — that is correct and intentional.

    Step 2 — Compose README.md following the locked structure:

    1. Top: hero pitch verbatim from <interfaces>.
    2. `## What This Is` — bullet list verbatim from <interfaces>.
    3. `## What This Is Not` — bullet list verbatim from <interfaces>.
    4. `## Status` — phase progress per the template; verify counts against ROADMAP.md (Phase 1-4 + 04.1 + 04.2 complete; Phase 5 + 6 ⏳; 65/65 v1 reqs mapped, 55/65 complete).
    5. `## Getting Started` — open with the canonical-file note:
       ```markdown
       ## Getting Started

       > **Canonical version:** [`docs/getting-started.md`](docs/getting-started.md).
       > The same content is embedded below for readers evaluating the
       > project; the canonical file may diverge slightly between releases
       > (run `git diff docs/getting-started.md README.md` to check).
       ```
       Then embed the body of docs/getting-started.md from `## Prerequisites` through `## What Just Happened?` VERBATIM — DO NOT demote heading levels (per D-06 "embed verbatim" requirement). The tutorial's `## Prerequisites`, `## 1. Clone and Build`, ..., `## What Just Happened?` headings are preserved as H2s in the README, sitting nested under the README's own `## Getting Started` H2. (Markdown renders adjacent H2s fine; the canonical-version note + the relative position make the embedded section's scope obvious.) The conceptual block H3s under `## What Just Happened?` (e.g., `### ${ctx.expr} Interpolation`) likewise stay at H3.
    6. `## Where to Go Next` — same body as docs/getting-started.md::"Next Steps" section but tightened slightly to remove "You've now seen Skytime end-to-end" preamble; just the bullet list of cross-links.
    7. `## Project Layout` — render the tree from <interfaces> Project Layout template.
    8. `## Contributing` (3-4 paragraphs):
       ```markdown
       ## Contributing

       Skytime is a single-author project right now. Issues and PRs welcome.

       Run the full test suite before opening a PR:

       ```sh
       go build ./...
       go vet ./...
       go test -race ./...
       go generate ./pkg/parser/  # refresh docs/reference/builtins.md if you touched a builtin
       ```

       The codebase enforces a few firewalls via tests under
       [`tests/`](tests/):

       - `tests/firewall_cli_test.go` — only `pkg/cli` and `cmd/skytime` may import cobra/charm-log/lipgloss
       - `pkg/activity/firewall_test.go` — only `pkg/activity`, `pkg/interpreter`, `pkg/worker`, `pkg/cli` may import `go.temporal.io/sdk/...`
       - `tests/differential_test.go` — every `examples/skeleton/*.star` runs through both static `validator.Validate` AND a dry-run interpreter; drift fails CI
       - `tests/docgen_drift_test.go` — `docs/reference/builtins.md` must match a fresh `go generate ./pkg/parser/` run; drift fails CI

       Adding a new DSL builtin? Add a `// skytime:doc summary="..."`
       block above the `builtinXxx` function in `pkg/parser/builtins.go`
       and run `go generate ./pkg/parser/`. The reference doc updates
       automatically.
       ```
    9. `## License` — based on Task 1's discovery:
       - If LICENSE exists: `## License\n\nSee [LICENSE](LICENSE).`
       - If not: `## License\n\nLicense to be selected before v1. The repository is currently published as the work-in-progress source; treat as private/unlicensed for downstream use until a license file lands.`

    Step 3 — Write the file using Write tool. Aim for 500-800 lines total.

    Step 4 — Verify the doc passes the grep checklist.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && wc -l README.md && grep -nE '^## ' README.md && grep -c 'parse/execute\|parse-time\|execute-time' README.md && grep -c 'docs/getting-started.md\|docs/architecture.md\|docs/reference/builtins.md\|docs/for-flow-authors\|docs/for-extension-developers' README.md && grep -c 'check_user' README.md && grep -c 'expression_if.star' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l README.md` shows >= 400 and <= 1000 lines (D-02 mid-size; tighter than the strict 500-800 to allow some slack)
    - `grep -c -E '^## ' README.md` returns >= 14 (8 README H2 sections + 6 verbatim-embedded H2s from docs/getting-started.md: Prerequisites, 1. Clone..., 2. Start..., 3. Discover..., 4. Run the Happy..., 5. Run the Fail..., What Just Happened?)
    - `grep -c '^## Prerequisites' README.md` returns 1 (verbatim-embedded H2 from getting-started.md preserved per D-06)
    - `grep -c '^## 1\. Clone and Build' README.md` returns 1 (verbatim-embedded numbered step H2 preserved)
    - `grep -c '^## What Just Happened?' README.md` returns 1 (verbatim-embedded H2 preserved)
    - For each embedded H2, the corresponding line in docs/getting-started.md must be byte-equal: `diff <(grep -E '^## (Prerequisites|1\. Clone|2\. Start|3\. Discover|4\. Run|5\. Run|What Just)' docs/getting-started.md) <(grep -E '^## (Prerequisites|1\. Clone|2\. Start|3\. Discover|4\. Run|5\. Run|What Just)' README.md)` returns 0 lines (identical)
    - `grep -nE '^## (What This Is|What This Is Not|Status|Getting Started|Where to Go Next|Project Layout|Contributing|License)' README.md` returns 8 matches (the README's own 8 H2 sections; embedded tutorial H2s are additional and verified separately)
    - `grep -c 'parse/execute' README.md` returns >= 2 (hero + What Is or Status)
    - `grep -c 'two-tier\|two tier\|Two-Tier' README.md` returns >= 1 (the authoring model concept)
    - `grep -c 'docs/getting-started.md' README.md` returns >= 1 (canonical attribution)
    - `grep -c 'docs/architecture.md' README.md` returns >= 1
    - `grep -c 'docs/reference/builtins.md\|reference/builtins.md' README.md` returns >= 1
    - `grep -c 'docs/for-flow-authors\|for-flow-authors' README.md` returns >= 1
    - `grep -c 'docs/for-extension-developers\|for-extension-developers' README.md` returns >= 1
    - `grep -c 'examples/README.md\|examples/skeleton' README.md` returns >= 1
    - `grep -c 'cli-binary.md' README.md` returns >= 1
    - `grep -c 'check_user' README.md` returns >= 2 (in embedded tutorial)
    - `grep -c 'expression_if.star' README.md` returns >= 2 (in embedded tutorial)
    - `grep -c 'skytime dev-server' README.md` returns >= 2 (in embedded tutorial commands)
    - `grep -c 'Phase 1\|Phase 2\|Phase 3\|Phase 4\|Phase 5\|Phase 6\|Phase 04.1\|Phase 04.2' README.md` returns >= 6 (Status section enumerates phases)
    - `grep -c 'Starlark\|starlark' README.md` returns >= 5
    - `grep -c 'Temporal\|temporal' README.md` returns >= 5
    - `grep -c 'No string compilation\|no string compilation' README.md` returns >= 1 (mentioned in What This Is Not OR linked)
    - `grep -c '^skytime/$\|^skytime/' README.md` returns >= 1 (Project Layout tree)
    - `grep -c 'cmd/skytime\|cmd/skytime-docgen' README.md` returns >= 2
    - `grep -c '> Starlark-defined durable workflows on Temporal' README.md` returns 1 (verbatim hero blockquote)
  </acceptance_criteria>
  <done>
    README.md is the repo front door. Hero pitch + What This Is/Is Not + Status + embedded getting-started tutorial + Where to Go Next + Project Layout + Contributing + License sections all present. Length 400-1000 lines per D-02 mid-size. Tutorial content embedded verbatim from docs/getting-started.md with attribution. Cross-links to all major docs.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification:**
- README.md exists with all 8 H2 sections in locked order
- Hero pitch + What Is/Is Not bullet lists present
- Status section enumerates all phases including ⏳ for 5-6
- Getting Started section embeds docs/getting-started.md body VERBATIM (per D-06) — the tutorial's H2 headings (## Prerequisites, ## 1. Clone..., ..., ## What Just Happened?) are preserved as-is; no heading demotion; total README H2 count >= 14 (8 own + 6 embedded)
- Project Layout tree shows top-level structure
- All major docs cross-linked (architecture, reference, audience landings, examples)
- License section based on LICENSE file existence
- File length 400-1000 lines
</verification>

<success_criteria>
- README.md is mid-size (D-02)
- Tutorial body embedded verbatim (D-06)
- Reader can evaluate project without clicking out
- Cross-links into deeper docs are present
- Hero pitch + What Is/Is Not + Status + embedded tutorial cover the 30-second + 5-minute reader paths
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-documentation-and-source-driven-reference-generator/04.3-08-SUMMARY.md` documenting:
- Final file created (1: README.md — overwriting any prior README; note any preserved content from prior file)
- Section structure (8 H2)
- Length (line count)
- Embedded tutorial source (docs/getting-started.md)
- LICENSE handling decision (linked or "TBD")
- Cross-links inventory
- Any deviations
</output>
