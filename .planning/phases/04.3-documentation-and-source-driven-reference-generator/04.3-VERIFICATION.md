---
phase: 04.3-documentation-and-source-driven-reference-generator
verified: 2026-05-04T22:30:00Z
status: human_needed
score: 19/19 must-haves verified (D-01..D-19); 9/9 plans verified
re_verification:
  is_re_verification: false
source_plans:
  - 04.3-01-PLAN.md
  - 04.3-02-PLAN.md
  - 04.3-03-PLAN.md
  - 04.3-04-PLAN.md
  - 04.3-05-PLAN.md
  - 04.3-06-PLAN.md
  - 04.3-07-PLAN.md
  - 04.3-08-PLAN.md
  - 04.3-09-PLAN.md
started: 2026-05-04T22:25:00Z
updated: 2026-05-04T22:30:00Z
human_verification:
  - test: "Render README.md in GitHub UI"
    expected: "Hero blockquote, embedded tutorial H2s, code blocks, and Project Layout tree all render without alignment glitches; relative links resolve to docs/* targets when clicked"
    why_human: "Markdown rendering quirks (table alignment, fenced code-block language hint highlighting, GitHub-flavored relative-link resolution) can only be observed in the actual GitHub web view"
  - test: "Click through audience-landing cross-links"
    expected: "Both docs/for-flow-authors/README.md and docs/for-extension-developers/README.md mutual links resolve; architecture.md anchor links (#no-string-compilation, #no-context-bleed) jump to the correct H3 sections"
    why_human: "Anchor-link resolution depends on the rendering platform's slugification rules; only a human can confirm the H3 anchors auto-generated by GitHub line up with the linked targets"
  - test: "Read docs/architecture.md and docs/getting-started.md end-to-end for prose readability"
    expected: "Tone is pragmatic and code-forward (no marketing speak); ASCII diagram is legible at common terminal widths; conceptual blocks ground new concepts in code citations"
    why_human: "Subjective prose quality and pedagogical flow cannot be verified programmatically — requires a fresh-eyes read"
  - test: "Walk the embedded tutorial as a first-time reader"
    expected: "git clone → go build → ./skytime dev-server → ./skytime info … → ./skytime run … happy + fail paths all execute as documented and produce output that approximately matches the snapshot blocks"
    why_human: "End-to-end reproducibility against a fresh checkout (with temporal CLI installed) requires running the actual commands; programmatic verification cannot exercise the live Temporal server path"
  - test: "Verify cross-links in cli.md, builtins.md, http.md, examples/README.md all resolve in GitHub view"
    expected: "All ../{architecture,reference/builtins,reference/cli,getting-started,cli-binary}.md links + ../../{pkg,examples,tests}/* relative paths render as live links and target the correct files"
    why_human: "Relative-path correctness across the docs/ tree is best confirmed by clicking each link in a rendered view; a programmatic find/grep can confirm targets exist but not that the relative path is correct from the source file's location"
---

# Phase 04.3: documentation and source-driven reference generator — Verification Report

**Phase Goal (from ROADMAP.md):** Ship the project's first complete documentation set + a Copybara-style source-driven reference generator. Two streams in one phase: (1) `cmd/skytime-docgen` — a stdlib-only Go binary that walks `pkg/parser/globals.go` + `pkg/parser/builtins.go` AST + `// skytime:doc` markers to render `docs/reference/builtins.md` from source (D-07..D-14); (2) hand-written narrative — README.md (with embedded getting-started tutorial), docs/architecture.md, docs/getting-started.md, docs/reference/cli.md, docs/for-flow-authors/extensions/http.md, examples/README.md, plus audience-split landings (docs/for-flow-authors/README.md, docs/for-extension-developers/README.md). Plain markdown only — no static-site infra. Coverage v1: the 8 Starlark builtins (D-10) + the 4 shipped subcommands + the bundled HTTP extension; mkdocs/extension-auto-gen/CLI-auto-gen are deferred.

**Verified:** 2026-05-04T22:30:00Z
**Status:** human_needed (every must_have + every D-NN decision verified by code/grep; 5 items remain that can only be settled by a human reading the rendered docs end-to-end)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths Derived from Phase Goal

| #  | Truth                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                                              |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | A reader can hit `https://github.com/mikelalcon/skytime` and evaluate Skytime without clicking through (D-02 mid-size README + D-06 verbatim tutorial) | ✓ VERIFIED | `README.md` exists at repo root (430 lines, in 400-1000 D-02 band). 8 own H2 + 7 verbatim-embedded tutorial H2s. Byte-equality between embedded H2s and `docs/getting-started.md` confirmed via `diff <(grep ^## docs/getting-started.md) <(grep ^## README.md)` returning exit 0. Hero blockquote present.                                          |
| 2  | A reader picks an audience on day one (D-03 + D-04 audience split)                                                                                     | ✓ VERIFIED | Two top-level subdirectories present: `docs/for-flow-authors/` and `docs/for-extension-developers/`, each with a `README.md` landing (110 + 126 lines respectively). Mutual cross-links: flow-authors → for-extension-developers ×2, extension-developers → for-flow-authors ×2.                                                                      |
| 3  | The `cmd/skytime-docgen` binary generates `docs/reference/builtins.md` from source markers (D-07..D-14)                                                | ✓ VERIFIED | `cmd/skytime-docgen/{doc,types,main,walk,marker,render}.go` + 4 test files exist. Stdlib-only verified: `grep -E "github.com/spf13/cobra|charm.land/log|charm.land/lipgloss|charmbracelet" cmd/skytime-docgen/*.go` returns 0 matches. Imports = `bytes`, `encoding/json`, `flag`, `fmt`, `go/ast`, `go/parser`, `go/token`, `io`, `os`, `path/filepath`, `sort`, `strconv`, `strings`, `text/template` (all stdlib). |
| 4  | The 8 v1 Starlark builtins are documented in registration order (D-10)                                                                                 | ✓ VERIFIED | `docs/reference/builtins.md` 278 lines, 8 H2 in registration order: `flow → step → if_cond → script → for_each_parallel → call_flow → result → fail`. `pkg/parser/globals.go` has 8 `starlark.NewBuiltin` registrations. 8 `// skytime:doc summary=` lines + 8 returns + 8 since + 8 example lines (112 marker lines total).                          |
| 5  | Drift between source and generated docs is gated in CI (D-13)                                                                                          | ✓ VERIFIED | `tests/docgen_drift_test.go::TestDocgenDrift` exists; `go test -count=1 -run TestDocgenDrift ./tests/...` PASSES. Re-running `go run ./cmd/skytime-docgen --pkg ./pkg/parser --out /tmp/x.md` produces 0-byte diff vs. checked-in `docs/reference/builtins.md` — idempotent.                                                                          |
| 6  | A `go generate ./pkg/parser/` command refreshes the reference (D-14)                                                                                   | ✓ VERIFIED | `pkg/parser/generate.go` carries `//go:generate go run github.com/mikelalcon/skytime/cmd/skytime-docgen --pkg . --out ../../docs/reference/builtins.md` plus a package doc comment explaining the refresh + CI drift gate.                                                                                                                            |
| 7  | The architecture overview is required reading for both audiences (D-15)                                                                                | ✓ VERIFIED | `docs/architecture.md` (396 lines, 9 H2 + 3 H3). 16 `parse-time|execution-time` mentions, 1 `WORKFLOW BOUNDARY` ASCII divider, both audience landings cross-link to it (flow-authors ×4, extension-developers ×1).                                                                                                                                    |
| 8  | The 4 shipped CLI subcommands are documented (D-16)                                                                                                    | ✓ VERIFIED | `docs/reference/cli.md` (277 lines) has 4 H2 subcommand sections in fixed order: `## skytime run`, `## skytime validate`, `## skytime info`, `## skytime dev-server`. Each has 6 H3 subsections (Synopsis, Motivation, Flags, Exit Codes, Example, See Also).                                                                                          |
| 9  | The bundled HTTP extension is documented for flow authors (D-17)                                                                                       | ✓ VERIFIED | `docs/for-flow-authors/extensions/http.md` (440 lines) covers all 5 ops in source order: `### get`, `### head`, `### post`, `### put`, `### delete`. D4-14 idempotence + RFC-7231 PUT/DELETE divergence + 4xx/5xx classification + JIT credentials all present.                                                                                       |
| 10 | The skeleton fixture corpus is indexed (D-18)                                                                                                          | ✓ VERIFIED | `examples/README.md` (261 lines) has 3 fixture H2s in complexity order: `## simple_check.star`, `## parallel_fanout.star`, `## expression_if.star (canonical tutorial)`. Each has 4 H3 subsections (What/How/Expected/See Also).                                                                                                                       |
| 11 | A reader walks from `git clone` to a successful workflow in 5-10 min (D-05 tutorial scope)                                                             | ✓ VERIFIED | `docs/getting-started.md` (297 lines, in 250-450 target band). 8 H2 sections in locked order: Prerequisites → 1. Clone and Build → 2. Start the Dev Server → 3. Discover the Flows → 4. Run the Happy Path → 5. Run the Fail Path → What Just Happened? → Next Steps. Walks `expression_if.star::check_user` happy + fail paths.                      |
| 12 | The library-root firewall is preserved (CONTEXT.md "established patterns")                                                                             | ✓ VERIFIED | `cmd/skytime-docgen/*.go` imports zero forbidden CLI deps. `tests/firewall_cli_test.go` last touched in `406ee95` (quick-260504-m65-02) — NOT modified by phase 04.3. Full test suite `go test -count=1 -race ./...` passes (14 packages green).                                                                                                      |

**Score:** 12/12 derived truths verified.

---

## Per-Decision Verdict (D-01..D-19)

| ID    | Decision                                                                                                                                            | Status      | Evidence                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | Plain markdown in repo root + `docs/` tree; no static-site infra                                                                                    | ✓ HONORED   | No `mkdocs.yml`, `_config.yml`, `docusaurus.config.js`, or `.github/workflows/pages*.yml`. All docs are plain `.md`. README.md sits at repo root.                                              |
| D-02  | README.md mid-size with embedded tutorial (~500-800 lines target)                                                                                   | ✓ HONORED   | README.md is 430 lines — within the documented 400-1000 band acceptable for mid-size. Embeds the full canonical tutorial verbatim under `## Getting Started`.                                  |
| D-03  | Two top-level audience guides (`docs/for-flow-authors/` + `docs/for-extension-developers/`)                                                         | ✓ HONORED   | Both directories exist; both have `README.md` landings. Reader picks audience on day one via README.md "Where to Go Next" cross-links.                                                         |
| D-04  | Each audience guide has its own README pointing into the relevant subset (no separate "two-tier authoring" page)                                    | ✓ HONORED   | `docs/for-flow-authors/README.md` (110 lines, 5 H2) + `docs/for-extension-developers/README.md` (126 lines, 5 H2). Each has Start Here + Reference Material sections + mutual cross-links.     |
| D-05  | Getting-started tutorial = run-an-example smoke (5-10 min) using `expression_if.star::check_user`                                                   | ✓ HONORED   | `docs/getting-started.md` walks `git clone` → `skytime dev-server` → `skytime run examples/skeleton/expression_if.star --flow check_user --input '{"user_id":"octocat"}'`.                     |
| D-06  | Tutorial canonical at `docs/getting-started.md`; embedded verbatim in README.md                                                                     | ✓ HONORED   | Byte-equality `diff` of the 7 embedded H2s between `docs/getting-started.md` and `README.md` returns exit 0 (identical).                                                                       |
| D-07  | Build the docgen tool in this phase                                                                                                                 | ✓ HONORED   | `cmd/skytime-docgen/` directory exists with 11 source files. Build clean.                                                                                                                      |
| D-08  | Tool location: `cmd/skytime-docgen/` (sibling to `cmd/skytime/`)                                                                                    | ✓ HONORED   | `cmd/skytime/` and `cmd/skytime-docgen/` are siblings. `package main` in both.                                                                                                                 |
| D-09  | AST walker targets `pkg/parser/builtins.go` + `pkg/parser/globals.go`; detects via `starlark.NewBuiltin` and `starlark.UnpackArgs`                  | ✓ HONORED   | `cmd/skytime-docgen/walk.go::WalkRegistry` walks `newParseTimeGlobals` `StringDict` `CompositeLit`; `WalkBuiltins::extractUnpackParams` finds first `UnpackArgs`/`UnpackPositionalArgs` per FuncDecl. Live-source smoke test passes. |
| D-10  | Coverage v1 = 8 Starlark builtins (`flow`, `step`, `script`, `if_cond`, `result`, `fail`, `for_each_parallel`, `call_flow`)                         | ✓ HONORED   | All 8 H2 sections present in `docs/reference/builtins.md` in registration order. `pkg/parser/globals.go` has 8 `starlark.NewBuiltin` calls — counted exactly 8.                                |
| D-11  | Marker syntax = `// skytime:doc key="value"` block above each `builtinXxx`; multi-line via repeated keys                                            | ✓ HONORED   | 112 marker lines across 8 builtinXxx in `pkg/parser/builtins.go`. Multi-line summaries via repeated `// skytime:doc summary="…"` keys (e.g., flow's two-line summary).                         |
| D-12  | Output = `docs/reference/builtins.md` — single page with sections, signature, parameter table, summary, example, see-also                           | ✓ HONORED   | All 8 sections have `**Signature:**` line + `\| Param \| Type \| Required \| Description \|` table + `**See also:**` line. 8/8 each (verified via grep).                                       |
| D-13  | Generated docs checked in; CI verifies drift                                                                                                        | ✓ HONORED   | `docs/reference/builtins.md` checked into the repo. `tests/docgen_drift_test.go::TestDocgenDrift` PASSES on freshly-checked-out source. Re-run produces 0-byte diff (idempotent).             |
| D-14  | `//go:generate skytime-docgen ./...` directive on the parser package                                                                                | ✓ HONORED   | `pkg/parser/generate.go` carries `//go:generate go run github.com/mikelalcon/skytime/cmd/skytime-docgen --pkg . --out ../../docs/reference/builtins.md` (absolute module path, works from any cwd). |
| D-15  | Architecture overview at `docs/architecture.md` — required reading for both audiences                                                               | ✓ HONORED   | `docs/architecture.md` (396 lines). Both audience landings link to it: `for-flow-authors/README.md` ×4, `for-extension-developers/README.md` ×1. ASCII boundary diagram present.               |
| D-16  | CLI reference at `docs/reference/cli.md` — covers `skytime run`, `validate`, `info`, `dev-server`                                                   | ✓ HONORED   | `docs/reference/cli.md` (277 lines) — 4 H2 subcommand sections in fixed order. Note: doc claims 8 persistent flags (matches actual `pkg/cli/flags.go`); plan's interfaces table claimed 10. Source-of-truth fidelity preserved per plan 04 deviation log. |
| D-17  | Extension I/O reference at `docs/for-flow-authors/extensions/http.md` — bundled HTTP extension                                                      | ✓ HONORED   | 440 lines. 5 ops in source order. D4-14 idempotence ×5, RFC-7231 ×10, ErrNonRetryable ×1, 4xx/5xx ×8, credentials ×15.                                                                          |
| D-18  | `examples/README.md` indexes 3 skeleton fixtures: `simple_check.star`, `parallel_fanout.star`, `expression_if.star`                                 | ✓ HONORED   | 261 lines. 3 fixture H2s in complexity order. Run commands match shipped flow names (plan 06 deviation: corrected `check_repo`/`batch_repos` → `simple_check`/`parallel_fanout`/`check_user`). |
| D-19  | Phase scope = built-today only (Phase 1 → 04.2 + quick tasks); no stub pages                                                                        | ✓ HONORED   | No stub pages observed. Tutorial uses Phase 04.2's `expression_if.star`. CLI doc covers exactly the 4 shipped subcommands. Phase 5 (`temporal_test`/`skytime test`) intentionally absent.      |

**Score:** 19/19 decisions honored.

---

## Required Artifacts

| Artifact                                              | Expected                                                                                  | Status     | Details                                                                                          |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------ |
| `cmd/skytime-docgen/`                                 | Stdlib-only Go binary (walker + marker parser + renderer)                                 | ✓ VERIFIED | 11 files (doc/types/main/walk/walk_test/marker/marker_test/render/render_test + testdata/×2). Stdlib imports only. |
| `pkg/parser/builtins.go`                              | 112 `// skytime:doc` marker lines across 8 builtinXxx                                     | ✓ VERIFIED | `grep -c "// skytime:doc" pkg/parser/builtins.go` → 112.                                         |
| `pkg/parser/generate.go`                              | `//go:generate` directive                                                                 | ✓ VERIFIED | Directive present (verified via `cat`).                                                          |
| `docs/reference/builtins.md`                          | 8 H2 sections in registration order, generated artifact, checked in                       | ✓ VERIFIED | 278 lines, 8 H2s in correct order; idempotent regeneration.                                      |
| `docs/architecture.md`                                | Parse/execute split + ASCII diagram + carve-out citations                                 | ✓ VERIFIED | 396 lines, 9 H2 + 3 H3, ASCII diagram present, D4.1-22 + D4.2-05 carve-outs cited verbatim.      |
| `docs/getting-started.md`                             | 5-10 min tutorial, expression_if.star canonical                                           | ✓ VERIFIED | 297 lines, 8 H2 + 4 H3 conceptual blocks; happy + fail paths covered.                            |
| `docs/reference/cli.md`                               | 4 subcommands × 6 H3 sections + persistent flags                                          | ✓ VERIFIED | 277 lines, 4 subcommand H2s with 6 H3s each + Persistent Flags table.                            |
| `docs/for-flow-authors/extensions/http.md`            | 5 ops + idempotence + 4xx/5xx + credentials                                               | ✓ VERIFIED | 440 lines, 5 op H3s in source order, D4-14 + RFC-7231 + ErrNonRetryable all covered.             |
| `docs/for-flow-authors/README.md`                     | Audience landing for Starlark consultants                                                 | ✓ VERIFIED | 110 lines, 5 H2 sections, 4 architecture.md links, 2 cross-links to extension-developers landing. |
| `docs/for-extension-developers/README.md`             | Audience landing for Go extension developers                                              | ✓ VERIFIED | 126 lines, 5 H2 sections, 1 architecture.md link, 2 cross-links to flow-authors landing.         |
| `examples/README.md`                                  | 3 fixtures indexed in complexity order                                                    | ✓ VERIFIED | 261 lines, 3 fixture H2s, run commands aligned with shipped flow names.                          |
| `README.md`                                           | Repo front door with embedded tutorial                                                    | ✓ VERIFIED | 430 lines (D-02 mid-size band), 8 own H2 + 7 verbatim-embedded tutorial H2s, hero blockquote, Project Layout tree. |
| `tests/docgen_drift_test.go`                          | CI drift gate                                                                             | ✓ VERIFIED | Test passes; lives in `package firewall_test`; uses `findModuleRootDocgen` helper.               |

---

## Key Link Verification

| From                                                  | To                                                  | Via                                            | Status     |
| ----------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------- | ---------- |
| `cmd/skytime-docgen/walk.go`                          | `pkg/parser/globals.go`                             | AST walk over `newParseTimeGlobals` StringDict | ✓ WIRED    |
| `cmd/skytime-docgen/walk.go`                          | `pkg/parser/builtins.go`                            | FuncDecl traversal of `builtinXxx` bodies      | ✓ WIRED    |
| `cmd/skytime-docgen/marker.go`                        | `pkg/parser/builtins.go`                            | `// skytime:doc` comment-block parsing         | ✓ WIRED    |
| `cmd/skytime-docgen/render.go`                        | `docs/reference/builtins.md`                        | `text/template` rendering                      | ✓ WIRED    |
| `pkg/parser/generate.go (//go:generate)`              | `cmd/skytime-docgen` binary                         | `go generate` invocation                       | ✓ WIRED    |
| `tests/docgen_drift_test.go`                          | `docs/reference/builtins.md`                        | bytewise compare via `go run skytime-docgen`   | ✓ WIRED    |
| `README.md` → `docs/getting-started.md`               | Verbatim embed                                      | Byte-equal H2 headings                         | ✓ WIRED    |
| `docs/for-flow-authors/README.md` ↔ `for-extension-developers/README.md` | Mutual cross-link                              | Relative paths                                 | ✓ WIRED    |
| `docs/architecture.md` ← both audience landings       | Required-reading link                               | Relative `../architecture.md`                  | ✓ WIRED    |

---

## Data-Flow Trace (Level 4)

| Artifact                       | Data Source                                          | Produces Real Data | Status      |
| ------------------------------ | ---------------------------------------------------- | ------------------ | ----------- |
| `docs/reference/builtins.md`   | `pkg/parser/{globals,builtins}.go` AST + markers      | Yes — 8 builtins   | ✓ FLOWING   |
| `cmd/skytime-docgen` (binary)  | Live `pkg/parser/*.go` source                         | Yes — JSON or MD   | ✓ FLOWING   |
| Drift test                     | `go run` of `cmd/skytime-docgen`                      | Yes — 0-byte diff  | ✓ FLOWING   |

---

## Behavioral Spot-Checks

| Behavior                                                        | Command                                                                        | Result                  | Status   |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------ | ----------------------- | -------- |
| Full test suite passes with race detector                       | `go test -count=1 -race ./...`                                                 | 14 packages green       | ✓ PASS   |
| Drift test specifically passes                                  | `go test -count=1 -run TestDocgenDrift ./tests/...`                            | ok                      | ✓ PASS   |
| `go vet` clean                                                  | `go vet ./...`                                                                 | (empty output)          | ✓ PASS   |
| Idempotent regeneration                                         | `go run ./cmd/skytime-docgen --pkg ./pkg/parser --out /tmp/x.md && diff /tmp/x.md docs/reference/builtins.md` | 0 lines diff            | ✓ PASS   |
| `cmd/skytime-docgen` is stdlib-only (firewall preserved)        | `grep -E "github.com/spf13/cobra\|charm.land/log\|charm.land/lipgloss\|charmbracelet" cmd/skytime-docgen/*.go` | 0 matches               | ✓ PASS   |
| 8 builtins enumerated in registration order                     | `grep -E "^## " docs/reference/builtins.md`                                    | flow→step→if_cond→script→for_each_parallel→call_flow→result→fail | ✓ PASS   |
| 4 CLI subcommands documented                                    | `grep -E "^## skytime " docs/reference/cli.md`                                 | run, validate, info, dev-server | ✓ PASS |
| 5 HTTP ops documented in source order                           | `grep -E "^### (get\|head\|post\|put\|delete)$" docs/for-flow-authors/extensions/http.md` | 5 in source order       | ✓ PASS   |
| Embedded tutorial H2s byte-equal between docs/getting-started.md and README.md | `diff <(grep ^## docs/getting-started.md…) <(grep ^## README.md…)` | exit 0 (identical)      | ✓ PASS   |
| Working tree clean                                              | `git status --short`                                                           | (empty)                 | ✓ PASS   |
| All 15 plan-task commits exist in git                           | `git cat-file -e <sha>` × 15                                                   | all FOUND               | ✓ PASS   |

---

## Requirements Coverage

D-01..D-19 are CONTEXT.md decision IDs serving as phase requirements (per phase-init note). All decisions accounted for in the per-decision verdict table above.

| Decision | Status      | Source plan       |
| -------- | ----------- | ----------------- |
| D-01     | ✓ HONORED   | 04.3-08 (README)  |
| D-02     | ✓ HONORED   | 04.3-08           |
| D-03     | ✓ HONORED   | 04.3-09           |
| D-04     | ✓ HONORED   | 04.3-09           |
| D-05     | ✓ HONORED   | 04.3-07           |
| D-06     | ✓ HONORED   | 04.3-07 + 04.3-08 |
| D-07     | ✓ HONORED   | 04.3-01           |
| D-08     | ✓ HONORED   | 04.3-01           |
| D-09     | ✓ HONORED   | 04.3-01           |
| D-10     | ✓ HONORED   | 04.3-01 + 04.3-02 |
| D-11     | ✓ HONORED   | 04.3-02           |
| D-12     | ✓ HONORED   | 04.3-02           |
| D-13     | ✓ HONORED   | 04.3-02           |
| D-14     | ✓ HONORED   | 04.3-02           |
| D-15     | ✓ HONORED   | 04.3-03           |
| D-16     | ✓ HONORED   | 04.3-04           |
| D-17     | ✓ HONORED   | 04.3-05           |
| D-18     | ✓ HONORED   | 04.3-06           |
| D-19     | ✓ HONORED   | (all plans)       |

No orphaned decisions.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

None observed in this verification pass. Specifically:

- No `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers in newly-created docs or generated artifacts.
- No "coming soon"/"not yet implemented" placeholder prose in any documentation file.
- No empty stub pages.
- No empty function returns or hollow placeholder implementations in `cmd/skytime-docgen/*.go`.
- The auto-generated `docs/reference/builtins.md` uses em-dash (U+2014) sentinel for empty cells — this is the intentional convention from `skytime info`'s lipgloss table, not a stub indicator.

---

## Human Verification Required

See the `human_verification:` block in the frontmatter for the complete list. Summary:

1. **Render README.md in GitHub UI** — confirm hero blockquote, embedded tutorial H2s, code blocks, Project Layout tree, and all relative cross-links render cleanly.
2. **Click through audience-landing cross-links** — confirm `architecture.md#no-string-compilation` and `architecture.md#no-context-bleed` anchor links resolve to the correct H3 sections in GitHub's auto-generated slugs.
3. **Read `docs/architecture.md` and `docs/getting-started.md` end-to-end** — judge prose readability, ASCII diagram legibility at common terminal widths, and pedagogical flow.
4. **Walk the embedded tutorial as a first-time reader** — exercise `git clone → go build → ./skytime dev-server → ./skytime info … → ./skytime run …` happy + fail paths against a fresh checkout to confirm the documented commands and approximate-output snapshots match observed behavior.
5. **Verify all cross-links in `cli.md`, `builtins.md`, `http.md`, `examples/README.md` resolve in GitHub view** — relative-path correctness across the docs tree is best confirmed visually.

---

## Gaps Summary

No gaps found. Every must-have truth is verified, every D-NN decision is honored, every artifact exists with substantive content and correct wiring, and every key link is wired. The full test suite passes with `-race`; `go vet` is clean; the drift test confirms `docs/reference/builtins.md` is in lock-step with `pkg/parser/{globals,builtins}.go` source markers; `cmd/skytime-docgen` is stdlib-only (library-root firewall preserved verbatim).

The five items in `human_verification` are deliberate carve-outs for prose-quality, markdown-rendering, and end-to-end-reproducibility judgment that programmatic checks cannot make.

---

_Verified: 2026-05-04T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
