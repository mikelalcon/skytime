---
phase: 07.1-http-webhook-receiver-github-source
verified: 2026-05-08T00:00:00Z
status: human_needed
score: 5/5 must-haves verified (3 fully automatable; 2 require manual UAT)
human_verification:
  - test: "End-to-end GitHub webhook delivery against a live repo"
    expected: "Open issue in test repo → comment 'received, processing...' appears within seconds → label 'processed' applies on second activity"
    why_human: "Requires live GitHub repo + gh CLI auth + gh webhook forward + Temporal dev-temporal + extbin server. Cannot run in CI. Must-have #1 + #4 confirmed automatable through unit/handler tests, but the prompt explicitly directed that walkthrough end-to-end against a real repo is human UAT."
  - test: "Crash-recovery durability demonstration (kill -9 between activities)"
    expected: "Kill extbin server after add_comment activity completes but before add_label fires → restart server with same command → label still applies via Temporal event-history replay; client.DescribeWorkflowExecution shows the workflow completed across the restart"
    why_human: "Must-have #5 — requires real Temporal server + real network delivery + manual SIGKILL between two activities. The cross-activity continuation shape (locked Plan 07 deviation from the roadmap-text 30s sleep) is a kill-restart-between-activities demo, not a sleep-based one; verifiable only by a human running the documented commands and watching the dashboard / DescribeWorkflowExecution result."
---

# Phase 7.1: HTTP webhook receiver + GitHub source Verification Report

**Phase Goal (CONTEXT-locked, NOT verbatim roadmap text):** Mount the actual HTTP receiver in `skytime server`. Ship `http.webhook(...)` (extends `pkg/extension/builtin/http`) and `github.webhook(...)` (extends `examples/http-github-webhook/extensions/github`). Each handler routes through signature validation → payload map lambda → idempotency key lambda → `client.ExecuteWorkflow` with `WorkflowIDReusePolicy=REJECT_DUPLICATE`. GitHub HMAC-SHA256 against `X-Hub-Signature-256`; secrets resolved JIT via `CredentialHandler.Resolve(ctx, id)`. Walkthrough docs in two locations + crash-recovery demo using cross-activity kill-restart (NOT a 30s sleep — Skytime's bridge does not expose `workflow.Sleep` to Starlark).

**Verified:** 2026-05-08
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to ROADMAP § Phase 7.1 Success Criteria)

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                                                                | Status        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A `.star` declaring `trigger(flow=..., source=github.webhook(events=["issues"], secret_credential="gh-webhook-secret"))` causes `skytime server` to mount `POST /webhook/github`. A delivery with valid `X-Hub-Signature-256` triggers the workflow; invalid signature returns HTTP 401 and skips `ExecuteWorkflow`. (NAMESPACE: prompt locked `github.webhook` per D-7.1-02 — overrides roadmap text `triggers.github_webhook`.) | ✓ VERIFIED    | `examples/http-github-webhook/extensions/github/webhook.go:44-49` exports `GithubWebhookKind="github.webhook"`, `GithubWebhookPath="/webhook/github"`, `GithubWebhookMethod=POST`, `GithubWebhookSignatureAlgo="sha256"`, `GithubWebhookSignatureHeader="X-Hub-Signature-256"`. `pkg/cli/server.go:174` calls `receiver.Mount(mux, w, deps)`. `TestHandler_GitHubValidSignature` (PASS), `TestHandler_GitHubBadSignature` (PASS, asserts 401 + zero ExecuteWorkflow), `TestHandler_GitHubMissingSignature` (PASS). Live-delivery against real GitHub = human UAT. |
| 2   | A `.star` declaring `http.webhook(path="/hooks/custom", method="POST", secret_credential=None)` causes `skytime server` to mount `POST /hooks/custom`. The body's lambda-mapped payload becomes the workflow input. (NAMESPACE: prompt locked `http.webhook` per D-7.1-01 — overrides roadmap text `triggers.generic_http_webhook`.)                                                                                                                                                  | ✓ VERIFIED    | `pkg/extension/builtin/http/webhook.go:37` declares `httpWebhookKind="http.webhook"`. Kwargs `path`, `method`, `secret_credential` (None-allowed), `signature_algo` (sha256/sha1/sha512 allowlist), `signature_header` validated at parse time. `TestHandler_UnsignedHTTPWebhook` (PASS) + `TestSignature_SHA512` (PASS) verify both unsigned + custom-algo paths reach `ExecuteWorkflow` with the lambda-mapped input. |
| 3   | Two GitHub deliveries with the same `X-GitHub-Delivery` ID dedupe — only the first triggers a workflow run; the second receives 200 OK with `"duplicate; skipped"` envelope; no second workflow appears in `client.ListWorkflow`.                                                                                                                                                                                                                                                                                                                                                                                | ✓ VERIFIED    | `pkg/extension/receiver/handler.go:209` sets `WorkflowIDReusePolicy: WORKFLOW_ID_REUSE_POLICY_REJECT_DUPLICATE` AND line 210 sets `WorkflowExecutionErrorWhenAlreadyStarted: true` (Pitfall 1). Line 222: `errors.As(execErr, &alreadyStarted)` (NOT string match — Pitfall 6). `TestHandler_RedeliveryDedup` (PASS) verifies first delivery → 200 + workflow_id; second → 200 + `{"status":"duplicate; skipped",...}` + zero second `ExecuteWorkflow` call. |
| 4   | The example project README's "GitHub webhook walkthrough (5 minutes)" section runs end-to-end against `skytime dev-temporal` + `gh webhook forward`, taking a reader from `git clone` to a webhook-triggered workflow.                                                                                                                                                                                                                                                                                                                                                                                                                                            | ⚠️ HUMAN UAT  | `examples/http-github-webhook/README.md:27` heading present. Required commands present: `gh extension install cli/gh-webhook` (1 occurrence — Pitfall 8), `gh webhook forward` (4 occurrences), `openssl rand` (1), `GITHUB_WEBHOOK_SECRET` (3), `webhook_demo.star` (2 cross-references), `received, processing` (3), `processed` (2). NO "30 seconds later" anti-claim (0 across all docs). Reader walkthrough END-TO-END against real GitHub repo is human UAT. |
| 5   | The crash-recovery demo runs as documented: a flow triggered via webhook continues from event history after `kill -9 $SERVER_PID` + restart; verifiable via dashboard / `client.DescribeWorkflowExecution`. (PROMPT NOTE: Plan 07 locked the cross-activity kill-restart shape — NOT 30s sleep, since Skytime bridge does not expose `workflow.Sleep` to Starlark.)                                                                                                                                                                                                                                                                                                                                                       | ⚠️ HUMAN UAT  | `examples/http-github-webhook/webhook_demo.star` ships 2 `step()` calls (gh.add_comment then gh.add_label) — confirmed by `grep -cE 'step\(' = 2`. `docs/walkthroughs/github-webhook.md:169` "Step 7: Crash-recovery demonstration" documents kill-restart BETWEEN activities. `TestWebhookDemo_BootsCleanly` PASS (parses + registers trigger + asserts `Source.Kind() == "github.webhook"`). The kill-restart durability proof requires real Temporal + real GitHub + manual SIGKILL = human UAT. |

**Score:** 5/5 truths verified by code + automated tests. Two truths (#4, #5) require live UAT against real GitHub + Temporal for end-to-end confirmation; the prompt explicitly directs these be classified `human_needed`, not `gaps_found`.

### Required Artifacts

| Artifact                                                                                  | Expected                                                            | Status      | Details                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pkg/extension/receiver/` package (10 files)                                              | Foundation: signature, workflow_id, status, log, mounter primitives | ✓ VERIFIED  | doc.go (1.6KB), http_mounter.go (1.4KB), signature.go (2.5KB), workflow_id.go (1.4KB), status.go (4.5KB), log.go (1.6KB), receiver.go (7.4KB), handler.go (16.6KB) — all present, plus 7 _test.go files + 7 testdata fixtures. `go test ./pkg/extension/receiver/... -race` PASS.        |
| `pkg/extension/receiver/handler.go`                                                       | 9-step pipeline (defer/recover → MaxBytesReader → ReadAll → JIT credential → HMAC → 415 → JSON unmarshal → fan-out → worst-status response) | ✓ VERIFIED  | All 9 steps locked in doc comment lines 33-50. Pipeline order verified: line 77 MaxBytesReader, 78 ReadAll, 115 validateHMAC, 132 writeUnsupportedMediaTypeResponse, 140 json.Unmarshal, 166 ShouldDispatch fan-out, 209-210 REJECT_DUPLICATE + WorkflowExecutionErrorWhenAlreadyStarted, 222 errors.As. `bytes.Equal` count = 0 (uses hmac.Equal). `.Reveal()` count = 3, all in `credentialBytes()` lines 303-311, each preceded by `// SECURITY:`. |
| `pkg/extension/builtin/http/webhook.go`                                                   | `http.webhook` source factory (D-7.1-01)                            | ✓ VERIFIED  | 12KB. `httpWebhookKind="http.webhook"`. Module attribute wired in `Initialize()` (modified `http.go`). 14 white-box tests + 3 accessor tests. SignatureAlgo/SignatureHeader/SecretCredID accessors present.                                                                            |
| `examples/http-github-webhook/extensions/github/webhook.go`                               | `github.webhook` source factory (D-7.1-02)                          | ✓ VERIFIED  | 7.5KB. Six locked exported constants verified at lines 44-49. `webhookFactory` wired into `skytimeGitHub.Initialize()`. 12 factory tests + 3 init regression tests. `signature_algo`/`signature_header` kwargs absent (TRIG-09 source-grep firewall).                                  |
| `pkg/cli/server.go` listener bind                                                         | net.Listen pre-bind + receiver.Mount + http.Server with D-7.1-12 timeouts + listener-first shutdown | ✓ VERIFIED  | Line 174 `receiver.Mount(mux, w, deps)`, 187 `net.Listen("tcp", addr)` foreground (Pitfall 9), 198-205 http.Server with all 5 D-7.1-12 timeouts (ReadHeaderTimeout 10s, ReadTimeout 30s, WriteTimeout 30s, IdleTimeout 60s, MaxHeaderBytes 64KB), 207 srv.Serve in goroutine, 230 srv.Shutdown(drainCtx) BEFORE worker.Stop. |
| `pkg/worker/options.go` + `worker.WithSDKFactory`                                         | Test seam to unskip 3 Phase 7 signal-loop t.Skip stubs              | ✓ VERIFIED  | `var _ SDKFactory = sdkWorkerNew` compile-time assert; NewWorker has variadic `...Option` tail; `pkg/cli/server_test.go` 0 occurrences of `TODO(phase-7.1)`. Three previously-skipped tests now PASS: TestServerCmd_DrainOnSIGTERM, TestServerCmd_DrainTimeoutExpiry, TestServerCmd_SecondSignalForceExit. |
| `examples/http-github-webhook/webhook_demo.star`                                          | Crash-recovery demo flow (cross-activity, NO sleep)                 | ✓ VERIFIED  | `grep -cE 'step\(' = 2` (exactly two: gh.add_comment + gh.add_label). `github.webhook(events=["issues"], secret_credential="github_webhook_secret")` source. `idempotency_key=lambda req: req.headers["X-GitHub-Delivery"]`. NO `step(sleep=...)` or `core.sleep(...)`. NO "30 seconds later" claim. `TestWebhookDemo_BootsCleanly` PASS. |
| `examples/http-github-webhook/README.md` walkthrough section                              | `## GitHub webhook walkthrough (5 minutes)` heading per D-7.1-17    | ✓ VERIFIED  | Heading present (line 27). Required subsections: Prerequisites, Setup, Run, Crash-recovery demo, Where to next. `gh extension install cli/gh-webhook` present (Pitfall 8). NO "30 seconds later".                                                                                       |
| `docs/walkthroughs/github-webhook.md`                                                     | NEW long-form walkthrough per D-7.1-17 (H1 + 11 H2)                 | ✓ VERIFIED  | File exists (260+ lines). H1 + 11 H2 sections present (Prerequisites + 7 numbered Steps + Troubleshooting + Decoding the structured log line + What's next). NO "30 seconds later". Anti-claim gate (`TestWalkthrough_GitHubWebhookHeadings`) PASS.                                    |
| `tests/firewall_credential_redaction_test.go::targetDirs`                                 | Extended to include `pkg/extension/receiver` + `examples/http-github-webhook/extensions/github` | ✓ VERIFIED  | Lines 46-51: `targetDirs` slice has 5 entries; line 50 = `pkg/extension/receiver`; line 51 = `examples/http-github-webhook/extensions/github`. `TestCredentialRedactionFirewall` PASS — scanned 5 dirs, 0 violations.                                                                |
| `tests/receiver_hmac_only_test.go::TestReceiverFirewall_HMACOnly`                         | NEW cross-package AST-walk firewall (no `bytes.Equal` in receiver/) | ✓ VERIFIED  | File exists. Test PASS — AST-walks `pkg/extension/receiver/*.go` (excluding _test.go); 0 `bytes.Equal` call sites detected.                                                                                                                                                            |
| `tests/walkthrough_github_webhook_headings_test.go`                                       | Headings + anti-claim gate                                          | ✓ VERIFIED  | File exists. Test PASS — 12 required headings + 10 load-bearing references present; "30 seconds later" absent.                                                                                                                                                                       |

### Key Link Verification (Wiring)

| From                                                       | To                                                  | Via                                                  | Status   | Details                                                                                                                                                                |
| ---------------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pkg/cli/server.go` RunE                                   | `receiver.Mount(mux, w, deps)`                      | direct call line 174 after `worker.Start()`         | WIRED    | D-7.1-10 boot-order locked: worker first, then mount, then listen. `TestServerCmd_ListenerBindsAfterWorkerStart` asserts stages[0]=worker_started, stages[1]=listener_started. |
| `receiver.Mount` walk                                      | `worker.Triggers().All()`                           | iteration over registry, type-assert `HTTPMounter`   | WIRED    | `pkg/extension/receiver/receiver.go::Mount` walks `w.Triggers().All()`, groups by `mountKey{kind,path,method}`, registers HandleFunc per path with `dispatchByMethod`. |
| `*githubWebhookSource`                                     | `receiver.HTTPMounter` (sub-interface)              | `HTTPMount() (path, method)`                         | WIRED    | Lines 88-92: returns `("/webhook/github", "POST")`. Compile-time assert `var _ extension.TriggerSource = (*githubWebhookSource)(nil)`.                                  |
| `*httpWebhookSource`                                       | `receiver.HTTPMounter`                              | `HTTPMount() (path, method)`                         | WIRED    | `pkg/extension/builtin/http/webhook.go::HTTPMount` returns the parse-validated path/method tuple.                                                                       |
| Handler                                                    | `CredentialHandler.Resolve(ctx, id)` (JIT)          | `deps.CredentialHandler.Resolve(r.Context(), secretCredID)` | WIRED    | Resolves only when `secretCredID != ""`. Resolved Secret never logged; bytes feed only `validateHMAC` via `credentialBytes()` (3 `.Reveal()` sites, all `// SECURITY:` annotated). |
| Handler signature validation                               | `crypto/hmac.Equal` (NOT `bytes.Equal`)             | `validateHMAC(body, secret, algo, headerValue)`     | WIRED    | `signature.go::validateHMAC` uses `hmac.Equal`. `bytes.Equal` count in receiver = 0. Two firewall layers: in-file `TestSignature_HMACEqualNotBytesEqual` + cross-package AST `TestReceiverFirewall_HMACOnly`. |
| Handler                                                    | `client.ExecuteWorkflow` with REJECT_DUPLICATE      | StartWorkflowOptions WorkflowIDReusePolicy + WorkflowExecutionErrorWhenAlreadyStarted | WIRED    | Lines 209-210. Pitfall 1 mitigation: without WorkflowExecutionErrorWhenAlreadyStarted=true, REJECT_DUPLICATE silently succeeds. Pitfall 6: `errors.As` (line 222), NOT `err.Error()` string match. |
| `webhook_demo.star`                                        | `github.webhook` factory + extension                | parser → `Initialize()` Members map                  | WIRED    | `TestWebhookDemo_BootsCleanly` (PASS): parses .star, registers flow `webhook_demo`, registers trigger with `Source.Kind()=="github.webhook"`.                            |

### Data-Flow Trace (Level 4)

| Artifact                                  | Data Variable          | Source                                                            | Produces Real Data | Status     |
| ----------------------------------------- | ---------------------- | ----------------------------------------------------------------- | ------------------ | ---------- |
| Handler workflow input                    | `input map[string]any` | `evalLambdaToMap(MapLambda, reqStruct)` over JSON-parsed body     | Yes                | ✓ FLOWING  |
| Handler workflow ID                       | `workflowID string`    | `composeWorkflowID(t, evalLambdaToString(IdempotencyLambda, ...))` | Yes (deterministic + per-trigger pos hash)  | ✓ FLOWING  |
| Handler signature byte stream             | `bodyBytes []byte`     | `io.ReadAll(MaxBytesReader(r.Body))` BEFORE json.Unmarshal        | Yes (raw wire bytes; Pitfall 2 mitigated) | ✓ FLOWING  |
| Handler credential bytes                  | `secretBytes []byte`   | `credentialBytes(deps.CredentialHandler.Resolve(...).Bearer/Basic/APIKey)` | Yes (JIT)          | ✓ FLOWING  |
| Banner mount entry                        | `entry["mount"]`       | type-assert `t.Source.(receiver.HTTPMounter)`                     | Yes for HTTP sources; cron sources omit key | ✓ FLOWING  |
| Listener network bind                     | `addr` flag            | `net.Listen("tcp", addr)` synchronous on RunE foreground          | Yes (errors surface inline; Pitfall 9 mitigated) | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                   | Command                                                                                                  | Result                                                  | Status |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------ |
| Full repo green                                            | `go test ./... -count=1 -race`                                                                           | All 22 packages PASS                                    | ✓ PASS |
| Receiver package tests                                     | `go test ./pkg/extension/receiver/... -race`                                                             | PASS                                                    | ✓ PASS |
| Github extension webhook tests                             | `go test ./examples/http-github-webhook/extensions/github -race`                                         | PASS                                                    | ✓ PASS |
| HTTP webhook builtin tests                                 | `go test ./pkg/extension/builtin/http -race`                                                             | PASS                                                    | ✓ PASS |
| CLI server boot-order + drain tests (3 unskipped + 4 new)  | `go test ./pkg/cli -race`                                                                                | 15 tests PASS (1 SKIP unrelated SelfHosted mTLS)        | ✓ PASS |
| Worker WithSDKFactory tests                                | `go test ./pkg/worker -race -run TestNewWorker_WithSDKFactory`                                           | PASS                                                    | ✓ PASS |
| Credential redaction firewall (extended)                   | `go test ./tests -race -run TestCredentialRedactionFirewall`                                             | "scanned 5 target dir(s); 0 %+v / %#v"                  | ✓ PASS |
| HMAC-only firewall (cross-package)                         | `go test ./tests -race -run TestReceiverFirewall_HMACOnly`                                               | "scanned receiver/*.go; no bytes.Equal call sites"       | ✓ PASS |
| Walkthrough headings + anti-claim gate                     | `go test ./tests -race -run TestWalkthrough_GitHubWebhookHeadings`                                       | PASS (12 H + 10 refs present; "30 seconds later" absent) | ✓ PASS |
| webhook_demo boots cleanly                                 | `go test ./examples/http-github-webhook -run TestWebhookDemo_BootsCleanly`                               | PASS (flow registered + trigger.Source.Kind()=="github.webhook") | ✓ PASS |
| Signature behavior: valid → 200, invalid → 401, missing → 401 | `go test ./pkg/extension/receiver -run "TestHandler_GitHubValidSignature\|GitHubBadSignature\|GitHubMissingSignature"` | All PASS                                                | ✓ PASS |
| REJECT_DUPLICATE redelivery dedup                          | `go test ./pkg/extension/receiver -run TestHandler_RedeliveryDedup`                                      | PASS                                                    | ✓ PASS |
| Fan-out different WorkflowIDs (per-trigger pos hash)       | `go test ./pkg/extension/receiver -run TestHandler_FanOutDifferentWorkflowIDs`                           | PASS                                                    | ✓ PASS |
| Live `gh webhook forward` against real repo                | (manual)                                                                                                 | NOT RUN — requires gh auth + test repo                  | ? SKIP — human UAT (must-have #4) |
| Crash-recovery kill-restart durability (between activities) | (manual SIGKILL + Temporal dashboard)                                                                   | NOT RUN — requires running Temporal + manual procedure  | ? SKIP — human UAT (must-have #5) |

### Requirements Coverage

| Requirement | Source Plan(s)                | Description                                                                                                                                                                | Status        | Evidence                                                                                                                                                                                                                                                                                                                                |
| ----------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TRIG-06     | 04, 05, 06                    | Built-in HTTP listener mounts handler per registered HTTP-shaped trigger source. Handlers route signature validation → payload map → idempotency → ExecuteWorkflow.        | ✓ SATISFIED   | `receiver.Mount` walks worker.Triggers, groups by mountKey, registers HandleFunc per path. `pkg/cli/server.go:174` calls Mount; lines 187/207/230 wire net.Listen + srv.Serve + listener-first Shutdown. Plan 05 worker.WithSDKFactory test seam unskipped 3 Phase 7 stubs.                                                            |
| TRIG-07     | 03, 04b                       | github.webhook source factory shipping `POST /webhook/github`.                                                                                                              | ✓ SATISFIED   | `examples/http-github-webhook/extensions/github/webhook.go` — six locked exported constants; HMAC-SHA256 + X-Hub-Signature-256 hardcoded; `signature_algo`/`signature_header` kwargs absent (firewall test). Wired into `skytimeGitHub.Initialize()` Members map. NOTE: Roadmap text says `triggers.github_webhook`; CONTEXT D-7.1-02 lockdown chose `github.webhook` namespace per Phase 7 D-07-08 (sources live under owning extension's namespace). |
| TRIG-08     | 02, 04b                       | Generic HTTP webhook source factory with arbitrary path/method.                                                                                                              | ✓ SATISFIED   | `pkg/extension/builtin/http/webhook.go` ships `http.webhook(path=, method=, secret_credential=, signature_algo=, signature_header=)` with sha256/sha1/sha512 allowlist, parse-time validation. NOTE: Roadmap text says `triggers.generic_http_webhook`; CONTEXT D-7.1-01 lockdown chose `http.webhook` (extends existing `pkg/extension/builtin/http` alongside `http.endpoint`). |
| TRIG-09     | 01, 03, 04b, 08               | GitHub webhook signature uses HMAC-SHA256 against X-Hub-Signature-256; secret resolved JIT via `CredentialHandler.Resolve`.                                                  | ✓ SATISFIED   | `signature.go::validateHMAC` uses `hmac.Equal`. github.webhook hardcodes the algo/header (D-7.1-04). JIT `CredentialHandler.Resolve` invoked once per request inside `makeHandler`. Two firewall tiers: in-file (`TestSignature_HMACEqualNotBytesEqual`) + cross-package AST (`TestReceiverFirewall_HMACOnly`).               |
| TRIG-10     | 01, 04b                       | Idempotency: `idempotency_key` lambda → Temporal WorkflowID with WorkflowIDReusePolicy=REJECT_DUPLICATE; redeliveries with same X-GitHub-Delivery dedup.                    | ✓ SATISFIED   | `composeWorkflowID(t, userKey)` produces `{flow}/{posHash}/{userKey}` per D-7.1-08. Handler sets WorkflowIDReusePolicy=REJECT_DUPLICATE + WorkflowExecutionErrorWhenAlreadyStarted=true (Pitfall 1). `errors.As(execErr, &alreadyStarted)` (Pitfall 6). `TestHandler_RedeliveryDedup` PASS.                                                |
| EX-05       | 07, 08                        | Example README walkthrough using `gh webhook forward`; crash-recovery demo: kill server mid-execution, restart, watch workflow complete.                                     | ⚠️ NEEDS HUMAN | All artifacts shipped: webhook_demo.star (cross-activity, no sleep), README "## GitHub webhook walkthrough (5 minutes)" section, docs/walkthroughs/github-webhook.md long-form (H1 + 11 H2 + Troubleshooting + log-line decoder), `getting-started.md` cross-link. Anti-claim gate forbids "30 seconds later". End-to-end UAT against real GitHub + manual SIGKILL = human verification. |

**No orphaned requirements.** All 6 requirement IDs declared in PLAN frontmatters are covered by SUMMARY frontmatter `requirements-completed:` entries: Plan 01 [TRIG-09, TRIG-10], Plan 02 [TRIG-08], Plan 03 [TRIG-07], Plan 04 [TRIG-06], Plan 04b [TRIG-07, TRIG-08, TRIG-09, TRIG-10], Plan 05 [TRIG-06], Plan 06 [TRIG-06], Plan 07 [EX-05], Plan 08 [EX-05, TRIG-09]. Every one of TRIG-06 / TRIG-07 / TRIG-08 / TRIG-09 / TRIG-10 / EX-05 appears in at least one plan's `requirements:` field.

### Anti-Patterns Found

| File                                             | Line  | Pattern                                       | Severity | Impact                                                                                                                                                            |
| ------------------------------------------------ | ----- | --------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)                                           | —     | —                                             | —        | No `TODO(phase-7.1)` strings in `pkg/cli/server_test.go` (Plan 05 closed all 3). Zero `bytes.Equal` in receiver. Zero `%+v` / `%#v` in any covered targetDir. No "30 seconds later" doc claim. No `step(sleep=...)` placeholder. Zero placeholder/stub sentinel patterns surfaced by the standard scans. |

`grep -E "TODO|FIXME|XXX|HACK|PLACEHOLDER" pkg/extension/receiver/*.go pkg/extension/builtin/http/webhook.go examples/http-github-webhook/extensions/github/webhook.go pkg/cli/server.go examples/http-github-webhook/webhook_demo.star` returns no actionable items in production code (only allowed historical phase references in doc comments).

### Human Verification Required

#### 1. End-to-End GitHub Webhook Delivery (Must-Have #1 + #4)

**Test:**
```bash
# Terminal 1
brew install temporal
temporal server start-dev

# Terminal 2 (from repo root)
SECRET=$(openssl rand -hex 32)
echo "[credentials.github_webhook_secret]" >> ~/.skytime-credentials
echo "type = \"bearer\"" >> ~/.skytime-credentials
echo "token = \"$SECRET\"" >> ~/.skytime-credentials
chmod 600 ~/.skytime-credentials
go run ./examples/http-github-webhook/cmd/extbin server \
  --rootdir ./examples/http-github-webhook \
  --addr :8080 \
  --task-queue skytime-example

# Terminal 3
gh extension install cli/gh-webhook
gh webhook forward \
  --repo=<your-username>/test-repo \
  --events=issues \
  --url=http://localhost:8080/webhook/github \
  --secret="$SECRET"

# Terminal 4
gh issue create --repo <your-username>/test-repo --title "test" --body "trigger"
```

**Expected:**
- Comment "received, processing..." appears on the issue within seconds.
- Server logs `POST /webhook/github 200 ... source=github.webhook event=issues flows=[webhook_demo] workflow_ids=[webhook_demo/<hash>/<delivery-id>]`.
- Label "processed" applies on the issue shortly after.

**Why human:** Requires live GitHub repo + gh CLI auth + real network delivery. Cannot run in CI.

#### 2. Crash-Recovery Durability Demo (Must-Have #5)

**Test:** Repeat steps from #1, but after seeing the comment posted, send `kill -9 <extbin server PID>` BEFORE the label step fires. Then restart `extbin server` with the same command.

**Expected:**
- After restart, the label "processed" still applies on the issue (Temporal event-history replay picks up the `webhook_demo` workflow at the second activity).
- `temporal workflow describe --workflow-id webhook_demo/<hash>/<delivery-id>` shows the workflow completed with both activities recorded across the kill-restart boundary.

**Why human:** Requires real Temporal + manual SIGKILL between activities + dashboard observation. Cross-activity continuation IS the durability proof (locked Plan 07 shape — NOT a 30s sleep, since Skytime bridge does not expose `workflow.Sleep` to Starlark).

### Gaps Summary

**No code gaps.** All 5 must-haves have shipping artifacts, all key links wire end-to-end, all automated tests pass under `go test ./... -count=1 -race`. The two outstanding items (#4 walkthrough end-to-end, #5 kill-restart durability) are explicit human-UAT concerns per the prompt directive: "If any criterion REQUIRES manual UAT (e.g., live `gh webhook forward` against a real GitHub repo, or the kill-restart durability demo), classify as `human_needed` rather than `gaps_found` — these are ship-quality concerns that need a human's eyes/repo, not code gaps."

The locked CONTEXT-evolved namespaces (`http.webhook`, `github.webhook`) supersede the roadmap-text `triggers.*` proposal — that evolution is documented in CONTEXT.md D-7.1-01/02 and consistently honored across all 9 plans, all production code, all tests, and all walkthrough docs. The Plan 07 cross-activity-continuation demo shape supersedes the original D-7.1-16 30s-sleep idea — also locked in CONTEXT, also consistently honored, also gated by an anti-claim test.

---

_Verified: 2026-05-08_
_Verifier: Claude (gsd-verifier)_
