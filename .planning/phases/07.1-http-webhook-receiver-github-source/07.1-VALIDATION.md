---
phase: 7.1
slug: http-webhook-receiver-github-source
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
---

# Phase 7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` + `httptest` + `testify` (require, assert) + `go.temporal.io/sdk/testsuite` |
| **Config file** | None — Go tests configured via package-local `*_test.go` |
| **Quick run command** | `go test ./pkg/extension/receiver/... ./pkg/extension/builtin/http/... ./examples/http-github-webhook/extensions/github/... -count=1 -race` |
| **Full suite command** | `go test ./... -count=1 -race` |
| **Estimated runtime** | ~5s quick; ~30s full |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/extension/receiver/... ./pkg/extension/builtin/http/... ./examples/http-github-webhook/extensions/github/... -count=1 -race`
- **After every plan wave:** Run `go test ./... -count=1 -race`
- **Before `/gsd:verify-work`:** Full suite must be green; firewall + grep gate tests confirm no regressions in credential redaction or `dev-server` literals
- **Max feedback latency:** ~5 seconds (quick run); ~30 seconds (full suite)

---

## Per-Task Verification Map

> Plan/Task IDs are placeholders pending PLAN.md creation. Update during plan-checker pass.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 7.1-01-XX | 01 (receiver foundation) | 1 | TRIG-06 | unit | `go test ./pkg/extension/receiver -run TestMount_FanOutGrouping -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-01-XX | 01 (receiver foundation) | 1 | TRIG-10 | unit | `go test ./pkg/extension/receiver -run TestComposeWorkflowID -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-02-XX | 02 (http.webhook factory) | 1 | TRIG-08 | unit | `go test ./pkg/extension/builtin/http -run TestHttpWebhook_RejectsBadAlgo -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-02-XX | 02 (http.webhook factory) | 1 | TRIG-08 | unit | `go test ./pkg/extension/builtin/http -run TestHttpWebhook_RejectsBadMethod -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-03-XX | 03 (github.webhook factory) | 1 | TRIG-07 | unit | `go test ./examples/http-github-webhook/extensions/github -run TestGithubWebhookFactory -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-03-XX | 03 (github.webhook factory) | 1 | TRIG-07 | unit | `go test ./examples/http-github-webhook/extensions/github -run TestGithubWebhook_NoSecretInConfig -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-09 | unit | `go test ./pkg/extension/receiver -run TestHandler_GitHubValidSignature -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-09 | unit | `go test ./pkg/extension/receiver -run TestHandler_GitHubBadSignature -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-09 | unit | `go test ./pkg/extension/receiver -run TestHandler_GitHubMissingSignature -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-08 | unit | `go test ./pkg/extension/receiver -run TestHandler_UnsignedHTTPWebhook -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-08 | unit | `go test ./pkg/extension/receiver -run TestSignature_SHA512 -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-07 | unit | `go test ./pkg/extension/receiver -run TestHandler_GitHubEventFilter -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-10 | integration | `go test ./pkg/extension/receiver -run TestHandler_RedeliveryDedup -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-04-XX | 04 (dispatch handler) | 2 | TRIG-10 | unit | `go test ./pkg/extension/receiver -run TestHandler_FanOutDifferentWorkflowIDs -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-05-XX | 05 (worker.WithSDKFactory + unskip) | 2 | TRIG-06 | unit | `go test ./pkg/cli -run TestServerCmd_DrainOnSIGTERM -count=1 -race` | ✅ exists (currently t.Skip) | ⬜ pending |
| 7.1-05-XX | 05 (worker.WithSDKFactory + unskip) | 2 | TRIG-06 | unit | `go test ./pkg/cli -run TestServerCmd_DrainTimeoutExpiry -count=1 -race` | ✅ exists (currently t.Skip) | ⬜ pending |
| 7.1-05-XX | 05 (worker.WithSDKFactory + unskip) | 2 | TRIG-06 | unit | `go test ./pkg/cli -run TestServerCmd_SecondSignalForceExit -count=1 -race` | ✅ exists (currently t.Skip) | ⬜ pending |
| 7.1-06-XX | 06 (server.go listener bind + drain) | 3 | TRIG-06 | integration | `go test ./pkg/cli -run TestServerCmd_ListenerBindsAfterWorkerStart -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-07-XX | 07 (webhook_demo.star) | 3 | EX-05 | integration | `go test ./examples/http-github-webhook -run TestWebhookDemo_BootsCleanly -count=1 -race` | ❌ W0 | ⬜ pending |
| 7.1-08-XX | 08 (walkthrough docs + firewall) | 3 | EX-05 | docs gate | `go test ./tests -run TestWalkthrough_GitHubWebhookHeadings -count=1` | ❌ W0 — new lightweight gate | ⬜ pending |
| 7.1-08-XX | 08 (walkthrough docs + firewall) | 3 | TRIG-09 | unit | `go test ./tests -run TestReceiverFirewall_HMACOnly -count=1 -race` | ❌ W0 — extend firewall test | ⬜ pending |
| 7.1-08-XX | 08 (walkthrough docs + firewall) | 3 | TRIG-09 | unit | `go test ./tests -run TestCredentialRedactionFirewall -count=1 -race` | ✅ exists; extend `targetDirs` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `pkg/extension/receiver/` directory + initial `doc.go` + `receiver_test.go` skeleton with TestMain — package does not yet exist
- [ ] `pkg/extension/receiver/testdata/` golden fixtures (7 files: `github_valid_signature.golden`, `github_bad_signature.golden`, `github_unconfigured_event.golden`, `github_redelivery.golden`, `http_unsigned_post.golden`, `http_signed_sha512.golden`, `fan_out_two_flows.golden`)
- [ ] `pkg/extension/builtin/http/webhook.go` + `webhook_test.go` (extends existing http extension; no new package)
- [ ] `examples/http-github-webhook/extensions/github/webhook.go` + `webhook_test.go` (extends existing github extension; no new package)
- [ ] `pkg/worker/options.go` — `WithSDKFactory` Option + test for option threading
- [ ] `pkg/cli/server_test.go` — unskip the 3 t.Skipped signal-loop tests (DrainOnSIGTERM, DrainTimeoutExpiry, SecondSignalForceExit). The fourth skip (ConnectClient_SelfHosted) is unrelated and remains skipped.
- [ ] `examples/http-github-webhook/webhook_demo.star` — new flow + trigger demonstrating crash-recovery
- [ ] `examples/http-github-webhook/README.md` — append `## GitHub webhook walkthrough (5 minutes)` section
- [ ] `docs/walkthroughs/github-webhook.md` — NEW long-form walkthrough including kill-restart demo
- [ ] `tests/firewall_credential_redaction_test.go` — extend `targetDirs` slice with `pkg/extension/receiver` and confirm `pkg/extension/builtin/http` + `examples/http-github-webhook/extensions/github` cover new files
- [ ] Framework install: NONE needed — testing infrastructure already covers Go stdlib + httptest + testify + testsuite

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Crash-recovery demo (Ctrl-C + restart → workflow completes from event history) | EX-05 | Requires real Temporal server (`skytime dev-temporal`) + signal sequence + visible label appearing in GitHub | (1) `gh extension install cli/gh-webhook` (2) `skytime dev-temporal &` (3) `extbin server &` (4) `gh webhook forward --repo=$USER/test-repo --events=issues --url=http://localhost:8080/webhook/github --secret=$GITHUB_WEBHOOK_SECRET` (5) Open issue → comment appears within seconds (6) `kill -INT %2` to interrupt extbin server (7) restart `extbin server` (8) Within 30s of original delivery, label `processed` appears on issue. Confirms durable workflow continuation. |
| `gh webhook forward` end-to-end against live Temporal (full walkthrough run) | EX-05 | Requires GitHub auth, a test repo, real network delivery from GitHub | Reader follows `examples/http-github-webhook/README.md` § "GitHub webhook walkthrough (5 minutes)" from `git clone` to first webhook-triggered workflow. Optionally: a CI smoke script (`.github/workflows/scripts/walkthrough_smoke.sh` extension) drives the same path with a fixture repo. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s (quick); < 30s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
