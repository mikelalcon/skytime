---
phase: 07.1
plan: 08
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 06, 07]
requirements: [EX-05, TRIG-09]
files_modified:
  - examples/http-github-webhook/README.md
  - docs/walkthroughs/github-webhook.md
  - docs/getting-started.md
  - tests/firewall_credential_redaction_test.go
  - tests/walkthrough_github_webhook_headings_test.go
autonomous: true

must_haves:
  truths:
    - "examples/http-github-webhook/README.md gains a new section titled exactly '## GitHub webhook walkthrough (5 minutes)' covering the 5-minute reader path per D-7.1-17."
    - "docs/walkthroughs/github-webhook.md is a NEW long-form walkthrough including prerequisites, secret setup, full step-by-step, the kill-restart crash-recovery demonstration, troubleshooting, and log line interpretation."
    - "docs/getting-started.md gains a cross-link to docs/walkthroughs/github-webhook.md at the natural follow-on point."
    - "tests/firewall_credential_redaction_test.go targetDirs is extended to include `pkg/extension/receiver` AND `examples/http-github-webhook/extensions/github` (Plan 03 production files; the existing pkg/extension/builtin path covers Plan 02 source files)."
    - "TestWalkthrough_GitHubWebhookHeadings (NEW lightweight gate) asserts the walkthrough doc exists with expected H1 + H2 sections."
    - "The walkthrough docs explicitly call out `gh extension install cli/gh-webhook` as a prerequisite (Pitfall 8)."
    - "All Phase 7.1 tests still pass under -race."
  artifacts:
    - path: "examples/http-github-webhook/README.md"
      provides: "5-minute quickstart walkthrough section."
    - path: "docs/walkthroughs/github-webhook.md"
      provides: "Full long-form walkthrough including kill-restart demonstration + troubleshooting + log line decoder."
    - path: "docs/getting-started.md"
      provides: "Cross-link to walkthrough at the natural follow-on point."
    - path: "tests/firewall_credential_redaction_test.go"
      provides: "Extended targetDirs covering pkg/extension/receiver + examples/http-github-webhook/extensions/github."
    - path: "tests/walkthrough_github_webhook_headings_test.go"
      provides: "TestWalkthrough_GitHubWebhookHeadings — lightweight presence gate for the new walkthrough doc."
  key_links:
    - from: "examples/http-github-webhook/README.md"
      to: "docs/walkthroughs/github-webhook.md"
      via: "Inline link from the 5-minute section to the long-form"
      pattern: "docs/walkthroughs/github-webhook"
    - from: "docs/walkthroughs/github-webhook.md"
      to: "examples/http-github-webhook/webhook_demo.star"
      via: "Reference to the demo flow file"
      pattern: "webhook_demo"
    - from: "tests/firewall_credential_redaction_test.go::TestCredentialRedactionFirewall"
      to: "pkg/extension/receiver"
      via: "targetDirs slice append"
      pattern: 'pkg.*extension.*receiver'
---

<objective>
Ship the EX-05 walkthrough docs in two locations per D-7.1-17 (5-minute README + full docs/walkthroughs page), extend the credential-redaction firewall to cover the new pkg/extension/receiver and examples/http-github-webhook/extensions/github packages (Plan 06's targetDirs only listed `pkg/dag`, `pkg/extension`, `pkg/extension/builtin`), and add a lightweight headings-presence gate so future docs refactors don't silently break the walkthrough.

Purpose: the demo flow (Plan 07) and the receiver dispatcher (Plan 04) are useless without a reader path that takes someone from `git clone` to a webhook-triggered workflow in five minutes. D-7.1-17 explicitly splits this across two locations: the example README's quickstart for fast scanners, plus a full walkthrough page for users hitting issues. Both link to each other.

The firewall extension is the third leg: pkg/extension/receiver is the new home of credential-handling code (the `.Reveal()` call sites in handler.go); the firewall MUST mechanically gate %+v / %#v across this package to preserve D-07-10. Plan 03's github.webhook source — under examples/http-github-webhook/extensions/github — is also new credential-handling code that the firewall did not previously cover (Plan 06 only walked `pkg/extension/builtin`, which does not include example-level extensions).

Output: 1 modified README + 1 new docs page + 1 modified getting-started + 1 modified firewall test + 1 new headings gate test.

NOTE on user decision fidelity: D-7.1-17 verbatim split (5min in README, full in docs/walkthroughs/); Pitfall 8 verbatim (gh extension install cli/gh-webhook prerequisite); D-7.1-15 log line interpretation as a troubleshooting subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.1-http-webhook-receiver-github-source/07.1-CONTEXT.md
@.planning/phases/07.1-http-webhook-receiver-github-source/07.1-RESEARCH.md
@examples/http-github-webhook/README.md
@docs/getting-started.md
@tests/firewall_credential_redaction_test.go

<interfaces>
Existing firewall test shape from Phase 7 Plan 06 (pseudocode summary):

The function TestCredentialRedactionFirewall in tests/firewall_credential_redaction_test.go currently builds a targetDirs slice containing three entries:
- filepath.Join(moduleRoot, "pkg", "dag")
- filepath.Join(moduleRoot, "pkg", "extension")
- filepath.Join(moduleRoot, "pkg", "extension", "builtin")

Plan 08 extends this slice with two additional entries:
- filepath.Join(moduleRoot, "pkg", "extension", "receiver") — Plan 01-04 production files
- filepath.Join(moduleRoot, "examples", "http-github-webhook", "extensions", "github") — Plan 03 production files

The walk pattern (filepath.Walk + go/parser parse + ast.Inspect for fmt.* calls + format-string position lookup in formatPos map + reject %+v / %#v in literal STRING args) is preserved verbatim. Test files (*_test.go) remain exempt.

gh CLI flag set verified by RESEARCH § Sources (gh-webhook v2.x):
- `gh extension install cli/gh-webhook` — install
- `gh webhook forward --repo=<owner>/<repo> --events=<comma-sep> --url=<url> --secret=<value>` — run
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5-minute walkthrough section to examples/http-github-webhook/README.md</name>
  <files>examples/http-github-webhook/README.md</files>
  <read_first>
    - examples/http-github-webhook/README.md (read full to find a natural insertion point and to mirror the README's heading style + code-block conventions)
    - examples/http-github-webhook/.skytime-credentials.example (Plan 07 added the github_webhook_secret entry — verify before writing the README which references it)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-CONTEXT.md (D-7.1-17 split content; D-7.1-16 webhook_demo.star reference)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-RESEARCH.md § Pitfall 8 (gh extension install cli/gh-webhook prerequisite must be the FIRST thing the reader does) and § Sources (verified gh webhook forward flag set: --repo, --events, --url, --secret)
  </read_first>
  <action>
Insert into examples/http-github-webhook/README.md a new section titled exactly `## GitHub webhook walkthrough (5 minutes)`. Place it AT THE NATURAL POSITION based on the README's existing structure — executor's judgment, but D-7.1-17 says "Quickstart section in `examples/http-github-webhook/README.md`" so place near the top after the basic-overview but before any flow-specific walkthroughs.

The section MUST contain (concrete content, not just placeholders):

1. **Heading**: exactly `## GitHub webhook walkthrough (5 minutes)`.

2. **One-paragraph hook** explaining what the walkthrough does and what the reader will see (open issue → comment appears → kill server → restart → label appears).

3. **Cross-link** to the full walkthrough at `docs/walkthroughs/github-webhook.md` (relative path from the README — verify with `ls`).

4. **Subsection `### Prerequisites`** containing:
   - The literal command `gh extension install cli/gh-webhook` (Pitfall 8 — readers without it hit "unknown command" on first try).
   - A note that `gh auth status` must succeed.
   - A note about needing a test repo with admin access.
   - A note about Temporal CLI (`brew install temporal`).

5. **Subsection `### Setup`** showing how to:
   - Generate a random secret with `openssl rand -hex 32` and export to `$GITHUB_WEBHOOK_SECRET`.
   - Append a `[credentials.github_webhook_secret]` entry to `~/.skytime-credentials` (matches the schema entry Plan 07 added to .skytime-credentials.example).
   - chmod 600 the credfile.

6. **Subsection `### Run`** showing four commands the reader runs in four terminals:
   - Terminal 1: `skytime dev-temporal` (starts the local Temporal dev server).
   - Terminal 2: `go run ./examples/http-github-webhook/cmd/extbin server --rootdir=examples/http-github-webhook/ --task-queue=demo --address=localhost:7233 --addr=:8080`.
   - Terminal 3: `gh webhook forward --repo=$USER/test-repo --events=issues --url=http://localhost:8080/webhook/github --secret=$GITHUB_WEBHOOK_SECRET`.
   - Terminal 4: `gh issue create --repo=$USER/test-repo --title "test webhook" --body "hello"`.

7. **Expected behavior section** stating:
   - A "received, processing..." comment appears within seconds.
   - About 30 seconds later, a "processed" label appears on the issue.

8. **Subsection `### Crash-recovery demo`** describing:
   - Kill Terminal 2's server with Ctrl-C BEFORE the 30-second wait completes.
   - Restart it with the same command.
   - The label still appears ~30 seconds after the original delivery — Temporal continued the workflow from event history.
   - One sentence on the headline durability story (workflow progress lives on Temporal's server, not the worker process).

9. **Subsection `### Where to next`** with three relative-path links:
   - Full walkthrough: docs/walkthroughs/github-webhook.md.
   - The demo flow source: ./webhook_demo.star.
   - HTTP webhook source factory docs: ../../docs/for-flow-authors/.

CONCRETE COMMAND VALUES the executor must include verbatim (these are user-facing copy-paste commands; precision matters):

- `gh extension install cli/gh-webhook`
- `gh webhook forward --help`
- `gh auth status`
- `openssl rand -hex 32`
- `chmod 600 ~/.skytime-credentials`
- `skytime dev-temporal`
- `go run ./examples/http-github-webhook/cmd/extbin server --rootdir=examples/http-github-webhook/ --task-queue=demo --address=localhost:7233 --addr=:8080`
- `gh webhook forward --repo=$USER/test-repo --events=issues --url=http://localhost:8080/webhook/github --secret=$GITHUB_WEBHOOK_SECRET`
- `gh issue create --repo=$USER/test-repo --title "test webhook" --body "hello"`

The phrase "received, processing..." (with the trailing ellipsis) and the label string "processed" MUST match the strings in webhook_demo.star (Plan 07).

Verify the README's Markdown renders (no broken code-fences) by running a quick markdown sanity check or by visually inspecting the diff.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero &amp;&amp; grep -F '## GitHub webhook walkthrough (5 minutes)' examples/http-github-webhook/README.md &amp;&amp; grep -F 'gh extension install cli/gh-webhook' examples/http-github-webhook/README.md &amp;&amp; grep -F 'docs/walkthroughs/github-webhook.md' examples/http-github-webhook/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF '## GitHub webhook walkthrough (5 minutes)' examples/http-github-webhook/README.md` returns exactly 1.
    - `grep -F '### Prerequisites' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F '### Setup' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F '### Run' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F '### Crash-recovery demo' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F 'gh extension install cli/gh-webhook' examples/http-github-webhook/README.md` returns ≥ 1 match (Pitfall 8).
    - `grep -F 'docs/walkthroughs/github-webhook.md' examples/http-github-webhook/README.md` returns ≥ 1 match (cross-link).
    - `grep -F 'webhook_demo.star' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F 'received, processing' examples/http-github-webhook/README.md` returns ≥ 1 match (matches Plan 07's flow body).
    - `grep -F 'processed' examples/http-github-webhook/README.md` returns ≥ 1 match (label string).
    - `grep -F 'gh webhook forward' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F 'GITHUB_WEBHOOK_SECRET' examples/http-github-webhook/README.md` returns ≥ 1 match.
    - `grep -F 'openssl rand' examples/http-github-webhook/README.md` returns ≥ 1 match.
  </acceptance_criteria>
  <done>The README's 5-minute walkthrough is in place with all locked commands and cross-links. Reader can scan it in under 30 seconds and copy commands.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/walkthroughs/github-webhook.md long-form walkthrough</name>
  <files>docs/walkthroughs/github-webhook.md</files>
  <read_first>
    - examples/http-github-webhook/README.md (the just-modified file — long-form must be CONSISTENT with 5-min, but more thorough)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-CONTEXT.md (D-7.1-17 long-form scope: prerequisites + secret setup + full step-by-step + kill-restart crash-recovery + troubleshooting + log line interpretation)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-RESEARCH.md § Sources (gh-webhook flag set), § Pitfall 8 (gh-webhook install), § Code Examples (per-request log line example), § Common Pitfalls 1-9 (translate the user-facing ones into troubleshooting entries)
    - examples/http-github-webhook/webhook_demo.star (Plan 07 — the demo flow this doc walks through)
    - docs/getting-started.md (find the natural follow-on insertion point for the cross-link in Task 3)
  </read_first>
  <action>
Create the new file `docs/walkthroughs/github-webhook.md`. Required structure (each H2 must be present; executor fills body):

1. **H1**: `# GitHub webhook walkthrough`. One-paragraph intro explaining the goal (trigger Skytime workflows from real GitHub events without tunnels or OAuth app registration; demonstrates Temporal durability).

2. **H2 `## Prerequisites`** — comprehensive list:
   - gh CLI installed + authenticated (link to https://cli.github.com/).
   - gh-webhook extension installed: `gh extension install cli/gh-webhook` then `gh webhook forward --help` to confirm.
   - Temporal CLI installed: `brew install temporal` on macOS, link to https://docs.temporal.io/cli for other platforms.
   - A test GitHub repository where the reader has admin access (so they can create issues, configure webhooks).
   - The Skytime example built or runnable: `git clone` + `cd skytime` + `go run ./examples/http-github-webhook/cmd/extbin --help` succeeds.

3. **H2 `## Step 1: Generate a webhook signing secret`** — `openssl rand -hex 32` → export to `$GITHUB_WEBHOOK_SECRET`. Explain that the SAME value is passed to both `gh webhook forward --secret=$VAR` AND saved in the credfile so Skytime's HMAC-SHA256 receiver validates X-Hub-Signature-256 (TRIG-09 reference).

4. **H2 `## Step 2: Add the secret to your credfile`** — mirror the format from .skytime-credentials.example; show the full command sequence including chmod 600. Explain the credfile location: `$HOME/.skytime-credentials` by default; `$SKYTIME_CREDFILE_PATH` to override.

5. **H2 `## Step 3: Start the local Temporal dev server`** — `skytime dev-temporal` in terminal 1; explain it spawns a `temporal server start-dev` subprocess; explain the default port 7233.

6. **H2 `## Step 4: Start the Skytime server with the webhook_demo flow`** — full command in terminal 2: `go run ./examples/http-github-webhook/cmd/extbin server --rootdir=examples/http-github-webhook/ --task-queue=demo --address=localhost:7233 --addr=:8080`. Explain each flag. Reference `examples/http-github-webhook/webhook_demo.star` as the source of the trigger + flow being mounted. Note that the startup banner will list the registered triggers including the mount path (D-7.1-15 trigger-line format).

7. **H2 `## Step 5: Forward GitHub webhooks to your local server`** — terminal 3: `gh webhook forward --repo=$USER/test-repo --events=issues --url=http://localhost:8080/webhook/github --secret=$GITHUB_WEBHOOK_SECRET`. Explain that gh-webhook authenticates via existing `gh` credentials and uses the GitHub Apps webhook API to bridge deliveries to your local URL — no OAuth app, no Smee.io, no ngrok.

8. **H2 `## Step 6: Trigger an event`** — `gh issue create --repo=$USER/test-repo --title "test" --body "hello"` from terminal 4. Expected behavior:
   - Within seconds: the comment "received, processing..." appears on the issue.
   - About 30 seconds later: the label "processed" appears on the issue.
   - The skytime server's terminal shows one structured log line per delivery (D-7.1-15 reference).

9. **H2 `## Step 7: Crash-recovery demonstration`** — full sequence:
   - With the comment posted but the 30-second wait still active, kill the skytime server with Ctrl-C in terminal 2.
   - Wait 5-10 seconds (anything before the 30-second timer fires).
   - Restart the skytime server with the SAME command.
   - Watch the "processed" label appear ~30 seconds after the ORIGINAL delivery (NOT after the restart).
   - Headline takeaway: the workflow's state lives on Temporal's server (durable history). The worker process is stateless. Killing it does not lose progress; restarting picks up where the timer left off.

10. **H2 `## Troubleshooting`** — at minimum these subsections (`### N. Symptom`):
    - **401 Unauthorized on every delivery**: the secret in the credfile does NOT match `--secret` passed to gh webhook forward. Use `openssl rand -hex 32` to regenerate and ensure both values are identical.
    - **`unknown command "webhook"` from gh**: install the extension via `gh extension install cli/gh-webhook` (Pitfall 8 verbatim).
    - **Status 502 Bad Gateway**: skytime server cannot reach Temporal. Verify `skytime dev-temporal` is running in terminal 1 and listening on `localhost:7233`. Source providers (gh webhook forward) WILL retry on 502, which is correct (D-7.1-14 reference).
    - **Comment posted but workflow stalls**: check the skytime server logs for `error_class=dispatch_failed`. Most common cause: an outbound `gh.add_label` call failed because the credfile's `github_token` is missing or lacks repo write permissions.
    - **Same delivery fires the workflow twice**: BUG — this should not happen. Verify `WorkflowExecutionErrorWhenAlreadyStarted: true` is set in pkg/extension/receiver/handler.go (Pitfall 1 / D-7.1-08). File a bug.

11. **H2 `## Decoding the structured log line`** — show ONE example line in charm-log default format and ONE in --json-log JSON form. Explain each field per D-7.1-15: method, path, status, duration_ms, source_kind, event, flows_dispatched, workflow_ids, error_class. List the locked error_class taxonomy: signature_mismatch, bad_request, lambda_panic, dispatch_failed, event_filtered, duplicate_skipped, ok.

12. **H2 `## What's next`** — links:
    - [`webhook_demo.star`](../../examples/http-github-webhook/webhook_demo.star) — the demo flow source.
    - [`docs/for-flow-authors/`](../for-flow-authors/) — flow author reference.
    - [`docs/architecture.md`](../architecture.md) — parse/execute split + lambda capture model.

Each H2 must be present (the headings test in Task 5 grep-asserts each one). The body content is the executor's craft — but every concrete command above MUST be reproducible verbatim (no rephrasing of CLI flags or env-var names).
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero &amp;&amp; test -f docs/walkthroughs/github-webhook.md &amp;&amp; grep -cF '## ' docs/walkthroughs/github-webhook.md</automated>
  </verify>
  <acceptance_criteria>
    - File docs/walkthroughs/github-webhook.md exists.
    - `grep -cF '# GitHub webhook walkthrough' docs/walkthroughs/github-webhook.md` returns ≥ 1 (H1).
    - `grep -F '## Prerequisites' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 1' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 2' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 3' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 4' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 5' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 6' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Step 7' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Troubleshooting' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## Decoding the structured log line' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '## What' docs/walkthroughs/github-webhook.md` returns ≥ 1 (matches "What's next" loosely; specific assertion in Task 5).
    - `grep -F 'gh extension install cli/gh-webhook' docs/walkthroughs/github-webhook.md` returns ≥ 1 (Pitfall 8).
    - `grep -F 'gh webhook forward' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F 'webhook_demo.star' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F 'X-Hub-Signature-256' docs/walkthroughs/github-webhook.md` returns ≥ 1 (TRIG-09 mention).
    - `grep -F 'WorkflowExecutionErrorWhenAlreadyStarted' docs/walkthroughs/github-webhook.md` returns ≥ 1 (Pitfall 1 troubleshooting).
    - `grep -F 'error_class' docs/walkthroughs/github-webhook.md` returns ≥ 1 (log-line decoder).
    - `grep -F 'signature_mismatch' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F 'duplicate_skipped' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - `grep -F '401' docs/walkthroughs/github-webhook.md` returns ≥ 1 (troubleshooting status code).
    - `grep -F '502' docs/walkthroughs/github-webhook.md` returns ≥ 1.
    - The doc reads as a full reader path from clone to demo, not a list of fragments.
  </acceptance_criteria>
  <done>The long-form walkthrough exists at docs/walkthroughs/github-webhook.md with all required H2 sections and concrete reproducible commands.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link from docs/getting-started.md to the new walkthrough</name>
  <files>docs/getting-started.md</files>
  <read_first>
    - docs/getting-started.md (read full — find the natural follow-on point AFTER the basic extbin run demo where readers would naturally ask "what about real triggers?")
    - docs/walkthroughs/github-webhook.md (just-created — verify the relative link target)
  </read_first>
  <action>
Add a short cross-link to docs/getting-started.md at the natural follow-on point — likely near the END of the basic tutorial, immediately after the "you've run your first flow" payoff and before the "what's next" section if one exists.

The link content (verbatim, but executor adapts surrounding markdown to the page's existing style):

> ## Triggering flows from real events
>
> The basic walkthrough above invoked your flow via `extbin run`. To trigger flows from external events (GitHub webhooks, cron schedules, etc.), see the long-form walkthrough at [`docs/walkthroughs/github-webhook.md`](walkthroughs/github-webhook.md). It demonstrates Temporal's durability story end-to-end: kill the worker mid-flow, restart, watch the workflow continue from event history.

If docs/getting-started.md already has a "What's next" or "Where to go from here" section, integrate the link there as a bullet point instead of a new H2 — executor's judgment on placement, but the link MUST resolve.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero &amp;&amp; grep -F 'walkthroughs/github-webhook.md' docs/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'walkthroughs/github-webhook.md' docs/getting-started.md` returns ≥ 1 match (cross-link present).
    - The link target is correct relative to docs/getting-started.md location: `walkthroughs/github-webhook.md` (since docs/getting-started.md is in docs/, sibling to docs/walkthroughs/).
    - The surrounding text contextualizes the link — at minimum, mentions "trigger" or "webhook" or "external events" so readers understand why to follow it.
  </acceptance_criteria>
  <done>getting-started.md cross-links to the walkthrough at the natural follow-on point. Discoverability gap closed.</done>
</task>

<task type="auto">
  <name>Task 4: Extend tests/firewall_credential_redaction_test.go targetDirs to cover Plan 01-04 + Plan 03 packages</name>
  <files>tests/firewall_credential_redaction_test.go</files>
  <read_first>
    - tests/firewall_credential_redaction_test.go (read full — current targetDirs slice at line ~44; matchViolations helper around line 122; module-root finder at line 190)
    - .planning/phases/07-trigger-primitive-server-shell/07-06-SUMMARY.md (firewall test was created in Phase 7 Plan 06; understand the targetDirs shape)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-CONTEXT.md (Phase 7.1's new packages: pkg/extension/receiver and examples/http-github-webhook/extensions/github both need firewall coverage)
  </read_first>
  <action>
Edit tests/firewall_credential_redaction_test.go::TestCredentialRedactionFirewall. Locate the targetDirs slice (currently 3 entries: `pkg/dag`, `pkg/extension`, `pkg/extension/builtin`).

APPEND two new entries:

- `filepath.Join(moduleRoot, "pkg", "extension", "receiver")` — Plan 01-04 production files. Critical: handler.go has multiple `.Reveal()` call sites for HMAC computation; the firewall MUST mechanically gate %+v / %#v on this package.
- `filepath.Join(moduleRoot, "examples", "http-github-webhook", "extensions", "github")` — Plan 03 production files. github.webhook source carries a credential ID; the firewall coverage extends to this example-level extension.

Update the doc comment at the top of the file (the package-level comment around line 1-23) to mention Phase 7.1's new directories. The existing comment says "Phase 7 ships zero concrete TriggerSource — only pkg/dag/trigger.go has the Trigger type. Phase 7.1 will extend the firewall when github.WebhookSource lands by walking the new extension subdirectory; today the directory list is conservative." — UPDATE to reflect that Phase 7.1 NOW lands those packages and the targetDirs reflects this.

Also update the t.Logf() success line: from "scanned %d target dir(s); no %%+v / %%#v in production code" — same string but the count changes from 3 to 5.

Run `go test ./tests -run TestCredentialRedactionFirewall -count=1 -race` — exits 0. If any production file in the new directories contains `%+v` or `%#v` in a fmt-call literal, the test fails — that's a real bug to fix in the relevant source plan (NOT in this plan).
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero &amp;&amp; go test ./tests -run TestCredentialRedactionFirewall -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'filepath.Join(moduleRoot, "pkg", "extension", "receiver")' tests/firewall_credential_redaction_test.go` returns 1 match.
    - `grep -F 'filepath.Join(moduleRoot, "examples", "http-github-webhook", "extensions", "github")' tests/firewall_credential_redaction_test.go` returns 1 match.
    - `grep -cF 'filepath.Join(moduleRoot' tests/firewall_credential_redaction_test.go` returns ≥ 5 (3 original + 2 new).
    - `go test ./tests -run TestCredentialRedactionFirewall -count=1 -race` exits 0.
    - Test passes — meaning no `%+v` or `%#v` in fmt calls in pkg/extension/receiver or examples/http-github-webhook/extensions/github (verified mechanically by the firewall walk).
    - The doc comment at the top of the file is updated to mention Phase 7.1's new directories.
    - `go test ./... -count=1 -race` exits 0 (full repo green).
  </acceptance_criteria>
  <done>The credential-redaction firewall mechanically gates %+v/%#v across all Phase 7.1 production packages. D-07-10 invariant preserved end-to-end.</done>
</task>

<task type="auto">
  <name>Task 5: Add tests/walkthrough_github_webhook_headings_test.go presence gate</name>
  <files>tests/walkthrough_github_webhook_headings_test.go</files>
  <read_first>
    - docs/walkthroughs/github-webhook.md (just-created in Task 2 — extract the H2 section names that the gate will assert)
    - tests/firewall_credential_redaction_test.go (template for `package firewall_test` external-test-package convention; the new file lives alongside per the existing tests/ structure)
    - tests/dev_server_grep_test.go (Phase 7 Plan 06 — similar lightweight gate pattern; mirror the structure)
  </read_first>
  <action>
Create tests/walkthrough_github_webhook_headings_test.go in `package firewall_test` (matches the file convention for tests/). Test function: `TestWalkthrough_GitHubWebhookHeadings`.

Test logic:
1. Find the module root (reuse the helper pattern from tests/firewall_credential_redaction_test.go — copy or import the `findModuleRootRedaction` style).
2. Read the file at `<moduleRoot>/docs/walkthroughs/github-webhook.md`.
3. Assert the file exists (require.FileExists or os.Stat + require.NoError).
4. Read the bytes.
5. For each REQUIRED heading string, assert `bytes.Contains(content, []byte(heading))`. The required strings (must match Task 2's H2 content):
   - "# GitHub webhook walkthrough" (H1)
   - "## Prerequisites"
   - "## Step 1: Generate a webhook signing secret"
   - "## Step 2: Add the secret to your credfile"
   - "## Step 3: Start the local Temporal dev server"
   - "## Step 4: Start the Skytime server with the webhook_demo flow"
   - "## Step 5: Forward GitHub webhooks to your local server"
   - "## Step 6: Trigger an event"
   - "## Step 7: Crash-recovery demonstration"
   - "## Troubleshooting"
   - "## Decoding the structured log line"
   - "## What's next"
6. For each Pitfall-8 / TRIG-09 / D-7.1-14 reference, assert presence:
   - `gh extension install cli/gh-webhook` (Pitfall 8)
   - `gh webhook forward` (the headline command)
   - `webhook_demo.star` (cross-link)
   - `X-Hub-Signature-256` (TRIG-09)
   - `error_class` (D-7.1-15)
7. Test should be FAST — no parse, no AST walk; just bytes.Contains.

Provide a clear failure message that names which heading is missing (e.g., `t.Errorf("docs/walkthroughs/github-webhook.md missing required heading %q", heading)`).

The test does NOT enforce that headings appear in order (some refactors may reorder); it only enforces presence. If the executor wants stricter ordering, that's a future test — out of scope for this plan.

Run `go test ./tests -run TestWalkthrough_GitHubWebhookHeadings -count=1 -race` — exits 0.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero &amp;&amp; go test ./tests -run TestWalkthrough_GitHubWebhookHeadings -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - File tests/walkthrough_github_webhook_headings_test.go exists.
    - `grep -F 'package firewall_test' tests/walkthrough_github_webhook_headings_test.go` returns 1 match.
    - `grep -F 'TestWalkthrough_GitHubWebhookHeadings' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match.
    - `grep -F 'github-webhook.md' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match (file path reference).
    - `grep -F '# GitHub webhook walkthrough' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match (H1 assertion).
    - `grep -F '## Prerequisites' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match.
    - `grep -F '## Troubleshooting' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match.
    - `grep -F 'gh extension install cli/gh-webhook' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match.
    - `grep -F 'X-Hub-Signature-256' tests/walkthrough_github_webhook_headings_test.go` returns ≥ 1 match.
    - `go test ./tests -run TestWalkthrough_GitHubWebhookHeadings -count=1 -race` exits 0.
    - `go test ./... -count=1 -race` exits 0 (full repo green).
  </acceptance_criteria>
  <done>The walkthrough doc has a mechanical headings gate. Future doc reformats that drop a required H2 will fail CI.</done>
</task>

</tasks>

<verification>
- examples/http-github-webhook/README.md gains the "## GitHub webhook walkthrough (5 minutes)" section.
- docs/walkthroughs/github-webhook.md exists with all required H2 sections.
- docs/getting-started.md cross-links to the walkthrough.
- tests/firewall_credential_redaction_test.go targetDirs covers pkg/extension/receiver AND examples/http-github-webhook/extensions/github.
- tests/walkthrough_github_webhook_headings_test.go enforces the H1 + 11 H2 sections.
- `go test ./... -count=1 -race` exits 0.
- All Pitfall references (8 specifically — gh-webhook install) appear verbatim in the docs.
</verification>

<success_criteria>
- A reader following the example README's 5-minute section can go from `git clone` to a webhook-triggered workflow in five minutes.
- A reader hitting issues finds detailed troubleshooting + log-line interpretation in the long-form walkthrough.
- The credential-redaction firewall mechanically covers all Phase 7.1 production packages (pkg/extension/receiver + the github.webhook source).
- Future doc refactors that accidentally drop a required H2 fail the lightweight headings gate.
- EX-05 ROADMAP success criteria 4 + 5 (walkthrough end-to-end + crash-recovery demo runs as documented) are achievable from these artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-http-webhook-receiver-github-source/07.1-08-SUMMARY.md`.
</output>
