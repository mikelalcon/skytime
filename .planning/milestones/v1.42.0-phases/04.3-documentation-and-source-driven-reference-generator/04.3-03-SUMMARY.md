---
phase: 04.3-documentation-and-source-driven-reference-generator
plan: 03
subsystem: docs
tags: [architecture, parse-execute-split, lambda-capture, ascii-diagram, two-tier]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: pkg/parser/globals.go (parse-time globals), pkg/dag/lambda.go (CapturedLambda + StarlarkLambda + ComputeLambdaID)
  - phase: 02-temporal-activity-execute-batch
    provides: pkg/activity/ExecuteBatch (single generic activity), firewall test
  - phase: 03-lambda-serialization-decision-interpreter-worker
    provides: pkg/interpreter (SkytimeWorkflow + FlowRegistry), pkg/interpreter/cancel_watchdog.go, pkg/worker (bootRegistry, Build IDs)
  - phase: 04.3
    provides: Phase boundary (D-15 architecture page = required reading for both audiences); D-19 scope (only what is built today)
provides:
  - docs/architecture.md as the required-reading architecture page for both audiences (D-15)
  - Verbatim ASCII boundary diagram (no Mermaid in v1)
  - Cross-link spine into reference (plan 02), getting-started (plan 07), and audience landings (plan 09)
affects: [04.3-04, 04.3-05, 04.3-07, 04.3-08, 04.3-09]

# Tech tracking
tech-stack:
  added: []  # No new dependencies — pure prose
  patterns:
    - "Doc cites concrete source files for every architectural claim (pkg/parser/globals.go, pkg/dag/lambda.go, pkg/interpreter/workflow.go, pkg/activity/execute_batch.go, pkg/bridge/lambda_call.go, pkg/interpreter/registry.go, pkg/interpreter/cancel_watchdog.go)"
    - "Verbatim quotation of D4.1-22 + D4.2-05 carve-outs from PROJECT.md to keep architectural-rule wording single-sourced"
    - "ASCII art boundary diagram (no Mermaid/PlantUML tooling) for v1 per CONTEXT.md specifics"

key-files:
  created:
    - "docs/architecture.md (396 lines, 9 H2 + 3 H3 sections)"
  modified: []

key-decisions:
  - "Architecture page lives at docs/architecture.md per D-15; required reading for both audiences"
  - "ASCII boundary diagram reproduced verbatim from CONTEXT.md interfaces block — locked content"
  - "D4.1-22 + D4.2-05 carve-out blockquotes copied verbatim from PROJECT.md (single source of truth for architectural rules)"
  - "Cross-links to docs/for-flow-authors/README.md and docs/for-extension-developers/README.md anticipate plan 09 outputs (link targets do not exist yet — intentional forward reference)"

patterns-established:
  - "Doc voice: pragmatic, code-forward, no marketing speak (matches CLAUDE.md's existing tone)"
  - "Each major section opens with a load-bearing sentence + cites the source-of-truth file"
  - "ASCII art for diagrams (no Mermaid/PlantUML in v1)"

requirements-completed: [D-15, D-19]

# Metrics
duration: 4min
completed: 2026-05-05
---

# Phase 04.3 Plan 03: Architecture overview required reading Summary

**docs/architecture.md as the required-reading architecture page for both audiences — parse/execute split prosaically AND via ASCII boundary diagram, with verbatim D4.1-22 / D4.2-05 carve-out quotations and source-file citations to pkg/parser, pkg/interpreter, pkg/bridge, pkg/activity, pkg/dag.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-05T00:10:03Z
- **Completed:** 2026-05-05T00:14:19Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Wrote docs/architecture.md with the locked 9 H2 + 3 H3 structure per the plan's `<interfaces>` section.
- Reproduced the ASCII boundary diagram verbatim (parse → boundary → execute → activity → extension I/O).
- Quoted D4.1-22 and D4.2-05 carve-outs verbatim from PROJECT.md so architectural-rule wording is single-sourced.
- Cited pkg/parser/globals.go, pkg/dag/lambda.go (CapturedLambda + ComputeLambdaID), pkg/interpreter/workflow.go (SkytimeWorkflow factory), pkg/activity/execute_batch.go (ExecuteBatch), pkg/bridge/lambda_call.go (CallLambda), pkg/interpreter/registry.go (FlowRegistry), pkg/interpreter/cancel_watchdog.go (workflow.Context.Done() bridge).
- Cross-linked to docs/reference/builtins.md (plan 02), docs/getting-started.md (plan 07), docs/for-flow-authors/README.md + docs/for-extension-developers/README.md (plan 09), and existing docs/cli-binary.md.

## Task Commits

1. **Task 1: Write docs/architecture.md with locked section structure + ASCII diagram** — `c2b6f98` (docs)

## Files Created/Modified

- `docs/architecture.md` — 396 lines. 9 H2 sections (The Parse/Execute Split, The Boundary Diagram, Why Starlark at Parse Time, Why Temporal at Execution Time, Lambda Capture Model, Deterministic Re-parse on Workflow Start, The Three "No" Rules, Two-Tier Authoring Model, Where to Go Next) + 3 H3 sub-sections (No String Compilation, No Dynamic Activities, No Context Bleed).

## Section Structure (9 H2 + 3 H3)

| # | Heading | Lines | Anchor claims |
|---|---------|-------|---------------|
| H2-1 | The Parse/Execute Split | ~50 | parse-time = no I/O + lambda capture + DAG build; execute-time = SkytimeWorkflow + ExecuteBatch + history |
| H2-2 | The Boundary Diagram | ~35 | ASCII art verbatim from CONTEXT.md; "How to read this diagram" prose |
| H2-3 | Why Starlark at Parse Time | ~25 | deterministic, sandboxed (20-key lambda subset), expressive enough, parser-pure |
| H2-4 | Why Temporal at Execution Time | ~17 | durable, retryable, composable; SkytimeWorkflow is THE one workflow |
| H2-5 | Lambda Capture Model | ~62 | CapturedLambda struct verbatim + D-18 ID format + carve-out blockquotes |
| H2-6 | Deterministic Re-parse on Workflow Start | ~30 | Option B mechanism; FlowRegistry; WorkflowInput; Build IDs are operational |
| H2-7 | The Three "No" Rules | ~65 | Three H3 sub-sections below |
| H3-7a | No String Compilation | — | D4.1-22 carve-out blockquote + rationale + desugarer cite |
| H3-7b | No Dynamic Activities | — | firewall test name + ACT-01 single-activity rationale |
| H3-7c | No Context Bleed | — | bridge.CallLambda fresh-thread + cancel_watchdog rationale |
| H2-8 | Two-Tier Authoring Model | ~21 | Go/Starlark audience split; reusable across customers vs per-customer |
| H2-9 | Where to Go Next | ~10 | bullet list of forward links (incl. existing docs/cli-binary.md) |

## Key Claims and Source Citations

| Claim | Source file |
|-------|-------------|
| Parse-time globals enumeration (DSL primitives + extensions) | `pkg/parser/globals.go::newParseTimeGlobals` |
| Lambda capture model + D-18 ID format | `pkg/dag/lambda.go::CapturedLambda` + `dag.ComputeLambdaID` |
| Execute-time entry point | `pkg/interpreter::SkytimeWorkflow` (registered name; factory `NewWorkflow`) |
| Single generic activity for all I/O | `pkg/activity/execute_batch.go::Activity.ExecuteBatch` (ACT-01) |
| Lambda evaluation primitive | `pkg/bridge/lambda_call.go::CallLambda` (fresh thread per call, Pitfall #1) |
| FlowRegistry frozen-after-boot | `pkg/interpreter/registry.go` |
| Cancel-channel bridge | `pkg/interpreter/cancel_watchdog.go` (D3-21) |
| Worker boot + Build ID opt-in | `pkg/worker::bootRegistry` (Phase 03 plan 04) |
| Firewall meta-test | `pkg/activity/firewall_test.go::TestNoTemporalImportsOutsideAllowList` (allow-list = `{activity, interpreter, worker}`) |
| Lambda-time predeclared globals (20-key subset) | `pkg/bridge/lambda_globals.go` (PARSE-03, D-20) |
| Parse-time ${ctx.expr} desugarer | `pkg/parser/builtins.go::desugarInterpolation` (D4.1-22) |

## Verbatim Quotations

- D4.1-22 carve-out (parser-time syntactic sugar) — quoted twice (Lambda Capture Model + No String Compilation sections) to keep both architectural references load-bearing.
- D4.2-05 carve-out (parse-time top-level `fail("msg")`) — quoted in Lambda Capture Model section.
- PROJECT.md "no string compilation" rule — quoted in No String Compilation H3.
- PROJECT.md Key Decision row "Starlark over CEL or custom DSL" — quoted in Why Starlark at Parse Time.
- PROJECT.md Key Decision row "Lambda serialization via re-parse on workflow start (Option B) + Build IDs" — quoted in Deterministic Re-parse on Workflow Start.

## Required-Phrase Inventory Verification

All required phrases from `<interfaces>::Required Phrase Inventory` are present:

| Phrase | Required count | Actual count |
|--------|---------------|--------------|
| `parse-time` | ≥ 5 | 11 |
| `execution-time` / `execute-time` | ≥ 5 | 6 |
| `no string compilation` / `No String Compilation` | ≥ 1 | 2 |
| `no dynamic activities` / `No Dynamic Activities` | ≥ 1 | 2 |
| `no context bleed` / `No Context Bleed` | ≥ 1 | 3 |
| `content-hash` / `FlowRegistry` | ≥ 1 | 8 |
| `CapturedLambda` / `starlark.Function` | ≥ 1 | 15 |
| `ExecuteBatch` | ≥ 1 | 5 |
| `SkytimeWorkflow` | ≥ 1 | 4 |
| `D4.1-22` | ≥ 1 | 3 |
| `D4.2-05` | ≥ 1 | 2 |
| `two-tier` / `Two-Tier` | ≥ 1 | 2 |
| `docs/reference/builtins.md` | ≥ 1 | 2 |
| `docs/getting-started.md` | ≥ 1 | 1 |
| `WORKFLOW BOUNDARY` (ASCII divider) | = 1 | 1 |
| ASCII diagram arrows (`pkg/parser ──►` / `workflow.Go ──►`) | ≥ 2 | 2 |
| `pkg/(parser|interpreter|bridge|activity|dag)` citations | ≥ 5 | 25 |

## Decisions Made

- **Existing CLI doc cross-link:** Added a forward link to `docs/cli-binary.md` (already exists) under "Where to Go Next" alongside the planned plan-09 audience landings — gives the page a working internal link from day one.
- **Carve-out quotation strategy:** Both D4.1-22 and D4.2-05 carve-outs reproduced verbatim from PROJECT.md inside the Lambda Capture Model section (where the lambda-capture infrastructure they ride on is explained) AND D4.1-22 quoted again in the No String Compilation H3 section (where the rule it carves out is stated). Single-source of architectural-rule wording in PROJECT.md; doc references it twice for context.

## Deviations from Plan

None — plan executed exactly as written. The plan was extremely tightly specified (locked section structure, verbatim ASCII diagram, required-phrase inventory). All 22 acceptance-criteria grep checks pass on first verification pass after one minor adjustment (added two more `execution-time` / `execute-time` references in natural locations to clear the ≥ 5 floor — three of the five existing references had used the unhyphenated "execute time" / "execution time" form which the grep regex did not match).

## Issues Encountered

- After initial write the `execution-time|execute-time` grep count was 3 (need ≥ 5) because some prose used unhyphenated spacing ("at execute time", "execution time"). Fixed by hyphenating two instances and adding one new "execution-time" sentence in the Why Temporal at Execution Time section. No other grep counts needed adjustment.
- Similarly, the introductory sentence enumerated only "no context bleed" of the three "no" rules; updated to enumerate all three so the Three "No" Rules section gets a clean table-of-contents from the introduction (raised "no dynamic activities" from 1 to 2 occurrences and "no context bleed" stayed at 3).

## User Setup Required

None — pure prose addition.

## Next Phase Readiness

- docs/architecture.md is ready as required-reading for both audiences.
- Cross-links forward to:
  - `docs/reference/builtins.md` — produced by plan 04.3-02 (auto-generated builtins reference). Link target exists if plan 02 has shipped; otherwise it is a forward reference that resolves once plan 02 completes.
  - `docs/getting-started.md` — produced by plan 04.3-07 (run-an-example tutorial). Forward reference.
  - `docs/for-flow-authors/README.md` and `docs/for-extension-developers/README.md` — produced by plan 04.3-09 (audience landings). Forward references.
  - `docs/cli-binary.md` — already exists in repo (Phase 04 output).

## Open Items for Downstream Plans

- **Plan 04.3-04 (CLI reference):** No coordination needed — this doc references existing `docs/cli-binary.md`; if plan 04 renames or reshapes it, update the "Where to Go Next" bullet.
- **Plan 04.3-07 (getting-started tutorial):** docs/getting-started.md must exist at the linked path. The architecture doc points readers there as the next-step tutorial.
- **Plan 04.3-09 (audience landings):** docs/for-flow-authors/README.md and docs/for-extension-developers/README.md must exist at the linked paths. Both are referenced explicitly.
- **Plan 04.3-08 (README):** README.md should link back into docs/architecture.md as the architectural deep-dive (this doc is the place that explains the parse/execute split in depth).

## Self-Check: PASSED

- `[x]` File exists: `docs/architecture.md` (396 lines)
- `[x]` Commit `c2b6f98` exists in git log on branch `main`
- `[x]` All 22 acceptance-criteria grep checks pass (line count, H2/H3 counts and order, locked phrase inventory, source-file citations, ASCII diagram divider + arrows)

---
*Phase: 04.3-documentation-and-source-driven-reference-generator*
*Completed: 2026-05-05*
