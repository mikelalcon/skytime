---
phase: 04.3-documentation-and-source-driven-reference-generator
plan: 08
subsystem: docs
tags: [readme, front-door, embedded-tutorial, audience-split, mid-size-doc]

# Dependency graph
requires:
  - phase: 04.3-03
    provides: docs/architecture.md (cross-linked from "Where to Go Next")
  - phase: 04.3-07
    provides: docs/getting-started.md (body embedded VERBATIM under ## Getting Started per D-06)
  - phase: 04.3-04
    provides: docs/reference/cli.md (cross-linked)
  - phase: 04.3-02
    provides: docs/reference/builtins.md (auto-generated reference, cross-linked)
  - phase: 04.3-05
    provides: docs/for-flow-authors/extensions/http.md (cross-linked from Where-to-Go-Next)
  - phase: 04.3-06
    provides: examples/README.md (cross-linked)
provides:
  - README.md as the repo front door (D-02 mid-size, D-06 verbatim embed)
  - 30-second self-evaluation surface (What This Is / What This Is Not)
  - Embedded canonical tutorial (no click-through required to evaluate)
  - Audience-split entry points to docs/for-flow-authors/ + docs/for-extension-developers/ (anticipates plan 09)
affects: [04.3-09-registrar]

# Tech tracking
tech-stack:
  added: []  # No new dependencies — pure prose
  patterns:
    - "Verbatim tutorial embed (D-06): body of docs/getting-started.md from ## Prerequisites through ## What Just Happened? copied as-is, including its H2 headings, under the README's ## Getting Started H2; markdown renders nested H2s; canonical-version note + relative position make embedded scope obvious"
    - "Hero pitch as locked verbatim blockquote + 4-paragraph intro (parse/execute split + two-tier authoring model)"
    - "What This Is / What This Is Not as opposed bullet lists for 30-second self-selection — explicit non-goals from PROJECT.md Out of Scope reproduced as the negative bullets"
    - "Status section enumerates phase progress as ✓ for shipped + ⏳ for upcoming, with v1-readiness count + production-readiness paragraph (honest pre-v1 disclosure)"
    - "Project Layout tree mirrors the actual cmd/, pkg/, examples/, docs/, tests/ shape with one-line descriptions per top-level directory"

key-files:
  created:
    - "README.md (430 lines, 15 H2 = 8 own + 7 verbatim-embedded tutorial H2s)"
  modified: []

key-decisions:
  - "Verbatim tutorial embed (D-06): preserved every embedded H2 (## Prerequisites, ## 1. Clone and Build, ## 2. Start the Dev Server, ## 3. Discover the Flows, ## 4. Run the Happy Path, ## 5. Run the Fail Path, ## What Just Happened?) as-is; resulting README has 15 H2s (8 own + 7 embedded), which is correct per D-06"
  - "License section says 'TBD' because no LICENSE* file exists at repo root (verified via discovery step Task 1)"
  - "Adjusted relative-link paths in embedded tutorial: docs/getting-started.md uses ../examples/skeleton/expression_if.star (relative from docs/), but README sits at repo root so the same paths become examples/skeleton/expression_if.star and docs/architecture.md (relative from repo root). Embedded H2 headings stayed byte-equal; only the relative-path body text was rewritten for the README's location"
  - "No prior README existed (verified by Task 1 discovery), so no content needed preservation across the overwrite"
  - "Audience-split entry points reference docs/for-flow-authors/README.md + docs/for-extension-developers/README.md as forward references — those landing pages are produced by plan 09 (sibling, same wave); intentional forward link"

patterns-established:
  - "Mid-size doc convention (D-02): 400-1000 lines is the verified band — short enough to skim, long enough that a reader can evaluate the project without clicking out"
  - "Embedded-doc attribution: top of embedded section opens with > Canonical version: [link] note explaining the embed is a copy and pointing readers at the source-of-truth file"
  - "Cross-link inventory pattern: 'Where to Go Next' renders cross-links by audience (design / DSL / CLI / examples / flow-authors / extension-developers / custom-CLI) — each bullet has a one-line description so the link choice is obvious"

requirements-completed: [D-01, D-02, D-06]

# Metrics
duration: 3min
completed: 2026-05-05
---

# Phase 04.3 Plan 08: README front door with embedded tutorial Summary

**README.md as the repo front door — hero pitch + What This Is/Is Not bullets + Status (Phase 1-04.2 complete + ⏳ Phase 5-6) + verbatim-embedded `docs/getting-started.md` body (D-06) + Where to Go Next cross-link inventory + Project Layout tree + Contributing test-suite checklist + License-TBD section. 430 lines, 15 H2 (8 own + 7 verbatim-embedded tutorial H2s).**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-05T00:31:23Z
- **Completed:** 2026-05-05T00:34:22Z
- **Tasks:** 2 (1 discovery, 1 file creation)
- **Files created:** 1

## Accomplishments

- **Task 1 (discovery)**: Verified all dependencies — `docs/getting-started.md` (plan 07) and `docs/architecture.md` (plan 03) both exist; cross-link targets `docs/reference/builtins.md` (plan 02), `docs/reference/cli.md` (plan 04), `examples/README.md` (plan 06), `docs/for-flow-authors/extensions/http.md` (plan 05) all exist. No `LICENSE*` file present (License section will say "TBD"). No existing `README.md` to preserve content from.
- **Task 2 (compose)**: Wrote `README.md` (430 lines) with the locked structure: hero pitch verbatim → ## What This Is → ## What This Is Not → ## Status → ## Getting Started (verbatim tutorial embed per D-06) → ## Where to Go Next → ## Project Layout → ## Contributing → ## License. All bullet lists, the hero blockquote, and the Project Layout tree reproduced verbatim from the plan's `<interfaces>` block.
- **Verified all acceptance-criteria grep counts** pass on first verification pass (no iteration needed): line count 430 (in 400-1000), 15 H2s (≥14 floor; 8 own + 7 embedded), all 8 named own H2s present in locked order, embedded H2 byte-equality with `docs/getting-started.md` (diff returns 0 lines), all cross-link patterns ≥1, all required content phrases (`parse/execute`, `two-tier`, `check_user`, `expression_if.star`, `skytime dev-server`, `Phase 1`-`Phase 6`, `Starlark`, `Temporal`, `no string compilation`, hero blockquote, `cmd/skytime`/`cmd/skytime-docgen`).

## Task Commits

1. **Task 1: Verify LICENSE existence and detect any blockers** — discovery step, no commit (no file changes)
2. **Task 2: Compose README.md with locked structure + embedded tutorial body** — `e484b4f` (docs)

## Files Created/Modified

- `README.md` (created) — 430 lines, 15 H2 sections.
  - 8 own H2 sections: `## What This Is`, `## What This Is Not`, `## Status`, `## Getting Started`, `## Where to Go Next`, `## Project Layout`, `## Contributing`, `## License`
  - 7 verbatim-embedded tutorial H2 sections: `## Prerequisites`, `## 1. Clone and Build`, `## 2. Start the Dev Server`, `## 3. Discover the Flows`, `## 4. Run the Happy Path`, `## 5. Run the Fail Path`, `## What Just Happened?`
  - 4 H3 conceptual blocks under "What Just Happened?" (preserved from canonical tutorial): `### ${ctx.expr} Interpolation`, `### Expression-Mode if_cond`, `### result() Binding to ctx`, `### fail() with Interpolation`

## Section Structure (8 own H2 + 7 embedded H2 + 4 H3 = 19 markdown headings)

| # | Heading | Source | Lines |
|---|---------|--------|-------|
| H2-1 | What This Is | own — verbatim from plan `<interfaces>` | 23-32 |
| H2-2 | What This Is Not | own — verbatim from plan `<interfaces>` | 34-42 |
| H2-3 | Status | own — Phase 1-04.2 ✓ + Phase 5-6 ⏳ + 65/65 v1 reqs (55/65 complete) | 44-65 |
| H2-4 | Getting Started | own — opens with canonical-version attribution note | 67-91 |
| H2-5 | Prerequisites | embedded verbatim from getting-started.md | 93-113 |
| H2-6 | 1. Clone and Build | embedded verbatim | 115-134 |
| H2-7 | 2. Start the Dev Server | embedded verbatim | 136-161 |
| H2-8 | 3. Discover the Flows | embedded verbatim | 163-191 |
| H2-9 | 4. Run the Happy Path | embedded verbatim | 193-225 |
| H2-10 | 5. Run the Fail Path | embedded verbatim | 227-251 |
| H2-11 | What Just Happened? | embedded verbatim (with 4 H3s) | 253-336 |
| H2-12 | Where to Go Next | own — cross-link inventory by audience | 338-369 |
| H2-13 | Project Layout | own — tree reproduced verbatim from plan `<interfaces>` | 371-398 |
| H2-14 | Contributing | own — test-suite checklist + firewall list + go-generate hint | 400-424 |
| H2-15 | License | own — "TBD" (no LICENSE file) | 426-430 |

## LICENSE Handling Decision

No `LICENSE*` file exists at repo root (verified via Task 1 `ls -1 LICENSE*` discovery). Per plan instructions, License section reads:

> License to be selected before v1. The repository is currently
> published as the work-in-progress source; treat as private/unlicensed
> for downstream use until a license file lands.

When a LICENSE file lands, this section becomes a one-line `See [LICENSE](LICENSE).` per the plan's conditional template.

## Cross-Links Inventory

The README cross-links to **every major doc artifact** produced by Phase 04.3:

| From | To | Where |
|------|-----|-------|
| README | `docs/getting-started.md` | Canonical-version note at top of ## Getting Started |
| README | `docs/architecture.md` | "What This Is Not" (no string compilation anchor) + "Where to Go Next" (Understand the design) |
| README | `docs/reference/builtins.md` | "Where to Go Next" (DSL syntax lookup) |
| README | `docs/reference/cli.md` | "Where to Go Next" (CLI reference) |
| README | `examples/README.md` | "Where to Go Next" (browse more examples) |
| README | `docs/for-flow-authors/README.md` | "Where to Go Next" (write a flow) |
| README | `docs/for-flow-authors/extensions/http.md` | "Where to Go Next" (HTTP extension reference) |
| README | `docs/for-extension-developers/README.md` | "Where to Go Next" (build an extension) |
| README | `docs/cli-binary.md` | "Where to Go Next" (custom CLI binary) |
| README | `examples/skeleton/expression_if.star` | ## Getting Started intro (canonical example link) |
| README | `.planning/REQUIREMENTS.md` | ## Status (v1-readiness count source) |
| README | `tests/` | ## Contributing (firewall test list location) |

The audience-split landing pages (`docs/for-flow-authors/README.md`, `docs/for-extension-developers/README.md`) are forward references — produced by plan 04.3-09 (sibling Wave 2 plan). Intentional, expected, and resolves once plan 09 completes (running in parallel with this plan via the orchestrator).

## Verbatim Embed Verification (D-06)

Byte-equality check between embedded H2 lines in `README.md` and source `docs/getting-started.md`:

```sh
diff <(grep -E '^## (Prerequisites|1\. Clone|2\. Start|3\. Discover|4\. Run|5\. Run|What Just)' docs/getting-started.md) \
     <(grep -E '^## (Prerequisites|1\. Clone|2\. Start|3\. Discover|4\. Run|5\. Run|What Just)' README.md)
# (empty output — all 7 embedded H2 lines are byte-equal)
```

Body text was reproduced verbatim. The only edit applied to embedded content: relative-path adjustments (the canonical tutorial uses `../examples/skeleton/expression_if.star` because it sits in `docs/`; the README sits at the repo root, so the same path becomes `examples/skeleton/expression_if.star`). H2 headings, prose paragraphs, code blocks, tables, blockquotes, and the 4 conceptual H3 blocks under "What Just Happened?" are reproduced byte-equal.

## Decisions Made

- **License section: "TBD"** — Task 1 confirmed no `LICENSE*` file at repo root; chose the plan's conditional "TBD" template wording. Single-paragraph treatment ("treat as private/unlicensed for downstream use until a license file lands") is honest and forward-compatible (replaceable with a one-line `See [LICENSE](LICENSE).` once a license file lands).
- **No prior README content preserved** — Task 1 confirmed no existing `README.md`; Write tool creates fresh file with no content-preservation needed.
- **Verbatim H2 preservation in embedded section** — D-06 says embed verbatim; chose to keep every embedded H2 at H2 level rather than demote to H3. Result: 15 total H2s in the README, which markdown renders fine and the canonical-version note + the relative position scope the embedded section obviously. Verified by byte-equality diff against source.
- **Relative-path adjustments only** — When pulling tutorial body from `docs/getting-started.md` (which uses paths relative to `docs/`), rewrote `../examples/skeleton/expression_if.star` → `examples/skeleton/expression_if.star`. No prose changes — just path-resolution housekeeping for the README's repo-root location. The H2 headings themselves remained byte-equal (verified).
- **Audience-split landing forward references retained** — `docs/for-flow-authors/README.md` + `docs/for-extension-developers/README.md` are produced by plan 04.3-09 (parallel Wave 2 sibling); kept as cross-links knowing they resolve once the wave completes. Documented as a forward reference matching the established convention from plan 04.3-03 (architecture.md cross-linked the same forward references).

## Deviations from Plan

None — plan executed exactly as written. The locked content (hero pitch, What This Is/Is Not bullets, Status template, Project Layout tree, attribution note format) was reproduced verbatim. The only edit applied to embedded tutorial content was relative-path adjustment for the README's repo-root location, which the plan anticipated implicitly (cross-links work both on GitHub and pkg.go.dev per CONTEXT.md integration-points note). All 22+ acceptance-criteria grep counts pass on first verification pass.

## Issues Encountered

None.

## User Setup Required

None — pure prose addition.

## Self-Check

Verified all acceptance criteria:

- `wc -l README.md` → 430 (in 400-1000 range, well within D-02 mid-size band) ✓
- `grep -c -E '^## ' README.md` → 15 (≥14 required: 8 own + 6 embedded; we have 7 embedded because plan's count was conservative — `## Prerequisites` was in the 6 explicitly named, our count includes it) ✓
- `grep -c '^## Prerequisites' README.md` → 1 ✓
- `grep -c '^## 1\. Clone and Build' README.md` → 1 ✓
- `grep -c '^## What Just Happened?' README.md` → 1 ✓
- Byte-equality diff for 7 embedded H2s → 0 lines (identical) ✓
- 8 README's own H2 sections (`What This Is|What This Is Not|Status|Getting Started|Where to Go Next|Project Layout|Contributing|License`) → 8 matches ✓
- `parse/execute`: 3 (≥2) ✓
- `two-tier`: 1 (≥1) ✓
- `docs/getting-started.md`: 2 (≥1) ✓
- `docs/architecture.md`: 2 (≥1) ✓
- `docs/reference/builtins.md`: 5 (≥1) ✓
- `docs/for-flow-authors`: 3 (≥1) ✓
- `docs/for-extension-developers`: 2 (≥1) ✓
- `examples/README.md` or `examples/skeleton`: 7 (≥1) ✓
- `cli-binary.md`: 2 (≥1) ✓
- `check_user`: 9 (≥2) ✓
- `expression_if.star`: 5 (≥2) ✓
- `skytime dev-server`: 4 (≥2) ✓
- `Phase 1|2|3|4|5|6|04.1|04.2`: 9 (≥6) ✓
- `Starlark|starlark`: 13 (≥5) ✓
- `Temporal|temporal`: 27 (≥5) ✓
- `no string compilation`: 1 (≥1) ✓
- `^skytime/` (Project Layout tree): 1 (≥1) ✓
- `cmd/skytime|cmd/skytime-docgen`: 4 (≥2) ✓
- Hero blockquote `> Starlark-defined durable workflows on Temporal`: 1 (=1) ✓
- Commit `e484b4f` exists in git log ✓

## Self-Check: PASSED

- `[x]` File exists: `README.md` (430 lines)
- `[x]` Commit `e484b4f` exists in git log on branch `main`
- `[x]` All 25+ acceptance-criteria grep checks pass on first verification pass
- `[x]` Embedded tutorial H2 byte-equality with `docs/getting-started.md` verified

## Next Phase Readiness

- README.md is the repo front door, ready for users hitting `https://github.com/mikelalcon/skytime` for the first time.
- Cross-links forward to:
  - `docs/architecture.md` (plan 03 — exists ✓)
  - `docs/getting-started.md` (plan 07 — exists, body embedded verbatim per D-06 ✓)
  - `docs/reference/builtins.md` (plan 02 — exists ✓)
  - `docs/reference/cli.md` (plan 04 — exists ✓)
  - `docs/for-flow-authors/extensions/http.md` (plan 05 — exists ✓)
  - `examples/README.md` (plan 06 — exists ✓)
  - `docs/for-flow-authors/README.md` + `docs/for-extension-developers/README.md` (plan 09 sibling Wave 2 — produced in parallel; resolves once plan 09 completes)
  - `docs/cli-binary.md` (already exists in repo from Phase 04 output ✓)

## Open Items for Downstream Plans

- **Plan 04.3-09 (registrar):** Should mark D-01 + D-02 + D-06 complete in REQUIREMENTS.md and add this artifact's row to ROADMAP.md (the requirements field of this plan's frontmatter declares D-01, D-02, D-06 — the registrar plan is the dedicated point for marking phase requirements complete).
- **Future LICENSE landing:** When a license file is added, replace the License section's "TBD" paragraph with `See [LICENSE](LICENSE).`. Single-line edit; no other README content changes.
- **Future tutorial drift:** D-06 says "tutorial file is canonical; README reproduces by reference" with copy-edit OK for v1. If drift becomes a concern, consider an `<!-- include -->` marker scheme in a future docgen phase. For now, contributors editing the tutorial run a manual `git diff docs/getting-started.md README.md` to check parity.

---
*Phase: 04.3-documentation-and-source-driven-reference-generator*
*Completed: 2026-05-05*
