---
phase: 04.1-dynamic-step-kwargs-lambda-accepting-step-action-fn-variant-for-runtime-built-action-kwargs
plan: 07
subsystem: docs
tags: [examples, corpus, requirements, project-md, differential-corpus, dryrun, end-to-end, wave-4, phase-finalization]

# Dependency graph
requires:
  - plan: 04.1-02
    provides: parser-time `${ctx.expr}` desugarer + ctx.<name> walker (DSL-13)
  - plan: 04.1-03
    provides: `step(action_fn=...)` / `step(block_fn=...)` / `step(name=...)` parser surface (DSL-11, DSL-12)
  - plan: 04.1-04
    provides: parse-time block_fn idempotency lint + runtime fallback in validate_batch (VAL-04)
  - plan: 04.1-05a
    provides: i.resolveKwargs evaluating *StarlarkLambda values inside ActionRef.Kwargs at workflow time
  - plan: 04.1-05b
    provides: workflow-side action_fn/block_fn dispatch + flow.NameFn / step.NameFn resolution + Freeze + fail() callsite preservation
  - plan: 04.1-06
    provides: live progress block renderer + `--verbose` static fallback (CLI-06, CLI-07)
  - phase: 04-static-validation-tier-cli-skeleton
    provides: tests/differential_test.go corpus harness + dryrun AlwaysOkDispatch
provides:
  - examples/skeleton/simple_check.star — rewritten using `${ctx.repo}` interpolation in step name + path kwarg, drives `--input '{"repo":...}'` end-to-end
  - examples/skeleton/parallel_fanout.star — rewritten using `step(block_fn=lambda ctx: [...])` + `for_each_parallel(items=lambda ctx: ctx.repos, ...)` + `call_flow` per-item
  - .planning/PROJECT.md amendment — D4.1-22 carve-out paragraph appended to "no string compilation" directive (parser-time syntactic sugar permitted, runtime template engines forbidden, future expansions require new ADR)
  - .planning/REQUIREMENTS.md — 7 new IDs registered and marked Complete (DSL-11, DSL-12, DSL-13, VAL-04, CLI-06, CLI-07, EX-FIX-01); v1 total 55→62; traceability table extended
  - .planning/STATE.md — Phase 04.1 completion record; D4.1-22..25 captured in decisions accumulator
  - tests/e2e_skytime_run_test.go — `--input` payload realigned to new corpus input shape (`repo_path` → `repo`)
affects:
  - Future Phase 5+ (E2E runtime) — example corpus is the canonical onboarding fixture; rewritten files prove dynamic-kwargs + interpolation works end-to-end
  - /gsd:verify-work — phase is ready for the regression-and-verifier gate
  - PROJECT.md "no string compilation" rule — anyone reading the spec now sees the parser-time desugaring carve-out alongside the original directive

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Differential corpus pattern: rewritten fixtures must pass both static parse AND dryrun (AlwaysOkDispatch) — proves resolveKwargs threads through whether the activity is real or stubbed."
    - "REQUIREMENTS.md registration discipline: every new requirement ID gets (a) the descriptive bullet under its category, (b) a row in the traceability table mapped to its phase, (c) a header count update, (d) a 'Last updated' footer note. All four happen in one commit."
    - "PROJECT.md carve-outs use blockquote (>) formatting + verbatim D-decision citation — flags the architectural exception without inflating the directives list."
    - "End-of-plan e2e fixup pattern (Rule 1 auto-fix): rewriting the canonical corpus often surfaces stale assumptions in e2e fixtures hardcoded against the old input schema. Caught at differential-test runtime, fixed inline, no architectural change required."

key-files:
  created:
    - .planning/phases/04.1-dynamic-step-kwargs-lambda-accepting-step-action-fn-variant-for-runtime-built-action-kwargs/04.1-07-SUMMARY.md
  modified:
    - examples/skeleton/simple_check.star
    - examples/skeleton/parallel_fanout.star
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - tests/e2e_skytime_run_test.go

key-decisions:
  - "D4.1-22 carve-out is now permanent in PROJECT.md — parser-time desugaring (e.g., `${ctx.expr}` → `lambda ctx: str(ctx.expr)`) is NOT string compilation; runtime template engines (CEL, Jinja, etc.) remain forbidden. Future expansions of this carve-out require a new ADR."
  - "D4.1-23: example corpus rewrite ships the new features. simple_check.star demonstrates step name + path kwarg interpolation; parallel_fanout.star demonstrates block_fn + for_each_parallel over runtime list."
  - "D4.1-24: parallel_fanout.star uses `step(block_fn=...)` with three idempotent gh.get calls — passes the parser's best-effort static idempotency analysis (D4.1-11) without falling back to runtime."
  - "D4.1-25: differential corpus continues to pass on rewritten fixtures (parse + dryrun agreement) — load-bearing regression net for the entire phase."
  - "Task 3c (manual TTY demo) skipped per user — the automated PTY-style live-block tests in pkg/cli/progress_live_test.go and the static-fallback tests in pkg/cli/progress_test.go pin the same surface; the Manual-Only Verifications section of VALIDATION.md retains the optional UAT path for future re-runs."

patterns-established:
  - "Phase finalization checklist: rewrite canonical corpus → register requirements → amend PROJECT.md if the phase carved out a spec exception → run differential corpus + race detector + decision-coverage grep → update STATE.md + ROADMAP.md → optional manual demo. All four metadata files (PROJECT.md, REQUIREMENTS.md, STATE.md, ROADMAP.md) updated atomically."
  - "Manual-only checkpoints with `gate=optional` are skippable when automated coverage is sufficient; the rationale must be captured in the SUMMARY.md (skip reason + automated proxy tests pinning the same property)."

requirements-completed: [DSL-11, DSL-12, DSL-13, VAL-04, CLI-06, CLI-07, EX-FIX-01]

# Metrics
duration: ~7min
completed: 2026-05-03
---

# Phase 04.1 Plan 07: Corpus Rewrite + PROJECT.md Carve-Out + Phase Finalization Summary

**Rewrote `examples/skeleton/{simple_check,parallel_fanout}.star` to drive on `--input` via `${ctx.expr}` interpolation + `step(block_fn=...)`, registered 7 new requirements (DSL-11/12/13, VAL-04, CLI-06/07, EX-FIX-01), appended the D4.1-22 carve-out to PROJECT.md's "no string compilation" rule, and locked the differential corpus regression net — Phase 04.1 ready for `/gsd:verify-work`.**

## Performance

- **Duration:** ~7 min (commits span 09:12:33 → 09:19:29 -0400 on 2026-05-03)
- **Started:** 2026-05-03T13:12:33Z
- **Completed:** 2026-05-03T13:19:29Z
- **Tasks executed:** 4 of 5 (Task 3c skipped per user choice — manual TTY demo, gate=optional)
- **Files modified:** 6

## Accomplishments

- **Canonical corpus rewritten** (D4.1-23, D4.1-24). `examples/skeleton/simple_check.star` now interpolates `${ctx.repo}` in both the step name AND the `gh.get(path=...)` kwarg, so `skytime run ... --input '{"repo":"octocat/Hello-World"}'` actually hits `/repos/octocat/Hello-World` (Phase 4 hardcoded that URL). `examples/skeleton/parallel_fanout.star` uses `step(block_fn=lambda ctx: [...])` to build three idempotent gh.get reads per repo, fanned out via `for_each_parallel(items=lambda ctx: ctx.repos)` calling a `check_one` helper flow.
- **PROJECT.md amended** with the D4.1-22 carve-out paragraph (verbatim) appended to the "no string compilation" directive. The carve-out clarifies that parser-time syntactic sugar (e.g., `${ctx.expr}` → `lambda ctx: str(ctx.expr)`) is NOT string compilation; runtime template engines remain forbidden; future expansions require a new ADR.
- **REQUIREMENTS.md registered 7 new IDs** — DSL-11/12/13 under DSL category, VAL-04 under Static Validation, CLI-06/07 under CLI, EX-FIX-01 under Examples. All marked `[x] Complete`. Traceability table extended; v1 total bumped 55→62; "Last updated" footer set to 2026-05-02.
- **Differential corpus pinned** — `go test ./tests/ -run TestDifferentialCorpus -count=1` passes on both rewritten fixtures (D4.1-25). The dryrun path threaded through resolveKwargs without any AlwaysOkDispatch changes — the workflow-side resolveKwargs (04.1-05a) handles lambda-valued kwargs whether the activity is real or stubbed.
- **All automated gates green** — `go test -race ./... -count=1` (14 packages, all pass), `go vet ./...` clean, decision-coverage grep emits no MISSING for D4.1-01..D4.1-25, end-to-end e2e harness aligned with new corpus.
- **Phase 04.1 ready for verifier** — STATE.md updated; ROADMAP.md plan-progress sync to follow this commit.

## Task Commits

Plan 04.1-07 executed across 5 atomic commits (one Auto-Rule 1 fixup + four planned task commits):

1. **Task 1 — Corpus rewrite** (`bfb29a0`, docs): rewrote both `.star` files; D4.1-23, D4.1-24.
2. **Task 1 — REQUIREMENTS.md** (`9fade98`, docs): registered DSL-11/12/13, VAL-04, CLI-06/07, EX-FIX-01; updated traceability table + counts.
3. **Task 1 deviation — e2e fixup** (`927f235`, test): aligned `tests/e2e_skytime_run_test.go` `--input` payload with new corpus input shape (`repo_path` → `repo`); Auto-Rule 1 (Bug); D4.1-23.
4. **Task 2 — PROJECT.md carve-out** (`5e2935a`, docs): appended D4.1-22 paragraph to "no string compilation" directive.
5. **Tasks 3a + 3b — STATE.md update** (`02c19a9`, docs): recorded phase completion + D4.1-23/24/25 in decisions accumulator.

**Plan metadata:** added in this commit (this SUMMARY.md + final docs commit) — see plan-completion commit hash returned by the orchestrator.

## Files Created/Modified

- **`examples/skeleton/simple_check.star`** (modified, 56 LOC) — sequential step + script + if_cond using `${ctx.repo}` interpolation; demos `--input`-driven dynamic kwargs.
- **`examples/skeleton/parallel_fanout.star`** (modified, 59 LOC) — `for_each_parallel` over runtime list + `step(block_fn=...)` + `call_flow` per-item; demos batch dispatch with parse-time idempotency check.
- **`.planning/PROJECT.md`** — D4.1-22 carve-out blockquote appended to "no string compilation" bullet; explicit Phase 04.1 reference + verbatim ADR pointer.
- **`.planning/REQUIREMENTS.md`** — DSL-11/12/13, VAL-04, CLI-06/07, EX-FIX-01 entries; traceability rows; v1 total 55→62; "Last updated" footer.
- **`.planning/STATE.md`** — Phase 04.1 completion record in decisions accumulator (D4.1-22..25).
- **`tests/e2e_skytime_run_test.go`** — `--input` payload realigned to `{"repo":"octocat/Hello-World"}` matching new corpus.

## Decisions Made

- **Skip Task 3c (manual TTY demo).** The user replied `skip` at the optional checkpoint. Rationale: automated coverage in `pkg/cli/progress_live_test.go` (TTY-forced + ANSI byte-stream assertions, spinner cadence, truncation, Close lifecycle) and `pkg/cli/progress_test.go` (verbose static fallback, non-TTY static path) pins the same surface. Visual-quality judgement ("looks like Bazel/cargo") cannot be automated; VALIDATION.md retains this in the Manual-Only Verifications table for future UAT — `/gsd:verify-work` or a future demo session can revisit if a real-TTY pass is desired.
- **No dispatch.go change needed** for the dryrun path. The workflow-side resolveKwargs (Plan 04.1-05a) evaluates lambda-valued kwargs deterministically before the activity is invoked — whether the activity is the real Activity_TypedAction or AlwaysOkDispatch's stub. Differential corpus passing first-try confirmed this; no changes to `pkg/validator/dryrun/dispatch.go` were needed.
- **Header count bumped to 62 (not 61)** because EX-FIX-01 was also new (the plan listed 6 IDs but EX-FIX-01 from the plan frontmatter `requirements:` field belongs in the corpus rewrite — registered alongside DSL/VAL/CLI for one atomic commit). v1 total: 55 + 7 = 62.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] e2e harness input-shape misalignment**
- **Found during:** Task 1 verification (`go test ./tests/...`)
- **Issue:** `TestE2E_SkytimeRun_Happy` in `tests/e2e_skytime_run_test.go` hardcoded `--input '{"repo_path":"octocat/Hello-World"}'` against the Phase 4 corpus's `inputs={"repo_path": "string"}` declaration. The Phase 04.1 rewrite (D4.1-23) renamed the input to `"repo"` and added `${ctx.repo}` interpolation; the e2e flow was failing with `struct has no .repo attribute` on every run.
- **Fix:** Updated the test's `--input` payload to `{"repo":"octocat/Hello-World"}` matching the new corpus input declaration. No code changes to the corpus or interpreter — the rewritten fixture is the source of truth and the e2e fixture must follow.
- **Files modified:** `tests/e2e_skytime_run_test.go`
- **Verification:** `go test ./tests/ -count=1` exits 0; `go test -race ./... -count=1` clean.
- **Committed in:** `927f235` (`test(04.1-07): align e2e simple_check happy test with rewritten corpus input shape`)

---

**Total deviations:** 1 auto-fixed (1 bug — e2e fixture stale assumption surfaced by corpus rewrite)
**Impact on plan:** Auto-fix kept the plan running without an architectural escalation. The fixture rename is the canonical D4.1-23 demonstration — every consumer (e2e tests, docs, demo scripts) must follow. No scope creep.

## Issues Encountered

- None during automated tasks. The single Rule 1 auto-fix above was caught immediately by the differential test gate, fixed inline, and re-verified before proceeding to Task 2.
- Manual demo (Task 3c) skipped per user direction — captured under "Decisions Made" with the rationale and automated proxy coverage.

## Self-Check: PASSED

- **Files exist:**
  - `examples/skeleton/simple_check.star` — FOUND (modified, 7 `${ctx.` matches in name + path kwargs)
  - `examples/skeleton/parallel_fanout.star` — FOUND (modified, `block_fn = lambda ctx` present, `for_each_parallel` preserved)
  - `.planning/PROJECT.md` — FOUND (Parser-time syntactic sugar carve-out at line 72; cites D4.1-22; "new ADR" sentence intact)
  - `.planning/REQUIREMENTS.md` — FOUND (DSL-11/12/13/VAL-04/CLI-06/07/EX-FIX-01 all `[x] Complete`; traceability rows; v1 total = 62)
  - `.planning/STATE.md` — FOUND (Phase 04.1 entries in decisions accumulator)
  - `tests/e2e_skytime_run_test.go` — FOUND (input payload realigned to `repo`)
  - `.planning/phases/04.1-.../04.1-07-SUMMARY.md` — FOUND (this file)
- **Commits exist on `main`:**
  - `bfb29a0` docs(04.1-07): rewrite examples/skeleton corpus — FOUND
  - `9fade98` docs(04.1-07): register DSL-11/12/13... in REQUIREMENTS.md — FOUND
  - `927f235` test(04.1-07): align e2e simple_check happy test — FOUND
  - `5e2935a` docs(04.1-07): append D4.1-22 carve-out to PROJECT.md — FOUND
  - `02c19a9` docs(04.1-07): record phase 04.1 completion in STATE.md — FOUND
- **Done criteria:**
  - `grep -E "\\$\\{ctx\\.repo\\}" examples/skeleton/simple_check.star` returns 7 matches ✓ (both name + path kwargs)
  - `grep -E "block_fn = lambda ctx" examples/skeleton/parallel_fanout.star` returns a match ✓
  - `grep -E "for_each_parallel" examples/skeleton/parallel_fanout.star` returns a match ✓
  - `grep -cE "DSL-11|DSL-12|DSL-13|VAL-04|CLI-06|CLI-07|EX-FIX-01" .planning/REQUIREMENTS.md` ≥ 6 ✓ (returns 16+ counting traceability + footer)
  - `grep -E "62 total" .planning/REQUIREMENTS.md` matches ✓
  - `go test ./tests/ -run TestDifferentialCorpus -count=1` exits 0 ✓
  - `go test -race ./... -count=1` exits 0 across all 14 packages ✓
  - `go vet ./...` exits 0 ✓
  - Decision-coverage grep emits no MISSING for D4.1-01..25 ✓
  - PROJECT.md "Parser-time syntactic sugar" carve-out present ✓; "lambda ctx: str(ctx.expr)" example present ✓; "new ADR" closing sentence present ✓.

## Next Phase Readiness

- **Phase 04.1 is complete and ready for `/gsd:verify-work`.** All 8 plan SUMMARY files now exist on disk (01, 02, 03, 04, 05a, 05b, 06, 07). The differential corpus is the regression net for the dynamic-kwargs surface; the race detector pins concurrent live-block correctness; PROJECT.md carries the architectural carve-out with citation; REQUIREMENTS.md carries the requirement-traceability mapping.
- **Optional UAT:** `Task 3c` manual demo can still be run on a real TTY by anyone wanting visual confirmation of the live block — instructions remain in VALIDATION.md's Manual-Only Verifications section.
- **No blockers** for the next phase (Phase 5 — E2E test harness, per ROADMAP.md). Phase 5 will benefit directly from the rewritten corpus as input fixtures and from the workflow-side resolveKwargs / action_fn / block_fn dispatch as runtime substrate.

---
*Phase: 04.1-dynamic-step-kwargs-lambda-accepting-step-action-fn-variant-for-runtime-built-action-kwargs*
*Plan: 07*
*Completed: 2026-05-03*
