---
milestone: v1.43.0
name: Durability + Triggers
audited: 2026-05-13
status: tech_debt
scores:
  requirements: 33/33
  phases: 7/7
  integration: 8/8
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-milestone
    items:
      - "Stale REQUIREMENTS.md traceability — 27 of 33 rows in `## Traceability` still show Status `Pending` with `Source Plan` as `—`, despite VERIFICATION.md (passed) + SUMMARY frontmatter (`requirements_completed`) confirming completion. Affects: TRIG-01..10, SERVER-01..03, SCHED-01..03, UI-01..04, CLI-08..12, CLI-13, EX-05 (27 rows). Correctly Complete: LOG-01, LOG-02, AUTH-01..04 (6 rows). Bookkeeping only — no code gap."
  - phase: 07-trigger-primitive-server-shell
    items:
      - "VERIFICATION.md frontmatter `status: human_needed` is stale. Both UAT items (SIGTERM signal-loop end-to-end + manual banner-ordering smoke) were closed by Phase 7.1's `worker.WithSDKFactory` Option, which unskipped `TestServerCmd_DrainOnSIGTERM`, `TestServerCmd_DrainTimeoutExpiry`, `TestServerCmd_SecondSignalForceExit` (per 07.1-VERIFICATION.md line 49). 07-HUMAN-UAT.md frontmatter should be marked `resolved`."
      - "Plan 04 carries an Option-B accept on `fakeWebhookExt` test-helper duplication (deferred to a future test-infrastructure quick task per Plan 04 deviation note). No production impact."
  - phase: 07.2.1-structured-logging-step-builtin
    items:
      - "`log.fatal(...)` explicitly deferred (waits for workflow-cancellation design)."
      - "Plans 03/04/05 SUMMARY frontmatter use empty `requirements-completed:` fields; LOG-01/02 ownership lives in Plan 02 and the body-section `[x]` markers. Trace satisfied via VERIFICATION.md; SUMMARY bookkeeping informal."
  - phase: 07.3-dashboard-manual-trigger-page
    items:
      - "Plans 03/04/05 SUMMARY frontmatter use empty `requirements-completed:` lists (UI-01..04 are tracked via Plan 00 wave-0 scaffold + Plan 02's `requirements: [UI-02]` field). Trace satisfied via VERIFICATION.md; SUMMARY bookkeeping informal."
  - phase: 07.4-extbin-consolidation-tech-debt-cleanup
    items:
      - "Accepted deferral: Plan 03 Optional Step 3 (harness-mediated CLI-10 E2E partial-mock test) waits for a concrete consumer. `runConfig.credHandler` is stored but never read by `runOneFile/runOneTest/buildExecuteBatchCallback` — by design per D-7.4-11 (OnActivity short-circuits before Resolve). User-accepted 2026-05-13 per 07.4-HUMAN-UAT.md line 22."
      - "07.4-HUMAN-UAT.md frontmatter `status: partial` is stale — body shows `resolved 2026-05-13 — user accepted Plan 03's deferral`. Bookkeeping update needed."
  - phase: 07.5-auth-documentation
    items:
      - "07.5-HUMAN-UAT.md carries 2 pending subjective doc-quality items (medium-audience prose readability + GitHub rendered anchor-link navigation). Cannot be automated — non-blocking per verifier. Will surface in `/gsd:progress` until exercised."
      - "Plan 01 acceptance criterion grepped for `^go 1.25$` but `go mod tidy` produced `go 1.25.0` in `docs/for-extension-developers/snippets/go.mod` — functionally equivalent, CI green."
deferred_to_v1.44_plus:
  - "WORK-V2-01: `pkg/worker/credentials/RefreshingAPIKeyCredentials` wrapper — defer until at least one customer hits the friction."
  - "WORK-V2-02: `pkg/worker/credentials/ReloadingTLSCredentials` cert-watcher — same deferral pattern."
  - "TRIG-V2-01: webhook signature schemes beyond HMAC-SHA256 (Stripe timestamp+signature, AWS SNS) — extension authors can ship their own; first-party support waits on customer demand."
  - "TRIG-V2-02: 6-field cron syntax with seconds — defer (5-field POSIX is Temporal Schedules' native input)."
  - "TRIG-V2-03: cross-source idempotency framework with shared dedup store — promote when 3+ customers ask for the same shape."
  - "UI-V2-01: WebSocket / SSE-augmented real-time updates (vs polling) — SSE shipped 7.3 already covers the v1 need."
  - "UI-V2-02: authentication on the dashboard itself — same trust boundary as worker process for v1.43."
  - "UI-V2-03: multi-tenancy in dashboard (per-namespace/team/queue filtering) — single-tenant scope for v1.43."
outstanding_uat:
  - phase: 07
    items: 2
    note: "stale — items resolved by Phase 7.1; UAT file frontmatter not updated"
  - phase: 07.4
    items: 1
    note: "stale — user accepted in body; frontmatter not updated"
  - phase: 07.5
    items: 2
    note: "open — created 2026-05-13; subjective doc-quality, non-blocking"
nyquist:
  compliant_phases: [07.2.1, 07.3]
  partial_phases: [07, 07.1, 07.2]
  missing_phases: [07.4, 07.5]
  overall: 2/7
  note: "Nyquist 'wave_0_complete' false for most phases. Phase 7.4 + 7.5 missing VALIDATION.md entirely. Acceptable for this milestone — 7.4 is a tech-debt cleanup phase and 7.5 is doc-only (CI snippets-build + drift-test IS the validation strategy)."
---

# Milestone v1.43.0 — Audit Report

**Audited:** 2026-05-13
**Status:** `tech_debt` — all requirements satisfied, integration verified, no critical blockers; accumulated bookkeeping debt needs review before milestone archival

## Scope

| Phase | Goal | Status | VERIFICATION |
|---|---|---|---|
| 7 | Trigger primitive + server shell + dev-server rename | Complete | passed (frontmatter says `human_needed` but items closed by 7.1) |
| 7.1 | HTTP webhook receiver + GitHub source factory | Complete | passed |
| 7.2 | Cron triggers via Temporal Schedules | Complete | passed |
| 7.2.1 | Structured logging step builtin (`log.info/.warn/.error/.debug`) | Complete | passed |
| 7.3 | Stdlib-only dashboard + manual trigger page (SSE-powered) | Complete | passed |
| 7.4 | extbin consolidation + tech-debt cleanup | Complete | passed |
| 7.5 | Production auth-rotation docs (GCP/AWS/Azure/mTLS) | Complete | passed |

## Score Summary

| Dimension | Score | Detail |
|---|---|---|
| Requirements | **33/33** | All TRIG (×10), SERVER (×3), SCHED (×3), UI (×4), CLI (×6), EX (×1), AUTH (×4), LOG (×2) |
| Phases | **7/7** | All completed; all VERIFICATION.md present |
| Integration flows | **8/8** | webhook ingress, cron reconcile, manual trigger, server lifecycle, log pipeline, auth doc isolation, extbin consolidation, firewall tests |
| Cross-phase wiring | **6/6** | Verified end-to-end by `go test ./tests/... -count=1` (PASS) + AST firewalls (5/5 PASS) |

## Part A — Cross-Phase Integration

All 6 cross-phase end-to-end flows verified by direct code reading + firewall tests:

1. **External event → workflow (webhook ingress):** `pkg/extension/receiver/handler.go:219` → `flowlaunch.Execute` → `client.ExecuteWorkflow("SkytimeWorkflow", ...)` at `pkg/cli/server/web/flowlaunch/launch.go:125` (the single production site). `tests/firewall_execute_workflow_test.go::TestExecuteWorkflow_CallSiteCount` (=2: flowlaunch + run.go) PASS.
2. **Cron → workflow execution:** `pkg/extension/builtin/core/cron.go:272` registers source factory; `pkg/cli/server.go:241` invokes `schedules.ReconcileCronSchedules`; `pkg/extension/schedules/schedules.go:298` calls `flowlaunch.BuildWorkflowInput` (3rd site); `pkg/interpreter/cron_input.go::extractScheduledTime` wired into `pkg/interpreter/workflow.go:51-64` recovering `scheduled_time` + `actual_time` (`workflow.Now` for replay safety).
3. **Manual trigger → workflow (dashboard):** `pkg/cli/server/web/handlers.go::triggerHandler` (line 208 JSON-strict same-origin check at line 283 `sameOriginOK`) reuses the `flowlaunch.Execute` seam.
4. **Server lifecycle (SIGTERM drain ordering):** `pkg/cli/server.go` — `webResult.Shutdown()` emits SSE `event: shutdown` BEFORE TCP close (`:377`), then `srv.Shutdown(drainCtx)` (`:388`), then `w.Stop()` (`:398`). Phase 7.3 B3 ordering preserved.
5. **Log builtin pipeline:** `pkg/parser/globals.go:84-90` registers `log.{info,warn,error,debug}`; `pkg/parser/builtins_log.go:83` reuses `${ctx.expr}` desugarer; `pkg/interpreter/walk_log.go:58` `workflow.GetLogger(ctx)` per-dispatch; `pkg/cli/log_filter.go::logKindFilterHandler` + `pkg/cli/progress_static.go:71` suppress bookend `kind=log` frames in human mode.
6. **Auth doc snippets module isolation:** `docs/for-extension-developers/snippets/go.mod` is standalone (`grep mikelalcon/skytime` → zero `require` rows, only own module declaration); `.github/workflows/ci.yml:47-48` adds `snippets build + vet + test` step (D-7.5-07 — no separate workflow file).

**Firewall tests** (`go test ./tests/... -count=1` → `ok 5.626s`):
- `TestExecuteWorkflow_CallSiteCount` ✓
- `TestBuildWorkflowInput_CallSiteCount` ✓
- `TestSourceAgnosticRenderer` ✓
- `TestNoExternalHTTPTemplateInWeb` ✓
- `TestWalkthroughDashboardHeadings` ✓
- `TestNoDevServerLiteralRemains` ✓

## Part B — Requirements Coverage (33/33 satisfied)

All 33 milestone REQ-IDs are functionally satisfied (verified by code reading + VERIFICATION.md `passed` + SUMMARY frontmatter listing). The trace table in `REQUIREMENTS.md` is stale for 27 rows — see tech-debt section.

### Per category

| Category | Count | Phase(s) | Status |
|---|---|---|---|
| TRIG (trigger primitive + sources) | 10 | 7, 7.1 | 10/10 satisfied (trace stale) |
| SERVER (long-running subcommand) | 3 | 7 | 3/3 satisfied (trace stale) |
| SCHED (cron via Temporal Schedules) | 3 | 7.2 | 3/3 satisfied (trace stale) |
| UI (dashboard) | 4 | 7.3 | 4/4 satisfied (trace stale) |
| CLI (API + rename) | 6 | 7, 7.4 | 6/6 satisfied (trace stale; CLI-10 surface satisfied — harness E2E user-deferred) |
| EX (example project addition) | 1 | 7.1 | 1/1 satisfied (trace stale) |
| AUTH (production auth docs) | 4 | 7.5 | 4/4 satisfied (trace already correct) |
| LOG (structured logging step builtin) | 2 | 7.2.1 | 2/2 satisfied (trace already correct) |

**No unsatisfied, no orphaned, no partial-blocked requirements.**

## Part C — Tech Debt and Bookkeeping

See YAML frontmatter `tech_debt` and `outstanding_uat` blocks above for the full breakdown. Summary:

- **27 stale trace-table rows** in `REQUIREMENTS.md` — Status `Pending` should be `Complete`; `Source Plan` column should be populated. Pattern already correct for AUTH-01..04 + LOG-01..02 (6 rows out of 33).
- **2 stale UAT-file frontmatters** (Phase 7, Phase 7.4) where body content shows resolution but `status:` field reads `partial`.
- **2 open UAT items** (Phase 7.5) — subjective doc-quality (medium-audience prose readability + GitHub anchor navigation); non-blocking, can't be automated.
- **1 user-accepted code-deferral** (Phase 7.4 Plan 03 harness E2E for CLI-10) — `runConfig.credHandler` stored but never read; surface contract honored; harness wiring waits for a concrete consumer.
- **2 phases missing VALIDATION.md** (Phase 7.4, Phase 7.5) — acceptable for a tech-debt cleanup phase and a doc-only phase whose validation strategy is the drift-test + `snippets-build` CI gate.

## Part D — Deferred to v1.44+

Tracked in REQUIREMENTS.md `## v1.44+ Requirements (Deferred)`. Captured in frontmatter `deferred_to_v1.44_plus`. Summary: 3 trigger v2 features, 2 worker credentials helpers, 3 dashboard v2 features.

## Part E — Verdict

**Status:** `tech_debt`

The milestone has shipped functionally — every code-level requirement is satisfied, every cross-phase integration is wired and tested, all firewall tests are green, and the example project (`http-github-webhook`) demonstrates the durability-after-crash + webhook ingress + cron schedules + dashboard + manual trigger flows end-to-end against a live Temporal cluster. PROJECT.md's running narrative is accurate.

The remaining work before `/gsd:complete-milestone` is **bookkeeping**:

1. Flip 27 trace-table rows from `Pending` → `Complete` and populate `Source Plan`. The convention is already correct for AUTH/LOG rows.
2. Update 2 stale HUMAN-UAT.md frontmatters (Phase 7, Phase 7.4) to reflect their resolved body content.
3. Decide whether Phase 7.5's 2 subjective doc-quality UAT items get exercised before or after milestone archival.

Either approach is reasonable:
- **A. Complete milestone with debt tracked** — let the trace-table flip + UAT bookkeeping happen as part of `/gsd:complete-milestone` archival. The audit explicitly documents what needs cleaning.
- **B. Plan a cleanup phase** — formalize the bookkeeping into a small phase (could be a one-plan `/gsd:quick` task) so the closing milestone state is clean.

No code gap, no broken integration, no orphaned requirement.
