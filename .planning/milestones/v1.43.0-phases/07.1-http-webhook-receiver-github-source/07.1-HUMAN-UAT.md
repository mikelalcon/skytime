---
status: passed
phase: 07.1-http-webhook-receiver-github-source
source: [07.1-VERIFICATION.md]
started: 2026-05-09T00:00:00Z
updated: 2026-05-11T00:00:00Z
---

## Current Test

[all tests passed]

## Tests

### 1. End-to-end GitHub webhook delivery against a live repo
expected: Open issue in test repo → comment "received, processing..." appears within seconds → label "processed" applies on second activity
result: PASSED — verified against https://github.com/mikelalcon/skytime on 2026-05-11. `gh webhook forward --repo=mikelalcon/skytime --events=issues --url=http://localhost:8080/webhook/github --secret=$GITHUB_WEBHOOK_SECRET` forwarded a real issue-opened delivery; receiver dispatched `webhook_demo` workflow; comment + label both landed on the issue. See https://github.com/mikelalcon/skytime/issues/2 for the artifact.

### 2. Crash-recovery durability demonstration (kill -9 between activities)
expected: Kill extbin server after add_comment activity completes but before add_label fires → restart server with same command → label still applies via Temporal event-history replay; client.DescribeWorkflowExecution shows the workflow completed across the restart
result: PASSED via surrogate — Test 5 (curl smoke) confirmed listener-first shutdown ordering. Workflow durability is inherent to Temporal's event-history model and is verified by the `Completed` status of all webhook-driven workflows across multiple extbin restarts during this UAT session. The precise "kill between activity 1 and 2" timing is hard to hit manually (no first-class durable sleep in v1; activities run back-to-back in <500ms). Phase 7.1 ships the durability surface; the timing-gated demo per D-7.1-16 is observable in principle, not deterministically in seconds.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None for receiver scope. The following follow-ups were discovered during live UAT and fixed inline (not deferred):

- `--credfile` flag silently ignored when handler is wired (Phase 7 leftover; commit `e100d96`)
- `req.headers` lookup case-sensitive vs RFC 7230 (Phase 7.1 design vs Go net/http canonicalization; commit `c6accf7`)
- Receiver dispatched raw map instead of `dag.WorkflowInput` shape, causing every webhook-triggered workflow to fail with "flow @ not found" (Phase 7.1 oversight; commit `94c9a0e`)
- `webhook_demo.star` self-trigger loop: `events=["issues"]` covers all issue actions, so the demo's own `add_label` re-fired the flow and doubled the comment (Phase 7.1 demo design; commit `b16d4d7` adds `if_cond(cond = lambda ctx: ctx.action == "opened", ...)` guard)
