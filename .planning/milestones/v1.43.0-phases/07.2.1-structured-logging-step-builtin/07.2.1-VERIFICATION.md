---
phase: 07.2.1-structured-logging-step-builtin
slug: structured-logging-step-builtin
verified: 2026-05-12T00:00:00Z
status: passed
score: 5/5 must-haves verified (smoke walkthrough confirmed end-to-end 2026-05-12)
requirements_verified:
  - LOG-01
  - LOG-02
human_verification:
  - test: "Run docs/walkthroughs/cron-schedules-smoke.sh end-to-end"
    expected: "Per-fire stdout shows `[skytime/log] smoke_cron fired: ...` line; NO `event=step_dispatch kind=log` or `event=step_complete kind=log` frames appear in human-mode output"
    result: "passed (2026-05-12) — see 07.2.1-HUMAN-UAT.md for the captured stdout sample and the cross-platform script fixes that were applied during verification"
---

# Phase 07.2.1 — Structured Logging Step Builtin: Verification Report

**Phase Goal:** Replace the `print() or {...}` script-step footgun with first-class `log.info / log.warn / log.error / log.debug` step types that reuse the `${ctx.expr}` desugarer, route through `workflow.GetLogger(ctx)` at the matching slog level, surface a single `[skytime/log] ...` line per call in `skytime server` human stdout (bookend `kind=log` frames suppressed by the renderer), and are replay-safe.

**Verified:** 2026-05-11
**Status:** human_needed (5/5 automated must-haves verified; 1 documented manual smoke confirmation remains)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `.star` authors can declare `log.info(...)` / `.warn` / `.error` / `.debug` as flow-body steps with `${ctx.expr}` interpolation via the existing D4.1-22 desugarer | ✓ VERIFIED | `pkg/dag/control.go:73` defines `LogStep{Level, Msg, MsgFn, AttrsLambdaID}`; `pkg/parser/builtins_log.go:122/136/150/164` define all four builtins; `pkg/parser/builtins_log.go:83` reuses `p.desugarInterpolation`; `pkg/parser/globals.go:84-90` exposes the `log` module with all four attrs; `examples/http-github-webhook/weekly_digest.star:80-82` shows the real authoring shape compiling cleanly (full suite green) |
| 2 | Each `log.<level>(...)` step routes through `workflow.GetLogger(ctx).<Level>` with `[skytime/log]` prefix at the matching slog severity | ✓ VERIFIED | `pkg/interpreter/walk_log.go:58` calls `workflow.GetLogger(ctx)` inside the function body (not at construction); `walk_log.go:170-187` switches on `n.Level` and dispatches `logger.Debug/Info/Warn/Error(prefixedMsg, keyvals...)`; prefix is the literal `"[skytime/log] "` (line 170); `pkg/interpreter/workflow.go:219-220` wires `case *dag.LogStep:` into `walkNode` |
| 3 | `weekly_digest.star` swaps the `print() or {"logged": True}` workaround for a single `log.info(...)` step; `docs/walkthroughs/cron-schedules.md` shows the cleaner shape; `docs/reference/builtins.md` documents all four levels | ✓ VERIFIED | `weekly_digest.star`: 2× `log.info(` calls, 0× active `print(` (only line 75 comment about replaced code), 0× `output_alias="_log"`; `cron-schedules.md:284` shows `INFO [skytime/log] weekly digest complete: ...`; `builtins.md:316/343/370/397` contains `## log.info`, `## log.warn`, `## log.error`, `## log.debug` |
| 4 | Log steps are replay-safe — exactly N records emitted per N `log.<level>` calls even when Temporal replays the workflow | ✓ VERIFIED | `pkg/interpreter/replay_determinism_test.go:156` `TestReplay_LogStep_ExactlyThreeRecords` runs through `TestWorkflowEnvironment` and asserts exactly 3 records for 3 calls; `:198` `TestReplay_LogStep_AttrsByteStable` asserts byte-stable attrs across replay; both PASS with `-race -count=1` |
| 5 | Parser rejects `log.<level>` outside `flow(...)` bodies, rejects non-literal msg, and surfaces position-aware errors for malformed interpolation | ✓ VERIFIED | `pkg/parser/finalize.go:278` `validateLogStepPlacement` enforces placement (called at finalize.go:117); `pkg/parser/finalize_test.go:146/165/175/191` covers reject-module-scope + accept-inside-flow/ifcond/foreach; `pkg/parser/builtins_log_test.go:96/115` covers `TestBuiltinLog_RejectNonLiteralMsg` + `TestBuiltinLog_RejectMalformedInterp`; targeted run `go test ./pkg/parser/ -run "TestValidateLogStepPlacement_\|TestBuiltinLog_" -race -count=1` PASS |

**Score:** 5/5 truths verified

### Required Artifacts (Levels 1–3 + Data-Flow)

| Artifact | Expected | Exists | Substantive | Wired | Data Flows | Status |
|----------|----------|--------|-------------|-------|------------|--------|
| `pkg/dag/control.go` | `LogStep` struct with `Pos / Level / Msg / MsgFn *CapturedLambda / AttrsLambdaID` + `Node` impl | ✓ | ✓ (struct + 3 Node methods at L73/110/113) | ✓ (used by parser, walker, marshal) | ✓ (real field carrier — Msg flows to walkLog) | ✓ VERIFIED |
| `pkg/dag/marshal.go` | `LogStep.MarshalJSON` with `"kind":"LogStep"` discriminator and `logStepJSON` shape | ✓ | ✓ (`logStepJSON` at L181; `MarshalJSON` at L200) | ✓ (used by node marshaler) | ✓ | ✓ VERIFIED |
| `pkg/parser/builtins_log.go` | `builtinLogInfo / Warn / Error / Debug` + shared `buildLogStep` + literal-msg assertion | ✓ | ✓ (5 funcs at L34/122/136/150/164 + `assertLogMsgLiteralAt` at L184 + `describeNonLiteralExpr` at L250) | ✓ (registered in globals.go:84-90) | ✓ (produces real `*dag.LogStep` via `desugarInterpolation` at L83) | ✓ VERIFIED |
| `pkg/parser/globals.go` | `log` module with `info/warn/error/debug` builtins | ✓ | ✓ (L84-90) | ✓ (returned by `predeclaredFor`/equivalent registration path) | ✓ | ✓ VERIFIED |
| `pkg/parser/finalize.go` | `validateLogStepPlacement` finalize pass + call site | ✓ | ✓ (`validateLogStepPlacement` at L278; called at L117) | ✓ | ✓ (gates `p.allLogSteps` accumulator) | ✓ VERIFIED |
| `pkg/parser/parser.go` | `allLogSteps []*dag.LogStep` accumulator field | ✓ | ✓ (L99-106 with explanatory comment) | ✓ (consumed by `validateLogStepPlacement`) | ✓ (each builtin appends) | ✓ VERIFIED |
| `pkg/interpreter/walk_log.go` | `walkLog(ctx, *dag.LogStep)` calling `workflow.GetLogger(ctx)` per dispatch + bookend `kind=log` slog frames | ✓ | ✓ (walkLog at L57; logger acquired at L58 inside body; bookend slog calls at L69/83 with `"kind","log"`; level-switch at L171-187) | ✓ (`case *dag.LogStep:` at `workflow.go:219-220`) | ✓ (Msg + AttrsLambdaID flow through to the slog record) | ✓ VERIFIED |
| `pkg/interpreter/workflow.go` | `nonLogTotal` counter excluding `*dag.LogStep` from `i.stepTot` | ✓ | ✓ (L178-185 — counter loop + assignment) | ✓ (used by progress renderer for [N/M]) | ✓ | ✓ VERIFIED |
| `pkg/cli/log_filter.go` | `logKindFilterHandler` slog.Handler decorator dropping `kind=log` + `event∈{step_dispatch, step_complete}` records | ✓ | ✓ (struct + Enabled/Handle/WithAttrs/WithGroup at L28/42/50/75/81) | ✓ (wired into `setupServerLogging` at `pkg/cli/render.go:194` non-JSON branch) | ✓ (no leak: filter only depends on `log/slog` + stdlib) | ✓ VERIFIED |
| `pkg/cli/progress_static.go` | `progressHandler` early-return on `kind=="log"` for both dispatch and complete events | ✓ | ✓ (dispatch check at L78; complete check at L135) | ✓ (called by setupServerLogging path) | ✓ | ✓ VERIFIED |
| `examples/http-github-webhook/weekly_digest.star` | 1+ `log.info(...)` calls, no `print(` active, no `output_alias="_log"` | ✓ | ✓ (2× `log.info(`; `print(` only in comment at L75; no `_log` alias) | ✓ (loaded by example boot scan) | ✓ | ✓ VERIFIED |
| `docs/walkthroughs/cron-schedules.md` | Shows `[skytime/log] weekly digest complete: ...` (NOT `[skytime/print]`) | ✓ | ✓ (L284 + L303 explanatory paragraph) | n/a (docs) | n/a | ✓ VERIFIED |
| `docs/reference/builtins.md` | Sections `## log.info`, `## log.warn`, `## log.error`, `## log.debug` | ✓ | ✓ (L316/343/370/397) | n/a (regenerated by docgen — drift test passes) | n/a | ✓ VERIFIED |
| `pkg/interpreter/replay_determinism_test.go` | `TestReplay_LogStep_*` using `TestWorkflowEnvironment` | ✓ | ✓ (`TestReplay_LogStep_ExactlyThreeRecords` at L156; `TestReplay_LogStep_AttrsByteStable` at L198) | ✓ (runs in `go test ./pkg/interpreter/`) | ✓ | ✓ VERIFIED |
| `pkg/parser/builtins_log_test.go` | `TestBuiltinLog_*Accept*` + `*Reject*` | ✓ | ✓ (9 Test funcs at L27/48/63/74/84/96/115 — accept literal/interpolated/with-attrs/empty/multi-line + reject non-literal/malformed-interp) | ✓ | ✓ | ✓ VERIFIED |
| `pkg/parser/finalize_test.go` | `TestValidateLogStepPlacement_*` | ✓ | ✓ (4 Test funcs at L146/165/175/191) | ✓ | ✓ | ✓ VERIFIED |

### Key Link Verification

| From | To | Via | Status | Detail |
|------|----|----|--------|--------|
| `.star` `log.info(...)` call | `pkg/parser/builtins_log.go::buildLogStep` | `starlark.NewBuiltin("log.info", p.builtinLogInfo)` registered in `pkg/parser/globals.go:84-90` | ✓ WIRED | Module entry returns the parser-bound builtin trampoline; full parse exercised by `TestBuiltinLog_Accept*` |
| `pkg/parser` builtin | `*dag.LogStep` node | `*dag.LogStep` constructed inside `buildLogStep` + appended to `p.allLogSteps` (parser.go:99-106) | ✓ WIRED | `buildLogStep` calls `p.desugarInterpolation(msg, pos)` (L83) and stores the resulting `*CapturedLambda` in `LogStep.MsgFn`; `allLogSteps` consumed by `validateLogStepPlacement` at finalize.go:117 |
| `pkg/interpreter::walkNode` | `pkg/interpreter::walkLog` | `case *dag.LogStep: return i.walkLog(ctx, n)` at workflow.go:219-220 | ✓ WIRED | One-line dispatch, no aliasing layers; verified by `TestWalkLog_*` integration |
| `pkg/interpreter::walkLog` | `workflow.GetLogger(ctx).<Level>` | Acquired inside function body at walk_log.go:58 (not field-captured); level-switch dispatches one of `Debug/Info/Warn/Error` at L171-187 | ✓ WIRED | Anti-check: `grep -nE 'var\s+\w*[Ll]ogger\s*=\s*workflow\.GetLogger' pkg/interpreter/walk_log.go` returns 0 hits — logger acquisition is per-dispatch, replay-safe |
| `pkg/cli::setupServerLogging` (non-JSON) | `newLogKindFilterHandler` | `pkg/cli/render.go:194-...` wraps the charmlog handler when `jsonMode==false`; JSON branch bypasses | ✓ WIRED | Anti-check: `pkg/cli/log_filter.go` imports only `log/slog` + stdlib — no dependency on `pkg/dag` or `pkg/interpreter` |
| `pkg/cli::progressHandler` | `kind=="log"` early-return | `progress_static.go:78` (dispatch) + `:135` (complete) | ✓ WIRED | Verified by `TestProgress_KindLogSuppressed_*` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `LogStep.MsgFn` | resolved `msg` string at workflow time | `p.desugarInterpolation(msg, pos)` at builtins_log.go:83 → `*CapturedLambda` evaluated by `walk_log.go::evaluateMsg` reading `ctx` state | ✓ (real Starlark lambda over real ctx) | ✓ FLOWING |
| `LogStep.AttrsLambdaID` | structured attrs dict | optional `attrs=lambda ctx: dict` resolved from `WorkflowInput.Lambdas`, converted via `starlarkValueToSlogAttr` (walk_log.go:194) | ✓ | ✓ FLOWING |
| `workflow.GetLogger(ctx).<Level>` call | slog record with `[skytime/log] <msg>` + attrs | populated from `msg + keyvals` built inside walkLog | ✓ | ✓ FLOWING |
| `nonLogTotal` in `i.stepTot` | total displayed in `[N/M]` progress | computed by `workflow.go:178-185` iterating the parsed DAG and excluding `*dag.LogStep` | ✓ | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite all-green | `go test ./... -race -count=1` | 24 packages all `ok`, 0 failures | ✓ PASS |
| `go vet` clean across module | `go vet ./...` | exit 0, no output | ✓ PASS |
| Replay log tests pass with race detector | `go test ./pkg/interpreter/ -run "TestReplay_LogStep_" -race -count=1` | `ok ... 1.494s` — 2 named tests run, both PASS | ✓ PASS |
| Parser rejection tests pass | `go test ./pkg/parser/ -run "TestValidateLogStepPlacement_\|TestBuiltinLog_" -race -count=1` | `ok ... 1.311s` (13 named test funcs in the targeted set) | ✓ PASS |
| Docgen drift test green (proof builtins.md is regenerable) | `go test ./tests/ -run TestDocgenDrift -race -count=1` | `ok ... 1.506s` | ✓ PASS |
| `weekly_digest.star` migration grep | `grep -c "log\.info(" examples/http-github-webhook/weekly_digest.star` | `2` | ✓ PASS |
| Walkthrough doc migration grep | `grep -c "log\.info(" docs/walkthroughs/cron-schedules.md` | `2` | ✓ PASS |
| Builtin docgen entries grep | `grep -cE "^## log\.(info\|warn\|error\|debug)" docs/reference/builtins.md` | `4` | ✓ PASS |
| REQUIREMENTS.md LOG-01 checked | `grep -c '^- \[x\] \*\*LOG-01\*\*' .planning/REQUIREMENTS.md` | `1` | ✓ PASS |
| REQUIREMENTS.md LOG-02 checked | `grep -c '^- \[x\] \*\*LOG-02\*\*' .planning/REQUIREMENTS.md` | `1` | ✓ PASS |
| Traceability table LOG-01 row | `grep -c '^\| LOG-01 \|' .planning/REQUIREMENTS.md` | `1` | ✓ PASS |
| Traceability table LOG-02 row | `grep -c '^\| LOG-02 \|' .planning/REQUIREMENTS.md` | `1` | ✓ PASS |
| Anti-check: no package-level `var logger = workflow.GetLogger` in walk_log.go | `grep -nE 'var\s+\w*[Ll]ogger\s*=\s*workflow\.GetLogger' pkg/interpreter/walk_log.go` | 0 hits | ✓ PASS |
| Anti-check: `pkg/cli/log_filter.go` does not depend on `pkg/dag` or `pkg/interpreter` | grep for those import paths | 0 hits | ✓ PASS |
| Anti-check: `print()` still routes through `workflow.GetLogger` (D-7.2.1-09 coexistence) | grep `[skytime/print]` in `pkg/interpreter/lambda_eval.go` | hit at L35 (`workflow.GetLogger(ctx).Info("[skytime/print] "+msg, ...)` retained) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description (short) | Status | Evidence |
|-------------|--------------|---------------------|--------|----------|
| **LOG-01** | 01-dag-logstep-type, 02-parser-builtins, 03-walker-counter-replay, 05-migration-walkthrough | `.star` author surface: `log.info / .warn / .error / .debug(msg, attrs=lambda ctx: dict)` inside `flow(...)` bodies with `${ctx.expr}` interpolation; module-scope placement and non-literal msg rejected with position-aware `*dag.ParseError` | ✓ SATISFIED | `pkg/parser/builtins_log.go` + `globals.go:84-90` (surface); `pkg/parser/finalize.go:278` + tests at `finalize_test.go:146/165/175/191` (placement); `pkg/parser/builtins_log_test.go:96/115` (non-literal + malformed interp reject); REQUIREMENTS.md:135 row marked Complete |
| **LOG-02** | 01-dag-logstep-type, 03-walker-counter-replay, 04-renderer-suppression, 05-migration-walkthrough | Workflow-time routing via `workflow.GetLogger(ctx).<Level>` with `[skytime/log] <msg>` prefix; replay-safe (ReplayLogger suppression); `skytime server` human stdout shows one line per call, bookend `kind=log` frames suppressed by `logKindFilterHandler` + `progressHandler`; JSON-log mode passes everything through | ✓ SATISFIED | `pkg/interpreter/walk_log.go:58/170-187` (routing + prefix); `pkg/interpreter/replay_determinism_test.go:156/198` (replay-safe — 2 PASS); `pkg/cli/log_filter.go` + `render.go:194` wiring (non-JSON suppression); `pkg/cli/progress_static.go:78/135` (renderer early-return); `pkg/interpreter/workflow.go:178-185` (nonLogTotal counter); REQUIREMENTS.md:136 row marked Complete |

**No orphaned requirements:** REQUIREMENTS.md maps only LOG-01 and LOG-02 to Phase 07.2.1, both declared in plan frontmatters (LOG-01 in 4 plans, LOG-02 in 4 plans).

### Anti-Patterns Found

None.

A walk of all phase-modified files (extracted from plan SUMMARYs key-files) found:
- `pkg/dag/control.go`, `pkg/dag/marshal.go`, `pkg/parser/builtins_log.go`, `pkg/parser/globals.go`, `pkg/parser/finalize.go`, `pkg/parser/parser.go`, `pkg/interpreter/walk_log.go`, `pkg/interpreter/walk_log_helpers.go`, `pkg/interpreter/workflow.go`, `pkg/cli/log_filter.go`, `pkg/cli/render.go`, `pkg/cli/server.go`, `pkg/cli/progress_static.go`, plus tests and docs.

Patterns checked: `TODO|FIXME|XXX|HACK|PLACEHOLDER`, `placeholder|coming soon|will be here|not yet implemented|not available`, empty `return null/{}/[]` in production paths, `console.log`-only handlers (n/a — Go), hardcoded empty literals at call sites.

The only hits found were in comments documenting historical context (e.g., `weekly_digest.star:75` mentioning the replaced `print()` workaround) — those are intentional and explanatory, not unfinished work.

### Human Verification Required

One nominally manual confirmation, deliberately listed under `Manual-Only Verifications` in VALIDATION.md because it requires a running Temporal dev-server + skytime server + a real ~60s cron fire:

#### 1. End-to-end smoke walkthrough

**Test:** `bash docs/walkthroughs/cron-schedules-smoke.sh`
**Expected:**
- On the first cron fire, `skytime server` human stdout shows a single line per fire of the form `INFO [skytime/log] weekly digest complete: scheduled=<ts> actual=<ts> authors=<n>`.
- No `event=step_dispatch kind=log` lines and no `event=step_complete kind=log` lines appear in human-mode output (those are suppressed by `logKindFilterHandler` + `progressHandler`).
- The `[skytime/print]` prefix is NOT used (the script-step workaround is gone).
**Why human:** Requires a live Temporal Schedule reconciliation and a real cron fire wait (~60s); cannot run inside `go test`. The smoke script already exists from Phase 7.2 — this phase only changes the expected stdout line shape.

### Gaps Summary

No gaps. All five must-haves are verified by automated tests + code inspection; the single remaining manual confirmation is the documented `cron-schedules-smoke.sh` end-to-end walkthrough, which is by design a manual operator check (Temporal Schedule reconciliation + cron fire timing).

---

## Score Summary

- **Truths verified:** 5 / 5
- **Artifacts verified (Levels 1–4):** 16 / 16
- **Key links wired:** 6 / 6
- **Behavioral spot-checks PASS:** 15 / 15
- **Requirements satisfied:** 2 / 2 (LOG-01, LOG-02)
- **Anti-checks PASS:** 3 / 3 (no captured logger, no log_filter→dag/interpreter dep, print() coexistence retained)
- **Anti-patterns:** 0

**Overall status:** `human_needed` — every automated check is green; one manual smoke walkthrough remains (documented in VALIDATION.md `Manual-Only Verifications`).

---

_Verified: 2026-05-11_
_Verifier: Claude (gsd-verifier)_
