---
phase: 07.2.1
plan: 05
type: execute
wave: 3
depends_on:
  - "07.2.1-03"
  - "07.2.1-04"
files_modified:
  - examples/http-github-webhook/weekly_digest.star
  - docs/walkthroughs/cron-schedules.md
  - docs/reference/builtins.md
  - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - LOG-01
  - LOG-02
must_haves:
  truths:
    - "examples/http-github-webhook/weekly_digest.star uses a single log.info(...) step in place of the script(id=\"log_digest\", fn=lambda ctx: print(...) or {\"logged\": True}, output_alias=\"_log\") block (D-7.2.1-11; LOG-01 user-visible proof)"
    - "examples/http-github-webhook/weekly_digest.star contains NO surviving `print() or {...}` workaround shapes (D-7.2.1-11 audit)"
    - "docs/walkthroughs/cron-schedules.md sample stdout excerpt shows `[skytime/log] weekly digest complete: ...` (the new shape per D-7.2.1-13; LOG-02 user-visible proof) instead of `[skytime/print] weekly digest complete: ...`"
    - "docs/walkthroughs/cron-schedules.md narrative explains the new log.<level>(...) step builtin in place of the prior `print()` D3-22 explanation"
    - "docs/reference/builtins.md regenerated from `// skytime:doc` markers added in Plan 02 — contains four new entries (log.info, log.warn, log.error, log.debug)"
    - "tests/docgen_drift_test.go passes (docs/reference/builtins.md is in sync with `go run ./cmd/skytime-docgen` output)"
    - "examples/http-github-webhook/weekly_digest.star still parses cleanly (`skytime validate` or equivalent parser test exits 0)"
    - "07.2.1-VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true"
    - "07.2.1-VALIDATION.md test infrastructure table is filled in (Framework=go test, commands populated, runtime estimated)"
    - "07.2.1-VALIDATION.md per-task verification map lists all 11 tasks across plans 01-05 with their `<automated>` commands drawn from each task's <verify> block"
    - ".planning/REQUIREMENTS.md gains LOG-01 and LOG-02 entries in the bulleted-requirements list AND in the Traceability table (per CONTEXT.md line 178 known-gap — IDs were referenced from ROADMAP § 07.2.1 but never recorded in REQUIREMENTS.md until this sub-step)"
    - ".planning/REQUIREMENTS.md Traceability rows for LOG-01 and LOG-02 cite this phase (Phase 07.2.1) and list source plans 01-05"
  artifacts:
    - path: "examples/http-github-webhook/weekly_digest.star"
      provides: "Canonical example demonstrating log.info(...) as a flow step"
      contains: "log.info("
    - path: "docs/walkthroughs/cron-schedules.md"
      provides: "End-to-end walkthrough showing the new log step in operator stdout"
      contains: "[skytime/log] weekly digest complete"
    - path: ".planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md"
      provides: "Filled-in validation strategy with framework=go test, per-task verification map, nyquist_compliant flag"
      contains: "nyquist_compliant: true"
    - path: "docs/reference/builtins.md"
      provides: "Regenerated auto-gen reference including log.info/warn/error/debug entries"
      contains: "log.info"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Backfill LOG-01 and LOG-02 entries in the v1.43.0 requirements list AND in the Traceability table"
      contains: "LOG-01"
  key_links:
    - from: "examples/http-github-webhook/weekly_digest.star"
      to: "pkg/parser/builtins_log.go::builtinLogInfo (Plan 02)"
      via: "log.info(...) is parsed by builtinLogInfo at parse time"
      pattern: "log\\.info\\("
    - from: "docs/walkthroughs/cron-schedules.md"
      to: "pkg/interpreter/walk_log.go (Plan 03)"
      via: "stdout sample shows the [skytime/log] prefix emitted by walkLog"
      pattern: "\\[skytime/log\\] weekly digest complete"
    - from: "docs/reference/builtins.md"
      to: "pkg/parser/builtins_log.go // skytime:doc markers (Plan 02)"
      via: "cmd/skytime-docgen reads skytime:doc markers and regenerates the reference"
      pattern: "log\\.(info|warn|error|debug)"
---

<objective>
Migrate the canonical cron example (`examples/http-github-webhook/weekly_digest.star`) from the legacy `script(id="log_digest", fn=lambda ctx: print(...) or {"logged": True}, output_alias="_log")` workaround to a single `log.info(...)` step that exercises the new builtin landed by Plans 01-04. Update the walkthrough narrative + sample stdout excerpt in `docs/walkthroughs/cron-schedules.md` to show the new shape (`[skytime/log]` tag from Plan 03's walker, not `[skytime/print]`). Regenerate `docs/reference/builtins.md` so the four new `// skytime:doc`-tagged builtins from Plan 02 surface in the auto-gen reference. Fill in `07.2.1-VALIDATION.md` with the now-known test infrastructure and per-task verification map.

This is the user-visible PROOF that Phase 7.2.1 delivered:
- LOG-01 (the new step builtin) is demonstrated by the example using `log.info(...)` instead of the workaround
- LOG-02 (the walker surfaces it in operator stdout) is demonstrated by the walkthrough excerpt showing `[skytime/log] weekly digest complete: ...`

Purpose: Plans 01-04 land the implementation; this plan delivers the demonstration corpus and closes the validation loop. After this plan ships, a reader following `docs/walkthroughs/cron-schedules.md` sees the new shape end-to-end; the auto-gen reference points at the four builtins; the phase's validation strategy is sign-off-ready.

Output:
- `examples/http-github-webhook/weekly_digest.star` — `script(id="log_digest", ...)` block at lines 78-86 replaced by a single `log.info(...)` step; trigger declaration unchanged
- `docs/walkthroughs/cron-schedules.md` — excerpt at lines 277-301 updated: sample stdout shows `[skytime/log]` instead of `[skytime/print]`, narrative explains the new `log.<level>` builtin
- `docs/reference/builtins.md` — regenerated via `go generate ./pkg/parser/` (consumes the Plan 02 `// skytime:doc` markers)
- `.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` — frontmatter `nyquist_compliant: true`, `wave_0_complete: true`; test infrastructure table populated; per-task verification map filled with the 11 tasks (Plan 01: 2 tasks, Plan 02: 2, Plan 03: 2, Plan 04: 3, Plan 05: 2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-CONTEXT.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-RESEARCH.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-01-dag-logstep-type-PLAN.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-02-parser-builtins-PLAN.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-03-walker-counter-replay-PLAN.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-04-renderer-suppression-PLAN.md
@examples/http-github-webhook/weekly_digest.star
@docs/walkthroughs/cron-schedules.md
@.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md

<interfaces>
<!-- Key shapes the migration touches — extracted from CONTEXT D-7.2.1-11 + D-7.2.1-19 and verified at planning time. -->

From examples/http-github-webhook/weekly_digest.star (lines 71-86, current shape — TO BE REPLACED):
```python
        # Visible effect: the Starlark `print` builtin is routed to
        # workflow.GetLogger by the interpreter (D3-22 / lambda_eval.go),
        # so this line surfaces in `skytime server` stdout on every
        # cron fire with the [skytime/print] tag. Lets operators confirm
        # a fire produced output without leaving the terminal. Returns
        # an empty dict so the script step has a state binding (Starlark
        # `print` returns None; `None or {...}` yields the dict).
        script(
            id = "log_digest",
            fn = lambda ctx: print(
                "weekly digest complete: scheduled=" + ctx.scheduled_time +
                " actual=" + ctx.actual_time +
                " authors=" + str(len(ctx.grouped.authors)),
            ) or {"logged": True},
            output_alias = "_log",
        ),
```

From docs/walkthroughs/cron-schedules.md (lines 277-301, current shape — TO BE UPDATED):
```
INFO skytime workflow start ...
INFO skytime cron-fired workflow ...
INFO skytime event=flow_start ...
INFO skytime event=step_complete kind=script label=grouped status=ok ...
INFO skytime event=step_complete kind=step label="fetch repo info" status=ok ...
INFO skytime event=step_complete kind=for_each_parallel label="items=1" status=ok ...
INFO [skytime/print] weekly digest complete: scheduled=2026-05-11T21:01:00Z actual=2026-05-11T21:01:00Z authors=1 ...
INFO skytime event=flow_complete ok_count=4 err_count=0 total_ms=237
```

After Plan 03 (walker) + Plan 04 (renderer suppression), the operator stdout becomes:
```
INFO skytime workflow start ...
INFO skytime event=flow_start step_count=4   ← step_count=4 (log step excluded from M per D-7.2.1-13)
INFO skytime event=step_complete kind=script label=grouped status=ok ...
INFO skytime event=step_complete kind=step label="fetch repo info" status=ok ...
INFO skytime event=step_complete kind=for_each_parallel label="items=1" status=ok ...
INFO [skytime/log] weekly digest complete: scheduled=2026-05-11T21:01:00Z actual=2026-05-11T21:01:00Z authors=1   ← new tag from walk_log.go
INFO skytime event=flow_complete ok_count=4 err_count=0 total_ms=237
```
(NO `event=step_dispatch kind=log` or `event=step_complete kind=log` lines — Plan 04's logKindFilterHandler suppresses them. The user-message record stays.)

From .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md (current placeholder shape):
```markdown
---
phase: 07.2.1
slug: structured-logging-step-builtin
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 07.2.1 — Validation Strategy

## Test Infrastructure
| Property | Value |
|----------|-------|
| **Framework** | {pytest 7.x / jest 29.x / vitest / go test / other} |
| **Config file** | {path or "none — Wave 0 installs"} |
| **Quick run command** | `{quick command}` |
| **Full suite command** | `{full command}` |
| **Estimated runtime** | ~{N} seconds |
... etc ...
```

Plan task summary (drawn from PLANs 01-04 — referenced for the VALIDATION map):
- Plan 01: Task 1 (LogStep type) → `go test ./pkg/dag/ -run "TestLogStep_NodeInterface|TestLogStep_Kind|TestLogStep_Position|TestLogStep_AllFourLevelsConstructible|TestLogStep_FieldShape" -race -count=1`
- Plan 01: Task 2 (LogStep JSON) → `go test ./pkg/dag/ -run "TestLogStep_MarshalJSON" -race -count=1`
- Plan 02: Task 1 (builtinLog factories) → `go test ./pkg/parser/ -run "TestBuiltinLog_" -race -count=1`
- Plan 02: Task 2 (validateLogStepPlacement) → `go test ./pkg/parser/ -run "TestValidateLogStepPlacement_" -race -count=1`
- Plan 03: Task 1 (walkLog) → `go test ./pkg/interpreter/ -run "TestWalkLog_|TestWalkBody_LogStepsExcludedFromTotal" -race -count=1`
- Plan 03: Task 2 (replay determinism) → `go test ./pkg/interpreter/ -run "TestReplay_LogStep_" -race -count=1`
- Plan 04: Task 1 (logKindFilterHandler) → `go test ./pkg/cli/ -run "TestLogKindFilterHandler_" -race -count=1`
- Plan 04: Task 2 (setupServerLogging wiring) → `go test ./pkg/cli/ -run "TestServer_LogFilter" -race -count=1`
- Plan 04: Task 3 (progressHandler suppression) → `go test ./pkg/cli/ -run "TestProgress_KindLogSuppressed_|TestProgress_NonLogDispatch_StillRenders|TestProgress_NonLogCompleteErr_CapturesLastErr" -race -count=1`
- Plan 05: Task 1 (example migration + walkthrough) → `go test ./examples/... -race -count=1` + `go test ./tests/... -run TestDocgenDrift -race -count=1` + grep gates on the .star and .md content
- Plan 05: Task 2 (VALIDATION fill-in) → manual sign-off + frontmatter grep gate

From .planning/REQUIREMENTS.md + .planning/ROADMAP.md "Phase 07.2.1" section:
- LOG-01: "Authors can write `log.info/warn/error/debug(msg, attrs=...)` as a step inside flow(...) bodies"
- LOG-02: "Workflow-time emission surfaces in `skytime server` operator stdout via workflow.GetLogger at the matching slog level, decorated `[skytime/log] msg`"

(Note: REQUIREMENTS.md does not yet contain LOG-01/LOG-02 explicit entries; the IDs are anchored in ROADMAP.md § Phase 07.2.1. The Plan 02 + Plan 03 frontmatter already references them and the requirements-coverage gate accepts roadmap-attached IDs.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate weekly_digest.star + update cron-schedules.md walkthrough excerpt + regenerate docs/reference/builtins.md</name>
  <files>examples/http-github-webhook/weekly_digest.star, docs/walkthroughs/cron-schedules.md, docs/reference/builtins.md</files>
  <read_first>
    - examples/http-github-webhook/weekly_digest.star (read full file — confirm the `script(id="log_digest", ...)` block still spans roughly lines 78-86 with the `print(...) or {"logged": True}` shape; the surrounding `script(id="seed_authors", ...)`, `step(name="fetch repo info", ...)`, and `for_each_parallel(...)` blocks are PRESERVED unchanged; the `trigger(...)` at lines 108-120 is unchanged)
    - docs/walkthroughs/cron-schedules.md (read lines 270-310 — confirm the sample stdout excerpt at lines 277-286 still shows `[skytime/print] weekly digest complete: ...` and the narrative at lines 297-301 still references "the flow's own `print(...)` step" and "Starlark `print(msg)` call inside a script is routed through `workflow.GetLogger` (D3-22)")
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-02-parser-builtins-PLAN.md (skim the `// skytime:doc` markers added on builtinLogInfo/Warn/Error/Debug — confirm summary/param_msg/desc_msg/param_attrs/desc_attrs/since fields landed; the regenerated docs/reference/builtins.md output depends on these)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-03-walker-counter-replay-PLAN.md (confirm the walker emits `[skytime/log] ` prefix verbatim — the cron-schedules.md sample stdout excerpt must match this exactly)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-04-renderer-suppression-PLAN.md (confirm Plan 04 suppresses kind=log dispatch/complete in human mode — the sample stdout must NOT include those lines)
    - docs/walkthroughs/cron-schedules-smoke.sh (confirm the smoke test does NOT hard-code `[skytime/print]` as an assertion string — if it does, that's a separate edit; grep `[skytime/print]` and check)
    - pkg/parser/generate.go (read — confirm the `//go:generate` directive at line ~11 and that `go generate ./pkg/parser/` invokes `cmd/skytime-docgen`)
    - tests/docgen_drift_test.go (read — confirm the drift test runs `skytime-docgen` and diffs against the checked-in `docs/reference/builtins.md`; this test must pass after regeneration)
  </read_first>
  <action>
**1) Migrate `examples/http-github-webhook/weekly_digest.star`**: replace the `# Visible effect: ...` comment block (lines 71-77) + the `script(id="log_digest", ...)` block (lines 78-86) with a single `log.info(...)` step + a brief explanatory comment. The new block:

```python
        # Visible effect: log.info(...) emits a structured slog record at
        # workflow time via workflow.GetLogger(ctx).Info (Phase 7.2.1
        # walk_log.go) so this line surfaces in `skytime server` stdout
        # on every cron fire decorated with the [skytime/log] tag.
        # Replaces the prior `script(id="log_digest", fn=lambda ctx: print(...) or {"logged": True})`
        # workaround — log is a side-channel step (no output_alias, no
        # state binding), and the human renderer suppresses the bookend
        # step_dispatch/step_complete frames so operators see exactly
        # one [skytime/log] line per fire (D-7.2.1-13).
        log.info(
            "weekly digest complete: scheduled=${ctx.scheduled_time} actual=${ctx.actual_time} authors=${len(ctx.grouped.authors)}",
        ),
```

Concretely, the file becomes (showing only the affected section — leave everything else, including the `trigger(...)` block at lines 108-120, UNCHANGED):

```python
        # Parallel fan-out: one summary line per author. Mirrors the
        # for_each_parallel pattern from issue_triage / pr_to_webhook.
        for_each_parallel(
            items = lambda ctx: ctx.grouped.authors,
            item = "author",
            steps = [
                script(
                    id = "line",
                    fn = lambda ctx: {
                        "author": ctx.author,
                        "summary": "weekly digest for " + ctx.author + " at " + ctx.scheduled_time,
                    },
                    output_alias = "line",
                ),
            ],
            max_concurrency = 4,
        ),
        # Visible effect: log.info(...) emits a structured slog record at
        # workflow time via workflow.GetLogger(ctx).Info (Phase 7.2.1
        # walk_log.go) so this line surfaces in `skytime server` stdout
        # on every cron fire decorated with the [skytime/log] tag.
        # Replaces the prior `script(id="log_digest", fn=lambda ctx: print(...) or {"logged": True})`
        # workaround — log is a side-channel step (no output_alias, no
        # state binding), and the human renderer suppresses the bookend
        # step_dispatch/step_complete frames so operators see exactly
        # one [skytime/log] line per fire (D-7.2.1-13).
        log.info(
            "weekly digest complete: scheduled=${ctx.scheduled_time} actual=${ctx.actual_time} authors=${len(ctx.grouped.authors)}",
        ),
    ],
)
```

The closing `],` + `)` for the `flow(...)` call follow immediately after the `log.info(...)` step exactly as they did before (no other structural changes).

**Migration acceptance check (post-edit, grep gates):**
- The string `output_alias = "_log"` MUST be absent from the file (the migration removes it).
- The string `script(\n            id = "log_digest"` MUST be absent (the script step is gone).
- The string `print(` MUST be absent from the file body OUTSIDE of comment lines (i.e. no `lambda ctx: print(...)` shape remains). A `print` mentioned inside a `#`-prefixed comment line is OK — those comments are now historical context.
- The string `log.info(` MUST be present exactly once.
- The string `${ctx.scheduled_time}` MUST be present (interpolation desugars via D4.1-22, exercised by Plan 02's builtinLogInfo + Plan 03's walker).

**2) Update `docs/walkthroughs/cron-schedules.md` (lines 277-301)**:

Replace the sample stdout block (lines 277-286) and the explanatory paragraph (lines 297-301). The new block:

```
INFO skytime workflow start flow_name=weekly_digest workflow_id=weekly_digest/aGcRb2Q8-2026-05-11T21:01:00Z ...
INFO skytime cron-fired workflow scheduled_time=2026-05-11T21:01:00Z actual_time=2026-05-11T21:01:00Z
INFO skytime event=flow_start flow_name=weekly_digest step_count=4
INFO skytime event=step_complete kind=script label=grouped status=ok ...
INFO skytime event=step_complete kind=step label="fetch repo info" status=ok duration_ms=237 ...
INFO skytime event=step_complete kind=for_each_parallel label="items=1" status=ok ...
INFO [skytime/log] weekly digest complete: scheduled=2026-05-11T21:01:00Z actual=2026-05-11T21:01:00Z authors=1
INFO skytime event=flow_complete ok_count=4 err_count=0 total_ms=237
```

(Note: `step_count=4` is unchanged because Plan 03's counter denominator excludes log steps — the four visible steps remain `seed_authors`, `fetch repo info`, `for_each_parallel`, and the implicit flow envelope. The `kind=log` step_dispatch/step_complete records are emitted by walkLog but suppressed by Plan 04's logKindFilterHandler in human mode.)

Replace the explanatory paragraph (currently lines 297-301):

> The `[skytime/print]` line is the flow's own `print(...)` step — any
> Starlark `print(msg)` call inside a script is routed through
> `workflow.GetLogger` (D3-22) and surfaces in server stdout. Replace
> that step with real activities (mail send, DB write, dashboard
> update) to make the fire have an external effect.

with:

> The `[skytime/log]` line is the flow's `log.info(...)` step — the
> Phase 7.2.1 structured logging builtin. Each `log.<level>(msg, attrs=...)`
> call (info/warn/error/debug) emits a slog record at the matching
> level via `workflow.GetLogger(ctx)` (replay-safe — the Temporal SDK's
> `ReplayLogger` suppresses on replay). The `${ctx.expr}` interpolation
> in the message reuses the same parse-time desugarer that `script(id=...)`
> uses, so log messages can reference workflow inputs and previous-step
> outputs deterministically. The human renderer suppresses the bookend
> `step_dispatch`/`step_complete` frames so operators see exactly one
> tagged line per fire; `--json-log` mode emits all three records for
> downstream log-analysis tooling (D-7.2.1-13 / D-7.2.1-14). Replace
> the example's `log.info` with real activities (mail send, DB write,
> dashboard update) to make the fire have an external effect.

**Walkthrough acceptance check (grep gates):**
- `grep -c "\\[skytime/log\\] weekly digest complete" docs/walkthroughs/cron-schedules.md` returns ≥ 1
- `grep -c "\\[skytime/print\\] weekly digest complete" docs/walkthroughs/cron-schedules.md` returns 0 (the old reference is removed from this excerpt; if `[skytime/print]` is mentioned elsewhere in the doc for general context about `print()` being preserved per D-7.2.1-09, that's OK — only the *cron sample stdout block* must remove it)
- `grep -c "log\\.info" docs/walkthroughs/cron-schedules.md` returns ≥ 1
- `grep -c "D-7.2.1" docs/walkthroughs/cron-schedules.md` returns ≥ 1 (the new explanatory paragraph references the phase decisions)

**3) Regenerate `docs/reference/builtins.md`** by running the docgen:

```bash
cd /Users/mikel/dev/ai/temporero && go generate ./pkg/parser/
```

This invokes `cmd/skytime-docgen --pkg . --out ../../docs/reference/builtins.md` (per `pkg/parser/generate.go` line 11), which reads the `// skytime:doc` markers added in Plan 02 (`builtinLogInfo/Warn/Error/Debug` factories in `pkg/parser/builtins_log.go`) and emits four new entries.

**Docgen acceptance check (grep gates after `go generate`):**
- `grep -c "^## log\\.info" docs/reference/builtins.md` returns ≥ 1
- `grep -c "^## log\\.warn" docs/reference/builtins.md` returns ≥ 1
- `grep -c "^## log\\.error" docs/reference/builtins.md` returns ≥ 1
- `grep -c "^## log\\.debug" docs/reference/builtins.md` returns ≥ 1

If the docgen heading format differs (e.g. `### log.info` or `# log.info`), use whatever shape `cmd/skytime-docgen/main.go` actually produces — read the existing entries (e.g. for `script` or `flow`) to identify the heading level convention BEFORE asserting.

**4) Smoke-test the migrated example parses cleanly** — verify Plans 01-04 integrate end-to-end against the canonical fixture:

```bash
cd /Users/mikel/dev/ai/temporero && go test ./examples/http-github-webhook/... -race -count=1
```

If the example does not have a dedicated test file, fall back to a direct parse via the parser's existing integration test harness. The phase's interpreter + parser unit tests (Plans 01-04) cover the implementation; this step proves the migrated `.star` file is well-formed against those tests.

If `./examples/http-github-webhook/...` has no Go tests, the alternative gate is:

```bash
cd /Users/mikel/dev/ai/temporero && go run ./cmd/skytime validate examples/http-github-webhook/weekly_digest.star
```

(exit code 0 = parses cleanly). If `skytime` binary path differs, adapt to whatever the project's `validate` subcommand is — likely `cmd/skytime`.

**5) Run the docgen drift test** to confirm the regenerated builtins.md is in sync:

```bash
cd /Users/mikel/dev/ai/temporero && go test ./tests/ -run TestDocgenDrift -race -count=1
```

Must exit 0. If it does not, the `go generate ./pkg/parser/` step did not produce a stable output — re-run and re-commit.

**6) Sync `.planning/REQUIREMENTS.md`** — close the known gap (CONTEXT.md line 178: LOG-01 and LOG-02 are referenced in ROADMAP § Phase 07.2.1 and in every Plan's frontmatter, but were never recorded in REQUIREMENTS.md). Read `.planning/REQUIREMENTS.md` first to verify the existing bullet shape (it uses `- [ ] **REQ-ID**: description` for v1.43.0 requirements and `| REQ-ID | Phase | Source Plan | Status |` for the Traceability table).

Add a new sub-section to the v1.43.0 Requirements block (alongside the existing TRIG / SERVER / SCHED / UI / CLI / EX / AUTH sub-sections) — pick a location after the AUTH section, before the v1.44+ Deferred section:

```markdown
### Structured Logging Step Builtin

- [ ] **LOG-01**: A `.star` author can write `log.info(msg, attrs=lambda ctx: dict)` (and `log.warn` / `log.error` / `log.debug`) as a step inside flow(...) bodies. The msg supports `${ctx.expr}` interpolation per D4.1-22; attrs is an optional lambda returning a dict of structured key-value pairs. Empty msg, multi-line msg, and missing attrs all parse cleanly; non-literal msg and module-scope placement reject with position-aware `*dag.ParseError`.
- [ ] **LOG-02**: At workflow time the walker emits a structured slog record via `workflow.GetLogger(ctx)` at the matching level (Info/Warn/Error/Debug) with the message decorated as `[skytime/log] <msg>`. The record is replay-safe (Temporal `ReplayLogger` suppresses on replay). In `skytime server` human stdout the user-message record surfaces as one line per `log.<level>` call; the bookend `event=step_dispatch kind=log` and `event=step_complete kind=log` records are suppressed by the renderer (`logKindFilterHandler` + `progressHandler` early-return on `kind=="log"`). In `--json-log` mode all three records pass through verbatim for downstream log-analysis tooling.
```

Add the matching rows to the Traceability table (insert AFTER the last row — the AUTH-04 row at line ~128):

```markdown
| LOG-01 | Phase 07.2.1 | 07.2.1-02-parser-builtins, 07.2.1-05-migration-walkthrough | Pending |
| LOG-02 | Phase 07.2.1 | 07.2.1-01-dag-logstep-type, 07.2.1-03-walker-counter-replay, 07.2.1-04-renderer-suppression, 07.2.1-05-migration-walkthrough | Pending |
```

Also update the "Coverage Summary" table at the bottom of REQUIREMENTS.md — add a new row for the LOG category:

```markdown
| LOG (structured logging step builtin) | 2 | 7.2.1 |
```

And bump the total in the prose line: `**Total:** 31 requirements...` becomes `**Total:** 33 requirements across 8 categories, mapped to 7 phases.` (verify the exact wording in the file; preserve any other prose).

REQUIREMENTS.md sync acceptance gates (read `.planning/REQUIREMENTS.md` to confirm the bullet shape BEFORE editing — the patterns below assume the existing `- [ ] **REQ-ID**:` convention; if the file shape has drifted, mirror what's already there):
- `grep -c '^- \[ \] \*\*LOG-01\*\*' .planning/REQUIREMENTS.md` returns ≥ 1 (bullet present)
- `grep -c '^- \[ \] \*\*LOG-02\*\*' .planning/REQUIREMENTS.md` returns ≥ 1
- `grep -c '^| LOG-01 |' .planning/REQUIREMENTS.md` returns ≥ 1 (Traceability row present)
- `grep -c '^| LOG-02 |' .planning/REQUIREMENTS.md` returns ≥ 1
- `grep -c '^### Structured Logging Step Builtin$' .planning/REQUIREMENTS.md` returns 1 (new sub-section header present)

If the existing bullet shape differs (e.g. tighter — `- **REQ-ID**:` without the checkbox), use whatever shape the file already uses; the grep gates above must be adjusted to match.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && grep -c "log.info(" examples/http-github-webhook/weekly_digest.star && grep -c "log.info(" docs/walkthroughs/cron-schedules.md && grep -c "^## log\.info\|^### log\.info\|^# log\.info" docs/reference/builtins.md && go test ./tests/ -run TestDocgenDrift -race -count=1 && grep -c '^- \[ \] \*\*LOG-01\*\*' .planning/REQUIREMENTS.md && grep -c '^- \[ \] \*\*LOG-02\*\*' .planning/REQUIREMENTS.md && grep -c '^| LOG-01 |' .planning/REQUIREMENTS.md && grep -c '^| LOG-02 |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "log.info(" examples/http-github-webhook/weekly_digest.star` returns ≥ 1
    - `grep -c "log\\.info(" docs/walkthroughs/cron-schedules.md` returns ≥ 1
    - `grep -c "output_alias = \"_log\"" examples/http-github-webhook/weekly_digest.star` returns 0 (the script id=log_digest block is gone)
    - `grep -c "output_alias=\"_log\"" examples/http-github-webhook/weekly_digest.star` returns 0 (no-spaces form also absent)
    - `grep -c "id = \"log_digest\"" examples/http-github-webhook/weekly_digest.star` returns 0
    - `grep -nE "^[^#]*\\bprint\\(" examples/http-github-webhook/weekly_digest.star` returns NO non-comment-line matches (any surviving `print` is inside `#`-prefixed comment context only)
    - `grep -c "\\${ctx.scheduled_time}" examples/http-github-webhook/weekly_digest.star` returns ≥ 1 (interpolation present)
    - `grep -c "\\[skytime/log\\] weekly digest complete" docs/walkthroughs/cron-schedules.md` returns ≥ 1
    - `grep -nE "INFO \\[skytime/print\\] weekly digest complete" docs/walkthroughs/cron-schedules.md` returns 0 lines (the old sample-stdout line is replaced)
    - `grep -c "Phase 7.2.1\\|D-7.2.1-13\\|D-7.2.1-14\\|log\\.<level>\\|structured logging" docs/walkthroughs/cron-schedules.md` returns ≥ 1 (new narrative references the phase)
    - `docs/reference/builtins.md` exists (file is the regenerated output)
    - `grep -cE "^(#|##|###) log\\.(info|warn|error|debug)" docs/reference/builtins.md` returns ≥ 4 (one heading per new builtin; the exact heading level depends on docgen's convention)
    - `go generate ./pkg/parser/` exits 0 (regeneration is clean)
    - `go test ./tests/ -run TestDocgenDrift -race -count=1` exits 0 (drift test passes — builtins.md matches a fresh skytime-docgen run)
    - End-to-end smoke: `go run ./cmd/skytime validate examples/http-github-webhook/weekly_digest.star` exits 0 (the migrated .star file parses cleanly under the combined Plan 01 + Plan 02 changes). If `validate` is not a subcommand, replace with the project's canonical parse-check invocation; otherwise `go test ./pkg/parser/... -race -count=1` covers the contract.
    - `go test ./pkg/parser/... ./pkg/dag/... ./pkg/interpreter/... ./pkg/cli/... -race -count=1` exits 0 (full cross-package suite green; no regressions introduced by the migration)
    - `.planning/REQUIREMENTS.md` contains a LOG-01 requirement bullet: `grep -c '^- \[ \] \*\*LOG-01\*\*' .planning/REQUIREMENTS.md` returns ≥ 1 (mirror the existing `- [ ] **REQ-ID**:` shape used by TRIG/SERVER/SCHED bullets near the top of the file; if the file shape has drifted, adapt the grep to match)
    - `.planning/REQUIREMENTS.md` contains a LOG-02 requirement bullet: `grep -c '^- \[ \] \*\*LOG-02\*\*' .planning/REQUIREMENTS.md` returns ≥ 1
    - `.planning/REQUIREMENTS.md` Traceability table has a LOG-01 row: `grep -c '^| LOG-01 |' .planning/REQUIREMENTS.md` returns ≥ 1
    - `.planning/REQUIREMENTS.md` Traceability table has a LOG-02 row: `grep -c '^| LOG-02 |' .planning/REQUIREMENTS.md` returns ≥ 1
    - `.planning/REQUIREMENTS.md` contains the new section header: `grep -c '^### Structured Logging Step Builtin$' .planning/REQUIREMENTS.md` returns 1
    - `.planning/REQUIREMENTS.md` Coverage Summary table gains a `LOG` row: `grep -c '^| LOG (structured logging step builtin) |' .planning/REQUIREMENTS.md` returns ≥ 1
  </acceptance_criteria>
  <done>
The example, walkthrough, and auto-gen reference all reflect Phase 7.2.1's new shape end-to-end. A reader walking through `docs/walkthroughs/cron-schedules.md` sees `[skytime/log] weekly digest complete: ...` in the sample stdout; the example fixture exercises the new builtin against Plans 01-04; the auto-gen reference catalogs all four `log.<level>` entries; the docgen drift test passes. LOG-01 is user-visible-proven by the example's `log.info(...)` call site; LOG-02 is user-visible-proven by the walkthrough's `[skytime/log]` sample line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill in 07.2.1-VALIDATION.md frontmatter + test infrastructure + per-task verification map</name>
  <files>.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md (full file — confirm the placeholder shape; lines 1-77; identify the frontmatter, test-infrastructure table, sampling-rate section, per-task verification map header, wave-0 section, manual-only verification section, sign-off section)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-01-dag-logstep-type-PLAN.md (extract Task 1 + Task 2 `<verify><automated>...</automated></verify>` commands)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-02-parser-builtins-PLAN.md (extract Task 1 + Task 2 `<verify>` commands)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-03-walker-counter-replay-PLAN.md (extract Task 1 + Task 2 `<verify>` commands)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-04-renderer-suppression-PLAN.md (extract Task 1 + Task 2 + Task 3 `<verify>` commands)
    - .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-05-migration-walkthrough-PLAN.md (this plan — extract Task 1 `<verify>` command; Task 2's verify is "the VALIDATION.md frontmatter is well-formed", which is self-referential — handled in acceptance criteria)
    - .planning/ROADMAP.md (Phase 07.2.1 section — confirm LOG-01 and LOG-02 are the two requirement IDs)
  </read_first>
  <action>
Replace the entire content of `.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` with:

```markdown
---
phase: 07.2.1
slug: structured-logging-step-builtin
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-11
updated: 2026-05-12
---

# Phase 07.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (Go stdlib testing + `github.com/stretchr/testify v1.11.1`) |
| **Config file** | none — Go test discovery is filesystem-driven (`*_test.go` colocated with source) |
| **Quick run command** | `go test ./pkg/dag/... ./pkg/parser/... ./pkg/interpreter/... ./pkg/cli/... -race -count=1` |
| **Full suite command** | `go test ./... -race -count=1` |
| **Estimated runtime** | ~30 seconds (pkg-scoped); ~90 seconds (full suite incl. tests/firewall, tests/docgen_drift) |

---

## Sampling Rate

- **After every task commit:** Run the task's own `<verify><automated>` command (see Per-Task Verification Map below)
- **After every plan wave:** Run `go test ./pkg/{dag,parser,interpreter,cli}/... -race -count=1`
- **Before `/gsd:verify-work`:** Full suite must be green (`go test ./... -race -count=1` exits 0)
- **Max feedback latency:** ~5 seconds (per-task scoped); ~30 seconds (pkg-wave scoped); ~90 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.2.1-01-01 | 01 | 1 | LOG-01 | unit | `go test ./pkg/dag/ -run "TestLogStep_NodeInterface\|TestLogStep_Kind\|TestLogStep_Position\|TestLogStep_AllFourLevelsConstructible\|TestLogStep_FieldShape" -race -count=1` | ✅ (Plan 01 Task 1) | ⬜ pending |
| 07.2.1-01-02 | 01 | 1 | LOG-01 | unit | `go test ./pkg/dag/ -run "TestLogStep_MarshalJSON" -race -count=1` | ✅ (Plan 01 Task 2) | ⬜ pending |
| 07.2.1-02-01 | 02 | 1 | LOG-01, LOG-02 | unit | `go build ./pkg/parser/... && go test ./pkg/parser/ -run "TestBuiltinLog_" -race -count=1` | ✅ (Plan 02 Task 1) | ⬜ pending |
| 07.2.1-02-02 | 02 | 1 | LOG-01, LOG-02 | unit | `go test ./pkg/parser/ -run "TestValidateLogStepPlacement_" -race -count=1` | ✅ (Plan 02 Task 2) | ⬜ pending |
| 07.2.1-03-01 | 03 | 2 | LOG-01, LOG-02 | unit (TestWorkflowEnvironment) | `go build ./pkg/interpreter/... && go test ./pkg/interpreter/ -run "TestWalkLog_\|TestWalkBody_LogStepsExcludedFromTotal" -race -count=1` | ✅ (Plan 03 Task 1) | ⬜ pending |
| 07.2.1-03-02 | 03 | 2 | LOG-01, LOG-02 | replay-determinism (TestWorkflowEnvironment) | `go test ./pkg/interpreter/ -run "TestReplay_LogStep_" -race -count=1` | ✅ (Plan 03 Task 2) | ⬜ pending |
| 07.2.1-04-01 | 04 | 2 | LOG-02 | unit | `go build ./pkg/cli/... && go test ./pkg/cli/ -run "TestLogKindFilterHandler_" -race -count=1` | ✅ (Plan 04 Task 1) | ⬜ pending |
| 07.2.1-04-02 | 04 | 2 | LOG-02 | unit (stderr capture) | `go test ./pkg/cli/ -run "TestServer_LogFilter" -race -count=1` | ✅ (Plan 04 Task 2) | ⬜ pending |
| 07.2.1-04-03 | 04 | 2 | LOG-02 | unit | `go test ./pkg/cli/ -run "TestProgress_KindLogSuppressed_\|TestProgress_NonLogDispatch_StillRenders\|TestProgress_NonLogCompleteErr_CapturesLastErr" -race -count=1` | ✅ (Plan 04 Task 3) | ⬜ pending |
| 07.2.1-05-01 | 05 | 3 | LOG-01, LOG-02 | integration (grep + docgen drift + parse smoke) | `grep -c "log.info(" examples/http-github-webhook/weekly_digest.star && grep -c "log.info(" docs/walkthroughs/cron-schedules.md && grep -cE "^(#\|##\|###) log\\.(info\|warn\|error\|debug)" docs/reference/builtins.md && go test ./tests/ -run TestDocgenDrift -race -count=1` | ✅ (Plan 05 Task 1) | ⬜ pending |
| 07.2.1-05-02 | 05 | 3 | LOG-01, LOG-02 | self-check (frontmatter grep) | `grep -c "^nyquist_compliant: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1 AND `grep -c "^wave_0_complete: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1 | ✅ (Plan 05 Task 2 — this very task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No Wave 0 setup needed — Existing infrastructure covers all phase requirements:

- `go test` is the project's canonical test runner (already used in `pkg/dag/`, `pkg/parser/`, `pkg/interpreter/`, `pkg/cli/`).
- `github.com/stretchr/testify v1.11.1` is already in `go.mod` (used pervasively by existing tests).
- `go.temporal.io/sdk/testsuite.TestWorkflowEnvironment` is already wired in `pkg/interpreter/replay_determinism_test.go` and `pkg/interpreter/test_helpers_test.go` — Plan 03's walker tests reuse this harness.
- `tests/docgen_drift_test.go` already exists from Phase 04.3; Plan 05 Task 1 regenerates `docs/reference/builtins.md` so the drift test passes.
- `pkg/cli/progress_test.go` already exists with table-driven `progressHandler` coverage — Plan 04 Task 3 extends it.
- `pkg/cli/server_test.go` already exists — Plan 04 Task 2 extends it.

No new test framework, new mock library, or new fixture infrastructure required. Every test command listed in the Per-Task Verification Map runs against the existing `go test` toolchain.

---

## Manual-Only Verifications

All phase behaviors have automated verification at the unit + integration level. The only nominally manual check is the smoke walkthrough end-to-end:

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `skytime server` cron fire produces `[skytime/log] weekly digest complete: ...` line in operator stdout, no `event=step_*` kind=log frames | LOG-02 | End-to-end requires a running Temporal server + worker + cron-reconciled schedule; the existing `docs/walkthroughs/cron-schedules-smoke.sh` covers it but takes ~60s to wait for a cron fire | Run `bash docs/walkthroughs/cron-schedules-smoke.sh` after Plan 05 Task 1 lands; confirm the per-fire stdout line shows `[skytime/log]` (NOT `[skytime/print]`), and no `event=step_dispatch kind=log` lines appear |

(The smoke script already exists from Phase 7.2; it is a manual confirmation step, not blocking automated CI.)

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (every row in the Per-Task Verification Map has a concrete `go test` command)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has its own go test invocation)
- [x] Wave 0 covers all MISSING references (none — existing infra suffices)
- [x] No watch-mode flags (every command exits after one run; `-count=1` disables Go's test result cache)
- [x] Feedback latency < 30 seconds (pkg-scoped runs)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending (set to `approved YYYY-MM-DD` when phase verification completes via `/gsd:verify-work`)
```

Verify each frontmatter line is present exactly as written — the executor MUST NOT introduce trailing whitespace or alternate YAML quoting.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && grep -c "^nyquist_compliant: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md && grep -c "^wave_0_complete: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md && grep -c "go test ./pkg/dag/ -run" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md && grep -c "TestLogKindFilterHandler_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` exists
    - `grep -c "^nyquist_compliant: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1
    - `grep -c "^status: ready$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1
    - `grep -c "Framework.*go test" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1
    - The Per-Task Verification Map contains 11 rows (one per task across Plans 01-05): `grep -cE "^\\| 07\\.2\\.1-0[1-5]-0[1-3] \\|" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 11
    - `grep -c "TestLogStep_NodeInterface" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 01 Task 1 command present)
    - `grep -c "TestBuiltinLog_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 02 Task 1 command present)
    - `grep -c "TestValidateLogStepPlacement_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 02 Task 2 command present)
    - `grep -c "TestWalkLog_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 03 Task 1 command present)
    - `grep -c "TestReplay_LogStep_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 03 Task 2 command present)
    - `grep -c "TestLogKindFilterHandler_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 04 Task 1 command present)
    - `grep -c "TestServer_LogFilter" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 04 Task 2 command present)
    - `grep -c "TestProgress_KindLogSuppressed_" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 04 Task 3 command present)
    - `grep -c "TestDocgenDrift" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (Plan 05 Task 1 command present)
    - `grep -c "LOG-01" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1 (requirement column populated)
    - `grep -c "LOG-02" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns ≥ 1
    - File does NOT contain any placeholder strings: `grep -cE "\\{pytest\\|\\{quick command\\|\\{full command\\|\\{N\\}\\|\\{path\\|\\{behavior\\}\\|\\{reason\\}\\|\\{steps\\}\\|REQ-\\{XX\\}" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 0
  </acceptance_criteria>
  <done>
07.2.1-VALIDATION.md is filled in with concrete go-test commands for every task across Plans 01-05, the frontmatter flips to `nyquist_compliant: true` + `wave_0_complete: true`, and the validation sign-off section's checklist items are checked except the final "approved YYYY-MM-DD" line (filled in by `/gsd:verify-work`). The phase is ready for execution.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `go test ./pkg/dag/... ./pkg/parser/... ./pkg/interpreter/... ./pkg/cli/... -race -count=1` — full implementation suite green (proves Plans 01-04 still integrate after the example migration; the migrated `.star` file's `log.info(...)` step exercises plans 02 + 03 + 04 end-to-end through any integration test harness)
2. `go test ./tests/ -run TestDocgenDrift -race -count=1` — docgen drift test green (proves docs/reference/builtins.md is in sync with the regenerated output)
3. `go vet ./...` — clean across the project
4. Validation gate: `grep -c "^nyquist_compliant: true$" .planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-VALIDATION.md` returns 1
5. Migration sanity (no surviving workaround): `grep -nE "print\\([^)]*\\) or \\{" examples/ docs/ pkg/parser/testdata/ 2>/dev/null` returns 0 matches (audit per D-7.2.1-11 — Plan 05's primary case is `weekly_digest.star`; any other surviving instance is a planner oversight and should be migrated in this task too, but at planning time we have no other known cases)
6. Walkthrough sanity: `grep -c "\\[skytime/log\\]" docs/walkthroughs/cron-schedules.md` returns ≥ 1 (the new shape is documented)
</verification>

<success_criteria>
- The canonical cron example (`weekly_digest.star`) demonstrates the Phase 7.2.1 builtin in production-realistic shape (one `log.info(...)` step inside a `flow(...)` body, with `${ctx.expr}` interpolation, no `print() or {...}` workaround)
- The walkthrough (`docs/walkthroughs/cron-schedules.md`) shows the new operator-stdout shape — `[skytime/log] weekly digest complete: ...` instead of `[skytime/print]` — and explains the new builtin to a reader following the cron tutorial
- The auto-gen reference (`docs/reference/builtins.md`) catalogs all four `log.<level>` entries from the `// skytime:doc` markers added in Plan 02
- The docgen drift test passes
- The phase's validation strategy (`07.2.1-VALIDATION.md`) is filled in end-to-end with concrete go-test commands for all 11 tasks, `nyquist_compliant: true`, `wave_0_complete: true` — ready for `/gsd:verify-work` sign-off
- LOG-01 is user-visible-proven (example fixture uses the new step)
- LOG-02 is user-visible-proven (walkthrough sample stdout shows the [skytime/log] tag emitted by Plan 03's walker, surviving Plan 04's renderer filter)
- ROADMAP Phase 07.2.1 success criteria #1 (new step builtin) + #2 (operator stdout shows clean tag, not the workaround) + #5 (example corpus migrated) are all satisfied
- No regressions in `go test ./... -race -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/07.2.1-structured-logging-step-builtin/07.2.1-05-migration-walkthrough-SUMMARY.md` documenting:
- Exact lines changed in `weekly_digest.star` (line range pre/post-edit; confirm the surrounding `trigger(...)` block was NOT touched)
- Exact lines changed in `docs/walkthroughs/cron-schedules.md` (the sample stdout block + the narrative paragraph that follows)
- The output of `go generate ./pkg/parser/` (any new entries in `docs/reference/builtins.md` listed)
- Confirmation that the docgen drift test passed on the first regeneration (or, if not, what required re-running to converge)
- Whether any other `print() or {...}` workaround instances surfaced during the D-7.2.1-11 audit grep — beyond `weekly_digest.star` (planning identifies only this case; SUMMARY confirms the audit found no others, or lists any additional migrations performed)
- Whether the smoke script `docs/walkthroughs/cron-schedules-smoke.sh` needed updates (planning expects no — it does not hard-code `[skytime/print]` as an assertion — but the SUMMARY confirms either way)
- Filled-in VALIDATION.md frontmatter + per-task map shape (snapshot of the final commands list)
</output>
