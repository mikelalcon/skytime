---
phase: 07.4-extbin-consolidation-tech-debt-cleanup
plan: 05
subsystem: docs
tags: [bookkeeping, requirements-traceability, http-extension, examples, audit-debt]

# Dependency graph
requires:
  - phase: 03-lambda-serialization-decision-interpreter-worker
    provides: "Phase 3 test evidence (TestWalkIfCond_ZeroHistoryEvents, TestWalkScript_ZeroHistoryEvents, 6 TestWalkForEach_*, 3 TestWalkCallFlow_*) that satisfies INTRP-03/04/05 — this plan flips the trace-table bookkeeping that lagged behind."
  - phase: 04-static-validation-tier-cli-skeleton
    provides: "Baked-in HTTP extension (pkg/extension/builtin/http) with http.endpoint(...) factory that this plan's new flow exercises."
  - phase: 06-example-project
    provides: "examples/http-github-webhook/ corpus (5 flows + flows_test.go's TestFlows_ParseAll/CoverageMatrix discovery harness) that this plan's new flow plugs into without harness changes."
provides:
  - "Closed v1.42.0 milestone audit bookkeeping debt (D-7.4-06): INTRP-03/04/05 trace-table now accurately reflects Phase 3's already-shipped test evidence."
  - "Closed v1.42.0 milestone audit gap (D-7.4-07): bare http.endpoint(...) is now exercised by examples/http-github-webhook/generic_http_check.star — the HTTP extension is no longer registered-but-unused."
  - "Sixth flow in the example corpus, picked up automatically by TestFlows_ParseAll and TestFlows_CoverageMatrix without harness modifications."
affects: [verify-phase-07.4, complete-milestone-v1.43.0]

# Tech tracking
tech-stack:
  added: []  # No new dependencies — this plan is bookkeeping + a parse-only .star file + a coverage-map row.
  patterns:
    - "Audit-debt close-out as a parallel-wave plan: small, dependency-free items folded into the same phase as larger refactors so they ship in a single transition rather than dribbling across phases."
    - "Parse-only flow + auto-discovery harness pattern: TestFlows_ParseAll's filepath.WalkDir picks up new *.star files without test-list edits; new flows only need a coverage-map row."
    - "Extension-registration parity: every extension wired into extbin's WithExtensions chain SHOULD have at least one .star flow that calls it directly (parse-only is sufficient) so the registration is observable in the corpus, not just in the binary."

key-files:
  created:
    - "examples/http-github-webhook/generic_http_check.star — parse-only flow exercising http.endpoint(...) + .head + .get"
    - ".planning/phases/07.4-extbin-consolidation-tech-debt-cleanup/07.4-05-SUMMARY.md (this file)"
  modified:
    - ".planning/milestones/v1.42.0-REQUIREMENTS.md — three checkbox flips ([ ]→[x]) and three trace-table Status flips (Pending→Complete) for INTRP-03/04/05"
    - "examples/http-github-webhook/flows_test.go — added expected coverage map row for generic_http_check (script + step_seq + if_cond)"
    - "examples/http-github-webhook/README.md — added Coverage matrix row + Flow-by-flow tour section for generic_http_check"

key-decisions:
  - "Honor file-convention column semantics in v1.42.0-REQUIREMENTS.md trace table: Source Plan column stays 'Phase 3' (per the file's existing INTRP-01 row pattern), Status column flips Pending→Complete. CONTEXT D-7.4-06 phrasing read literally would put 'Phase 03' in the Status column, but file consistency wins — outcome (audit gap closes; trace-table truthful) matches CONTEXT intent. Rationale recorded inline in Plan 05's Task 1 action block."
  - "README updates applied to BOTH discovered sections: the Coverage matrix table (mechanical pinning section, line ~211 — CI-gated by TestFlows_CoverageMatrix) AND the Flow-by-flow tour (line ~274 — narrative section). Both have flow lists; updating only one would leave the README internally inconsistent."
  - "aggregateRequired slice in flows_test.go LEFT UNCHANGED: the new flow's primitives (script + step_seq + if_cond) are already covered by other flows; aggregate-coverage assertion is unaffected. Subset coverage is the right shape — the new flow's purpose is HTTP-extension registration parity, not new primitive coverage."
  - "No live runtime test for the new flow — D-7.4-07 + D-7.4-08 explicit no-live-runtime; the example corpus's parse-only coverage convention is preserved."

patterns-established:
  - "Pattern: 'small audit-debt items fold into a parallel-wave plan in their own thematic phase' — when an audit surfaces multiple small bookkeeping items (checkbox flips, missing test fixtures, doc drift), batch them into one wave-1 plan inside the next-up phase rather than spawning N tiny phases."
  - "Pattern: 'parse-only fixture closes registration/usage gaps without CI smoke cost' — when an extension is registered but not exercised in any .star flow, a parse-only fixture using the bare extension surface closes the gap with zero runtime test surface."

requirements-completed: []  # Plan frontmatter requirements: [] — this plan flips already-validated requirements' bookkeeping, doesn't introduce new requirements.

# Metrics
duration: 5min
completed: 2026-05-13
---

# Phase 07.4 Plan 05: extbin consolidation + tech debt cleanup — audit bookkeeping + HTTP-extension usage parity Summary

**INTRP-03/04/05 trace-table flipped to Complete (D-7.4-06) and the bare `http.endpoint(...)` surface gained its first parse-only fixture (D-7.4-07), closing two v1.42.0 audit-debt items in a Wave-1 parallel plan with zero live-runtime test surface added.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-13T13:54Z (approx — plan execution start)
- **Completed:** 2026-05-13T13:59Z
- **Tasks:** 3
- **Files modified:** 4 (1 created: generic_http_check.star + this SUMMARY.md; 3 edited: v1.42.0-REQUIREMENTS.md, flows_test.go, README.md)

## Accomplishments

- **D-7.4-06 closed:** v1.42.0 milestone audit's INTRP-03/04/05 bookkeeping gap is fixed. Phase 3 verified all three requirements months ago with concrete test evidence (TestWalkIfCond_ZeroHistoryEvents, TestWalkScript_ZeroHistoryEvents, 6 TestWalkForEach_*, 3 TestWalkCallFlow_*); the trace-table never updated. Three checkboxes now `[x]`, three Status rows now `Complete`. The audit's "auto during /gsd:complete-milestone" hand-off is honored ahead of schedule — completing it earlier doesn't break that contract.
- **D-7.4-07 closed:** v1.42.0 audit's "HTTP extension registered but unused" gap is fixed. `examples/http-github-webhook/extbin` registers `skyhttp.New()` in its `WithExtensions(...)` chain, but the four pre-7.4 flows all routed through the higher-level `github` extension (which wraps `http.*` internally and masks the bare surface). New `generic_http_check.star` exercises `http.endpoint(...)` plus the `.head` and `.get` per-method builtins on an unauthenticated `https://httpbin.org` base URL; flow uses `script + step_seq + if_cond` primitives.
- **Auto-discovery harness validated:** `TestFlows_ParseAll`'s `filepath.WalkDir` discovered the new flow without any test-list edit; `TestFlows_CoverageMatrix` accepted the new coverage-map row and confirmed the new flow's actual DAG matches the declared primitive set; aggregate coverage assertion unchanged (subset coverage doesn't disturb the global sweep).
- **README kept honest:** both flow-listing sections in `examples/http-github-webhook/README.md` (Coverage matrix table at line ~211, Flow-by-flow tour at line ~274) updated so the README accurately reflects the corpus.

## Task Commits

Each task was committed atomically with `--no-verify` (per parallel-execution protocol; orchestrator validates hooks once after Wave 1 completes):

1. **Task 1: Flip INTRP-03/04/05 checkboxes + trace-table status (D-7.4-06)** — `0ad26e3` (docs)
2. **Task 2: Create generic_http_check.star — parse-only http.endpoint flow (D-7.4-07)** — `8ae732e` (feat)
3. **Task 3: Wire generic_http_check into coverage matrix + README** — `0d0d295` (test)

**Plan metadata commit:** pending (will be created after this SUMMARY.md write + STATE/ROADMAP updates).

## Files Created/Modified

- `.planning/milestones/v1.42.0-REQUIREMENTS.md` — flipped 3 checkboxes ([ ]→[x]) and 3 trace-table Status cells (Pending→Complete) for INTRP-03/04/05; Source Plan column unchanged (stays "Phase 3" per file convention).
- `examples/http-github-webhook/generic_http_check.star` — NEW. Parse-only flow demonstrating `http.endpoint(...)` factory + `.head` + `.get` per-method builtins. No credentials. Coverage: script + step_seq + if_cond.
- `examples/http-github-webhook/flows_test.go` — added `"generic_http_check"` row to the `expected` coverage map in `TestFlows_CoverageMatrix` (lines ~107-148). `aggregateRequired` slice unchanged.
- `examples/http-github-webhook/README.md` — added Coverage matrix table row for `generic_http_check` + brief explanatory note about why it's subset coverage; added Flow-by-flow tour section pointing readers at the parse-only fixture and the audit gap it closes.

## Decisions Made

- **Trace-table column semantics:** CONTEXT D-7.4-06 phrasing ("trace table moves from `Pending` to `Phase 03` for each") read literally would put `Phase 03` in the Status column, but the file's existing convention (line 196: `INTRP-01 | Phase 3 | Complete`) reserves `Phase 3` for Source Plan and uses `Complete`/`Pending` for Status. Honored file convention — outcome (audit-gap closes; trace-table truthful) matches CONTEXT intent. Rationale captured in Plan 05's Task 1 action block AND in this SUMMARY's frontmatter so future readers don't second-guess.
- **README scope:** discovered TWO flow-listing sections in `examples/http-github-webhook/README.md` (Coverage matrix at line 211, Flow-by-flow tour at line 274). Updated BOTH — leaving one stale would create internal inconsistency in the README. Plan 05's action block said "if a flow-inventory section exists, edit it"; the conservative reading is "all of them, since both are flow-inventory sections."
- **`aggregateRequired` left unchanged:** the new flow's primitives (`script` + `step_seq` + `if_cond`) are already covered by other flows in the corpus. Adding the new flow's primitive set to `aggregateRequired` would be a no-op assertion. Subset coverage is the right shape per Plan 05 Task 3 instructions.
- **No live runtime test:** D-7.4-07 + D-7.4-08 explicit no-live-runtime for new flows; matches the rest of the example corpus's parse-only coverage convention. Did NOT introduce a `*_smoke_test.go` for `generic_http_check`.

## Deviations from Plan

None — plan executed exactly as written. The "decisions made" entries above are clarifications/elaborations of guidance the plan provided (Task 1's column-semantics note, Task 3's "EITHER outcome accepted" README guidance), not deviations from the plan's directives.

## Issues Encountered

None. All three tasks completed cleanly. Verification commands all passed on first run:

- `grep '\[x\] \*\*INTRP-0[345]\*\*'` → 3 matches
- `grep '| INTRP-0[345] | Phase 3 | Complete |'` → 3 matches
- `grep '| INTRP-0[345] | Phase 3 | Pending |'` → 0 matches
- `go test ./examples/http-github-webhook/ -count=1 -timeout 120s` → PASS
- `go vet ./examples/http-github-webhook/...` → exit 0

## User Setup Required

None — no external service configuration required. The new flow uses an unauthenticated `https://httpbin.org` base URL for illustration purposes; since the flow is parse-only, the URL is never resolved at test time.

## Next Phase Readiness

- v1.42.0 audit bookkeeping debt fully closed (this plan's contribution to the broader Phase 07.4 effort).
- HTTP extension registration/usage parity restored — extension-registration audits going forward can grep for `http.*(` patterns in `examples/**/*.star` and find at least one match.
- TestFlows_ParseAll + TestFlows_CoverageMatrix continue to gate corpus consistency in CI; future flows added to `examples/http-github-webhook/` will be auto-discovered with no test-list edits required, and only need a coverage-map row.
- No blockers for Plan 02 (extbin collapse) or Plan 04 (extbin doc-comment lift), both of which depend on Plan 01's completion. This plan is independent of those.

## Self-Check: PASSED

**Files verified to exist:**
- `examples/http-github-webhook/generic_http_check.star` — FOUND (2572 bytes)
- `.planning/phases/07.4-extbin-consolidation-tech-debt-cleanup/07.4-05-SUMMARY.md` — FOUND (this file)

**Commits verified to exist on main:**
- `0ad26e3` (Task 1: docs INTRP checkbox flips) — FOUND
- `8ae732e` (Task 2: feat generic_http_check.star) — FOUND
- `0d0d295` (Task 3: test coverage map + README) — FOUND

**Acceptance criteria verified:**
- INTRP-03/04/05 checkboxes: 3 checked, 0 unchecked — PASS
- Trace-table: 3 Complete, 0 Pending — PASS
- Source Plan column unchanged — PASS (still "Phase 3" — confirmed via grep + visual inspection)
- New flow file exists and ends in .star (not _test.star) — PASS
- generic_http_check uses http.endpoint, NOT github.client — PASS (3 http.endpoint matches incl. docstring; 0 github.client matches)
- TestFlows_ParseAll picks up the new flow automatically — PASS (test output shows generic_http_check.star subtest)
- TestFlows_CoverageMatrix accepts the new row — PASS (test output shows generic_http_check subtest)
- aggregateRequired slice unchanged — PASS (no edits to lines 145-148 of flows_test.go)
- go vet clean — PASS (exit 0)
- Full example test suite green — PASS (2.354s ok)

---
*Phase: 07.4-extbin-consolidation-tech-debt-cleanup*
*Completed: 2026-05-13*
