---
phase: 07.5-auth-documentation
plan: 07
subsystem: infra
tags: [github-actions, ci, go, snippets, drift-test, auth-docs]

# Dependency graph
requires:
  - phase: 07.5-auth-documentation
    provides: snippets module with go.mod, four cloud-auth snippets, drift-test (TestMarkdownSnippetDrift)
provides:
  - CI gate that runs go build + vet + test on the snippets module on every push
  - Drift-test enforcement preventing markdown-vs-source divergence
  - Closure of SC2 (snippets compile cleanly against go.temporal.io/sdk@v1.42.0)
affects: [future doc updates, future Temporal SDK upgrades, phase 07.5 completion]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CI gates module compilation via working-directory step (no separate workflow file per D-7.5-07)"
    - "go test -count=1 used to defeat Go test caching for drift-detection"

key-files:
  created: []
  modified:
    - ".github/workflows/ci.yml"

key-decisions:
  - "Appended a single step to the existing ci job (D-7.5-07: unified CI surface)"
  - "Used working-directory rather than a separate job to share the Set up Go step and avoid runner duplication"
  - "Included go mod download explicitly so cloud-SDK deps are fetched before build (avoids ambiguity in cache-cold first runs)"

patterns-established:
  - "Module-scoped CI step pattern: working-directory + go mod download + go build/vet/test for non-root Go modules"

requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

# Metrics
duration: 2min
completed: 2026-05-13
---

# Phase 7.5 Plan 07: CI snippets-build step Summary

**Single-step append to .github/workflows/ci.yml that runs go build + vet + test inside docs/for-extension-developers/snippets, closing SC2 and gating the drift-test on every push.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-13T18:57:26Z
- **Completed:** 2026-05-13T18:58:31Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `snippets build + vet + test (auth-docs drift-test)` step at the end of the existing `ci` job
- Step runs `go mod download && go build ./... && go vet ./... && go test -count=1 ./...` scoped via `working-directory: docs/for-extension-developers/snippets`
- Preserved D-7.5-07: single workflow file, single `ci` job, no separate `snippets.yml` or `snippets-build.yml`
- All four AUTH-01..04 snippets now get compile + vet + drift-test coverage on every push
- Locally sanity-checked: `go build`, `go vet`, and `go test -count=1` all pass against the snippets module

## Task Commits

Each task was committed atomically (parallel mode, `--no-verify`):

1. **Task 1: Add snippets-build step to .github/workflows/ci.yml** — `61bfec2` (feat)

## Files Created/Modified

- `.github/workflows/ci.yml` — appended an 8-line step at the end of the steps array

## Decisions Made

- Chose **step-within-existing-job** over **separate job** because the existing job already has `Set up Go` configured at the right version (1.25). A separate job would require duplicating checkout + setup-go for marginal gain.
- Kept `go mod download` as an explicit pre-step so cold-cache runs surface dependency-resolution failures clearly (separate exit code) rather than being mixed into the `go build` output.
- `go test -count=1` matches the convention from the existing `go test (race)` step (line 34), defeating Go's test cache for deterministic re-runs.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `python3 -c "import yaml; ..."` (the suggested YAML validator in the plan) is not installed in this sandbox. Fell back to Ruby's `YAML.safe_load(File.read(...))` which confirmed the workflow parses cleanly, has one `ci` job with 9 steps, and the last step is `snippets build + vet + test (auth-docs drift-test)`. GitHub Actions will validate the YAML on the next push regardless.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 7.5 deliverable matrix complete: page exists (01-05), README+PROJECT.md links (06, sibling), snippets compile, drift-test guards everything, and **CI now gates the whole stack on every push**.
- AUTH-01..AUTH-04 requirements ready to be marked complete.
- Ready for phase verifier to confirm SC2 closure.

## Self-Check: PASSED

- FOUND: `.github/workflows/ci.yml` — modified, contains `snippets build`, `working-directory: docs/for-extension-developers/snippets`, `go mod download`, `go build ./...`, `go vet ./...`, `go test -count=1`
- FOUND: commit `61bfec2` in `git log --oneline`
- FOUND: single `ci:` job in workflow (`grep -c '^  ci:' .github/workflows/ci.yml` = 1)
- FOUND: 9 steps in ci job (Ruby YAML parse), last step = `snippets build + vet + test (auth-docs drift-test)`
- MISSING (confirmed absent, as required by D-7.5-07): `.github/workflows/snippets.yml`, `.github/workflows/snippets-ci.yml`, `.github/workflows/snippets-build.yml`
- Local sanity: `go build ./...`, `go vet ./...`, `go test -count=1 ./...` all pass inside the snippets module

---
*Phase: 07.5-auth-documentation*
*Completed: 2026-05-13*
