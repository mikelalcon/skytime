# Phase 7.5: Auth documentation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-13
**Phase:** 07.5-auth-documentation
**Areas discussed:** Snippet depth & runnability, CI verification for snippets, Cloud SDK dependency scope, Audience assumed knowledge

---

## Snippet depth & runnability

### Q1: How standalone is each Go snippet?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop-in function | Each snippet is a complete Go function with full body, error handling, imports listed at top. Reader pastes into their own package. RECOMMENDED. | ✓ |
| Full main.go + go.mod | Each snippet is a complete runnable program. | |
| Illustrative skeleton with TODOs | `// TODO: fetch token via your IAM client` placeholders. | |

**User's choice:** Drop-in function.

### Q2: How should secret/identifier values appear?

| Option | Description | Selected |
|--------|-------------|----------|
| Function params + env-var examples in prose | Function takes config; prose shows `os.Getenv(...)` calling pattern. RECOMMENDED. | ✓ |
| Hardcoded placeholders | `"YOUR_PROJECT_ID"` inline. | |
| Env-var lookups inline | Snippet itself calls `os.Getenv(...)`. | |

**User's choice:** Function params + env-var examples in prose.

### Q3: Error handling depth?

| Option | Description | Selected |
|--------|-------------|----------|
| Full Go-idiomatic returns | `fmt.Errorf("context: %w", err)` everywhere. RECOMMENDED. | ✓ |
| Bare returns with comments | `if err != nil { return nil, err } // add context here`. | |
| `// handle error` elision | Shortest; not production-ready. | |

**User's choice:** Full Go-idiomatic returns.

### Q4: Each snippet's preamble (Assumes/Substitute) location?

| Option | Description | Selected |
|--------|-------------|----------|
| Inline before each snippet | Bullet lists immediately before the snippet. RECOMMENDED. | ✓ |
| Top-of-page common preamble + per-snippet diffs | Shared assumptions up top; per-snippet additions only. | |
| Footnote-style references | Glossary at the bottom; callouts in snippets. | |

**User's choice:** Inline before each snippet.

---

## CI verification for snippets + Cloud SDK dependency scope

### Q1: How are snippets compile-verified to satisfy ROADMAP SC2?

| Option | Description | Selected |
|--------|-------------|----------|
| Parallel snippet files + markdown reference | `docs/.../snippets/{gcp,aws,azure,mtls}.go` exist as compilable files; markdown excerpts/references them; CI builds them; drift-test diffs markdown vs files. RECOMMENDED. | ✓ |
| Markdown extraction + compile in CI | Shell tooling extracts code fences from markdown, writes to temp dir, runs `go vet`. | |
| Manual one-time verification | Author compiles locally; no CI gate. | |

**User's choice:** Parallel snippet files + markdown reference.

### Q2: Where do cloud SDK dependencies live?

| Option | Description | Selected |
|--------|-------------|----------|
| Separate snippets-only `go.mod` | `docs/.../snippets/go.mod` for cloud SDKs; library go.mod stays clean. RECOMMENDED. | ✓ |
| Add cloud SDKs to main go.mod | Simpler build infra; bloats library transitives. | |
| No compile-test for cloud-specific imports | Markdown-only for cloud bits; risky. | |

**User's choice:** Separate snippets-only `go.mod`.

### Q3: Where does the CI hook live?

| Option | Description | Selected |
|--------|-------------|----------|
| Add to existing `.github/workflows/ci.yml` | Unified CI surface. RECOMMENDED. | ✓ |
| Separate workflow file | `.github/workflows/docs.yml`; cleaner separation, more files. | |

**User's choice:** Add to existing `.github/workflows/ci.yml`.

---

## Audience assumed knowledge

### Q1: How much cloud-IAM background does the doc assume?

| Option | Description | Selected |
|--------|-------------|----------|
| Medium — Skytime user + cloud experience, not WIF/IRSA expert | 2-3 sentences IAM context per section + link out to vendor docs. RECOMMENDED. | ✓ |
| Terse — reader is cloud-IAM expert | One-line context; snippet does the talking. | |
| Verbose — reader starts from zero | Setup checklists; competes with vendor docs. | |

**User's choice:** Medium.

### Q2: Page intro vs per-section intros?

| Option | Description | Selected |
|--------|-------------|----------|
| Page intro + per-section intros | Top: rotation pattern + how to pick section. Each section: 2-3 sentence cloud-IAM context. RECOMMENDED. | ✓ |
| Page intro only, sections jump to snippets | Tightest doc; leans on inline preamble. | |

**User's choice:** Page intro + per-section intros.

### Q3: PROJECT.md "Web UI / dashboard" Out-of-Scope wording?

| Option | Description | Selected |
|--------|-------------|----------|
| Use v1.43-DRAFT-PLAN.md spec wording verbatim | Drop the spec'd line in. RECOMMENDED. | ✓ |
| Polish to match existing PROJECT.md tone | Slight stylistic gain; more work. | |

**User's choice:** Verbatim from v1.43-DRAFT-PLAN.md.

---

## Claude's Discretion

- Drift-test mechanism choice (custom Go test reading both files; markdown-fence-tag annotations; embedmd-style external tool).
- Whether the snippets module joins `go.work` or stays standalone.
- Exact identifier names in function signatures.
- Whether to include a tiny `main_test.go` exercising constructors with mocked cloud clients.
- Whether the page-intro "Pick your section" directory is plain prose, table, or `<details>` block.
- Top-level README doc-index update (link text + placement).
- Whether to add per-cloud "rotation cadence" guidance.

## Deferred Ideas

- `pkg/worker/credentials/` helpers (out of scope per v1.43-DRAFT-PLAN.md; defer to v1.44+).
- Skytime application-credential rotation (separate domain from Temporal cluster credentials).
- Authentication on the dashboard itself.
- Multi-tenancy in dashboard.
- WebSocket / SSE for triggers in dashboard.
- Snippet for plain Temporal Cloud API key (no rotation) — doesn't demonstrate `client.Credentials` rotation.
- Per-cloud rotation cadence guidance (planner discretion to include if natural).
