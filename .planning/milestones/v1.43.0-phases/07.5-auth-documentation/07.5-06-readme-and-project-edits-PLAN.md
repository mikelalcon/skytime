---
phase: 07.5-auth-documentation
plan: 06
type: execute
wave: 3
depends_on: ["07.5-05"]
files_modified:
  - README.md
  - docs/for-extension-developers/README.md
  - .planning/PROJECT.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
requirements_addressed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "The top-level README.md doc-index links to docs/for-extension-developers/temporal-auth.md (satisfies SC1 'linked from main README's documentation index')"
    - "The for-extension-developers/README.md sub-index also references temporal-auth.md (good doc hygiene)"
    - "PROJECT.md ## Out of Scope section contains the verbatim 'Web UI / dashboard ...' carve-out paragraph per D-7.5-10"
  artifacts:
    - path: "README.md"
      provides: "Top-level doc-index with the temporal-auth.md link"
      contains: "temporal-auth.md"
    - path: "docs/for-extension-developers/README.md"
      provides: "Sub-index reference to the new page"
      contains: "temporal-auth.md"
    - path: ".planning/PROJECT.md"
      provides: "Out-of-Scope dashboard carve-out (verbatim from v1.43-DRAFT-PLAN.md)"
      contains: "Web UI / dashboard — example dashboard ships under"
  key_links:
    - from: "README.md"
      to: "docs/for-extension-developers/temporal-auth.md"
      via: "markdown link in the Where to Go Next section"
      pattern: "\\[.*\\]\\(docs/for-extension-developers/temporal-auth\\.md\\)"
---

<objective>
Wire the new temporal-auth.md page into the project's doc-index surfaces (top-level README, for-extension-developers sub-index) and append the verbatim "Web UI / dashboard" carve-out to PROJECT.md's Out of Scope section.

Purpose: Closes SC1's "linked from main README's documentation index" requirement and D-7.5-10's PROJECT.md edit. Pure markdown edits — no code touched, no `pkg/` files.

Output: Three single-edit markdown changes; auth-docs are now discoverable from top-level entry points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-auth-documentation/07.5-CONTEXT.md
@.planning/v1.43-DRAFT-PLAN.md
@README.md
@docs/for-extension-developers/README.md
@.planning/PROJECT.md
@docs/for-extension-developers/temporal-auth.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add temporal-auth.md link to top-level README.md doc index</name>
  <files>README.md</files>
  <read_first>
    - README.md (locate the "Where to Go Next" section around line 339-391 — that's the doc-index area; the auth-docs entry slots in between the existing extension-developers link and the cli-binary link)
    - docs/for-extension-developers/temporal-auth.md (confirm the target file exists with expected H2 sections)
    - .planning/phases/07.5-auth-documentation/07.5-CONTEXT.md (D-7.5-08 medium-audience tone for the link text)
  </read_first>
  <action>
    Edit `README.md`. Locate this existing bullet in the "Where to Go Next" section (around line 386-388):

    ```
    - **Build an extension** —
      [`docs/for-extension-developers/README.md`](docs/for-extension-developers/README.md)
      is the landing page for Go developers writing extensions.
    ```

    Immediately after that bullet (and before the "Build a custom CLI" bullet which follows it), insert this new bullet:

    ```
    - **Connect to production Temporal clusters** —
      [`docs/for-extension-developers/temporal-auth.md`](docs/for-extension-developers/temporal-auth.md)
      ships four working credential-rotation snippets (GCP WIF →
      Secret Manager, AWS IRSA → Secrets Manager, Azure Workload
      Identity → Key Vault, self-hosted mTLS with SIGHUP reload) so
      your worker can authenticate to Temporal Cloud or a self-hosted
      cluster with rotation-friendly `client.Credentials`.
    ```

    Use the `Edit` tool with the exact string match for the original bullet (the three lines starting with `- **Build an extension** —`) and the new combined text (original + the new auth bullet appended).

    Do NOT modify any other part of README.md.
  </action>
  <verify>
    <automated>grep -q '\](docs/for-extension-developers/temporal-auth.md)' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\[.*\](docs/for-extension-developers/temporal-auth.md)' README.md` exits 0 (link with the new doc path exists)
    - `grep -q 'temporal-auth.md' README.md` exits 0
    - `grep -q 'Connect to production Temporal clusters' README.md` exits 0 (the new bullet's heading text)
    - `grep -q 'GCP WIF' README.md` exits 0 (mentions one of the four matrices — SC4 traceability hint)
    - `grep -q 'self-hosted mTLS' README.md` exits 0
    - The pre-existing "Build an extension" bullet is preserved:
      - `grep -q 'Build an extension' README.md` exits 0
      - `grep -q 'landing page for Go developers writing extensions' README.md` exits 0
    - The pre-existing "Build a custom CLI" bullet is preserved: `grep -q 'Build a custom CLI' README.md` exits 0
    - No lines deleted or moved: the file's line count grew by ~6 lines (the new bullet), not shrunk
  </acceptance_criteria>
  <done>Top-level README.md doc index references temporal-auth.md with descriptive link text covering the four-matrix scope.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add temporal-auth.md reference to docs/for-extension-developers/README.md</name>
  <files>docs/for-extension-developers/README.md</files>
  <read_first>
    - docs/for-extension-developers/README.md (the sub-index; "Reference Material" section starts around line 33)
    - docs/for-extension-developers/temporal-auth.md (the target)
    - .planning/phases/07.5-auth-documentation/07.5-CONTEXT.md (good doc hygiene note: the new page also belongs in this sub-index, not just the top-level README)
  </read_first>
  <action>
    Edit `docs/for-extension-developers/README.md`. The file currently has a "Reference Material" section starting at line 33 with bullets pointing to cli-binary.md, http.md, cli.md, builtins.md. Add a new bullet at the END of that bulleted list (before the "Credential resolution" H2 at around line 53), so the structure becomes:

    ```
    - **[Custom CLI Guide](../cli-binary.md)** — wiring your extension ...
      ...credential handler contract.
    - **[Bundled HTTP Extension Reference](../for-flow-authors/extensions/http.md)** — ...
      ... will reproduce.
    - **[CLI Reference](../reference/cli.md)** — exit-code semantics, ...
      ... that render cleanly.
    - **[Builtins Reference](../reference/builtins.md)** — the DSL surface
      flow-authors call into your extension from.
    - **[Temporal Authentication Patterns](temporal-auth.md)** — four
      production credential-rotation snippets (GCP WIF, AWS IRSA, Azure
      Workload Identity, self-hosted mTLS+SIGHUP) for connecting your
      worker to Temporal Cloud or a self-hosted cluster with rotation-
      friendly `client.Credentials`. Distinguishes *cluster* credentials
      (Temporal cluster connection) from *application* credentials (the
      credfile / `cli.WithCredentialHandler` surface above).
    ```

    Use `Edit` to insert this new bullet immediately after the "Builtins Reference" bullet. The next H2 ("Credential resolution: `pkg/extension/credfile/`") remains where it is — your insertion happens between the Builtins bullet and the H2.

    Do NOT modify any other section of this file.
  </action>
  <verify>
    <automated>grep -q '\[Temporal Authentication Patterns\](temporal-auth.md)' docs/for-extension-developers/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\[Temporal Authentication Patterns\](temporal-auth.md)' docs/for-extension-developers/README.md` exits 0
    - `grep -q 'temporal-auth.md' docs/for-extension-developers/README.md` exits 0
    - `grep -q 'cluster.*credentials.*application.*credentials' docs/for-extension-developers/README.md` exits 0 (distinction surfaced in the bullet text)
    - The existing "Credential resolution: `pkg/extension/credfile/`" H2 still present: `grep -q '^## Credential resolution' docs/for-extension-developers/README.md` exits 0
    - Pre-existing bullets preserved:
      - `grep -q 'Custom CLI Guide' docs/for-extension-developers/README.md` exits 0
      - `grep -q 'Bundled HTTP Extension Reference' docs/for-extension-developers/README.md` exits 0
      - `grep -q 'CLI Reference' docs/for-extension-developers/README.md` exits 0
      - `grep -q 'Builtins Reference' docs/for-extension-developers/README.md` exits 0
  </acceptance_criteria>
  <done>The for-extension-developers sub-index references temporal-auth.md and surfaces the cluster-vs-application credential distinction in the link text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append verbatim "Web UI / dashboard" carve-out to PROJECT.md ## Out of Scope</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (locate the "## Out of Scope" section near line 68-78 — that's where the new carve-out lands)
    - .planning/v1.43-DRAFT-PLAN.md (line 141 — the VERBATIM source of the carve-out wording; D-7.5-10 mandates no tonal polish)
    - .planning/phases/07.5-auth-documentation/07.5-CONTEXT.md (D-7.5-10: "verbatim from v1.43-DRAFT-PLAN.md spec. No tonal polish.")
  </read_first>
  <action>
    Edit `.planning/PROJECT.md`. Locate the "### Out of Scope" section. After the existing "Production observability UI" bullet (around line 72), append this NEW bullet VERBATIM (the exact wording is locked by D-7.5-10 and lifted byte-for-byte from `.planning/v1.43-DRAFT-PLAN.md` line 141):

    ```
    - **Web UI / dashboard** — example dashboard ships under `examples/.../server/web/` as a teaching reference for the customer integration pattern; not a Skytime product feature, no framework dependencies, no roadmap for expansion.
    ```

    Notes:
    - The text "Web UI / dashboard — example dashboard ships under `examples/.../server/web/` as a teaching reference for the customer integration pattern; not a Skytime product feature, no framework dependencies, no roadmap for expansion." is the load-bearing string. D-7.5-10 explicitly says "no tonal polish".
    - The bullet wrapper (`- **Web UI / dashboard** — ...`) MUST match the formatting of the other Out of Scope bullets so the file remains markdown-consistent.
    - Insert AFTER the existing "Production observability UI" bullet (so the order is: Hot-reload, Plugin/gRPC, Production observability UI, **Web UI / dashboard** (new), Multi-tenant hosted SaaS, CEL/string-based expressions, Starlark unit-test tier, Workflow versioning helpers).

    Do NOT modify any other section of PROJECT.md. Do NOT polish, paraphrase, or expand the carve-out wording — the user explicitly chose the verbatim text (D-7.5-10).
  </action>
  <verify>
    <automated>grep -q 'Web UI / dashboard.*example dashboard ships under' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Web UI / dashboard' .planning/PROJECT.md` exits 0
    - `grep -q 'example dashboard ships under' .planning/PROJECT.md` exits 0
    - `grep -q 'teaching reference for the customer integration pattern' .planning/PROJECT.md` exits 0
    - `grep -q 'not a Skytime product feature' .planning/PROJECT.md` exits 0
    - `grep -q 'no framework dependencies' .planning/PROJECT.md` exits 0
    - `grep -q 'no roadmap for expansion' .planning/PROJECT.md` exits 0
    - The full one-line verbatim string is intact: `grep -q 'example dashboard ships under .examples/.../server/web/. as a teaching reference for the customer integration pattern; not a Skytime product feature, no framework dependencies, no roadmap for expansion' .planning/PROJECT.md` exits 0
    - Pre-existing Out-of-Scope bullets preserved (none deleted):
      - `grep -q 'Hot-reload of .star files' .planning/PROJECT.md` exits 0
      - `grep -q 'Plugin / gRPC / out-of-process extensions' .planning/PROJECT.md` exits 0
      - `grep -q 'Production observability UI' .planning/PROJECT.md` exits 0
      - `grep -q 'Multi-tenant hosted SaaS' .planning/PROJECT.md` exits 0
      - `grep -q 'CEL or string-based expressions' .planning/PROJECT.md` exits 0
      - `grep -q 'Starlark unit-test tier' .planning/PROJECT.md` exits 0
      - `grep -q 'Workflow versioning helpers' .planning/PROJECT.md` exits 0
    - The "### Out of Scope" header is still on line ~68 (file structure preserved): `grep -q '^### Out of Scope$' .planning/PROJECT.md` exits 0
  </acceptance_criteria>
  <done>PROJECT.md Out of Scope section gains the verbatim "Web UI / dashboard" carve-out per D-7.5-10. No other content modified.</done>
</task>

</tasks>

<verification>
- README.md doc index links to temporal-auth.md with descriptive text — closes SC1 ("linked from the main README's documentation index")
- docs/for-extension-developers/README.md sub-index also references the new page
- PROJECT.md ## Out of Scope contains the verbatim carve-out — closes D-7.5-10
- No code files touched; pure markdown edits
</verification>

<success_criteria>
- All three doc-index surfaces (top-level README, for-extension-developers README, PROJECT.md) reference temporal-auth.md or contain the carve-out
- Verbatim wording from v1.43-DRAFT-PLAN.md is preserved in PROJECT.md
- No content deleted from any file; only new bullets appended
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-auth-documentation/07.5-06-SUMMARY.md`.
</output>
