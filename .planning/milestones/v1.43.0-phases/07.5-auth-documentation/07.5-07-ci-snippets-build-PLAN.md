---
phase: 07.5-auth-documentation
plan: 07
type: execute
wave: 3
depends_on: ["07.5-05"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
requirements_addressed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "CI runs go build, go vet, and go test on the docs/for-extension-developers/snippets module on every push"
    - "The snippets-build step lives inside the existing .github/workflows/ci.yml (not a new workflow file) per D-7.5-07"
    - "If markdown drifts from the .go files, CI fails — closing SC2 ('Each Go snippet compiles cleanly against go.temporal.io/sdk@v1.42.0 verified by a go vet or build test')"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Existing CI workflow with a new snippets-build step"
      contains: "snippets-build"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "docs/for-extension-developers/snippets/"
      via: "working-directory + go build/vet/test commands"
      pattern: "docs/for-extension-developers/snippets"
---

<objective>
Add a `snippets-build` step to the existing CI workflow that runs `go build ./...`, `go vet ./...`, and `go test ./...` inside the snippets module. This closes SC2 ("Each Go snippet compiles cleanly against go.temporal.io/sdk@v1.42.0") by adding the missing CI gate.

Purpose: Without this step, drift between markdown fences and .go files would land silently. With it, every push runs the drift-test plus the compile/vet/test gates.

Output: One new step in `.github/workflows/ci.yml`. No new workflow file (per D-7.5-07 — unified CI surface).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-auth-documentation/07.5-CONTEXT.md
@.github/workflows/ci.yml
@docs/for-extension-developers/snippets/go.mod
@docs/for-extension-developers/snippets/drift_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add snippets-build step to .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current workflow — 46 lines, single `ci` job with 8 existing steps; the new step lands at the end of the steps list)
    - .planning/phases/07.5-auth-documentation/07.5-CONTEXT.md (D-7.5-07: NEW step in the EXISTING ci.yml — not a new workflow file)
    - docs/for-extension-developers/snippets/go.mod (confirm the module path the working-directory points at)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`. The file currently ends with the `walkthrough smoke` step at line 42-45:

    ```yaml
          - name: walkthrough smoke (EX-04 — public GitHub API)
            env:
              EXTBIN: /tmp/extbin
            run: bash .github/workflows/scripts/walkthrough_smoke.sh
    ```

    Append the following new step AFTER the `walkthrough smoke` step, as the LAST step in the `ci` job's steps array. Use 6-space indentation (one level deep under `steps:`) to match the existing entries.

    The exact YAML block to append:

    ```yaml

          - name: snippets build + vet + test (auth-docs drift-test)
            working-directory: docs/for-extension-developers/snippets
            run: |
              go mod download
              go build ./...
              go vet ./...
              go test -count=1 ./...
    ```

    Notes:
    - Blank line BEFORE `- name:` (matches the spacing between the existing steps in the file).
    - `working-directory:` is the key construct — it scopes `go ...` commands to the snippets module, isolating its `go.mod` from the main module's.
    - `go mod download` ensures cloud-SDK deps (cloud.google.com/go/secretmanager, aws-sdk-go-v2, azure-sdk-for-go) are fetched into the GHA runner's module cache before the build step needs them.
    - `go build ./...` + `go vet ./...` confirm each snippet compiles cleanly against the pinned `go.temporal.io/sdk v1.42.0` — closes SC2.
    - `go test -count=1 ./...` runs `TestMarkdownSnippetDrift` (from `drift_test.go`), which catches markdown-vs-source drift across all four snippets — closes the drift-test infrastructure that SC2 / D-7.5-05 mandated.
    - `-count=1` defeats Go test caching so the suite always re-runs when the workflow re-fires (matches the existing `go test (race)` step's `-count=1` convention on line 34).

    Use `Edit` to make this single append. Do NOT modify any other existing step. Do NOT add a new job — D-7.5-07 explicitly says the step lives in the existing `ci` job (one workflow file to maintain).

    The Go version is already set up by the existing "Set up Go" step (line 21-25) and applies to all subsequent steps; no need to re-declare it inside the new step.

    After the edit, verify the file parses as valid YAML by running:

    ```bash
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
    ```

    (or `yq` / `actionlint` if installed). This catches indentation errors before the workflow lands on GitHub.
  </action>
  <verify>
    <automated>grep -q 'snippets build' .github/workflows/ci.yml &amp;&amp; grep -q 'docs/for-extension-developers/snippets' .github/workflows/ci.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'snippets build' .github/workflows/ci.yml` exits 0
    - `grep -q 'working-directory: docs/for-extension-developers/snippets' .github/workflows/ci.yml` exits 0
    - `grep -q 'go mod download' .github/workflows/ci.yml` exits 0
    - `grep -q 'go build ./...' .github/workflows/ci.yml` exits 0
    - `grep -q 'go vet ./...' .github/workflows/ci.yml` exits 0
    - `grep -q 'go test -count=1' .github/workflows/ci.yml` exits 0 (catches drift-test as well as any other test in the snippets module)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (YAML parses cleanly)
    - The file is still a SINGLE workflow with a SINGLE job (per D-7.5-07 — no new job added): `grep -c '^  ci:' .github/workflows/ci.yml` returns `1` AND there is no second job under `jobs:`
    - The existing steps are preserved:
      - `grep -q 'Set up Go' .github/workflows/ci.yml` exits 0
      - `grep -q 'go test (race)' .github/workflows/ci.yml` exits 0
      - `grep -q 'walkthrough smoke' .github/workflows/ci.yml` exits 0
      - `grep -q 'extbin test' .github/workflows/ci.yml` exits 0
    - The new step is the LAST step in the steps array (appears after walkthrough smoke):
      - `awk '/- name: walkthrough smoke/{found=1; next} found && /- name: snippets build/{print "ORDERED"; exit}' .github/workflows/ci.yml | grep -q ORDERED`
    - No new workflow file was created: `! test -f .github/workflows/snippets.yml && ! test -f .github/workflows/snippets-ci.yml` (D-7.5-07 enforces single-workflow surface)
  </acceptance_criteria>
  <done>CI workflow gains the snippets-build step inside the existing `ci` job; YAML parses cleanly; no new workflow file. SC2's compile/vet/test gate is in place.</done>
</task>

</tasks>

<verification>
- One new step added to .github/workflows/ci.yml inside the existing `ci` job
- Step runs `go mod download && go build ./... && go vet ./... && go test -count=1 ./...` inside `docs/for-extension-developers/snippets`
- File parses as valid YAML
- No new workflow file created (D-7.5-07)
- All four AUTH-01..04 snippets get compile + vet + drift-test coverage on every push, closing SC2
</verification>

<success_criteria>
- snippets-build step exists in `.github/workflows/ci.yml`
- The step runs go build + vet + test inside the snippets module via `working-directory`
- File remains a single workflow with a single `ci` job (D-7.5-07)
- Phase 07.5 deliverable matrix is complete: page exists + linked + snippets compile + drift-test guards everything + CI gates the whole thing
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-auth-documentation/07.5-07-SUMMARY.md`.
</output>
