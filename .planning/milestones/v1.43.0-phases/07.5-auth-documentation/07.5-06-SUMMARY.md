---
phase: 07.5-auth-documentation
plan: 06
subsystem: docs
tags: [docs, readme, project-md, doc-index, out-of-scope-carve-out, temporal-auth, locked-verbatim]

# Dependency graph
requires:
  - phase: 07.5-auth-documentation
    plan: 05
    provides: temporal-auth.md fully populated (all four AUTH sections — GCP/AWS/Azure/mTLS — ready to be linked); Wave-2 complete
provides:
  - Top-level README.md "Where to Go Next" section gains "Connect to production Temporal clusters" bullet linking docs/for-extension-developers/temporal-auth.md with descriptive text covering all four credential-rotation matrices (GCP WIF → Secret Manager, AWS IRSA → Secrets Manager, Azure Workload Identity → Key Vault, self-hosted mTLS+SIGHUP) — closes SC1 "linked from the main README's documentation index"
  - docs/for-extension-developers/README.md "Reference Material" sub-index gains "Temporal Authentication Patterns" bullet surfacing the cluster-vs-application credential distinction (good doc hygiene; readers landing in the directory discover the new page)
  - .planning/PROJECT.md "### Out of Scope" section gains verbatim "Web UI / dashboard" carve-out per D-7.5-10 (no tonal polish; locked text from CONTEXT.md D-7.5-10 and v1.43-DRAFT-PLAN.md line 141)
affects: [07.5-07-ci-snippets-build]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Doc-index wiring pattern: new auth-docs page is referenced from THREE surfaces (top-level README discoverability, sub-index discoverability, PROJECT.md scope alignment) — same pattern as Phase 7.3 dashboard doc and Phase 7.1 github-webhook walkthrough"
    - "Verbatim-locked carve-out preservation: D-7.5-10 explicitly says 'no tonal polish'; copy-paste from CONTEXT.md (which itself quoted v1.43-DRAFT-PLAN.md byte-for-byte) — drift-test does not cover PROJECT.md so the only enforcement is care"

key-files:
  created: []
  modified:
    - "README.md (added 'Connect to production Temporal clusters' bullet in 'Where to Go Next' section, between 'Build an extension' and 'Build a custom CLI')"
    - "docs/for-extension-developers/README.md (added 'Temporal Authentication Patterns' bullet at the end of 'Reference Material', before the 'Credential resolution' H2)"
    - ".planning/PROJECT.md (appended 'Web UI / dashboard' bullet to '### Out of Scope' section, after 'Production observability UI' and before 'Multi-tenant hosted SaaS')"

key-decisions:
  - "Three single-edit changes only — no code touched, no pkg/ files, no Go build. Plan was deliberately scoped to mechanical doc-index wiring + the locked carve-out so it could parallel-execute with 07.5-07 (CI work) without file overlap."
  - "PROJECT.md carve-out preserved byte-for-byte from the plan's <action> block, which itself reproduces CONTEXT.md D-7.5-10 and v1.43-DRAFT-PLAN.md line 141 verbatim (with the trailing period that matches the bullet's punctuation convention)."
  - "Sub-index bullet text emphasizes the cluster-vs-application credential distinction because that's the single most common conflation point for the audience — the same distinction the temporal-auth.md page intro spends its first H2 section on."
  - "Top-level README bullet text enumerates all four cloud-IAM matrices in the link text (GCP WIF, AWS IRSA, Azure Workload Identity, self-hosted mTLS+SIGHUP) so readers see the scope before clicking, and SC4 traceability is hinted at without depending on a follow-up read."
  - "Bullet placement matters: 'Connect to production Temporal clusters' lands AFTER 'Build an extension' and BEFORE 'Build a custom CLI' because it's a deployment / production-operations concern that mirrors the extension-authoring → CLI-wiring → production-deploy progression a reader is likely to follow."

requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

# Metrics
duration: ~3min
completed: 2026-05-13
---

# Phase 07.5 Plan 06: README + PROJECT.md Doc-Index Edits Summary

**Three small markdown edits land the auth-docs page in the project's doc-index surfaces and append the locked "Web UI / dashboard" Out-of-Scope carve-out to PROJECT.md per D-7.5-10. SC1 ("linked from the main README's documentation index") closes; D-7.5-10 closes.**

## Performance

- **Duration:** ~3 min wall clock
- **Started:** 2026-05-13T18:57:36Z
- **Completed:** 2026-05-13T18:59:42Z
- **Tasks:** 3
- **Files created:** 0
- **Files modified:** 3 (README.md, docs/for-extension-developers/README.md, .planning/PROJECT.md)
- **Lines added:** ~15 (7 + 7 + 1)
- **Lines deleted:** 0

## Accomplishments

### Task 1 — Top-level README.md doc-index

Added a six-line "Connect to production Temporal clusters" bullet to the "Where to Go Next" section, between the existing "Build an extension" bullet and "Build a custom CLI" bullet. The link text spells out all four cloud-IAM matrices (GCP WIF → Secret Manager, AWS IRSA → Secrets Manager, Azure Workload Identity → Key Vault, self-hosted mTLS with SIGHUP reload) so readers see the scope before clicking through. Commit `f499b7b`.

### Task 2 — for-extension-developers sub-index

Added a "Temporal Authentication Patterns" bullet to the "Reference Material" section of `docs/for-extension-developers/README.md`, immediately after the "Builtins Reference" bullet and before the "Credential resolution: `pkg/extension/credfile/`" H2. The bullet text deliberately surfaces the *cluster credentials* vs *application credentials* distinction — the single most common conflation point for the audience landing in this directory. Commit `3d8d968`.

### Task 3 — PROJECT.md "Web UI / dashboard" carve-out

Appended the verbatim D-7.5-10 carve-out to the `### Out of Scope` section of `.planning/PROJECT.md`, between the existing "Production observability UI" bullet and the "Multi-tenant hosted SaaS" bullet. The wording is locked — copied byte-for-byte from CONTEXT.md D-7.5-10 (which itself quotes v1.43-DRAFT-PLAN.md line 141). No tonal polish, no Oxford comma added, no rewriting for clarity. The Out-of-Scope section now lists eight carve-outs in this order: Hot-reload, Plugin/gRPC, Production observability UI, **Web UI / dashboard** (new), Multi-tenant hosted SaaS, CEL/string-based expressions, Starlark unit-test tier, Workflow versioning helpers. Commit `9a07055`.

## Verification

Plan-level checks all passed:

- `grep -q '\](docs/for-extension-developers/temporal-auth.md)' README.md` — top-level link present
- `grep -q 'temporal-auth' docs/for-extension-developers/README.md` — sub-index entry present
- `grep -q 'Web UI / dashboard' .planning/PROJECT.md` — carve-out body present
- `grep -qF 'example dashboard ships under' .planning/PROJECT.md` — carve-out body intact (fixed-string match)
- `grep -qF 'no roadmap for expansion' .planning/PROJECT.md` — closing phrase verbatim
- `! grep -q 'snippets-build' README.md` — no CI surface change (plan 07's responsibility, not this plan's)
- `! test -f .github/workflows/snippets.yml` — no separate snippets workflow file (plan 07 appends to existing ci.yml)

Per-task acceptance criteria (from the plan):

- **Task 1:** all 8 grep gates pass — link present, descriptive text covers the four matrices (GCP WIF + self-hosted mTLS keywords surface), pre-existing "Build an extension" and "Build a custom CLI" bullets preserved verbatim.
- **Task 2:** link target present, cluster-vs-application distinction surfaced in the bullet text (verified with newline-flattened grep — the line-based regex in the plan's acceptance criteria is overly literal given the bullet's natural wrapping; the substantive criterion is met), pre-existing bullets and the "Credential resolution" H2 preserved.
- **Task 3:** all six verbatim-string tokens present, full one-line verbatim string intact via `-F`, all seven pre-existing Out-of-Scope bullets preserved, `### Out of Scope` header still on line 68.

## Deviations from Plan

None — plan executed exactly as written. The only "deviation" worth flagging is interpretive: Task 2's acceptance criterion `grep -q 'cluster.*credentials.*application.*credentials'` is a single-line regex that fails on the inserted bullet because markdown wrapping puts "cluster credentials" and "application credentials" on separate lines. The *substantive* criterion ("distinction surfaced in the bullet text") is met — both terms appear in the inserted bullet, the distinction is explicit ("Distinguishes *cluster* credentials (Temporal cluster connection) from *application* credentials"), and a newline-flattened grep confirms the pattern. Not flagging as a Rule-1 fix because the plan acceptance criterion is a quality gate over the intent ("did you surface the distinction?"), not over the bullet's line structure.

Similarly, Task 3's `grep -q 'Hot-reload of .star files'` line-based regex would fail because the markdown source has `` Hot-reload of `.star` files `` (backticks around `.star`) — the unescaped `.` in the regex still requires the literal `.` adjacent to `s`, but backticks intervene. The fixed-string equivalent (`grep -qF 'Hot-reload of \`.star\` files'`) passes. The substantive criterion ("pre-existing bullet preserved") is met. No edit needed.

## Commits

| Task | Hash      | Message                                                                              |
| ---- | --------- | ------------------------------------------------------------------------------------ |
| 1    | `f499b7b` | docs(07.5-06): link temporal-auth.md from top-level README                           |
| 2    | `3d8d968` | docs(07.5-06): add temporal-auth.md to for-extension-developers sub-index            |
| 3    | `9a07055` | docs(07.5-06): append Web UI / dashboard carve-out to PROJECT.md Out of Scope        |

All three commits used `--no-verify` per parallel-execution coordination (sibling plan 07.5-07 was executing concurrently against `.github/workflows/ci.yml`; pre-commit hooks run once via the orchestrator after both agents complete).

## Authentication Gates

None — this plan is pure markdown edits, no external services touched.

## Parallel Sibling Coordination

Plan 07.5-07 executed in parallel against `.github/workflows/ci.yml` (CI snippets-build step). File ownership was disjoint by design — this plan modified README.md / docs/for-extension-developers/README.md / .planning/PROJECT.md only. Sibling's commits (`61bfec2 feat(07.5-07): add snippets-build CI step`, `4c7c101 docs(07.5-07): complete CI snippets-build plan`) landed cleanly alongside this plan's three commits with no conflicts.

## Self-Check: PASSED

Files verified to exist with the expected content:

- `README.md` — `grep -q '\](docs/for-extension-developers/temporal-auth.md)' README.md` → FOUND
- `docs/for-extension-developers/README.md` — `grep -q '\[Temporal Authentication Patterns\](temporal-auth.md)'` → FOUND
- `.planning/PROJECT.md` — `grep -qF 'example dashboard ships under \`examples/.../server/web/\` as a teaching reference for the customer integration pattern; not a Skytime product feature, no framework dependencies, no roadmap for expansion'` → FOUND

Commits verified to exist:

- `f499b7b` (Task 1) → FOUND in `git log`
- `3d8d968` (Task 2) → FOUND in `git log`
- `9a07055` (Task 3) → FOUND in `git log`

## Closes

- **AUTH-01..04 traceability** — these requirements were already marked Complete by plans 02..05; this plan completes the "linked from the main README's documentation index" wiring that closes SC1 for the same requirements.
- **D-7.5-10** — PROJECT.md "Web UI / dashboard" Out-of-Scope carve-out lands verbatim.

## What's Next

- Plan 07.5-07 (CI snippets-build) committed in parallel — Phase 7.5 plans 06 + 07 complete the seven-plan phase.
- Orchestrator runs the post-parallel hook validation pass on the combined diff.
- Phase 07.5 transition: requirements AUTH-01..04 move from Active to Validated in PROJECT.md once the orchestrator finalizes; ROADMAP.md Phase 7.5 checkbox flips.
