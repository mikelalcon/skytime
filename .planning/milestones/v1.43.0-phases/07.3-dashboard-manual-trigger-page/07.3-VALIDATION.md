---
phase: 7.3
slug: dashboard-manual-trigger-page
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-12
updated: 2026-05-13 (revision iter-1: checker fixes)
---

# Phase 7.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` + `github.com/stretchr/testify` v1.11.1 (already in `go.mod`) |
| **Config file** | None — convention-based; tests live alongside source as `*_test.go` |
| **Quick run command** | `go test ./pkg/cli/server/web/... -race -count=1` |
| **Full suite command** | `go test ./... -race -count=1` |
| **Estimated runtime** | ~30–60 seconds quick; ~3–5 minutes full (with `-race`) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./pkg/cli/server/web/... -race -count=1`
- **After every plan wave:** Run `go test ./... -race -count=1`
- **Before `/gsd:verify-work`:** Full suite must be green AND manual browser UAT against `temporal server start-dev` documented in `docs/walkthroughs/dashboard.md`; `SKYTIME_RUN_DASHBOARD_SMOKE=1 bash docs/walkthroughs/dashboard-smoke.sh` exits 0
- **Max feedback latency:** ~60 seconds per task commit

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.3-00-1 | 00 | 0 | Wave 0 — flowlaunch + deliveries test stubs + doc.go skeletons | wave-0 | `go test ./pkg/cli/server/web/flowlaunch/ ./pkg/cli/server/web/deliveries/ -race -count=1` (SKIP) | ❌ created | ⬜ pending |
| 07.3-00-2 | 00 | 0 | Wave 0 — events + handlers + templates test stubs | wave-0 | `go test ./pkg/cli/server/web/... -race -count=1` (SKIP) | ❌ created | ⬜ pending |
| 07.3-00-3 | 00 | 0 | Wave 0 — firewall test stubs + dashboard.md skeleton | wave-0 | `go test ./tests/ -run 'TestExecuteWorkflow_CallSiteCount\|TestBuildWorkflowInput_CallSiteCount\|TestNoExternalHTTPTemplateInWeb\|TestSourceAgnosticRenderer' -race -count=1` (SKIP) | ❌ created | ⬜ pending |
| 07.3-01-1 | 01 | 1 | UI-04 — flowlaunch.Execute + BuildWorkflowInput unit tests | unit | `go test ./pkg/cli/server/web/flowlaunch/ -race -count=1` (4 PASS) | depends on 07.3-00-1 | ⬜ pending |
| 07.3-01-2 | 01 | 1 | UI-04 — 3 call-site refactor (receiver, schedules, run) | integration | `go build ./... && go test ./pkg/extension/receiver/... ./pkg/extension/schedules/... ./pkg/cli/... -race -count=1` | depends on 07.3-01-1 | ⬜ pending |
| 07.3-01-3 | 01 | 1 | UI-04 — AST call-site firewall (2 ExecuteWorkflow + 3 BuildWorkflowInput) | firewall | `go test ./tests/ -run 'TestExecuteWorkflow_CallSiteCount\|TestBuildWorkflowInput_CallSiteCount' -race -count=1 -v` (2 PASS) | depends on 07.3-01-2 | ⬜ pending |
| 07.3-02-1 | 02 | 1 | UI-02 — deliveries package: types + ringbuffer + redact (10 tests after revision iter-1: +TestRedactHeaders_MalformedHeaderNameStripped) | unit | `go test ./pkg/cli/server/web/deliveries/ -race -count=1` (10+ PASS) | depends on 07.3-00-1 | ⬜ pending |
| 07.3-02-2 | 02 | 1 | UI-02 — receiver.Deps DeliveryBuffer + OnDelivery wiring | integration | `go test ./pkg/extension/receiver/... -race -count=1` (Phase 7.1 regression intact) | depends on 07.3-02-1 | ⬜ pending |
| 07.3-02-3 | 02 | 1 | UI-02 — source-agnostic firewall (banned provider header names) | firewall | `go test ./tests/ -run TestSourceAgnosticRenderer -race -count=1 -v` (PASS + non-vacuous) | depends on 07.3-00-3 | ⬜ pending |
| 07.3-03-1 | 03 | 1 | UI-01 — events.Broadcaster (snapshot-under-lock + drop-oldest + shutdown) | unit | `go test ./pkg/cli/server/web/events/ -run 'TestBroadcaster.*' -race -count=1 -v` (5+ PASS) | depends on 07.3-00-2 | ⬜ pending |
| 07.3-03-2 | 03 | 1 | UI-01 — events.Poller (delta computation + replay heuristic + dedupe) | unit | `go test ./pkg/cli/server/web/events/ -run 'TestPoller.*' -race -count=1 -v` (5 PASS) | depends on 07.3-03-1 | ⬜ pending |
| 07.3-04-1 | 04 | 2 | UI-01+UI-02+UI-03+UI-04 — dashboard.html + templates + handlers + mount; 12 stubs flipped (revision iter-1: +TestTrigger_SameOriginCheck_JSONRequiresOrigin for M3) | unit + integration | `go test ./pkg/cli/server/web/ -race -count=1 -v` (12+ PASS) | depends on 07.3-01, 02, 03 | ⬜ pending |
| 07.3-04-2 | 04 | 2 | UI-01+UI-02+UI-03+UI-04 — pkg/cli/server.go wiring + 2 CLI flags (with negative-value validation, m3) + stdlib firewall (with M2 non-vacuity defense) + B3 shutdown reorder (webResult.Shutdown BEFORE srv.Shutdown) | integration + firewall | `go test ./pkg/cli/... ./tests/ -race -count=1` AND `go run ./cmd/skytime server --help \| grep -E 'replay-history-threshold\|temporal-web-ui' \| wc -l` returns 2 AND `grep -n 'webResult.Shutdown\|srv.Shutdown' pkg/cli/server.go` shows webResult first | depends on 07.3-04-1 | ⬜ pending |
| 07.3-04-3 | 04 | 2 | UI-01+UI-02+UI-03 — browser UAT smoke (checkpoint:human-verify) | manual | Browser load + manual trigger + SSE connection survives 60s | depends on 07.3-04-2 | ⬜ pending |
| 07.3-05-1 | 05 | 3 | All — dashboard.md walkthrough + headings firewall | doc + firewall | `go test ./tests/ -run TestWalkthroughDashboardHeadings -race -count=1 -v` (PASS) | depends on 07.3-04 | ⬜ pending |
| 07.3-05-2 | 05 | 3 | All — TestServerCmd_DrainOnSIGTERM extended to STRICTLY assert SSE `event: shutdown` frame before EOF (m4 + B3 — revision iter-1: no permissive __closed__ clause) | integration | `go test ./pkg/cli/ -run TestServerCmd_DrainOnSIGTERM -race -count=1 -v` (PASS) | depends on 07.3-04-2 | ⬜ pending |
| 07.3-05-3 | 05 | 3 | All — dashboard-smoke.sh script (10 end-to-end checks) | smoke | `SKYTIME_RUN_DASHBOARD_SMOKE=1 bash docs/walkthroughs/dashboard-smoke.sh` exits 0 | depends on 07.3-05-1 | ⬜ pending |
| 07.3-05-4 | 05 | 3 | All — final browser UAT (checkpoint:human-verify) | manual | Walk through `docs/walkthroughs/dashboard.md` end-to-end + all 5 firewalls confirmed non-vacuous | depends on 07.3-05-3 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 creates failing-but-compiling test scaffolds for every requirement before any implementation tasks run. Each test imports the to-be-created packages and uses `t.Skip("Wave 0 stub")` so the suite compiles green but execution is gated.

- [ ] `pkg/cli/server/web/handlers_test.go` — integration paths for UI-01 / UI-02 / UI-03 (`TestDashboard_RendersWorkflowList`, `TestSSE_InitialSnapshot`, `TestSSE_WriteTimeoutDisabled`, `TestTrigger_Success`, `TestTrigger_UnknownFlow`, `TestTrigger_BadJSON_DoesNotEchoInput`, `TestTrigger_SameOriginCheck`, `TestTrigger_SameOriginCheck_JSONRequiresOrigin` — M3 defense added during plan revision iter-1, `TestDashboard_FlowDropdown`)
- [ ] `pkg/cli/server/web/templates_test.go` — golden-file rendering check for the dashboard template (`TestTemplate_DashboardGolden`, `TestTemplate_AnchorIDsMatch`, `TestTemplate_EmptyStateHintRow`)
- [ ] `pkg/cli/server/web/events/broadcaster_test.go` — Subscribe/Publish race, drop-oldest, shutdown channel close (`TestBroadcaster_SnapshotOnSubscribe`, `TestBroadcaster_Publish_FanOut`, `TestBroadcaster_DropOldest`, `TestBroadcaster_Shutdown_ClosesSubscriberChannels`, `TestBroadcaster_UnsubscribeIsIdempotent`)
- [ ] `pkg/cli/server/web/events/poller_test.go` — fake `client.Client` driving delta computation (`TestPoller_EmitsWorkflowStarted`, `TestPoller_EmitsStatusChanged`, `TestPoller_EmitsReplayed_HistoryLengthJump`, `TestPoller_FetchError_DoesNotCrash`, `TestPoller_MergeOpenClosed_Dedupes`)
- [ ] `pkg/cli/server/web/deliveries/buffer_test.go` — ring rollover at 100 entries, concurrent Append/Snapshot race-free (`TestRingBuffer_AppendUnderCap`, `TestRingBuffer_LastHundred`, `TestRingBuffer_ConcurrentAppendSnapshot`, `TestRingBuffer_LenAccurate`)
- [ ] `pkg/cli/server/web/deliveries/redact_test.go` — header redaction matrix covering all 5 substrings + NAME sanitization defense (`TestRedactHeaders_Authorization`, `TestRedactHeaders_SecretTokenKeySignature_CaseInsensitive`, `TestRedactHeaders_NonSensitivePassthrough`, `TestRedactHeaders_MalformedHeaderNameStripped` — M4 defense added during plan revision iter-1, `TestTruncateValue_80Chars`, `TestTruncateValue_Passthrough`)
- [ ] `pkg/cli/server/web/flowlaunch/launch_test.go` — `executeFlow` error mapping (`TestExecute_StatusOK`, `TestExecute_DuplicateError`, `TestExecute_DispatchFailed`, `TestBuildWorkflowInput_ShapesInput`)
- [ ] `tests/firewall_execute_workflow_test.go` — AST call-site count tests (`TestExecuteWorkflow_CallSiteCount`, `TestBuildWorkflowInput_CallSiteCount`)
- [ ] `tests/firewall_web_stdlib_test.go` — bans non-stdlib HTTP/template imports under `pkg/cli/server/web/` (`TestNoExternalHTTPTemplateInWeb`)
- [ ] `tests/firewall_source_agnostic_test.go` — bans source-specific header names outside provider packages (`TestSourceAgnosticRenderer`)
- [ ] `docs/walkthroughs/dashboard.md` — manual UAT skeleton with 8+ H2 headings including `## Browser UAT` and `## Security Note`

**Framework install:** None — Go stdlib `testing` and `github.com/stretchr/testify` already in `go.mod`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dashboard visual feel (drop shadows, hover/focus, status-dot colors) | UI-01 / UI-02 / UI-03 | Subjective "slightly more polished" assertion (D-7.3-32); no automated visual-regression tool in scope | Open `http://localhost:8080/` after Plan 04 lands; eyeball status dots (blue/green/red/orange), hover states on table rows, panel drop shadows |
| Browser EventSource auto-reconnect after server restart | UI-01 | Requires real browser + server-restart timing; integration test only validates server-side close | Open dashboard; `pkill skytime`; restart `skytime server`; verify connection indicator returns to `● Connected` and new `snapshot` arrives without manual reload |
| `:target` flash animation when clicking a delivery's mapped-workflow link | UI-02 | CSS `@keyframes` + `:target` selector — no headless verification | Click a delivery row's mapped-workflow ID anchor; observe the matching workflow row scrolls into view and flashes for ~0.5s |
| Replay-detection heuristic (`HistoryLength` jump threshold) with real Temporal | UI-01 | Threshold (default 50) needs dev-test calibration against `temporal server start-dev` + restart-mid-workflow scenarios (Research Open Question 2 — locked default 50, CLI flag `--replay-history-threshold` for tuning) | Run a long workflow; restart worker mid-flight; verify dashboard shows `● Running (replayed N times)` once HistoryLength delta exceeds threshold |
| Connection-state indicator transitions | UI-01 | Requires real browser EventSource readyState changes | Open dashboard; verify `● Connected`; `pkill skytime`; verify `○ Reconnecting…`; restart; verify `● Connected` returns |

---

## Plan Revision Notes (iter-1)

Phase 7.3 plans were revised on 2026-05-13 (iter-1) to address Phase 7.3 checker feedback. Key changes affecting the validation contract:

- **New unit test added (Plan 02):** `TestRedactHeaders_MalformedHeaderNameStripped` — covers M4 defense (RFC 7230 token-charset regex on header names). Counted in the 10-test target for `go test ./pkg/cli/server/web/deliveries/`.
- **New unit test added (Plan 04):** `TestTrigger_SameOriginCheck_JSONRequiresOrigin` — covers M3 defense (JSON-strict same-origin). Counted in the 12-test target for `go test ./pkg/cli/server/web/`.
- **AST firewall test extended (Plan 01):** `TestBuildWorkflowInput_CallSiteCount` now walks BOTH `*ast.SelectorExpr` AND `*ast.Ident` to catch the in-package internal call inside `flowlaunch.Execute` (B1). Still exits 0 — counts 3 sites, not 2 or vacuous.
- **Drain test strictened (Plan 05):** `TestServerCmd_DrainOnSIGTERM` no longer accepts `__closed__` as a pass — it MUST observe an explicit `event: shutdown` frame BEFORE EOF (m4 + B3). Same command, same pass criterion (`--- PASS:`).
- **Shutdown order constraint (Plan 04):** an additional acceptance criterion is added — `grep -n 'webResult.Shutdown\|srv.Shutdown' pkg/cli/server.go` must show `webResult.Shutdown` line number STRICTLY LESS THAN `srv.Shutdown(` line (B3 fix).
- **CLI flag validation (Plan 04, m3):** `--replay-history-threshold=-1` must return a non-zero exit code with stderr containing `must be >= 0`.

These changes are NON-BREAKING to the existing per-task verify commands; they only ADD assertions / new test functions.

---

## Validation Sign-Off

- [x] All tasks have `<verify>` automated command or Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all 11 listed scaffolds + `docs/walkthroughs/dashboard.md`
- [x] No watch-mode flags (`-count=1` only)
- [x] Feedback latency < ~60s per task commit
- [x] `nyquist_compliant: true` set in frontmatter (planner populated per-task map)

**Approval:** locked 2026-05-13 — per-task map finalized; ready for `/gsd:execute-phase`
