---
phase: 07.3-dashboard-manual-trigger-page
plan: 00
subsystem: testing
tags: [wave-0, scaffolds, sse, ring-buffer, html-template, firewall, ast-walk, walkthrough]

# Dependency graph
requires:
  - phase: 07.1-http-webhook-receiver-github-source
    provides: receiver.Deps + handler.go (the c.ExecuteWorkflow call at line 225 that Plan 01 extracts)
  - phase: 07.2-cron-triggers-via-temporal-schedules
    provides: pkg/extension/schedules reconciler (the third BuildWorkflowInput call site Plan 01 wires)
provides:
  - 11 Wave 0 SKIP test scaffolds compiling green under `go test ./pkg/cli/server/web/... ./tests/`
  - pkg/cli/server/web/ subtree (4 packages: web, events, deliveries, flowlaunch) with doc.go-only contracts declaring stdlib-only + source-agnostic + single-seam constraints
  - docs/walkthroughs/dashboard.md skeleton with 10 H2 headings (Plan 05 fills in)
  - 38 SKIP markers naming exactly which plan + task in Waves 1–3 flips each one to active
affects: [07.3-01, 07.3-02, 07.3-03, 07.3-04, 07.3-05]

# Tech tracking
tech-stack:
  added: []  # NO production code or new deps; pure test scaffolding
  patterns:
    - "Wave 0 test scaffolds: package <pkgname> + import \"testing\" + t.Skip(\"Wave 0 stub: Plan NN. <assertion description>\") — pattern lets the whole suite stay green-with-skips during multi-wave execution"
    - "Doc-only package skeletons: each new package has a single doc.go with no exported symbols yet; doc comment names the constraint a firewall test will eventually enforce"

key-files:
  created:
    - "pkg/cli/server/web/doc.go (stdlib-only constraint declaration)"
    - "pkg/cli/server/web/events/doc.go"
    - "pkg/cli/server/web/deliveries/doc.go (source-agnostic constraint)"
    - "pkg/cli/server/web/flowlaunch/doc.go (single ExecuteWorkflow seam)"
    - "pkg/cli/server/web/flowlaunch/launch_test.go (4 SKIPs)"
    - "pkg/cli/server/web/deliveries/buffer_test.go (4 SKIPs)"
    - "pkg/cli/server/web/deliveries/redact_test.go (5 SKIPs)"
    - "pkg/cli/server/web/events/broadcaster_test.go (5 SKIPs)"
    - "pkg/cli/server/web/events/poller_test.go (5 SKIPs)"
    - "pkg/cli/server/web/handlers_test.go (8 SKIPs)"
    - "pkg/cli/server/web/templates_test.go (3 SKIPs)"
    - "tests/firewall_execute_workflow_test.go (2 SKIPs)"
    - "tests/firewall_web_stdlib_test.go (1 SKIP)"
    - "tests/firewall_source_agnostic_test.go (1 SKIP)"
    - "docs/walkthroughs/dashboard.md (10 H2 headings, Wave 0 skeleton)"
    - ".planning/phases/07.3-dashboard-manual-trigger-page/deferred-items.md (pre-existing dev-server grep-gate failure tracked)"
  modified: []

key-decisions:
  - "All 38 SKIP messages embed their target plan number and one-sentence assertion description, so Wave 1+ tasks can grep their next flip target instead of cross-referencing PLAN.md"
  - "Pre-existing TestNoDevServerLiteralRemains failure (introduced by 07.2.1 commit afead57 in docs/walkthroughs/cron-schedules-smoke.sh:192) is OUT-OF-SCOPE for Wave 0 per execute-plan SCOPE BOUNDARY rule — logged to deferred-items.md for separate fix"

patterns-established:
  - "Per-test SKIP message convention: each Wave 0 stub follows the form `t.Skip(\"Wave 0 stub: Plan NN. <verb> <observable> when <condition>\")` so Wave 1+ executors get a precise behavior contract without consulting PLAN.md"
  - "Package skeleton doc.go: every new sibling package gets ONE doc.go with only the package-level constraint comment; firewall tests under tests/ enforce the constraint"

requirements-completed: [UI-01, UI-02, UI-03, UI-04]  # Wave 0 contract surface satisfied; full requirement validation gated on Plans 01-05

# Metrics
duration: 4 min
completed: 2026-05-12
---

# Phase 07.3 Plan 00: Wave 0 Test Scaffolds Summary

**11 SKIP test scaffolds + 4 doc.go package skeletons + dashboard walkthrough skeleton — 38 total SKIPs that Wave 1+ tasks flip to active behavior assertions one-by-one**

## Performance

- **Duration:** 4 min (264 seconds)
- **Started:** 2026-05-12T17:09:58Z
- **Completed:** 2026-05-12T17:14:22Z
- **Tasks:** 3
- **Files created:** 16 (4 doc.go + 11 test files + 1 walkthrough skeleton)

## Accomplishments

- **Eleven SKIP test files** compile green under `go test ./pkg/cli/server/web/... ./tests/` for the Wave 0 outputs (the four firewall stubs land in `tests/` and pass alongside existing tests; the pre-existing `TestNoDevServerLiteralRemains` failure in `docs/walkthroughs/cron-schedules-smoke.sh:192` is out-of-scope, see Deferred).
- **Four `doc.go` package skeletons** under `pkg/cli/server/web/` declaring the stdlib-only / source-agnostic / single-seam contracts that Plans 01–04's firewall tests will eventually enforce.
- **Dashboard walkthrough skeleton** (`docs/walkthroughs/dashboard.md`) with 10 H2 headings — including the two load-bearing ones (`## Browser UAT`, `## Security Note`) that Plan 05's `TestWalkthroughDashboardHeadings` firewall will assert.
- **Pre-existing failure quarantined**: `TestNoDevServerLiteralRemains` already failing on `d41ffe5` (pre-Plan-00 HEAD) due to stale "dev-server" comment in `cron-schedules-smoke.sh:192` (introduced in Phase 7.2.1 `afead57`) — logged to `deferred-items.md` per SCOPE BOUNDARY rule, NOT auto-fixed.

## Task Commits

Each task was committed atomically:

1. **Task 1: doc.go + flowlaunch + deliveries Wave 0 stubs** — `2f8cc98` (test)
2. **Task 2: events + handlers + templates Wave 0 stubs** — `d41ffe5` (test)
3. **Task 3: firewall stubs + dashboard walkthrough skeleton** — `68d3edf` (test)

**Plan metadata commit:** (created with this SUMMARY.md + STATE.md update)

## Files Created/Modified

### Package doc.go skeletons (4)

- `pkg/cli/server/web/doc.go` — declares the stdlib-only constraint for the entire subtree and lists the three subpackages
- `pkg/cli/server/web/events/doc.go` — events package: broadcaster + workflow-list poller (D-7.3-02..08)
- `pkg/cli/server/web/deliveries/doc.go` — deliveries package: ring buffer + redaction (D-7.3-17..22); calls out the source-agnostic constraint
- `pkg/cli/server/web/flowlaunch/doc.go` — single ExecuteWorkflow seam (D-7.3-28..31); lists the 2 ExecuteWorkflow + 3 BuildWorkflowInput call sites the firewall asserts

### Test scaffolds (11 files, 38 SKIPs total)

- `pkg/cli/server/web/flowlaunch/launch_test.go` — 4 SKIPs (StatusOK, DuplicateError, DispatchFailed, BuildWorkflowInput_ShapesInput)
- `pkg/cli/server/web/deliveries/buffer_test.go` — 4 SKIPs (AppendUnderCap, LastHundred, ConcurrentAppendSnapshot, LenAccurate)
- `pkg/cli/server/web/deliveries/redact_test.go` — 5 SKIPs (Authorization, SecretTokenKeySignature_CaseInsensitive, NonSensitivePassthrough, TruncateValue_80Chars, TruncateValue_Passthrough)
- `pkg/cli/server/web/events/broadcaster_test.go` — 5 SKIPs (SnapshotOnSubscribe, Publish_FanOut, DropOldest, Shutdown_ClosesSubscriberChannels, UnsubscribeIsIdempotent)
- `pkg/cli/server/web/events/poller_test.go` — 5 SKIPs (EmitsWorkflowStarted, EmitsStatusChanged, EmitsReplayed_HistoryLengthJump, FetchError_DoesNotCrash, MergeOpenClosed_Dedupes)
- `pkg/cli/server/web/handlers_test.go` — 8 SKIPs (Dashboard_RendersWorkflowList, SSE_InitialSnapshot, SSE_WriteTimeoutDisabled, Trigger_Success, Trigger_UnknownFlow, Trigger_BadJSON_DoesNotEchoInput, Trigger_SameOriginCheck, Dashboard_FlowDropdown)
- `pkg/cli/server/web/templates_test.go` — 3 SKIPs (DashboardGolden, AnchorIDsMatch, EmptyStateHintRow)
- `tests/firewall_execute_workflow_test.go` — 2 SKIPs (ExecuteWorkflow_CallSiteCount, BuildWorkflowInput_CallSiteCount)
- `tests/firewall_web_stdlib_test.go` — 1 SKIP (NoExternalHTTPTemplateInWeb)
- `tests/firewall_source_agnostic_test.go` — 1 SKIP (SourceAgnosticRenderer)

### Walkthrough skeleton

- `docs/walkthroughs/dashboard.md` — H1 + 10 H2 headings (What you get / Prerequisites / Step 1–5 / Browser UAT / Security Note / Troubleshooting) + 10 `TODO(plan-05)` markers + U+25CF (●) and U+2713 (✓) Unicode characters Plan 05's headings firewall requires

### Process artifacts

- `.planning/phases/07.3-dashboard-manual-trigger-page/deferred-items.md` — out-of-scope items log

## Dashboard.md heading list

The 10 H2 headings in `docs/walkthroughs/dashboard.md` (in document order):

1. `## What you get`
2. `## Prerequisites`
3. `## Step 1 — Start the dev cluster`
4. `## Step 2 — Start \`skytime server\``
5. `## Step 3 — Open the dashboard`
6. `## Step 4 — Fire a webhook`
7. `## Step 5 — Manually trigger a flow`
8. `## Browser UAT`  ← Plan 05's headings firewall asserts presence
9. `## Security Note`  ← Plan 05's headings firewall asserts presence
10. `## Troubleshooting`

## Wave 1+ activation map

Each Wave 0 SKIP maps to a specific Plan + task that will flip it to active:

| Wave 0 test (count) | Flips in plan | Notes |
|---|---|---|
| `flowlaunch/launch_test.go` (4) | **Plan 01 Task 01-1** | Execute / BuildWorkflowInput unit tests |
| `tests/firewall_execute_workflow_test.go` (2) | **Plan 01 Task 01-3** | AST call-site count firewall (2 ExecuteWorkflow + 3 BuildWorkflowInput) |
| `deliveries/buffer_test.go` (4) | **Plan 02 Task 02-1** | RingBuffer last-100 + race |
| `deliveries/redact_test.go` (5) | **Plan 02 Task 02-1** | Header redaction matrix (+ M4 malformed-name defense lands here too per VALIDATION.md iter-1) |
| `tests/firewall_source_agnostic_test.go` (1) | **Plan 02 Task 02-3** | Banned provider header names |
| `events/broadcaster_test.go` (5) | **Plan 03 Task 03-1** | Snapshot-under-lock + drop-oldest + Shutdown |
| `events/poller_test.go` (5) | **Plan 03 Task 03-2** | Delta + replay heuristic + dedupe |
| `handlers_test.go` (8) | **Plan 04 Task 04-1** | Dashboard/SSE/manual-trigger handlers (+ M3 JSON-strict same-origin lands here per VALIDATION.md iter-1) |
| `templates_test.go` (3) | **Plan 04 Task 04-1** | Golden file + anchor IDs + empty-state hint |
| `tests/firewall_web_stdlib_test.go` (1) | **Plan 04 Task 04-2** | Stdlib-only firewall for pkg/cli/server/web/ subtree |
| `docs/walkthroughs/dashboard.md` (skeleton) | **Plan 05 Task 05-1** | Full walkthrough content + headings firewall |

**Total flips: 38 across 5 downstream plans.** Every Wave 1+ task ends with "flip Wave 0 SKIP to active assertion" so progress is visible task-by-task.

## Decisions Made

1. **All 38 SKIP messages embed plan number + assertion description** — Wave 1+ executors get a precise behavior contract without consulting PLAN.md. Pattern: `t.Skip("Wave 0 stub: Plan NN. <Asserts X when Y>")`.
2. **Pre-existing `TestNoDevServerLiteralRemains` failure quarantined, not auto-fixed** — `docs/walkthroughs/cron-schedules-smoke.sh:192` contains a stale `# dev-server's visibility store...` comment introduced by Phase 7.2.1 (`afead57`). Auto-fixing it would expand Plan 00's scope and mask the actual Phase 7.2.1 hygiene gap. Logged to `deferred-items.md` per execute-plan SCOPE BOUNDARY rule.

## Deviations from Plan

None — plan executed exactly as written. All 3 tasks produced exactly the files, function counts, and literal-string checks the plan's `<acceptance_criteria>` blocks specified. Zero auto-fix attempts triggered.

## Issues Encountered

**Pre-existing test failure surfaced during overall verification (out-of-scope):**

- During Plan 00's final verification (`go test ./pkg/cli/server/web/... ./tests/ -race -count=1`), `TestNoDevServerLiteralRemains` failed on `docs/walkthroughs/cron-schedules-smoke.sh:192` (stale "dev-server" literal forbidden by D-07-22 grep gate).
- Verified pre-existing via `git stash && go test` on parent commit `d41ffe5` — failure reproduced cleanly.
- Per SCOPE BOUNDARY rule: NOT caused by Plan 00 changes (no edits to that file in any of the 3 tasks). Logged to `.planning/phases/07.3-dashboard-manual-trigger-page/deferred-items.md` with reproduction steps + one-line fix recommendation for a follow-up.

Plan-00 outputs themselves (`go test ./pkg/cli/server/web/... -race -count=1` and `go test ./tests/ -run <four firewall stubs>`) all pass cleanly.

## Known Stubs

All 38 stubs are INTENTIONAL and form the contract surface for the Wave 0 → Wave 3 incremental implementation pattern. Each stub:

- Lives only in `*_test.go` files (zero production code stubs)
- Bodies are `t.Skip("Wave 0 stub: Plan NN. <description>")` with no further logic
- Maps to a specific Wave 1+ task that flips it to active (see "Wave 1+ activation map" above)

No stubs prevent Plan 00's goal — establishing the contract surface for downstream plans — from being achieved. The pattern IS the goal.

## Self-Check: PASSED

**Files exist:**

- ✓ `pkg/cli/server/web/doc.go`, `pkg/cli/server/web/events/doc.go`, `pkg/cli/server/web/deliveries/doc.go`, `pkg/cli/server/web/flowlaunch/doc.go`
- ✓ All 11 test files at expected paths
- ✓ `docs/walkthroughs/dashboard.md` with 10 H2 + 10 TODO(plan-05) markers
- ✓ `.planning/phases/07.3-dashboard-manual-trigger-page/deferred-items.md`

**Commits exist:**

- ✓ `2f8cc98` — Task 1 (doc.go + flowlaunch + deliveries stubs)
- ✓ `d41ffe5` — Task 2 (events + handlers + templates stubs)
- ✓ `68d3edf` — Task 3 (firewall stubs + dashboard skeleton)

**Verification commands pass:**

- ✓ `go test ./pkg/cli/server/web/... -race -count=1` — all 4 subpackages green (all SKIPs)
- ✓ `go test ./tests/ -race -count=1 -run 'TestExecuteWorkflow_CallSiteCount|TestBuildWorkflowInput_CallSiteCount|TestNoExternalHTTPTemplateInWeb|TestSourceAgnosticRenderer'` — all 4 SKIPs pass
- ✓ `go build ./pkg/cli/server/web/...` — clean build of all four doc.go-only packages
- ✓ 38 `t.Skip("Wave 0 stub` markers total across the 11 test files

## Next Phase Readiness

- **Plan 07.3-01 (executeFlow consolidation)** can start: `flowlaunch/launch_test.go` stubs ready to flip; `tests/firewall_execute_workflow_test.go` stubs ready for AST walker implementation
- **Plan 07.3-02 (deliveries)** can start: `deliveries/buffer_test.go` + `deliveries/redact_test.go` stubs + source-agnostic firewall stub ready
- **Plan 07.3-03 (events)** can start: `events/broadcaster_test.go` + `events/poller_test.go` stubs ready
- **Plan 07.3-04 (dashboard handlers/templates/wiring)** has stubs ready in `handlers_test.go`, `templates_test.go`, and `tests/firewall_web_stdlib_test.go`
- **Plan 07.3-05 (walkthrough + smoke)** has `docs/walkthroughs/dashboard.md` skeleton ready

**Blocker for full Phase 7.3 verification:** the pre-existing `TestNoDevServerLiteralRemains` failure (out-of-scope for this plan) will continue to fail `go test ./tests/` runs until the one-line fix in `deferred-items.md` lands. Plans 01–05 can run their narrow `-run` patterns against their own test files unaffected.

---
*Phase: 07.3-dashboard-manual-trigger-page*
*Completed: 2026-05-12*
