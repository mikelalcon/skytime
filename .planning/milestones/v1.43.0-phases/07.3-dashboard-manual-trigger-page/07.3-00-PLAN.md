---
phase: 07.3-dashboard-manual-trigger-page
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/cli/server/web/doc.go
  - pkg/cli/server/web/handlers_test.go
  - pkg/cli/server/web/templates_test.go
  - pkg/cli/server/web/events/doc.go
  - pkg/cli/server/web/events/broadcaster_test.go
  - pkg/cli/server/web/events/poller_test.go
  - pkg/cli/server/web/deliveries/doc.go
  - pkg/cli/server/web/deliveries/buffer_test.go
  - pkg/cli/server/web/deliveries/redact_test.go
  - pkg/cli/server/web/flowlaunch/doc.go
  - pkg/cli/server/web/flowlaunch/launch_test.go
  - tests/firewall_execute_workflow_test.go
  - tests/firewall_web_stdlib_test.go
  - tests/firewall_source_agnostic_test.go
  - docs/walkthroughs/dashboard.md
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Eleven test files exist under pkg/cli/server/web/ and tests/ with t.Skip(\"Wave 0 stub\") markers"
    - "Every test file declares its production target package via blank import or sentinel comment so Wave 1 implementation has a compile-failure cue if a package or symbol is missing"
    - "Running `go test ./pkg/cli/server/web/... ./tests/... -race -count=1` exits 0 with every new test reporting SKIP"
    - "docs/walkthroughs/dashboard.md skeleton exists with `## Browser UAT` heading and TODO markers; not yet wired into smoke script"
  artifacts:
    - path: "pkg/cli/server/web/doc.go"
      provides: "Package doc for pkg/cli/server/web declaring stdlib-only constraint"
    - path: "pkg/cli/server/web/handlers_test.go"
      provides: "Test stubs for GET / + SSE /api/events + POST /api/trigger"
    - path: "pkg/cli/server/web/templates_test.go"
      provides: "Golden-file rendering check stub"
    - path: "pkg/cli/server/web/events/broadcaster_test.go"
      provides: "Subscribe/Publish/drop-oldest/Shutdown stubs"
    - path: "pkg/cli/server/web/events/poller_test.go"
      provides: "Poller delta computation + replay heuristic stubs"
    - path: "pkg/cli/server/web/deliveries/buffer_test.go"
      provides: "RingBuffer last-100 + concurrent access stubs"
    - path: "pkg/cli/server/web/deliveries/redact_test.go"
      provides: "Header redaction matrix stub"
    - path: "pkg/cli/server/web/flowlaunch/launch_test.go"
      provides: "Execute error mapping stubs"
    - path: "tests/firewall_execute_workflow_test.go"
      provides: "AST call-site count firewall stub"
    - path: "tests/firewall_web_stdlib_test.go"
      provides: "Stdlib-only firewall stub for pkg/cli/server/web/ subtree"
    - path: "tests/firewall_source_agnostic_test.go"
      provides: "Source-agnostic-header firewall stub"
    - path: "docs/walkthroughs/dashboard.md"
      provides: "Browser UAT skeleton"
  key_links:
    - from: "pkg/cli/server/web/handlers_test.go"
      to: "pkg/cli/server/web/handlers.go (Wave 2)"
      via: "blank import or sentinel comment"
      pattern: "Wave 0 stub"
    - from: "tests/firewall_execute_workflow_test.go"
      to: "pkg/cli/server/web/flowlaunch/launch.go (Wave 1)"
      via: "AST walk over module root"
      pattern: "allowed map"
---

<objective>
Create eleven Wave 0 test scaffolds + a dashboard walkthrough skeleton so the entire Wave 1/2/3 implementation has a target shape and runs against `go test ./...` from the very first task. NO production code is created in this plan — only test files whose body is `t.Skip("Wave 0 stub: production code lands in Wave N")`.

Purpose: Establish the contract surface for all downstream plans. Every Wave 1+ task ends with "flip Wave 0 skip to active assertion" so progress is visible task-by-task.
Output: 11 test files compiling green-with-skips + docs/walkthroughs/dashboard.md skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-dashboard-manual-trigger-page/07.3-CONTEXT.md
@.planning/phases/07.3-dashboard-manual-trigger-page/07.3-RESEARCH.md
@.planning/phases/07.3-dashboard-manual-trigger-page/07.3-VALIDATION.md

<interfaces>
<!-- Module path and key existing types/exports the stubs reference -->

Module path: `github.com/mikelalcon/skytime`

Existing exports the Wave 1 implementation will use (do NOT touch in Wave 0; just reference in comments):
- `pkg/dag.WorkflowInput{FlowName, ContentHash, InitState}` — the shape passed to ExecuteWorkflow
- `pkg/interpreter.FlowRegistry` with method `ContentHashFor(flowName string) (string, bool)` and `Names() []string`
- `pkg/extension/receiver.Deps` struct (Plan 02 extends with `DeliveryBuffer` + `OnDelivery`)
- `pkg/worker.Worker` with `Registry() *interpreter.FlowRegistry`
- `go.temporal.io/sdk/client.Client` interface (`ListOpenWorkflow`, `ListClosedWorkflow`, `ExecuteWorkflow`)
- `go.temporal.io/api/enums/v1.WorkflowExecutionStatus`
- `go.temporal.io/api/serviceerror.WorkflowExecutionAlreadyStarted`

AST firewall test precedent:
- `tests/firewall_cli_test.go` (cobra/charm-log allow list)
- `tests/receiver_hmac_only_test.go` (HMAC cross-package firewall)
- `findModuleRootCLI(t)` helper in `tests/firewall_cli_test.go`
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create package skeleton (doc.go files) + flowlaunch launch_test.go + ring buffer + redact tests</name>
  <files>
    pkg/cli/server/web/doc.go,
    pkg/cli/server/web/events/doc.go,
    pkg/cli/server/web/deliveries/doc.go,
    pkg/cli/server/web/flowlaunch/doc.go,
    pkg/cli/server/web/flowlaunch/launch_test.go,
    pkg/cli/server/web/deliveries/buffer_test.go,
    pkg/cli/server/web/deliveries/redact_test.go
  </files>
  <read_first>
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-CONTEXT.md (locked decisions)
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-RESEARCH.md "Validation Architecture" + "Wave 0 Gaps" sections
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-VALIDATION.md (the 11 scaffolds list)
    - pkg/cli/log_filter.go (slog.Handler decorator precedent for doc.go style)
    - pkg/extension/receiver/receiver.go (Deps struct shape — Plan 02 will extend)
    - tests/firewall_cli_test.go (sentinel test pattern + `findModuleRootCLI`)
    - go.mod (module path = github.com/mikelalcon/skytime)
  </read_first>
  <action>
    Create four `doc.go` files (one per new package). Each is a single Go file:

    1. `pkg/cli/server/web/doc.go` — `package web` with a package comment that states:
       - "Package web hosts the stdlib-only dashboard for `skytime server` (Phase 7.3, UI-01..UI-04)."
       - "Constraint: NO third-party HTTP, template, or SSE libraries reachable from any subpackage. The firewall test `tests/firewall_web_stdlib_test.go` enforces this."
       - "Subpackages: events/ (broadcaster + poller), deliveries/ (ring buffer + redaction), flowlaunch/ (single ExecuteWorkflow seam per UI-04)."

    2. `pkg/cli/server/web/events/doc.go` — `package events` with comment:
       - "Package events provides the broadcaster + workflow-list poller that fan out SSE events to dashboard browsers (Phase 7.3 D-7.3-02..D-7.3-08)."
       - "Stdlib-only: net/http, sync, time, encoding/json, log/slog, container/list (if needed). NO charm-log, NO cobra."

    3. `pkg/cli/server/web/deliveries/doc.go` — `package deliveries` with comment:
       - "Package deliveries hosts the in-memory ring buffer + source-agnostic header redaction for webhook deliveries surfaced on the dashboard (UI-02; D-7.3-17..D-7.3-22)."
       - "Source-agnostic constraint: NO references to provider-specific header names (X-GitHub-Event, Stripe-Signature, etc.); the firewall test `tests/firewall_source_agnostic_test.go` enforces this."

    4. `pkg/cli/server/web/flowlaunch/doc.go` — `package flowlaunch` with comment:
       - "Package flowlaunch is the single production seam for `c.ExecuteWorkflow(\"SkytimeWorkflow\", dag.WorkflowInput{...})` (UI-04; D-7.3-28..D-7.3-31)."
       - "Call sites after Wave 1: webhook ingress (pkg/extension/receiver/handler.go), manual trigger POST (pkg/cli/server/web/handlers.go), cron reconcile (pkg/extension/schedules/schedules.go — via BuildWorkflowInput only)."
       - "The firewall test `tests/firewall_execute_workflow_test.go` asserts exactly 2 production `c.ExecuteWorkflow` call sites and exactly 3 `BuildWorkflowInput` call sites."

    Then create three test files:

    5. `pkg/cli/server/web/flowlaunch/launch_test.go` — `package flowlaunch` (white-box). Three functions:
       - `func TestExecute_StatusOK(t *testing.T) { t.Skip("Wave 0 stub: Plan 01 fills in. Asserts Execute returns StatusOK + workflowID when c.ExecuteWorkflow succeeds.") }`
       - `func TestExecute_DuplicateError(t *testing.T) { t.Skip("Wave 0 stub: Plan 01. Asserts Execute maps *serviceerror.WorkflowExecutionAlreadyStarted to StatusDuplicate via errors.As.") }`
       - `func TestExecute_DispatchFailed(t *testing.T) { t.Skip("Wave 0 stub: Plan 01. Asserts Execute wraps non-AlreadyStarted errors as StatusDispatchFailed with %w.") }`
       - `func TestBuildWorkflowInput_ShapesInput(t *testing.T) { t.Skip("Wave 0 stub: Plan 01. Asserts BuildWorkflowInput returns dag.WorkflowInput{FlowName, ContentHash, InitState} verbatim.") }`
       Add the import block with `"testing"` only (skip stubs don't need anything else).

    6. `pkg/cli/server/web/deliveries/buffer_test.go` — `package deliveries`. Four skip stubs:
       - `func TestRingBuffer_AppendUnderCap(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts Append + Snapshot(n) returns newest-first slice when count < cap.") }`
       - `func TestRingBuffer_LastHundred(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts 150 appends with cap=100 retain the last 100 in newest-first order.") }`
       - `func TestRingBuffer_ConcurrentAppendSnapshot(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts concurrent Append + Snapshot is race-free under -race.") }`
       - `func TestRingBuffer_LenAccurate(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts Len() == min(appends, cap).") }`

    7. `pkg/cli/server/web/deliveries/redact_test.go` — `package deliveries`. Five skip stubs (one per redaction substring + case-insensitivity + truncate):
       - `func TestRedactHeaders_Authorization(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts http.Header{\"Authorization\": [...]} renders to {\"Authorization\": \"<redacted>\"}.") }`
       - `func TestRedactHeaders_SecretTokenKeySignature_CaseInsensitive(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts X-Stripe-Signature, X-API-Key, X-CSRF-Token, X-Hub-Signature-256, X-Some-Secret all redact regardless of casing.") }`
       - `func TestRedactHeaders_NonSensitivePassthrough(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts Content-Type and User-Agent are NOT redacted.") }`
       - `func TestTruncateValue_80Chars(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts values >80 chars get '\\u2026' suffix.") }`
       - `func TestTruncateValue_Passthrough(t *testing.T) { t.Skip("Wave 0 stub: Plan 02. Asserts values <=80 chars are returned unchanged.") }`

    For ALL doc.go files: leave the file empty after the package comment (no exported symbols yet — those land in Wave 1).

    For ALL test files: include only `package <pkgname>` + `import "testing"` + the stub functions. Do NOT add unused imports.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && go test ./pkg/cli/server/web/... -race -count=1 -run '.*' -v 2>&1 | tee /tmp/wave0-task1.log | grep -E '(=== RUN|--- SKIP|--- PASS|--- FAIL|ok|FAIL)' | head -40 ; echo "---" ; grep -c 'SKIP: ' /tmp/wave0-task1.log</automated>
  </verify>
  <done>
    All four doc.go files exist; flowlaunch/launch_test.go has 4 SKIP stubs; deliveries/buffer_test.go has 4 SKIP stubs; deliveries/redact_test.go has 5 SKIP stubs. `go test ./pkg/cli/server/web/... -race -count=1` exits 0 reporting at least 13 SKIP results from these three test files. No imports beyond `testing` in the test files.
  </done>
  <acceptance_criteria>
    - File `pkg/cli/server/web/doc.go` exists and contains the literal string `Package web hosts the stdlib-only dashboard`
    - File `pkg/cli/server/web/events/doc.go` exists and contains `Package events provides the broadcaster`
    - File `pkg/cli/server/web/deliveries/doc.go` exists and contains `Source-agnostic constraint`
    - File `pkg/cli/server/web/flowlaunch/doc.go` exists and contains `single production seam`
    - `grep -c '^func Test' pkg/cli/server/web/flowlaunch/launch_test.go` returns `4`
    - `grep -c '^func Test' pkg/cli/server/web/deliveries/buffer_test.go` returns `4`
    - `grep -c '^func Test' pkg/cli/server/web/deliveries/redact_test.go` returns `5`
    - `grep -c 't.Skip("Wave 0 stub' pkg/cli/server/web/flowlaunch/launch_test.go pkg/cli/server/web/deliveries/buffer_test.go pkg/cli/server/web/deliveries/redact_test.go` totals 13
    - Command `go test ./pkg/cli/server/web/flowlaunch/ ./pkg/cli/server/web/deliveries/ -race -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create events/ + handlers + templates test scaffolds</name>
  <files>
    pkg/cli/server/web/events/broadcaster_test.go,
    pkg/cli/server/web/events/poller_test.go,
    pkg/cli/server/web/handlers_test.go,
    pkg/cli/server/web/templates_test.go
  </files>
  <read_first>
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-RESEARCH.md "Pattern 2: Broadcaster Fan-out" + "Pattern 3: Workflow-List Poller" + "Pitfall 1: Snapshot-on-Subscribe Race" + "Pitfall 8: SSE Long-Lived Connection vs WriteTimeout"
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-CONTEXT.md D-7.3-01..D-7.3-08 (SSE) + D-7.3-09..D-7.3-16 (workflow list shape) + D-7.3-23..D-7.3-27 (manual trigger)
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-VALIDATION.md "Wave 0 Requirements" checklist
    - pkg/cli/log_filter.go (slog stylistic reference)
  </read_first>
  <action>
    Create four test files. Each task function uses `t.Skip("Wave 0 stub: ...")` with a body comment naming the plan + the assertion it will make in Wave 1+.

    1. `pkg/cli/server/web/events/broadcaster_test.go` — `package events`. Five stubs:
       - `func TestBroadcaster_SnapshotOnSubscribe(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts new subscribers receive a snapshot captured under the same lock that registers their channel — no race window per Research Pitfall 1.") }`
       - `func TestBroadcaster_Publish_FanOut(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts Publish(ev) delivers to all current subscribers.") }`
       - `func TestBroadcaster_DropOldest(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts when a subscriber's 16-deep buffer is full, the OLDEST queued event is discarded so the newest event still arrives.") }`
       - `func TestBroadcaster_Shutdown_ClosesSubscriberChannels(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts Shutdown() closes all subscriber channels and the fan-out goroutine exits.") }`
       - `func TestBroadcaster_UnsubscribeIsIdempotent(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts calling the returned unsubscribe func twice does not panic and does not double-close.") }`

    2. `pkg/cli/server/web/events/poller_test.go` — `package events`. Five stubs:
       - `func TestPoller_EmitsWorkflowStarted(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts a workflow seen for the first time produces a Publish(Event{Name: \"workflow_started\"}).") }`
       - `func TestPoller_EmitsStatusChanged(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts status transition (RUNNING -> COMPLETED) produces a Publish(Event{Name: \"workflow_status_changed\"}).") }`
       - `func TestPoller_EmitsReplayed_HistoryLengthJump(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts HistoryLength delta exceeding the configured threshold (default 50) produces a Publish(Event{Name: \"workflow_replayed\"}).") }`
       - `func TestPoller_FetchError_DoesNotCrash(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts a transient ListWorkflow error logs at Warn level and the next tick retries.") }`
       - `func TestPoller_MergeOpenClosed_Dedupes(t *testing.T) { t.Skip("Wave 0 stub: Plan 03. Asserts a workflow present in both Open and Closed result sets is deduped by WorkflowID with Closed winning.") }`

    3. `pkg/cli/server/web/handlers_test.go` — `package web`. Seven stubs:
       - `func TestDashboard_RendersWorkflowList(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. GET / returns HTML with table containing one row per workflow from FlowRegistry/poller snapshot, status badge classes from D-7.3-11, id=\"wf-<workflowID>\" on each row.") }`
       - `func TestSSE_InitialSnapshot(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. GET /api/events returns Content-Type: text/event-stream and emits a single 'event: snapshot' frame with JSON payload {workflows:[], deliveries:[]} within ~50ms.") }`
       - `func TestSSE_WriteTimeoutDisabled(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. Asserts handler calls http.ResponseController(w).SetWriteDeadline(time.Time{}) so the http.Server.WriteTimeout: 30s defeat (Research Pitfall 8) holds.") }`
       - `func TestTrigger_Success(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. POST /api/trigger with {\"flow\": \"<registered>\", \"input\": {}} returns 200 + {\"workflow_id\": \"manual/<flow>/<32hex>\"}.") }`
       - `func TestTrigger_UnknownFlow(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. POST /api/trigger with unregistered flow returns 400 + {\"error\": \"flow not registered: <name>\"}.") }`
       - `func TestTrigger_BadJSON_DoesNotEchoInput(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. POST /api/trigger with malformed body returns 400 + {\"error\": \"input is not valid JSON\"} and the response body does NOT contain the original input bytes (per Research Pitfall 10).") }`
       - `func TestTrigger_SameOriginCheck(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. POST /api/trigger with Origin header pointing at a different host returns 403 (defense-in-depth per Research Open Q 3).") }`
       - `func TestDashboard_FlowDropdown(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. GET / response contains <option value=\"<flow>\"> for every name in worker.Registry().Names(), in alphabetical order.") }`

    4. `pkg/cli/server/web/templates_test.go` — `package web`. Three stubs:
       - `func TestTemplate_DashboardGolden(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. Renders the dashboard template against a fixed snapshot and compares to a golden file under pkg/cli/server/web/testdata/dashboard.html.golden.") }`
       - `func TestTemplate_AnchorIDsMatch(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. For each delivery's mapped_workflow_id, asserts the rendered HTML contains both id=\"wf-<id>\" on the workflow row AND href=\"#wf-<id>\" on the delivery row's anchor link (D-7.3-21).") }`
       - `func TestTemplate_EmptyStateHintRow(t *testing.T) { t.Skip("Wave 0 stub: Plan 04. Renders with zero workflows; asserts table contains the literal hint string 'No workflows yet \\u2014 fire a webhook, click Run below, or wait for cron.' (D-7.3-13).") }`

    All test files: `package <name>` + `import \"testing\"` only. Do NOT add unused imports.
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && go test ./pkg/cli/server/web/... -race -count=1 -run '.*' 2>&1 | tail -20 ; echo "---" ; grep -rh 't.Skip("Wave 0 stub' pkg/cli/server/web/events/ pkg/cli/server/web/handlers_test.go pkg/cli/server/web/templates_test.go | wc -l</automated>
  </verify>
  <done>
    Four test files exist with required SKIP stubs (5 + 5 + 8 + 3 = 21 stubs). `go test ./pkg/cli/server/web/...` exits 0 with all stubs reporting SKIP. Test packages compile (`package web`, `package events`).
  </done>
  <acceptance_criteria>
    - `grep -c '^func Test' pkg/cli/server/web/events/broadcaster_test.go` returns `5`
    - `grep -c '^func Test' pkg/cli/server/web/events/poller_test.go` returns `5`
    - `grep -c '^func Test' pkg/cli/server/web/handlers_test.go` returns `8`
    - `grep -c '^func Test' pkg/cli/server/web/templates_test.go` returns `3`
    - File `pkg/cli/server/web/handlers_test.go` contains the literal string `TestSSE_WriteTimeoutDisabled` and the comment text `SetWriteDeadline(time.Time{})`
    - File `pkg/cli/server/web/templates_test.go` contains the empty-state hint literal `No workflows yet`
    - File `pkg/cli/server/web/events/broadcaster_test.go` contains `TestBroadcaster_DropOldest`
    - `go test ./pkg/cli/server/web/... -race -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create firewall test scaffolds + dashboard walkthrough skeleton</name>
  <files>
    tests/firewall_execute_workflow_test.go,
    tests/firewall_web_stdlib_test.go,
    tests/firewall_source_agnostic_test.go,
    docs/walkthroughs/dashboard.md
  </files>
  <read_first>
    - tests/firewall_cli_test.go (lines 1-100 — pattern for AST-walking firewall + findModuleRootCLI helper)
    - tests/receiver_hmac_only_test.go (cross-package AST firewall precedent)
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-RESEARCH.md "Pattern 6: executeFlow Extraction" + "Call-Site AST Test (D-7.3-31)" section
    - .planning/phases/07.3-dashboard-manual-trigger-page/07.3-CONTEXT.md D-7.3-17 (source-agnostic) + D-7.3-28..31 (executeFlow consolidation)
    - .planning/phases/07.1-http-webhook-receiver-github-source/07.1-CONTEXT.md (Phase 7.1 walkthrough doc shape precedent — github-webhook.md)
    - docs/walkthroughs/cron-schedules.md (existing walkthrough shape; mimic the section headings)
  </read_first>
  <action>
    Three firewall test files + one markdown walkthrough skeleton.

    1. `tests/firewall_execute_workflow_test.go` — `package firewall_test`. Two skip stubs:
       - `func TestExecuteWorkflow_CallSiteCount(t *testing.T) { t.Skip("Wave 0 stub: Plan 01 fills in. Asserts c.ExecuteWorkflow appears at exactly 2 production call sites: pkg/cli/server/web/flowlaunch/launch.go and pkg/cli/run.go. Mirrors tests/receiver_hmac_only_test.go AST walk pattern.") }`
       - `func TestBuildWorkflowInput_CallSiteCount(t *testing.T) { t.Skip("Wave 0 stub: Plan 01 fills in. Asserts flowlaunch.BuildWorkflowInput appears at exactly 3 production call sites: pkg/cli/server/web/flowlaunch/launch.go (self), pkg/cli/run.go, pkg/extension/schedules/schedules.go.") }`

       Add only `"testing"` import. Body of each test is just `t.Skip(...)` plus a `// TODO(plan-01)` comment.

    2. `tests/firewall_web_stdlib_test.go` — `package firewall_test`. Single stub:
       - `func TestNoExternalHTTPTemplateInWeb(t *testing.T) { t.Skip("Wave 0 stub: Plan 04 fills in. AST-walks every .go file under pkg/cli/server/web/ and asserts the import paths intersect ONLY with a stdlib + go.temporal.io/sdk + go.temporal.io/api allow-list. No gorilla, r3labs/sse, tmaxmax/go-sse, htmx, safehtml, etc.") }`

    3. `tests/firewall_source_agnostic_test.go` — `package firewall_test`. Single stub:
       - `func TestSourceAgnosticRenderer(t *testing.T) { t.Skip("Wave 0 stub: Plan 02 fills in. Asserts source-specific header NAMES (X-GitHub-Event, X-GitHub-Delivery, Stripe-Signature, X-Hub-Signature-256) do NOT appear as string literals inside pkg/cli/server/web/ subtree. They may appear inside pkg/extension/builtin/triggers/github/, pkg/extension/builtin/http/, and existing receiver source factory packages.") }`

    4. `docs/walkthroughs/dashboard.md` — full skeleton (will be filled in Plan 05). Required headings (use `## ` for level 2):
       ```
       # Dashboard (Phase 7.3) — Walkthrough

       > **Status:** Skeleton (Wave 0). Manual UAT script lands in Plan 05.

       ## What you get

       TODO(plan-05): Three sections — live workflow list, recent webhook deliveries, manual trigger form. Live updates via Server-Sent Events.

       ## Prerequisites

       TODO(plan-05): `temporal server start-dev`, the `gh` CLI for `gh webhook forward`, a checkout of `examples/http-github-webhook/`.

       ## Step 1 — Start the dev cluster

       TODO(plan-05).

       ## Step 2 — Start `skytime server`

       TODO(plan-05).

       ## Step 3 — Open the dashboard

       TODO(plan-05): http://localhost:8080/

       ## Step 4 — Fire a webhook

       TODO(plan-05): `gh webhook forward` to localhost:8080.

       ## Step 5 — Manually trigger a flow

       TODO(plan-05): Pick a flow from the dropdown, paste `{}`, click Run.

       ## Browser UAT

       TODO(plan-05): Step-by-step manual verification covering:
       - Workflow row appears within one SSE event
       - Delivery row appears with redacted headers (Authorization, X-Hub-Signature-256, etc. all show `<redacted>`)
       - Delivery's mapped-workflow link scrolls + flashes the corresponding workflow row
       - Manual trigger inline feedback shows `✓ Started workflow ...`
       - Connection indicator shows `● Connected` and survives a server restart

       ## Security Note

       TODO(plan-05): The dashboard ships with **no auth and no CSRF token** (per Research Open Question 3). It is intended for localhost / internal-network use only. Production deploys MUST place the dashboard behind an authenticating reverse proxy (nginx + basic-auth, oauth2-proxy, Tailscale Serve). Auth integration is Phase 7.5+.

       ## Troubleshooting

       TODO(plan-05).
       ```

       Insert literal Unicode characters where indicated (`✓` = `✓`, `●` = `●`).
  </action>
  <verify>
    <automated>cd /Users/mikel/dev/ai/temporero && go test ./tests/ -race -count=1 -run 'TestExecuteWorkflow_CallSiteCount|TestBuildWorkflowInput_CallSiteCount|TestNoExternalHTTPTemplateInWeb|TestSourceAgnosticRenderer' 2>&1 | tail -10 ; echo "---" ; grep -c '^## ' docs/walkthroughs/dashboard.md ; grep -c 'TODO(plan-05)' docs/walkthroughs/dashboard.md</automated>
  </verify>
  <done>
    Three firewall test files exist with 4 SKIP stubs total. `docs/walkthroughs/dashboard.md` has 8 H2 headings including `## Browser UAT` and `## Security Note`, plus at least 7 TODO(plan-05) markers. All tests pass with SKIP.
  </done>
  <acceptance_criteria>
    - `grep -c '^func Test' tests/firewall_execute_workflow_test.go` returns `2`
    - `grep -c '^func Test' tests/firewall_web_stdlib_test.go` returns `1`
    - `grep -c '^func Test' tests/firewall_source_agnostic_test.go` returns `1`
    - File `tests/firewall_execute_workflow_test.go` contains the literal string `TestExecuteWorkflow_CallSiteCount` AND `TestBuildWorkflowInput_CallSiteCount`
    - File `tests/firewall_source_agnostic_test.go` contains the literal string `X-GitHub-Event` (inside the skip message — listing what is banned)
    - File `docs/walkthroughs/dashboard.md` contains the literal heading `## Browser UAT`
    - File `docs/walkthroughs/dashboard.md` contains the literal heading `## Security Note`
    - File `docs/walkthroughs/dashboard.md` contains the literal `●` (U+25CF) AND `✓` (U+2713) characters
    - `grep -c '^## ' docs/walkthroughs/dashboard.md` returns at least `8`
    - `go test ./tests/ -race -count=1 -run 'TestExecuteWorkflow_CallSiteCount|TestBuildWorkflowInput_CallSiteCount|TestNoExternalHTTPTemplateInWeb|TestSourceAgnosticRenderer'` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the full test surface to confirm Wave 0 leaves the build green:

```bash
go test ./pkg/cli/server/web/... ./tests/ -race -count=1
```

Expected: exits 0, all new tests report SKIP, no compilation errors, no NEW failing tests (existing tests must continue to pass).

Also verify package-level compilation succeeds even though packages have no exported symbols yet:

```bash
go build ./pkg/cli/server/web/... 2>&1
```

Expected: clean build (doc.go files compile by themselves).
</verification>

<success_criteria>
- 11 test files exist under the paths in `files_modified`
- Every test file's tests start with `t.Skip("Wave 0 stub:` (search returns 26+ matches across all files: 4 + 4 + 5 + 5 + 5 + 8 + 3 + 2 + 1 + 1 = 38 SKIPs)
- `docs/walkthroughs/dashboard.md` exists with the 8 required H2 headings
- `go test ./pkg/cli/server/web/... ./tests/ -race -count=1` exits 0
- No production code (.go files outside `*_test.go`) created except the four `doc.go` package skeleton files
- Each downstream plan (01–05) has at least one Wave 0 stub it will flip to active
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-dashboard-manual-trigger-page/07.3-00-SUMMARY.md` describing:
- Which 11 test files landed and the per-file stub count
- The 4 doc.go package skeletons created
- The dashboard.md heading list
- Wave 1+ activation map: for each Wave 0 test, which plan + task flips it to active
</output>
