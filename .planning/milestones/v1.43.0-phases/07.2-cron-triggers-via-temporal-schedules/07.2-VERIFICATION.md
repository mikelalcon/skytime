---
phase: 07.2-cron-triggers-via-temporal-schedules
verified: 2026-05-11T19:00:00Z
status: passed
score: 5/5 success criteria verified
---

# Phase 07.2: Cron Triggers via Temporal Schedules — Verification Report

**Phase Goal:** Cron triggers backed by Temporal Schedules (durable, server-side) — not in-process polling. Ship a cron source factory; at `skytime server` startup, reconcile Temporal Schedule resources to match the parsed registry (create / update / delete orphans). A safety knob governs whether to actually apply or dry-run.

**Verified:** 2026-05-11
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to ROADMAP Success Criteria)

| # | Truth (Success Criterion) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `.star` file declaring `trigger(flow="...", source=core.cron(...))` parses successfully and `skytime server --cron-reconcile` creates a Temporal Schedule (verifiable via `temporal schedule list`) | VERIFIED | `pkg/extension/builtin/core/cron.go` (cronFactory + cronSource) + `pkg/extension/schedules/schedules.go::ReconcileCronSchedules` (Create branch via `sc.Create`) + `pkg/cli/server.go:212-237` wires `--cron-reconcile` between `worker.Start()` and listener bind. UAT 2026-05-11: `--cron-reconcile` boot created exactly one `skytime/weekly_digest/<hash>` Schedule on a live cluster, confirmed via `temporal schedule list` |
| 2 | Removing the `trigger(...)` and restarting with `--cron-reconcile` deletes the Schedule from the cluster (orphan delete) | VERIFIED | `pkg/extension/schedules/schedules.go::diff` produces `Deletes` bucket for IDs in `actual` but not `desired`; `ReconcileCronSchedules` calls `sc.GetHandle(ctx, id).Delete(ctx)` per delete. UAT 2026-05-11: orphan-delete demo with commented-out `trigger(...)` block produced `cron-reconcile applied: 0 creates, 0 updates, 1 deletes`; `temporal schedule list` returned 0 Skytime entries |
| 3 | A "dry-run" variant reports the plan (creates/updates/deletes) without applying | VERIFIED | `pkg/cli/cron_plan.go` (newCronPlanCommand) registers `skytime cron-plan --rootdir=...` as a separate subcommand. RunE calls `schedules.ReconcileCronSchedules(..., apply=false, ...)` (line 87). Output rendered terraform-plan-style with `+/~/-` markers + plain-English "when" annotation via `writeCronPlanPretty` and `describeCron` in `cron_describe.go`. UAT 2026-05-11: dry-run printed plan, exited 0, zero cluster mutations |
| 4 | A 5-field POSIX cron is accepted at parse time; 6-field / malformed strings produce position-aware parse errors before any Schedule API call | VERIFIED | `pkg/extension/builtin/core/cron.go::fiveFieldParser = cronv3.NewParser(cronv3.Minute | cronv3.Hour | cronv3.Dom | cronv3.Month | cronv3.Dow)` (NO cronv3.Descriptor flag → macros rejected). All errors return `*dag.ParseError{Pos: callerPosition(thread), ...}`. Tests `TestCron_RejectSixField`, `TestCron_RejectMacros`, `TestCron_RejectMalformed`, `TestCron_RejectUnknownTimezone`, `TestCron_RejectInvalidOverlap`, `TestCron_RejectNegativeCatchup`, `TestCron_RejectMalformedCatchup` all pass |
| 5 | With a server up and a cron trigger fired by Temporal, the corresponding workflow appears in `client.ListWorkflow` at the scheduled time | VERIFIED (human UAT) | UAT 2026-05-11: per-minute cron fires produced `SkytimeWorkflow` executions visible via `temporal workflow list`, full lifecycle in stdout (`skytime workflow start` → `cron-fired workflow scheduled_time=...` → `event=flow_start` → 4× `event=step_complete` → `event=flow_complete ok_count=4 err_count=0`). Automated coverage via gated `docs/walkthroughs/cron-schedules-smoke.sh` (~80s wall clock, env-var `SKYTIME_RUN_CRON_SMOKE=1`) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `pkg/extension/builtin/core/core.go` | core extension shape + skycore.New() + Initialize module | VERIFIED | 54 lines; exports `New() extension.Extension`; Initialize returns `*starlarkstruct.Module{Name:"core", Members:{"cron": starlark.NewBuiltin("core.cron", cronFactory)}}`; `_ "time/tzdata"` blank import present |
| `pkg/extension/builtin/core/cron.go` | *cronSource type + cronFactory validator + init() factory registration | VERIFIED | 289 lines; fiveFieldParser (NO Descriptor flag); allowedOverlaps map; TriggerSource interface satisfied; init() calls `extension.RegisterTriggerSourceFactory(cronKind, ...)`; `CronSource = cronSource` exported alias; Schedule/Timezone/Overlap/CatchupWindow accessors all present |
| `pkg/extension/schedules/id.go` | ComposeScheduleID + IsSkytimeManaged + posHash | VERIFIED | 50 lines; `SkytimeScheduleIDPrefix = "skytime/"`; ComposeScheduleID returns `skytime/{flow}/{8-char-base64url-sha256}`; IsSkytimeManaged checks prefix |
| `pkg/extension/schedules/schedules.go` | ReconcileCronSchedules + diff + scheduleOptionsFor + updateSchedule | VERIFIED | 396 lines; ReconcileCronSchedules handles List → Diff → Create/Update/Delete; isAlreadyExists detection via serviceerror.AlreadyExists; errors.Join for failure aggregation; preserves operator State during Update; mapOverlapToEnum maps 4 strings to enumspb 1:1 |
| `pkg/extension/schedules/fake_schedule_client.go` | Test fake satisfying client.ScheduleClient | VERIFIED | 188 lines; records Create/Update/Delete calls; configurable List |
| `pkg/cli/server.go` | --cron-reconcile flag + reconcile call wired between worker.Start() and net.Listen() | VERIFIED | Line 212-237 calls `schedules.ReconcileCronSchedules(ctx, sc, w.Triggers().All(), w.Registry(), taskQueue, true, logger)` after worker.Start (line 196) and before net.Listen (line 259); emits `cron_reconcile_complete` testDrainHook stage; line 117 reassigns `cfg.sdkLogger = logger` so SDK logs surface (UAT fix 5270f86) |
| `pkg/cli/cron_plan.go` | skytime cron-plan subcommand for dry-run | VERIFIED | 233 lines; newCronPlanCommand returns cobra.Command; RunE parses rootdir, connects client, calls ReconcileCronSchedules with apply=false; renders terraform-plan style via writeCronPlanPretty |
| `pkg/cli/cron_describe.go` | Plain-English "when" line for cron expressions | VERIFIED | 173 lines (UAT fix 0465225+242dbdf) |
| `pkg/cli/registries.go` | loadRegistries helper for parsing rootdir without Temporal client | VERIFIED | 41 lines; wraps worker.LoadRegistries; test seam for cron-plan |
| `pkg/cli/options.go::WithScheduleClientFactory` | Test seam option | VERIFIED | Lines 75-90 |
| `pkg/cli/root.go::newCronPlanCommand` | cron-plan registered as subcommand | VERIFIED | Line 64: `root.AddCommand(newCronPlanCommand(cfg))` |
| `cmd/skytime/main.go` | skycore.New() registered | VERIFIED | Line 22 import + line 37 `cli.WithExtensions(skyhttp.New(), skycore.New())` |
| `examples/http-github-webhook/cmd/extbin/main.go` | skycore.New() registered | VERIFIED | Line 31 import + line 46 `skycore.New()` in WithExtensions list |
| `pkg/interpreter/cron_input.go` | extractScheduledTime parser (Pitfall 2 recovery) | VERIFIED | 56 lines (UAT fix 5270f86); regex captures RFC3339 suffix from WorkflowID; called from `pkg/interpreter/workflow.go:51` inside NewWorkflow before flow lookup |
| `examples/http-github-webhook/weekly_digest.star` | First cron-triggered example flow | VERIFIED | Declares `core.cron(schedule="* * * * *", timezone="America/New_York", overlap="skip")` + identity map/idempotency_key lambdas |
| `docs/walkthroughs/cron-schedules.md` | Long-form walkthrough | VERIFIED | 431 lines; documents prerequisites → cron-plan dry-run → server --cron-reconcile apply → temporal schedule list verify → orphan-delete demo |
| `docs/walkthroughs/cron-schedules-smoke.sh` | Executable end-to-end smoke | VERIFIED | 182 lines, executable (0o755); boots dev-temporal, applies cron trigger, waits for fire, asserts via temporal workflow list |
| `.github/workflows/scripts/walkthrough_smoke.sh` | CI gating | VERIFIED | Gated behind `SKYTIME_RUN_CRON_SMOKE=1` env var (line 126); ~80s wall clock |
| `pkg/parser/testdata/triggers/cron_req_attr_typo.star` | Fixture for cron req typo | VERIFIED | Present |
| `pkg/parser/trigger_test.go::TestTrigger_CronReqAttrTypo` | Parser walker test for cron sources | VERIFIED | Line 200; references `testdata/triggers/cron_req_attr_typo.star` |
| `tests/firewall_credential_redaction_test.go` | targetDirs extended | VERIFIED | Lines 58-59 add `pkg/extension/builtin/core` + `pkg/extension/schedules` |
| `go.mod` | `github.com/robfig/cron/v3 v3.0.1` direct require | VERIFIED | 1 match in go.mod + 2 matches in go.sum |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `pkg/extension/builtin/core/cron.go` | `github.com/robfig/cron/v3` | `cronv3.NewParser(cronv3.Minute|cronv3.Hour|cronv3.Dom|cronv3.Month|cronv3.Dow)` | WIRED | Line 24-26; NO `cronv3.Descriptor` flag (Pitfall 6, macros rejected) |
| `pkg/extension/builtin/core/cron.go` | `pkg/extension/trigger_unmarshal.go` | `extension.RegisterTriggerSourceFactory(cronKind, ...)` in init() | WIRED | Line 272 |
| `pkg/extension/builtin/core/cron.go` | `time/tzdata` | blank import in core.go (NOT cron.go) | WIRED | core.go line 17 |
| `pkg/extension/schedules/schedules.go` | `pkg/extension/builtin/core.CronSource` | `t.Source.(*skycore.CronSource)` type assertion | WIRED | Lines 153, 265 |
| `pkg/extension/schedules/schedules.go` | `go.temporal.io/sdk/client` | `client.ScheduleClient.Create/List` + `ScheduleHandle.Update/Delete` | WIRED | Lines 118, 134, 189, 356 |
| `pkg/extension/schedules/schedules.go` | `pkg/interpreter.FlowRegistry` | `flows.ContentHashFor(t.FlowName)` (Pitfall 7 mitigation) | WIRED | Lines 157, 269 |
| `pkg/cli/server.go` | `pkg/extension/schedules.ReconcileCronSchedules` | called after worker.Start, before net.Listen, with apply=true | WIRED | Lines 219-227; conditional on `cronReconcile` flag |
| `pkg/cli/cron_plan.go` | `pkg/extension/schedules.ReconcileCronSchedules` | called with apply=false; rendered to stdout | WIRED | Lines 81-89 |
| `pkg/cli/options.go::scheduleFactory` | server.go + cron_plan.go | test-seam override for ScheduleClient | WIRED | server.go:214-218, cron_plan.go:75-79 |
| `cmd/skytime/main.go` | `pkg/extension/builtin/core.New` | `cli.WithExtensions(skyhttp.New(), skycore.New())` | WIRED | Line 37 |
| `examples/http-github-webhook/cmd/extbin/main.go` | `pkg/extension/builtin/core.New` | `skycore.New()` in WithExtensions list | WIRED | Line 46 |
| `pkg/interpreter/workflow.go` | `pkg/interpreter/cron_input.go::extractScheduledTime` | called inside NewWorkflow when input.InitState is nil/missing scheduled_time | WIRED | workflow.go line 51-66 |
| `pkg/parser/req_walk.go` | `*cronSource.ReqSchema()` | type-assert via `interface{ ReqSchema() []string }` | WIRED | TestTrigger_CronReqAttrTypo proves the walker hits cron sources |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `cron-plan` output | `plan` | `schedules.ReconcileCronSchedules(ctx, sc, trigReg.All(), flowReg, taskQueue, false, logger)` reads parsed triggers + lists cluster schedules | Yes — trigReg.All() returns parsed triggers; sc.List returns Schedules; diff produces real Plan struct | FLOWING |
| `server --cron-reconcile` output | `plan` | Same call with `apply=true` | Yes — additionally calls sc.Create/Update/Delete | FLOWING |
| Cron-fired workflow's `ctx.scheduled_time` | InitState["scheduled_time"] | extractScheduledTime parses RFC3339 from WorkflowID suffix | Yes — UAT 2026-05-11 confirmed real per-minute fires produced parseable timestamps | FLOWING |
| `weekly_digest.star` cron trigger | `core.cron(...)` invocation | cronFactory validates schedule/timezone/overlap/catchup_window at parse time | Yes — parses to real `*cronSource` with all fields populated; participates in trigger registry | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Core extension tests pass | `go test ./pkg/extension/builtin/core/... -count=1 -race` | `ok github.com/mikelalcon/skytime/pkg/extension/builtin/core 1.386s` | PASS |
| Schedules package tests pass | `go test ./pkg/extension/schedules/... -count=1 -race` | `ok github.com/mikelalcon/skytime/pkg/extension/schedules 1.533s` | PASS |
| Interpreter cron_input tests pass | `go test ./pkg/interpreter/... -count=1 -race -run "TestExtractScheduledTime\|TestCron"` | `ok github.com/mikelalcon/skytime/pkg/interpreter 1.456s` | PASS |
| CLI server + cron-plan tests pass | `go test ./pkg/cli/... -count=1 -race -run "TestServer\|TestCronPlan\|TestRoot\|TestLoadRegistries"` | `ok github.com/mikelalcon/skytime/pkg/cli 2.572s` | PASS |
| Parser trigger tests (incl. TestTrigger_CronReqAttrTypo) pass | `go test ./pkg/parser/... -count=1 -race -run "TestTrigger"` | `ok github.com/mikelalcon/skytime/pkg/parser 1.399s` | PASS |
| Full module build | `go build ./...` | No output (success) | PASS |
| Full module vet | `go vet ./...` | No output (success) | PASS |
| Smoke script is executable | `test -x docs/walkthroughs/cron-schedules-smoke.sh` | EXECUTABLE | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| **SCHED-01** | 07.2-01, 07.2-04 | `core.cron(schedule=, timezone=, overlap=, catchup_window=)` source factory; 5-field POSIX accepted at parse time; returns TriggerSource not associated with HTTP. NOTE: original spec said `triggers.cron(...)`; shipped surface is `core.cron(...)` under the built-in `core` extension — intentional evolution documented in PLAN files. | SATISFIED | `pkg/extension/builtin/core/cron.go::cronFactory` + 12 passing TestCron_* tests covering parse-time validation and round-trip; example walkthrough uses `core.cron(...)` |
| **SCHED-02** | 07.2-02, 07.2-04 | Cron triggers backed by Temporal Schedules; `skytime server` startup creates/updates Schedule resources | SATISFIED | `pkg/extension/schedules/schedules.go::ReconcileCronSchedules` (Create + Update branches); wired in `pkg/cli/server.go:212-237`; UAT 2026-05-11 confirmed live cluster Schedule creation |
| **SCHED-03** | 07.2-02, 07.2-03, 07.2-04 | Schedule reconciliation creates/updates/deletes; safety knob controls deletion. NOTE: original spec said `--reconcile=strict|preserve|dry-run` (one flag, three modes); shipped surface is `--cron-reconcile` boolean on `skytime server` (criteria 1+2) + a separate `skytime cron-plan` subcommand for dry-run (criterion 3). Behaviorally equivalent — intentional evolution documented in PLAN files. | SATISFIED | `pkg/cli/server.go --cron-reconcile` (applies, with delete-orphan in diff.Deletes path); `pkg/cli/cron_plan.go skytime cron-plan` (apply=false, prints plan only); UAT 2026-05-11 confirmed orphan-delete demo |

No orphaned requirements: REQUIREMENTS.md lines 112-114 map SCHED-01/02/03 to Phase 7.2, and all three IDs appear in at least one PLAN's `requirements:` field. The two-surface evolution (`--cron-reconcile` + `cron-plan` instead of `--reconcile=strict|preserve|dry-run`) is captured in the spec-vs-implementation mapping and was approved during phase planning.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |

No production anti-patterns found. Comments referencing `TODO`/`FIXME` are not present in the production files inspected. The placeholder string in `cron_describe.go` (if any) was not found via spot-check — verified that `describeCron` returns real text via the walkthrough's plain-English "when" output.

### Human Verification Performed

Criteria #1, #2, #3, and #5 were already validated against a live `temporal server start-dev` cluster on 2026-05-11 (documented in `07.2-04-SUMMARY.md`). Outcomes:

| Test | Result | Documented in |
| --- | --- | --- |
| Cron trigger applied; Schedule visible via `temporal schedule list` (criterion #1) | PASS | 07.2-04-SUMMARY.md line 125 |
| Removing trigger + restarting produced 1 delete; `temporal schedule list` showed 0 Skytime entries (criterion #2) | PASS | 07.2-04-SUMMARY.md line 126 |
| `skytime cron-plan` dry-run printed terraform-style plan, exited 0, zero cluster mutations (criterion #3) | PASS | 07.2-04-SUMMARY.md line 127 |
| Per-minute cron fires produced `SkytimeWorkflow` executions visible in `temporal workflow list`; full lifecycle in stdout (criterion #5) | PASS | 07.2-04-SUMMARY.md line 129, 138-151 |

Mid-UAT defect fixes (commits 0465225, 242dbdf, 5270f86, a359259) landed BEFORE final UAT approval and have been confirmed present in the verified codebase:
- `pkg/cli/cron_plan.go` + `pkg/cli/cron_describe.go` ship the terraform-plan-style renderer with plain-English "when" annotation
- `pkg/cli/server.go:117` reassigns `cfg.sdkLogger = logger` (workflow lifecycle logs surface in `skytime server` stdout)
- `pkg/interpreter/cron_input.go::extractScheduledTime` parses Pitfall-2 RFC3339 suffix from WorkflowID; called from `pkg/interpreter/workflow.go:51` inside NewWorkflow

### Gaps Summary

None. All five ROADMAP success criteria are satisfied; all three requirement IDs (SCHED-01/02/03) are accounted for in PLAN frontmatter; all artifacts exist with substantive content and proper wiring; all automated tests pass; the human UAT against a live Temporal cluster approved criteria 1/2/3/5 on 2026-05-11. The spec-vs-implementation mapping (core.cron vs triggers.cron; `--cron-reconcile` + `cron-plan` vs `--reconcile=strict|preserve|dry-run`) is intentional and documented in PLAN files.

---

_Verified: 2026-05-11_
_Verifier: Claude (gsd-verifier)_
