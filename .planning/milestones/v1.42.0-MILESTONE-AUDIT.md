---
milestone: v1.42.0
audited: 2026-05-08
status: tech_debt
scores:
  requirements: 64/64
  phases: 9/9
  integration: 17/17
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 03-lambda-serialization-decision-interpreter-worker
    items:
      - "Stale REQUIREMENTS.md traceability — INTRP-03/04/05 checkboxes still `[ ]` and trace table says `Pending` despite VERIFICATION.md confirming all three SATISFIED with test evidence (TestWalkIfCond_ZeroHistoryEvents, TestWalkScript_ZeroHistoryEvents, 6 TestWalkForEach_*, 3 TestWalkCallFlow_*). Bookkeeping only — no code gap."
  - phase: 04.3-documentation-and-source-driven-reference-generator
    items:
      - "5 HUMAN-UAT items pending since 2026-05-04 (status: partial). Documentation rendering checks: GitHub UI render of README.md, audience-landing cross-link traversal, prose readability of architecture.md + getting-started.md, embedded tutorial walk-through as first-time reader, cross-link resolution in cli.md/builtins.md/http.md/examples/README.md."
      - "Missing VALIDATION.md (only phase in milestone without one)"
  - phase: cross-phase
    items:
      - "pkg/cli has no WithBuildID(string) Option. Custom binaries (extbin) inherit defaultBuildID='dev' and can only override via -ldflags. Pattern works but not API-discoverable. Affects INTRP-02 / WORK-02 surface ergonomics."
      - "pkg/testing has no WithCredentialHandler Option, and pkg/cli/test.go does not thread cfg.credHandler through. Non-blocking today (Phase 6's issue_triage_test.star mocks every action) but future tests using partial-mocks against real credentials would fail. Affects TEST-02 boundary, CLI-03."
      - "Phase 6 examples/http-github-webhook/ registers 3 extensions but only 2 are referenced in .star flows (HTTP unused). EX-01 contract claim is registration-level for HTTP; runtime use is in examples/skeleton/ instead."
      - "Phase 6 corpus has parse-coverage but not live runtime-coverage for 4 of 5 flows. Only public_repo_check.star is exercised under live Temporal in CI (walkthrough_smoke.sh). pr_to_webhook, issue_triage, batch_label_issues, weekly_digest have parse + DAG-shape verification but no end-to-end execution path. issue_triage indirectly covered via issue_triage_test.star with mocks."
nyquist:
  compliant_phases: [04.1]
  partial_phases: [01, 02, 03, 04, 04.2, 05, 06]
  missing_phases: [04.3]
  overall: 1/9
---

# Milestone v1.42.0 — Audit Report

**Audited:** 2026-05-08
**Status:** tech_debt — all requirements satisfied, integration verified, no critical blockers; accumulated debt needs review

## Scope

**Milestone:** v1.42.0 (codename: foundational v1)
**Phases (9):** 01, 02, 03, 04, 04.1, 04.2, 04.3, 05, 06
**Definition of done:** Production-grade durable-workflow library with: Starlark DSL → deterministic DAG → Temporal interpreter; static + Tier-3 test tiers; CLI; documentation set; example project (HTTP+GitHub+Webhook); CI on every push.

## Requirements Coverage

**Total:** 64/64 satisfied (49 v1 + 15 D-* docs requirements)

| Category | Count | Status |
|----------|-------|--------|
| DSL (01-15) | 15 | ✓ All satisfied |
| EXT (01-06) | 6 | ✓ All satisfied |
| PARSE (01-06) | 6 | ✓ All satisfied |
| INTRP (01-07) | 7 | ✓ All satisfied (3 stale checkboxes — bookkeeping fix below) |
| ACT (01-06) | 6 | ✓ All satisfied |
| WORK (01-03) | 3 | ✓ All satisfied |
| VAL (01-05) | 5 | ✓ All satisfied |
| CLI (01-07) | 7 | ✓ All satisfied |
| TEST (01-05) | 5 | ✓ All satisfied |
| EX (01-04, FIX-01) | 5 | ✓ All satisfied |
| D-* (01-19) | 19* | ✓ All satisfied via Phase 04.3 |

*Phase 04.3 requirements (D-01..D-19) are documentation requirements satisfied at PLAN level rather than being top-level v1 entries; counted toward 64 total.

## Phase Verification

| Phase | Status | Plans | Notes |
|-------|--------|-------|-------|
| 01 | ✓ passed | 5/5 | Type spine + parser + bridge foundations |
| 02 | ✓ passed | 3/3 | Generic activity + credentials |
| 03 | ✓ passed | 4/4 | Interpreter + worker; INTRP-03/04/05 trace-table stale |
| 04 | ✓ passed | 7/7 | Validator + CLI tree + HTTP extension |
| 04.1 | ✓ passed | 7/7 | Dynamic step kwargs + interpolation |
| 04.2 | ✓ passed | 7/7 | if_cond expression mode + result/fail |
| 04.3 | ✓ passed (human_needed) | 9/9 | 5 HUMAN-UAT items pending; missing VALIDATION.md |
| 05 | ✓ passed (human_needed → resolved) | 6/6 | Tier-3 harness; UAT resolved |
| 06 | ✓ passed | 9/9 | Example project + CI + latent-gap fixes back to Phase 5 harness |

## Cross-Phase Integration

**17/17 wiring points verified live.** All 4 declared E2E flows complete cleanly:

| Flow | Status | Verified by |
|------|--------|-------------|
| F1: author validate → run | ✓ wired | Phase 4 differential test on examples/skeleton/, walkthrough_smoke.sh on public_repo_check.star |
| F2: Tier-3 test → retries + replay | ✓ wired | Phase 5 harness, Phase 6 issue_triage_test.star (TEST-03 attempt-based retry) |
| F3: consultant custom binary | ✓ wired | extbin reuses cli.NewRootCommand; subprocess smoke test |
| F4: README walkthrough → 4 commands → flow runs | ✓ wired | walkthrough_smoke.sh runs documented commands byte-for-byte |

**Architectural firewalls (5/5 hold):**
- Temporal SDK reachable only from `pkg/{activity,interpreter,worker,cli,testing}` — verified live grep clean
- Cobra/charmlog reachable only from `pkg/cli` and `cmd/skytime` — verified live grep clean
- Extensions never import `go.temporal.io/sdk/activity` — verified
- `cmd/skytime-docgen` is stdlib-only — verified
- Test-mode parser globals (tester, ok, err, nonretryable) gated by WithTestMode() and WithTestPredeclared() — verified production parsers don't set these flags

**Contract drift:** None. WorkflowInput shape consistent across all consumer/producer sites; ExecuteBatch activity name string consistent; OperationDispatch keying (`<extName>.<opName>`) consistent.

**Build/vet:** `go build ./...` and `go vet ./...` both green.

## Tech Debt (Non-blocking)

### Bookkeeping (1 item — auto-fix during complete-milestone)

- **REQUIREMENTS.md INTRP-03/04/05 stale checkboxes.** Trace table says `Pending`; checkboxes are `[ ]`. Phase 3 VERIFICATION.md confirms all three SATISFIED with test evidence. Easy flip during `/gsd:complete-milestone`.

### Documentation Polish (1 item — Phase 04.3)

- **5 pending HUMAN-UAT items** in `.planning/phases/04.3-*/04.3-HUMAN-UAT.md`. All are documentation-rendering and prose-readability checks that need human eyes (GitHub UI render, cross-link traversal, walkthrough as first-time reader). Pending since 2026-05-04. Run `/gsd:verify-work 04.3` to walk them.

### Test Coverage (1 item — Phase 06)

- **4 of 5 example flows have parse-only coverage**, not live execution. `pr_to_webhook`, `issue_triage`, `batch_label_issues`, `weekly_digest` parse cleanly and have correct DAG shape pinned by `flows_test.go`, but none run end-to-end against a live Temporal worker in CI. Only `public_repo_check.star` is exercised by `walkthrough_smoke.sh`. `issue_triage` is partially covered indirectly via `issue_triage_test.star` (Tier-3 with mocks).
- **HTTP extension unused in Phase 6 corpus.** Registered by extbin but no `.star` flow calls `http.endpoint(...)`. EX-01's "3 extensions exercising every concern" is registration-level for HTTP; runtime use lives in `examples/skeleton/`.

### API Surface (2 items — cross-phase)

- **`pkg/cli` lacks `WithBuildID(string)` Option.** Custom binaries inherit `defaultBuildID = "dev"` from `pkg/worker`. Override is only via `-ldflags`. Surface ergonomics for INTRP-02 / WORK-02.
- **`pkg/testing` lacks `WithCredentialHandler` Option.** `pkg/cli/test.go` does not thread `cfg.credHandler` to `pkg/testing.RunCLI`. Non-blocking today because Phase 6's only Tier-3 test mocks every action; future patterns using real credentials in tests (or partial mocks) would fail. TEST-02 boundary, CLI-03.

## Nyquist Coverage

`workflow.nyquist_validation: true` is enabled.

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 01 | exists | false | `/gsd:validate-phase 01` |
| 02 | exists | false | `/gsd:validate-phase 02` |
| 03 | exists | false | `/gsd:validate-phase 03` |
| 04 | exists | false | `/gsd:validate-phase 04` |
| 04.1 | exists | **true** | — |
| 04.2 | exists | false | `/gsd:validate-phase 04.2` |
| 04.3 | **missing** | n/a | `/gsd:validate-phase 04.3` |
| 05 | exists | false | `/gsd:validate-phase 05` |
| 06 | exists | false | `/gsd:validate-phase 06` |

**Coverage: 1/9 compliant.** This is a milestone-wide validation backlog. Each phase's VALIDATION.md identifies specific Nyquist gaps. Filling these does not block milestone completion (no requirement is `unsatisfied`), but it's the largest single backlog category for v1.42.0.

## Verdict

**No critical blockers.** All 64 requirements satisfied. All 4 declared E2E flows wire cleanly. All 5 architectural firewalls hold. Build green, vet green.

**Tech debt across 7 categories** — none of which are unsatisfied requirements or wiring breaks. Decision point for the user:

- **Accept tech debt and complete v1.42.0** → `/gsd:complete-milestone`. Tech debt items get tracked in the next milestone (v1.43) backlog.
- **Address before completing** → `/gsd:plan-milestone-gaps`. Creates a gap-closure phase for the items above.

Recommended: **accept and complete**. The unsatisfied items are surface-area / coverage / docs polish, not correctness gaps. The Nyquist backlog is large enough that filling it is its own multi-phase project — better treated as a v1.43 thread than a v1.42 blocker.

## Files Reviewed

- `.planning/REQUIREMENTS.md` (traceability table + checkbox state)
- `.planning/ROADMAP.md` (phase definitions, success criteria)
- `.planning/phases/*/0*-VERIFICATION.md` (9 files, all `passed` or `passed (human_needed)`)
- `.planning/phases/*/0*-SUMMARY.md` (49 plan summaries; requirements-completed frontmatter)
- `.planning/phases/*/0*-VALIDATION.md` (8 files; one phase missing)
- `.planning/phases/04.3-*/04.3-HUMAN-UAT.md` (5 pending items)
- Cross-phase integration via gsd-integration-checker subagent (17 wiring points, 5 firewalls, 4 E2E flows)
